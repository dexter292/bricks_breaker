/** Discretion defaults for relative-drag feel (D-04 / D-05). */
export const PADDLE_GAIN = 1.25;
export const SMOOTH_ALPHA = 0.45;
/** Documented for gesture wiring; unused in pure math helpers. */
export const PAN_MIN_DISTANCE_PX = 6;
export const TAP_MAX_DISTANCE_PX = 10;
/** Mirror core logical width — input must not import core (LC-05). */
export const LOGICAL_WIDTH_VU = 360;
