/** Default sprite capacity for the Phase-1 spike stub. */
export const DEFAULT_SPRITE_CAP = 256;

/** Logical play-field width used by the stub integrator (unitless). */
export const LOGICAL_WIDTH = 360;

/** Logical play-field height used by the stub integrator (unitless). */
export const LOGICAL_HEIGHT = 640;

/** Default sprite half-extent for seed layout. */
export const DEFAULT_SPRITE_SIZE = 8;
