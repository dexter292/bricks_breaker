---
phase: 03-first-playable-render-input-bricks-lives-pause
plan: 04
type: execute
wave: 2
depends_on:
  - "03-00"
  - "03-01"
  - "03-02"
files_modified:
  - src/runtime/useGameLoop.ts
  - src/runtime/useSpikeLoop.ts
  - src/runtime/appStatePause.ts
autonomous: true
requirements:
  - PLT-01
  - PHYS-05
must_haves:
  truths:
    - "useGameLoop steps via stepRun with Intent{paddleX: paddleTarget, launch: launchFlag} and clears launch after consume"
    - "When uiPhase paused/countdown/won/lost: frameCallback.setActive(false) and accumulator reset"
    - "AppState inactive|background → pause + resetAccumulator; active does NOT setActive(true)"
    - "recordFrame uses letterboxed entity renderer; first frame after reactivate uses 16.67ms null dt"
  artifacts:
    - path: "src/runtime/useGameLoop.ts"
      provides: "playable fixed-timestep host with freeze + Intent wiring"
      exports: ["useGameLoop"]
    - path: "src/runtime/appStatePause.ts"
      provides: "subscribeAppStatePause helper"
      exports: ["subscribeAppStateAutoPause"]
  key_links:
    - from: "src/runtime/useGameLoop.ts"
      to: "src/core/stepRun.ts"
      via: "stepRun each FIXED_DT substep"
      pattern: "stepRun\\("
    - from: "src/runtime/useGameLoop.ts"
      to: "src/runtime/freeze.ts"
      via: "resetAccumulator on freeze"
      pattern: "resetAccumulator"
    - from: "src/runtime/appStatePause.ts"
      to: "AppState change listener"
      via: "inactive|background → onAutoPause; active → no auto-resume"
      pattern: "AppState.addEventListener"
---

<objective>
Evolve the spike frame host into `useGameLoop`: Intent from SharedValues, `stepRun`, letterboxed `recordFrame`, `setActive` freeze, and AppState auto-pause with accumulator reset and **no** auto-unfreeze (PLT-01, PHYS-05 host).

Purpose: Safe timestep + OS interruption policy before React overlays land.
Output: `useGameLoop.ts`, `appStatePause.ts`, thin `useSpikeLoop` re-export.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-RESEARCH.md
@.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-PATTERNS.md
@src/runtime/useSpikeLoop.ts
@src/runtime/freeze.ts
@src/core/index.ts
@src/render/recordSprites.ts
@docs/layer-contract.md

## Locked decisions
- D-13…D-16: Pause/Resume/Retry overlay later; AppState auto-pause; reset accumulator; tap Resume then 3s countdown (countdown UI in Plan 05); never auto-resume on active
- PHYS-06 retained: fixed dt, no React state per physics frame
- AGENTS.md: AppState is RN core — not an Expo SDK module

## ESLint
- runtime may import core + render + runtime — **not** input (compose gestures in app)

<interfaces>
From src/runtime/useSpikeLoop.ts (evolve):
```typescript
export function useSpikeLoop(...): SpikeLoopHandle;
// today: stepWorld with static Intent; stretch recordFrame
```

From Plan 01:
```typescript
export function stepRun(world: World, intent: Intent, dt: number): void;
export function resetWorld(...): void;
export function loadPhase3Grid(world: World): void;
```

From Plan 00:
```typescript
export function resetAccumulator(world: { accumulator: number }): void;
export function clampFrameDt(dtSec: number, maxFrameTime: number): number;
export function shouldFreezeForUiPhase(phase: UiPhase): boolean;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useGameLoop with stepRun Intent + freeze controls</name>
  <files>src/runtime/useGameLoop.ts, src/runtime/useSpikeLoop.ts</files>
  <read_first>src/runtime/useSpikeLoop.ts, src/runtime/freeze.ts, src/runtime/constants.ts, src/core/index.ts, src/render/recordSprites.ts, .planning/phases/03-first-playable-render-input-bricks-lives-pause/03-RESEARCH.md</read_first>
  <action>
1. Create `src/runtime/useGameLoop.ts` evolving useSpikeLoop patterns:

Inputs (args or options object):
- `paddleTarget: SharedValue<number>`
- `launchFlag: SharedValue<number>`
- `uiPhase: SharedValue<number>` — 0 playing, 1 paused, 2 countdown (same map as gesture hook)
- `drawOverlayFlag`, `hudFont` optional (keep PERF_OVERLAY path)
- safe/content size via existing `surfaceSize` SharedValue (parent sets from onSize / safe area)

On first frame allocate world → `resetWorld(w, seedG, seedC)` → `loadPhase3Grid(w)` → sync `paddleTarget` to `w.paddleX`.

Each frame when **not** frozen:
- `dt = clampFrameDt((frame.timeSincePreviousFrame ?? 16.67)/1000, MAX_FRAME_TIME)`
- accumulate; while ≥ FIXED_DT && steps < MAX_SUBSTEPS:
  - `intent = { paddleX: paddleTarget.value, launch: launchFlag.value }`
  - `stepRun(w, intent, FIXED_DT)`
  - if `launchFlag.value !== 0` → `launchFlag.value = 0` (edge consume after step)
  - accumulator -= FIXED_DT
- max-substeps → accumulator = 0
- `picture = recordFrame(...)`

Freeze API:
- Return `frameCallback` from `useFrameCallback(..., true)` **or** store and expose `setActive(active: boolean)` wrapper
- Export helper `applyFreeze(worldSv, setActive, frozen: boolean)` used by React effects: when frozen → `setActive(false)`; if world exists `resetAccumulator(world)`; when unfrozen after countdown → `setActive(true)` only (caller responsibility)
- Inside worklet: if `uiPhase` is paused/countdown **or** world.simPhase is WON/LOST → skip stepping (belt) but still allow picture refresh if active; prefer `setActive(false)` from JS when uiPhase changes so callback stops (D-14 / PLT-01)

2. `useSpikeLoop.ts` → re-export `useGameLoop` as deprecated alias **or** thin wrapper so SpikeScreen still typechecks until Plan 05 deletes it. Prefer:
```typescript
export { useGameLoop as useSpikeLoop } from './useGameLoop';
```
only if signatures compatible; otherwise keep old file calling stepWorld for cliff harness and have GameScreen use useGameLoop exclusively — **choose useGameLoop-only and update SpikeScreen minimally to compile** (blank Intent hold OK for cliff until Plan 05 removes cliff).

Minimum: `useGameLoop` exists; SpikeScreen still builds (adjust imports if needed without implementing overlays).

Do **not** import from `src/input`. Do **not** add AppState in this task (Task 2).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx eslint src/runtime/useGameLoop.ts && rg -n "stepRun|resetAccumulator|launchFlag" src/runtime/useGameLoop.ts && rg -n "from ['\\\"].*input" src/runtime; test $? -eq 1 && npx vitest run tests/runtime.freeze.test.ts tests/runtime.accumulator-reset.test.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `useGameLoop` exported; calls `stepRun` not bare `stepWorld` for playable path
    - Consumes `paddleTarget` + `launchFlag` SharedValues
    - No `src/input` imports under `src/runtime`
    - Freeze helpers referenced (`resetAccumulator` or equivalent accumulator=0)
    - `npm run test:core` exits 0
  </acceptance_criteria>
  <done>Playable loop hosts stepRun + Intent SharedValues with freeze hooks.</done>
</task>

<task type="auto">
  <name>Task 2: AppState auto-pause subscription (no auto-resume)</name>
  <files>src/runtime/appStatePause.ts, src/runtime/useGameLoop.ts</files>
  <read_first>src/runtime/useGameLoop.ts, src/runtime/freeze.ts, .planning/phases/03-first-playable-render-input-bricks-lives-pause/03-RESEARCH.md</read_first>
  <action>
1. Create `src/runtime/appStatePause.ts`:
```typescript
import { AppState, type AppStateStatus, type NativeEventSubscription } from 'react-native';

export function subscribeAppStateAutoPause(handlers: {
  onAutoPause: () => void;
}): NativeEventSubscription {
  return AppState.addEventListener('change', (next: AppStateStatus) => {
    if (next === 'inactive' || next === 'background') {
      handlers.onAutoPause();
    }
    // active: intentionally empty — never auto-resume (D-15)
  });
}
```

2. In `useGameLoop` (or a tiny `useAppStateAutoPause` colocated), `useEffect` subscribe:
   - `onAutoPause` → set React/uiPhase paused (via callback arg `onOsPause?: () => void` passed from GameScreen) **and** `frameCallback.setActive(false)` **and** `resetAccumulator` on world if present
   - Cleanup `subscription.remove()`

3. Document in comment: returning to `active` must leave frozen until Resume→countdown (Plan 05).

T-03-03 mitigation is this path. Manual 60s test remains Plan 05 checkpoint.

Pure unit coverage already in Plan 00 freeze tests — add a tiny Node test only if you extract a pure `shouldAutoPauseOnAppState(next: string): boolean` into freeze.ts or appStatePause.ts without importing AppState into the test file (optional; if added, put predicate in freeze.ts and test it).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "subscribeAppStateAutoPause|inactive|background" src/runtime/appStatePause.ts && rg -n "setActive\\(true\\)" src/runtime/appStatePause.ts; test $? -eq 1 && npx eslint src/runtime/appStatePause.ts src/runtime/useGameLoop.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `subscribeAppStateAutoPause` exists and handles `inactive` + `background`
    - `appStatePause.ts` does **not** call `setActive(true)` on `active`
    - useGameLoop (or documented export) wires the subscription with accumulator reset on pause
    - eslint clean; `npm run test:core` exits 0
  </acceptance_criteria>
  <done>OS background auto-pauses and resets accumulator without auto-resuming physics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS AppState → simulation clock | Background gaps can inject huge dt |
| SharedValue Intent → stepRun | Launch/paddle edges each substep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Intent assembly | mitigate | stepRun finite guards; clear launch after edge; non-finite paddle ignored in core |
| T-03-02 | Elevation (UX) | useGameLoop | accept | No playfield Resume here — overlays Plan 05 |
| T-03-03 | Tampering / DoS-of-fun | AppState + freeze | mitigate | setActive(false) + resetAccumulator; never setActive(true) on AppState active |
</threat_model>

<verification>
eslint runtime; stepRun+AppState wiring present; no auto-resume on active; core+freeze tests green.
</verification>

<success_criteria>
PLT-01 loop freeze + AppState policy implemented at the host layer; PHYS-05 Intent.launch reaches stepRun each substep.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-04-SUMMARY.md`
</output>
