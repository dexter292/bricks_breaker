---
phase: 03-first-playable-render-input-bricks-lives-pause
plan: 05
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
files_modified:
  - src/runtime/GameScreen.tsx
  - src/runtime/SpikeScreen.tsx
  - src/runtime/overlays/PauseOverlay.tsx
  - src/runtime/overlays/CountdownOverlay.tsx
  - src/runtime/overlays/ResultOverlay.tsx
  - app/GameHost.tsx
  - app/index.tsx
autonomous: false
requirements:
  - PHYS-01
  - PHYS-05
  - RUN-02
  - PLT-01
must_haves:
  truths:
    - "Player can relative-drag paddle, tap-serve while docked, break bricks, and see win/lose overlays"
    - "Pause overlay Resume Pressable → 3·2·1 countdown → unfreeze; playfield pan never resumes"
    - "AppState background returns to paused UI without physics catch-up; Retry resets lives+grid+docked"
    - "Gestures composed in app/ (LC-05); overlays after opaque canvas in JSX"
  artifacts:
    - path: "app/GameHost.tsx"
      provides: "LC-05 composition of useGameLoop + usePaddleGesture"
      exports: ["GameHost"]
    - path: "src/runtime/GameScreen.tsx"
      provides: "canvas + chrome + overlay stack"
      exports: ["GameScreen"]
    - path: "src/runtime/overlays/PauseOverlay.tsx"
      provides: "Paused + Resume + Retry"
    - path: "src/runtime/overlays/CountdownOverlay.tsx"
      provides: "3·2·1 Display numerals"
    - path: "src/runtime/overlays/ResultOverlay.tsx"
      provides: "Win/Lose + Retry"
  key_links:
    - from: "app/GameHost.tsx"
      to: "src/input/usePaddleGesture.ts"
      via: "compose gestures — runtime must not import input"
      pattern: "usePaddleGesture"
    - from: "PauseOverlay Resume"
      to: "countdown → setActive(true)"
      via: "Pressable accessibilityLabel Resume game"
      pattern: "Resume game"
    - from: "GameScreen"
      to: "GestureDetector"
      via: "playfield gesture from host props"
      pattern: "GestureDetector"
---

<objective>
Assemble the first playable screen: app-layer gesture+loop composition, GameScreen with safe-area letterbox sizing, Pause/Countdown/Result overlays per UI-SPEC, lives chrome, Retry reset — then human-verify the rally (all Phase 3 requirements).

Purpose: Deliver ROADMAP success criteria 1–5 as something you can hold and play.
Output: `GameHost` / `GameScreen` / overlays / `app/index` mount + blocking UAT checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-CONTEXT.md
@.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-UI-SPEC.md
@.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-RESEARCH.md
@.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-PATTERNS.md
@src/runtime/SpikeScreen.tsx
@src/runtime/useGameLoop.ts
@src/input/usePaddleGesture.ts
@app/index.tsx
@docs/layer-contract.md

## Locked decisions
- D-11…D-16: Resume only overlay Pressable; countdown 3s; AppState → pause; never auto-unfreeze
- D-17…D-19: 3 lives; Win/Lose minimal overlays + Retry; no confirmation
- UI-SPEC copy/a11y labels exact: Pause game / Resume game / Retry level; titles Paused / Win / Lose
- Overlays under `runtime/overlays/` (eslint: runtime ↛ ui)
- Compose input+runtime in `app/` (LC-05)

## Discretion
- Optional “Tap to launch” hint while docked
- Exact panel layout within UI-SPEC spacing/typography tokens
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overlays + GameScreen chrome (UI-SPEC)</name>
  <files>src/runtime/overlays/PauseOverlay.tsx, src/runtime/overlays/CountdownOverlay.tsx, src/runtime/overlays/ResultOverlay.tsx, src/runtime/GameScreen.tsx, src/runtime/SpikeScreen.tsx</files>
  <read_first>.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-UI-SPEC.md, src/runtime/SpikeScreen.tsx, src/render/GameCanvas.tsx, docs/layer-contract.md</read_first>
  <action>
1. Create overlays under `src/runtime/overlays/` (SpaceMono via parent fontFamily or Text style `fontFamily` matching spike; UI-SPEC sizes):
   - **PauseOverlay**: full-screen scrim `#000000` @ 60% opacity; panel `#12121f`; heading **Paused** (20/600); Pressables **Resume** (`accessibilityLabel="Resume game"`) and **Retry** (`accessibilityLabel="Retry level"`); min touch 44×44; `pointerEvents="auto"` on catcher so canvas receives no taps
   - **CountdownOverlay**: scrim; centered Display **48px** numerals `3`/`2`/`1` only; no Resume control; pointerEvents catcher
   - **ResultOverlay**: props `{ kind: 'win' | 'lose' }`; Win heading **Win** + optional **All clear**; Lose heading **Lose** (color `#E85D5D`) + optional **Out of lives**; CTA **Retry** only

2. Create `GameScreen.tsx` props-driven (no input imports):
```typescript
type Props = {
  picture: SharedValue<SkPicture>;
  surfaceSize: SharedValue<SkSize>;
  playfieldGesture: GestureType;
  uiPhase: 'playing' | 'paused' | 'countdown';
  result: null | 'win' | 'lose';
  lives: number;
  countdownNumeral: number | null; // 3|2|1
  onPause: () => void;
  onResume: () => void;
  onRetry: () => void;
};
```
Layout:
- Root `#000` flex 1
- Safe-area content box: use `useSafeAreaInsets()`; size GameCanvas to inset content (width = window - left - right, height = window - top - bottom) so letterbox matches safe area (D-03 / UI-SPEC)
- **Canvas first** (absolute fill of content box), then chrome/overlays after (opaque z-order)
- Top-trailing Pause Pressable (`accessibilityLabel="Pause game"`) visible only when `uiPhase==='playing' && !result`
- Top-leading Label `Lives · {N}` (discrete React state — LC-11)
- Optional docked hint **Tap to launch** when provided via prop `showServeHint`
- Wrap playfield with `GestureDetector` + `gesture={playfieldGesture}` covering the canvas area
- Conditionally render Pause / Countdown / Result overlays

3. `SpikeScreen.tsx` → re-export `GameScreen` or delete cliff UI and re-export from GameScreen — cliff-ramp not required for Phase 3 success (may hide behind existing CLIFF_RAMP flag off).

Styles: spacing tokens md/lg/xl/3xl from UI-SPEC; no cyan gameplay accents.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx eslint src/runtime/overlays src/runtime/GameScreen.tsx && rg -n "Resume game|Retry level|Pause game" src/runtime && rg -n "from ['\\\"].*input" src/runtime/GameScreen.tsx; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - Three overlay files exist with exact a11y labels for Resume/Retry/Pause
    - `GameScreen` does not import `src/input`
    - Canvas appears before overlays in JSX source order
    - eslint exits 0 on overlays + GameScreen
  </acceptance_criteria>
  <done>UI-SPEC pause/countdown/result chrome ready without LC-05 violations.</done>
</task>

<task type="auto">
  <name>Task 2: app/GameHost composition — gestures, loop, pause FSM, Retry</name>
  <files>app/GameHost.tsx, app/index.tsx</files>
  <read_first>app/index.tsx, src/runtime/useGameLoop.ts, src/input/usePaddleGesture.ts, src/runtime/appStatePause.ts, src/runtime/GameScreen.tsx, docs/layer-contract.md</read_first>
  <action>
1. Create `app/GameHost.tsx` (allowed to import runtime + input + render per eslint):
   - React state: `uiPhase: 'playing'|'paused'|'countdown'`, `countdownNumeral`, `lives`, `result`
   - SharedValues: mirror `uiPhase` / `simPhase` for worklets (update via discrete reactions or write SharedValue when React phase changes — **not** per physics frame)
   - `usePaddleGesture({ simPhase, uiPhase, camScale })`
   - `useGameLoop({ paddleTarget, launchFlag, uiPhase, … })` — pass `onOsPause` that sets React `uiPhase='paused'` (D-14/D-15)
   - Keep `camScale` SharedValue updated when surface/safe size changes: `scale = min(w/360,h/640)` (same as makeCamera)
   - **Pause button** → `uiPhase=paused`; freeze loop (`setActive(false)` + resetAccumulator)
   - **Resume** → start countdown: `uiPhase=countdown`; show 3, after 1000ms → 2, → 1, → then `uiPhase=playing` + `setActive(true)`. Clear timers on unmount. **Do not** apply paddle intent during countdown (uiPhase gates gestures)
   - **Retry** (pause or result) → reset world (expose `retry()` from useGameLoop that resetWorld+loadPhase3Grid+dock), clear result, lives=3, uiPhase=playing, setActive(true) or leave docked with active loop — docked serve is fine with loop active
   - Mirror lives/result from world on phase transitions only: use `useAnimatedReaction` sparingly **or** poll via a gated reaction when `simPhase` becomes WON/LOST/DOCKED after life loss — prefer reading SharedValue world in a reaction that `runOnJS` **only** on simPhase/lives edges (≤1/frame, not per tick). Document LC-07 exception is discrete phase edges only.
   - When `simPhase===WON/LOST` → set result, freeze loop, show ResultOverlay

2. `app/index.tsx`:
```typescript
import { GestureHandlerRootView } from 'react-native-gesture-handler';
import { SafeAreaProvider } from 'react-native-safe-area-context';
import { GameHost } from './GameHost';

export default function Index() {
  return (
    <GestureHandlerRootView style={{ flex: 1 }}>
      <SafeAreaProvider>
        <GameHost />
      </SafeAreaProvider>
    </GestureHandlerRootView>
  );
}
```
(If root already has providers in `_layout.tsx`, do not double-wrap — read `app/_layout.tsx` first and mount GameHost only.)

3. Ensure `npx eslint app src/runtime src/input` passes boundaries.

No new npm packages.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx eslint app src/runtime src/input && rg -n "usePaddleGesture|useGameLoop" app/GameHost.tsx && rg -n "from ['\\\"].*input" src/runtime; test $? -eq 1 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `app/GameHost.tsx` imports both `usePaddleGesture` and `useGameLoop`
    - `rg -n "from ['\\\"].*input" src/runtime` finds no matches
    - Resume path sets countdown before `setActive(true)` (rg countdown in GameHost)
    - `npm test` exits 0
    - `app/index.tsx` mounts GameHost (or equivalent)
  </acceptance_criteria>
  <done>Playable host wires input+loop+overlays with LC-05 composition and full pause/resume FSM.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device/sim UAT — rally, aim, pause, AppState 60s</name>
  <files>none</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/03-first-playable-render-input-bricks-lives-pause/03-VALIDATION.md</read_first>
  <what-built>
Automated core/input/freeze suites green; GameHost mounts playable letterboxed rally with relative-drag, tap serve, bricks, lives, pause/countdown/win/lose, AppState auto-pause wiring.
  </what-built>
  <action>
Pause for human UAT against ROADMAP success criteria 1–5 and D-11 gesture separation. Do not change code unless the human reports failures — then fix in a follow-up commit before re-requesting approval.
  </action>
  <how-to-verify>
1. Launch app on iOS Simulator and/or Android emulator (`npx expo run:ios` / `run:android` or existing dev-client).
2. **PHYS-01:** Drag anywhere on playfield — paddle moves relatively; lift finger and press elsewhere — paddle must **not** teleport to finger; feel snappy not syrupy.
3. **PHYS-05:** On life start ball rides paddle; tap (not drag) launches; no aim line; aim at a chosen brick within first minute and damage/destroy it.
4. **RUN-02:** Break all breakables → Win overlay; lose 3 lives → Lose overlay; Retry restores docked run.
5. **PLT-01:** Pause → Resume → see 3·2·1 → play continues. Mid-rally background app ≥60s → return still paused (no teleport); Resume + countdown; no multi-life jump.
6. **D-11:** While docked, pan across field moves paddle without launching; while paused, pan does not Resume (only Resume button).
  </how-to-verify>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npm test</automated>
  </verify>
  <resume-signal>Type "approved" if all 6 checks pass, or describe failures.</resume-signal>
  <acceptance_criteria>
    - Human replies `approved` or lists concrete failures tied to SC-1…5 / D-11
    - `npm test` remains green at checkpoint time
  </acceptance_criteria>
  <done>Phase 3 playable UAT signed off for roadmap success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Overlay Pressables → freeze FSM | Only explicit Resume may unfreeze after pause/OS |
| App composition → SharedValues | Host is sole place input meets runtime |
| Discrete runOnJS phase edges | Rare JS hops for lives/win/lose labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | GameHost Intent path | mitigate | Still routed through core finite guards; Retry full reset |
| T-03-02 | Elevation (UX) | overlays + GestureDetector | mitigate | Resume/Retry only Pressables; overlays pointerEvents catcher; serve gated; panActive |
| T-03-03 | Tampering / DoS-of-fun | AppState + Resume FSM | mitigate | OS pause freezes+resets; active never auto-unfreezes; countdown before setActive(true) |
</threat_model>

<verification>
eslint app+runtime+input; npm test green; human UAT checklist for SC-1…5 and D-11.
</verification>

<success_criteria>
A player can hold the phone and play a real rally with correct drag, serve, bricks, lives, win/lose, and safe pause/background resume.
</success_criteria>

<output>
After completion, create `.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-05-SUMMARY.md`
</output>
