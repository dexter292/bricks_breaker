---
phase: 07-feedback-neon-vfx-audio
plan: 03
type: execute
wave: 2
depends_on:
  - "00"
  - "01"
files_modified:
  - src/services/audio/types.ts
  - src/services/audio/mapping.ts
  - src/services/audio/expoAudioService.ts
  - src/services/audio/index.ts
  - tests/audio.mapping.test.ts
autonomous: true
requirements:
  - FX-03
must_haves:
  truths:
    - "AudioService interface exposes preload, playBatch, release"
    - "mapEventToSfx maps PADDLE_HIT/BRICK_HIT/BRICK_BREAK/POWERUP_CATCH/LIFE_LOST/WIN/LOSE and ignores WALL_HIT/BALL_OUT"
    - "Volumes follow Life/Win/Lose → Break → Paddle/Chip → Catch hierarchy"
    - "Voice pools reuse oldest at limit; soft-fail preload never throws into gameplay"
  artifacts:
    - path: "src/services/audio/types.ts"
      provides: "AudioService + SfxId"
    - path: "src/services/audio/mapping.ts"
      provides: "mapEventToSfx + SFX_VOLUME"
    - path: "src/services/audio/expoAudioService.ts"
      provides: "createExpoAudioService / createDefaultAudioService"
    - path: "tests/audio.mapping.test.ts"
      provides: "GREEN mapping + voice-limit tests"
  key_links:
    - from: "playBatch"
      to: "createAudioPlayer pools"
      via: "seekTo(0)+play per mapped SfxId"
      pattern: "createAudioPlayer|playBatch"
    - from: "EventCode"
      to: "SfxId"
      via: "mapEventToSfx"
      pattern: "mapEventToSfx"
---

<objective>
Ship modular `services/audio` on Expo SDK 57 `expo-audio`: pure event→SFX mapping, pooled `createAudioPlayer` voices, preload/release lifecycle with soft-fail.

Purpose: FX-03 needs frame-aligned overlapping SFX without blocking physics; mirrors storage/platform seam pattern.
Output: `src/services/audio/*` + GREEN `tests/audio.mapping.test.ts` (mock players; no native audio required in Vitest).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-feedback-neon-vfx-audio/07-CONTEXT.md
@.planning/phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md
@.planning/phases/07-feedback-neon-vfx-audio/07-RESEARCH.md
@.planning/phases/07-feedback-neon-vfx-audio/07-PATTERNS.md
@AGENTS.md
@src/services/storage/types.ts
@src/services/storage/asyncStorageStore.ts
@src/core/types.ts
@assets/sfx/

<interfaces>
From Expo SDK 57 audio docs (use these APIs only — not expo-av):
```typescript
import { createAudioPlayer, preload, setAudioModeAsync } from 'expo-audio';
await setAudioModeAsync({ playsInSilentMode: true });
preload(require('../../assets/sfx/brick_break.wav'));
const player = createAudioPlayer(source);
player.seekTo(0); player.play(); player.release();
```

SfxId union (D-20…D-24):
`'paddle_hit' | 'brick_chip' | 'brick_break' | 'powerup_catch' | 'life_lost' | 'win' | 'lose'`

Voice limits: brick_chip/brick_break=3; paddle_hit=2; powerup_catch=2; life_lost/win/lose=1

Volumes (linear gain, D-22):
life_lost/win/lose=1.0; brick_break=0.85; paddle_hit/brick_chip=0.7; powerup_catch=0.55
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: mapping.ts + AudioService types (RED→GREEN)</name>
  <files>src/services/audio/types.ts, src/services/audio/mapping.ts, src/services/audio/index.ts, tests/audio.mapping.test.ts</files>
  <read_first>src/core/types.ts, src/services/storage/types.ts, tests/audio.mapping.test.ts, .planning/phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md, AGENTS.md</read_first>
  <behavior>
    - mapEventToSfx(EventCode.PADDLE_HIT)==='paddle_hit'; BRICK_HIT→brick_chip; BRICK_BREAK→brick_break; POWERUP_CATCH→powerup_catch; LIFE_LOST→life_lost; WIN→win; LOSE→lose
    - mapEventToSfx(WALL_HIT) and mapEventToSfx(BALL_OUT) return null
    - SFX_VOLUME.life_lost === 1.0; SFX_VOLUME.brick_break === 0.85; SFX_VOLUME.paddle_hit === 0.7; SFX_VOLUME.powerup_catch === 0.55
    - selectVoiceIndex(cursor, poolLen) round-robin; at limit reuses oldest (cursor % len)
  </behavior>
  <action>
1. Create `src/services/audio/types.ts`:
   ```typescript
   export type SfxId =
     | 'paddle_hit'
     | 'brick_chip'
     | 'brick_break'
     | 'powerup_catch'
     | 'life_lost'
     | 'win'
     | 'lose';

   export interface AudioService {
     preload(): Promise<void>;
     playBatch(codes: ArrayLike<number>, count: number): void;
     release(): void;
   }
   ```

2. Create pure `src/services/audio/mapping.ts` with `mapEventToSfx`, `SFX_VOLUME` record matching hierarchy above, and `VOICE_LIMITS` record. No `expo-audio` import in this file.

3. Replace todos in `tests/audio.mapping.test.ts` with asserts for mapping + volume ordering:
   `SFX_VOLUME.life_lost >= SFX_VOLUME.brick_break >= SFX_VOLUME.paddle_hit >= SFX_VOLUME.powerup_catch`

4. Barrel `src/services/audio/index.ts` exporting types + mapping (service factory lands in Task 2).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/audio.mapping.test.ts && rg -n "mapEventToSfx|SFX_VOLUME" src/services/audio/mapping.ts && rg -n "playsInSilentMode|createAudioPlayer" src/services/audio/mapping.ts || true</automated>
  </verify>
  <acceptance_criteria>
    - `tests/audio.mapping.test.ts` has zero `it.todo`; vitest exits 0
    - `src/services/audio/mapping.ts` does **not** import `expo-audio`
    - `SFX_VOLUME` literals include `1`, `0.85`, `0.7`, `0.55` (or `1.0`)
    - `mapEventToSfx` switches on all seven gameplay EventCodes
  </acceptance_criteria>
  <done>Pure FX-03 mapping + volume hierarchy GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: expoAudioService pools + soft-fail</name>
  <files>src/services/audio/expoAudioService.ts, src/services/audio/index.ts, tests/audio.mapping.test.ts</files>
  <read_first>https://docs.expo.dev/versions/v57.0.0/sdk/audio/, src/services/storage/asyncStorageStore.ts, src/services/audio/mapping.ts, assets/sfx/paddle_hit.wav, AGENTS.md</read_first>
  <behavior>
    - createDefaultAudioService().preload() resolves even when native module missing (noop/memory impl)
    - playBatch with N rapid BRICK_HIT codes never creates more than VOICE_LIMITS.brick_chip players for that id
    - release() is idempotent
    - Does not use useAudioPlayer hook
  </behavior>
  <action>
1. Implement `src/services/audio/expoAudioService.ts` following storage soft-fail pattern:

   - `createExpoAudioService()` uses `createAudioPlayer` + `preload(require('../../../assets/sfx/<id>.wav'))` for each SfxId; `setAudioModeAsync({ playsInSilentMode: true })` during preload (D-24 / UI-SPEC).
   - Pool sizes from `VOICE_LIMITS`. On play: pick next voice (`cursor % len`), `seekTo(0)`, set volume from `SFX_VOLUME`, `play()`.
   - `createMemoryAudioService()` / fallback when import fails — records plays in an array for tests; never throws.
   - `createDefaultAudioService()` tries expo path, falls back to memory (UI-SPEC: preload failure soft-fail, no modal).

2. Add Vitest cases that inject a mock player factory (export `createAudioServiceWithPlayers(factory)` **or** test memory service play counts) proving oldest-reuse at limit (D-23).

3. Export factories from `src/services/audio/index.ts`.

4. Do **not** wire PlayingHost yet (Plan 05). Do **not** import from `core/` into services except reading EventCode numeric literals duplicated or imported — prefer importing `EventCode` from `../../core` **only if** eslint allows services→core; if banned, keep mapping on raw numbers `2,3,4,6,7,8,9` with comments. Check eslint: services allow only `services` today — **use numeric literals in mapping** to avoid new boundary, OR add `services → core` allow for types-only. Prefer **numeric literals + comments** citing EventCode names to avoid widening LC matrix in this plan.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/audio.mapping.test.ts && rg -n "createAudioPlayer|createDefaultAudioService|playsInSilentMode" src/services/audio/expoAudioService.ts && rg -n "useAudioPlayer" src/services/audio || true</automated>
  </verify>
  <acceptance_criteria>
    - `rg "useAudioPlayer" src/services/audio` returns no matches
    - `rg "createAudioPlayer" src/services/audio/expoAudioService.ts` matches
    - `rg "playsInSilentMode:\\s*true" src/services/audio/expoAudioService.ts` matches
    - `createDefaultAudioService` exported from `src/services/audio/index.ts`
    - vitest exits 0 including voice-reuse case
    - `rg "from '../../core'|from \"../../core\"" src/services/audio` returns no matches (numeric mapping only)
  </acceptance_criteria>
  <done>Modular AudioService with pools, soft-fail, and GREEN voice-limit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS AudioService ↔ native expo-audio | Soft-fail when native missing |
| playBatch input | Array of event codes from UI-thread batch — treat as trusted enum ints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Denial of Service | AudioPlayer alloc | mitigate | Fixed VOICE_LIMITS pools; release() on teardown; no per-hit `createAudioPlayer` |
| T-07-14 | Denial of Service | preload failure loops | mitigate | Soft-fail once to memory/noop; never block play with modal |
| T-07-15 | Elevation / Privacy | mic permission | mitigate | Already false in app.json (Plan 00); do not re-enable |
| T-07-16 | Spoofing / Legal | SFX assets | mitigate | Only `assets/sfx/*.wav` originals via require() |
</threat_model>

<verification>
`npx vitest run tests/audio.mapping.test.ts` exits 0; no `useAudioPlayer`; pools + soft-fail present.
</verification>

<success_criteria>
FX-03 AudioService is modular, pooled, soft-failing, and unit-tested without a device.
</success_criteria>

<output>
After completion, create `.planning/phases/07-feedback-neon-vfx-audio/07-03-SUMMARY.md`
</output>
