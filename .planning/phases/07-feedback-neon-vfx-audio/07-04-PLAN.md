---
phase: 07-feedback-neon-vfx-audio
plan: 04
type: execute
wave: 3
depends_on:
  - "00"
  - "02"
files_modified:
  - src/render/colors.ts
  - src/render/textures/bakeGlowSprites.ts
  - src/render/recordSprites.ts
autonomous: true
requirements:
  - FX-01
  - FX-02
must_haves:
  truths:
    - "Idle brick neon halo is blitted from baked SkImages — zero BlurMask in the brick loop"
    - "Draw order is navy→bricks+glow→cues→particles/flash→pickups→paddle→trail ghosts→ball"
    - "Cosmetic shake is canvas.translate inside letterbox save/restore only"
    - "Trail ghosts use white cores + cyan rim #67E8F9 on newest ghost when intensity≥0.75; never a Path ribbon"
  artifacts:
    - path: "src/render/textures/bakeGlowSprites.ts"
      provides: "bakeGlowSprites → SkImage map"
    - path: "src/render/recordSprites.ts"
      provides: "glow/trail/particle/shake draw integration"
    - path: "src/render/colors.ts"
      provides: "TRAIL_CYAN = #67E8F9"
  key_links:
    - from: "recordFrame"
      to: "VfxState"
      via: "optional vfx param / SharedValue read"
      pattern: "vfx|shake|trail"
    - from: "bakeGlowSprites"
      to: "brickFill colors"
      via: "SkImage per HP color ≤2 radius variants"
      pattern: "bakeGlow|drawAsImage|SkImage"
---

<objective>
Wire neon glow, ghost trails, particles, destroy flash, and cosmetic camera shake into the Skia `recordFrame` path using baked textures and UI-SPEC Game Token Priority — Shatter-inspired neon sci-fi art direction (D-01) via cyan/white tokens and baked halos.

Purpose: Spectacle must stay under the ball/paddle and never use live BlurMask (Android cliff / D-05).
Output: `bakeGlowSprites`, `TRAIL_CYAN`, extended `recordSprites.ts` consuming `VfxState`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md
@.planning/phases/07-feedback-neon-vfx-audio/07-CONTEXT.md
@.planning/phases/07-feedback-neon-vfx-audio/07-RESEARCH.md
@.planning/phases/07-feedback-neon-vfx-audio/07-PATTERNS.md
@src/render/recordSprites.ts
@src/render/colors.ts
@src/render/camera.ts
@src/vfx/index.ts

<interfaces>
Locked draw order (07-UI-SPEC):
```
1 letterbox/navy
2 idle brick fills + baked glow
3 damage cues
4 particles + destroy flash (≤100ms)
5 pickups
6 paddle
7 trail ghosts (oldest→newest)
8 ball
9 shake = translate on letterboxed group only
```

Colors:
- Trail core `#FFFFFF`; rim `#67E8F9`
- Particle body inherits brick HP colors; flecks `#FFFFFF` / `#67E8F9`
- Forbidden: live `BlurMask` / `#00ffaa` cliff color for glow
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TRAIL_CYAN + bakeGlowSprites</name>
  <files>src/render/colors.ts, src/render/textures/bakeGlowSprites.ts</files>
  <read_first>src/render/colors.ts, src/render/recordSprites.ts, .planning/phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md, .planning/phases/07-feedback-neon-vfx-audio/07-RESEARCH.md</read_first>
  <action>
1. In `src/render/colors.ts` add and export:
   ```typescript
   export const TRAIL_CYAN = '#67E8F9';
   ```
   Do not change existing HP / field / ball tokens.

2. Create `src/render/textures/bakeGlowSprites.ts` that, on the **JS cold path**, builds ≤2 radius halo `SkImage`s per brick fill color (`BRICK_HP3/HP2/HP1/UNBREAKABLE`) using Skia surface/`drawAsImage` (or equivalent Skia 2.12 API). Halo pad = 4–8 px (UI-SPEC xs/sm). Alpha band at full intensity ≈ 0.35–0.55 edge falloff.

3. Export `bakeGlowSprites(): GlowAtlas` (map key = color string or hp bucket → `{ soft: SkImage; strong?: SkImage }`). **Forbidden:** any function that applies `BlurMask` / `MaskFilter` during gameplay recordFrame.

4. Document in a one-line file comment: call from PlayingHost/level load **before** `setActive(true)` (wire in Plan 05).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "TRAIL_CYAN = '#67E8F9'" src/render/colors.ts && rg -n "bakeGlowSprites" src/render/textures/bakeGlowSprites.ts && ! rg -n "BlurMask|MaskFilter" src/render/textures/bakeGlowSprites.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/render/colors.ts` contains exact string `TRAIL_CYAN = '#67E8F9'`
    - `src/render/textures/bakeGlowSprites.ts` exports `bakeGlowSprites`
    - `rg "BlurMask" src/render/textures/bakeGlowSprites.ts` returns no matches
    - File references at most two radius variants in comments or identifiers (`soft`/`strong` or `r0`/`r1`)
  </acceptance_criteria>
  <done>Cyan token + baked glow atlas factory ready for recordFrame.</done>
</task>

<task type="auto">
  <name>Task 2: recordFrame VFX draw + shake translate</name>
  <files>src/render/recordSprites.ts</files>
  <read_first>src/render/recordSprites.ts, src/render/camera.ts, src/vfx/types.ts, src/vfx/trails.ts, src/vfx/particles.ts, src/vfx/shake.ts, .planning/phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md, .planning/phases/07-feedback-neon-vfx-audio/07-PATTERNS.md</read_first>
  <action>
1. Extend `recordFrame` signature to accept optional `vfx: VfxState | null`, `vfxIntensity: number`, and optional `glowAtlas` / flash state. When `vfx` is null, behavior matches pre-Phase-7 output (D-02 deletable). Visual language follows D-01 Shatter-inspired neon (baked halos, cyan rim `#67E8F9`, white cores).

2. Inside the existing `canvas.save(); translate(ox,oy); scale(...)` letterbox block, **after** scale, apply:
   `canvas.translate(shakeX, shakeY)` where offsets come from `shakeOffset(vfx.shakeAmp, …)` × intensity — **never** write World ball/paddle coordinates (D-19).

3. Draw order must match UI-SPEC Game Token Priority exactly:
   - Blit baked glow under/around each alive brick (scale glow alpha by `vfxIntensity`)
   - Existing brick fill + damage cues
   - Active particles as small circles (brick RGB + white/cyan flecks); destroy flash quad/circle alpha peak ≤0.45 @1.0 / ≤0.12 @0.2, duration gated by vfx flash life ≤100ms
   - Existing pickups → paddle
   - Trail ghosts: for each active ball, oldest→newest circles radius=`BALL_RADIUS`, core `#FFFFFF` alpha ramp ~0.15→0.55; cyan rim stroke `#67E8F9` **only** on newest ghost when `vfxIntensity >= 0.75`
   - Ball fill last at full white opacity

4. **Forbidden:** continuous Path ribbon trail; `BlurMask` in brick loop; drawing particles above paddle/ball; per-frame React state.

5. Keep worklet-local color literals where imports would become JS remotes (same pattern as `brickFillLocal`).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "TRAIL_CYAN|#67E8F9" src/render/recordSprites.ts && rg -n "shake|vfx" src/render/recordSprites.ts && ! rg -n "BlurMask" src/render/recordSprites.ts && rg -n "export function recordFrame" src/render/recordSprites.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `recordFrame` parameter list or nearby type includes `vfx` (grep `vfx` in function signature region)
    - `rg "BlurMask" src/render/recordSprites.ts` returns no matches
    - `rg "#67E8F9|TRAIL_CYAN" src/render/recordSprites.ts` matches
    - File contains trail draw before ball draw (ball loop/draw appears after trail mentions — verify with ordered line numbers via `rg -n "trail|ball"`)
    - Typecheck does not error on `src/render/recordSprites.ts` (fix any unrelated pre-existing errors outside this file)
  </acceptance_criteria>
  <done>Skia record path renders glow/trails/particles/shake under Game Token Priority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VfxState → Skia canvas | Cosmetic only; must not write World |
| Glow atlas images | Created on JS; read on UI thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Denial of Service | live BlurMask | mitigate | Baked SkImages only; acceptance bans BlurMask in recordSprites + bake file |
| T-07-18 | Denial of Service | unbounded particle draw | mitigate | Draw only `active` slots ≤ pool cap from Plan 02 |
| T-07-19 | Tampering | shake → sim coords | mitigate | translate in letterbox only; no World field writes in recordSprites |
| T-07-20 | Information disclosure | debug overlay | accept | Existing EXPO_PUBLIC_PERF_OVERLAY gate unchanged |
</threat_model>

<verification>
colors + bakeGlowSprites exports present; recordSprites has vfx/shake/cyan and no BlurMask; tsc clean for touched files.
</verification>

<success_criteria>
FX-01/FX-02 visuals are recorded into SkPicture with baked glow, ghost trails, budgeted particles, and cosmetic shake — deletable when vfx=null.
</success_criteria>

<output>
After completion, create `.planning/phases/07-feedback-neon-vfx-audio/07-04-SUMMARY.md`
</output>
