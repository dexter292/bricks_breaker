---
phase: 07-feedback-neon-vfx-audio
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app.json
  - eslint.config.js
  - docs/layer-contract.md
  - assets/sfx/paddle_hit.wav
  - assets/sfx/brick_chip.wav
  - assets/sfx/brick_break.wav
  - assets/sfx/powerup_catch.wav
  - assets/sfx/life_lost.wav
  - assets/sfx/win.wav
  - assets/sfx/lose.wav
  - tests/vfx.trails.test.ts
  - tests/vfx.particles.test.ts
  - tests/vfx.shake.test.ts
  - tests/vfx.intensity.test.ts
  - tests/events.fx.test.ts
  - tests/audio.mapping.test.ts
  - tests/runtime.event-drain.test.ts
  - .planning/phases/07-feedback-neon-vfx-audio/07-VALIDATION.md
autonomous: true
requirements:
  - FX-01
  - FX-02
  - FX-03
must_haves:
  truths:
    - "expo-audio is installed via npx expo install with mic/recording disabled in app.json plugin"
    - "ESLint allows runtime→vfx and render→vfx; scheduleOnRN banned everywhere except src/runtime/eventBridge.ts"
    - "Seven Wave 0 Vitest stub files exist so Plans 01–05 have automated verify targets"
    - "Seven original placeholder SFX wav files exist under assets/sfx/"
  artifacts:
    - path: "tests/vfx.trails.test.ts"
      provides: "FX-01 Nyquist stubs (it.todo)"
    - path: "tests/vfx.particles.test.ts"
      provides: "FX-02 particle Nyquist stubs"
    - path: "tests/vfx.shake.test.ts"
      provides: "FX-02 shake Nyquist stubs"
    - path: "tests/vfx.intensity.test.ts"
      provides: "FX-02 intensity Nyquist stubs"
    - path: "tests/events.fx.test.ts"
      provides: "FX-03 EventCode Nyquist stubs"
    - path: "tests/audio.mapping.test.ts"
      provides: "FX-03 audio mapping Nyquist stubs"
    - path: "tests/runtime.event-drain.test.ts"
      provides: "FX-03 multi-substep drain Nyquist stubs"
    - path: "eslint.config.js"
      provides: "runtime→vfx, render→vfx, eventBridge scheduleOnRN exception"
    - path: "docs/layer-contract.md"
      provides: "LC-07 amendment + LC-13 runtime→vfx / LC-14 render→vfx"
  key_links:
    - from: "07-VALIDATION.md Wave 0"
      to: "seven stub test files"
      via: "automated vitest paths resolve"
      pattern: "vfx\\.(trails|particles|shake|intensity)|events\\.fx|audio\\.mapping|runtime\\.event-drain"
---

<objective>
Wave 0 Nyquist + install: pin `expo-audio`, open VFX layer boundaries + one batched `scheduleOnRN` exception, author original placeholder SFX, and create `it.todo` stubs for all Phase 7 suites.

Purpose: Later plans must not claim MISSING test files; Expo 57 audio must land before AudioService; LC-07 must not block the required audio hop.
Output: Lockfile pin, app.json plugin (mic off), eslint + layer-contract update, seven stub tests, seven wav placeholders, VALIDATION.md Wave 0 marked complete.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-feedback-neon-vfx-audio/07-CONTEXT.md
@.planning/phases/07-feedback-neon-vfx-audio/07-RESEARCH.md
@.planning/phases/07-feedback-neon-vfx-audio/07-VALIDATION.md
@.planning/phases/07-feedback-neon-vfx-audio/07-PATTERNS.md
@.planning/phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md
@docs/layer-contract.md
@eslint.config.js
@AGENTS.md
@app.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-audio + LC-07/vfx boundaries + placeholder SFX</name>
  <files>package.json, package-lock.json, app.json, eslint.config.js, docs/layer-contract.md, assets/sfx/paddle_hit.wav, assets/sfx/brick_chip.wav, assets/sfx/brick_break.wav, assets/sfx/powerup_catch.wav, assets/sfx/life_lost.wav, assets/sfx/win.wav, assets/sfx/lose.wav</files>
  <read_first>AGENTS.md, https://docs.expo.dev/versions/v57.0.0/sdk/audio/, app.json, eslint.config.js, docs/layer-contract.md, .planning/phases/07-feedback-neon-vfx-audio/07-RESEARCH.md, package.json</read_first>
  <action>
1. Install with Expo pin only (per AGENTS.md / RESEARCH — never `npm install expo-av`):
   ```bash
   npx expo install expo-audio
   ```
   Assert `package.json` resolves to a `~57.0.x` or `57.0.x` pin matching SDK 57. Do **not** install `expo-av`, `react-native-sound`, or `react-native-audio-api` in this plan.

2. In `app.json` `expo.plugins`, append (mic/recording off per D-20 / T-07-01):
   ```json
   [
     "expo-audio",
     {
       "microphonePermission": false,
       "recordAudioAndroid": false,
       "enableBackgroundPlayback": false
     }
   ]
   ```

3. In `eslint.config.js`:
   - Change `runtime` allow `anyOf` from `['core', 'runtime', 'render']` to `['core', 'runtime', 'render', 'vfx']` (per D-02 / RESEARCH LC amendment).
   - Change `render` allow `anyOf` from `['core', 'render']` to `['core', 'render', 'vfx']`.
   - Keep `runtime → services` **disallowed**.
   - Narrow LC-07: keep the global `no-restricted-syntax` ban on `scheduleOnRN` / `runOnJS` for `src/runtime/**` and `src/render/**`, but add a **file-scoped override** for `src/runtime/eventBridge.ts` only that turns those selectors off (or allowlists them). Do **not** disable the ban on the whole `runtime/` folder.

4. Update `docs/layer-contract.md`:
   - Amend LC-07 Notes to: `No per-frame JS hops except ≤1 batched scheduleOnRN/frame from src/runtime/eventBridge.ts for audio drain (Phase 7)`.
   - Add allowed row **LC-13**: `runtime/` → `vfx/` | Direct worklet call | stepVfx / consumeEvents after each stepRun | ESLint boundaries
   - Add allowed row **LC-14**: `render/` → `vfx/` | Read-only VfxState | recordFrame draws trails/particles/shake offset | ESLint boundaries

5. Create `assets/sfx/` with seven **original** short mono PCM WAV placeholders (≤200ms each) named exactly:
   `paddle_hit.wav`, `brick_chip.wav`, `brick_break.wav`, `powerup_catch.wav`, `life_lost.wav`, `win.wav`, `lose.wav`.
   Generate via `sox` or a tiny Node script writing RIFF WAV — **not** third-party game samples (D-20). Distinct filenames are enough for wiring; tone content may be simple beeps.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; node -e "const p=require('./node_modules/expo-audio/package.json'); if(!String(p.version).startsWith('57.')) process.exit(1); console.log('expo-audio', p.version)" && rg -n '"expo-audio"' app.json && rg -n "microphonePermission.: false|recordAudioAndroid.: false" app.json && rg -n "anyOf: \['core', 'runtime', 'render', 'vfx'\]" eslint.config.js && rg -n "anyOf: \['core', 'render', 'vfx'\]" eslint.config.js && rg -n "eventBridge" eslint.config.js && rg -n "LC-13|LC-14|eventBridge" docs/layer-contract.md && test -f assets/sfx/paddle_hit.wav && test -f assets/sfx/brick_break.wav && test -f assets/sfx/lose.wav</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/expo-audio/package.json` version starts with `57.`
    - `rg "expo-av" package.json` returns no matches
    - `app.json` contains plugin entry `"expo-audio"` with `microphonePermission: false` and `recordAudioAndroid: false`
    - `eslint.config.js` runtime allow-list includes `vfx`; render allow-list includes `vfx`
    - `eslint.config.js` references `eventBridge` in an override files glob
    - `docs/layer-contract.md` contains `LC-13` and `LC-14`
    - All seven files exist under `assets/sfx/` with exact names above
  </acceptance_criteria>
  <done>expo-audio installed; mic off; LC-13/14 + eventBridge exception; original SFX placeholders present.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 it.todo stubs + VALIDATION.md</name>
  <files>tests/vfx.trails.test.ts, tests/vfx.particles.test.ts, tests/vfx.shake.test.ts, tests/vfx.intensity.test.ts, tests/events.fx.test.ts, tests/audio.mapping.test.ts, tests/runtime.event-drain.test.ts, .planning/phases/07-feedback-neon-vfx-audio/07-VALIDATION.md</files>
  <read_first>.planning/phases/07-feedback-neon-vfx-audio/07-VALIDATION.md, .planning/phases/07-feedback-neon-vfx-audio/07-RESEARCH.md, tests/runtime.freeze.test.ts, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-00-PLAN.md</read_first>
  <action>
Create seven Vitest stub files that import only `{ describe, it } from 'vitest'` — do **not** import `src/vfx/**` or `src/services/audio/**` yet.

1. `tests/vfx.trails.test.ts` (FX-01 / 07-W0-01):
   - `it.todo('trailLength(1.0) === 5')`
   - `it.todo('trailLength(0.2) === 2')`
   - `it.todo('trailLength never returns 0')`
   - `it.todo('pushTrail respects ring modulus and does not grow arrays')`

2. `tests/vfx.particles.test.ts` (FX-02 / 07-W0-02):
   - `it.todo('spawn chip count at intensity 1.0 is 4')`
   - `it.todo('spawn destroy count at intensity 1.0 is 12')`
   - `it.todo('hard cap 128 with oldest-eviction; never exceeds 192')`
   - `it.todo('spawn counts scale with round(base × intensity)')`

3. `tests/vfx.shake.test.ts` (FX-02 / 07-W0-03):
   - `it.todo('punchShake merges with max then caps at 2.5')`
   - `it.todo('stepShake multiplies amp by 0.85 and zeros below 0.05')`
   - `it.todo('intensity 0.2 → amp approaches 0')`
   - `it.todo('punch only for BRICK_BREAK / LIFE_LOST impulses')`

4. `tests/vfx.intensity.test.ts` (FX-02 / 07-W0-04):
   - `it.todo('intensityFromReduceMotion(false) === 1.0')`
   - `it.todo('intensityFromReduceMotion(true) === 0.2')`
   - `it.todo('clampIntensity clamps to [0,1]')`

5. `tests/events.fx.test.ts` (FX-03 / 07-W0-05):
   - `it.todo('EventCode includes POWERUP_CATCH LIFE_LOST WIN LOSE')`
   - `it.todo('pickup catch pushes POWERUP_CATCH')`
   - `it.todo('life decrement pushes LIFE_LOST')`
   - `it.todo('applyWinCheck pushes WIN; final life pushes LOSE')`

6. `tests/audio.mapping.test.ts` (FX-03 / 07-W0-06):
   - `it.todo('mapEventToSfx maps seven gameplay codes; ignores WALL_HIT/BALL_OUT')`
   - `it.todo('SFX_VOLUME hierarchy: life/win/lose > break > paddle/chip > catch')`
   - `it.todo('voice pool reuses oldest at per-category limit')`

7. `tests/runtime.event-drain.test.ts` (FX-03 / 07-W0-07):
   - `it.todo('snapshot after each stepRun retains events across clearEvents')`
   - `it.todo('audio batch length equals sum of per-substep event counts')`

8. Update `07-VALIDATION.md`:
   - `wave_0_complete: true` (leave `nyquist_compliant: false` until phase gate)
   - Check Wave 0 boxes for all seven stubs + expo-audio + ESLint
   - Replace TBD Task IDs with `07-W0-01`…`07-W0-07`, Plan `00`, File Exists ✅
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/vfx.trails.test.ts tests/vfx.particles.test.ts tests/vfx.shake.test.ts tests/vfx.intensity.test.ts tests/events.fx.test.ts tests/audio.mapping.test.ts tests/runtime.event-drain.test.ts && rg -n "wave_0_complete: true" .planning/phases/07-feedback-neon-vfx-audio/07-VALIDATION.md && rg -n "it\\.todo" tests/vfx.trails.test.ts tests/events.fx.test.ts tests/audio.mapping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All seven test files exist under `tests/`
    - Each file has ≥1 `it.todo` and zero `expect(` calls
    - No stub imports `src/vfx` or `src/services/audio`
    - Command exits 0
    - VALIDATION frontmatter `wave_0_complete: true`
  </acceptance_criteria>
  <done>Nyquist Wave 0 stubs + VALIDATION updated for FX-01/02/03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native audio module ↔ JS app | expo-audio can request mic/recording permissions |
| ESLint layer matrix ↔ runtime | scheduleOnRN hop is a controlled trust crossing UI→JS |
| Asset bundle ↔ originality | Third-party SFX would violate store/IP constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation / Privacy | expo-audio plugin | mitigate | `microphonePermission: false`, `recordAudioAndroid: false`, `enableBackgroundPlayback: false` in app.json |
| T-07-02 | Denial of Service | scheduleOnRN | mitigate | File-scoped allowlist only for `eventBridge.ts`; ban remains on rest of runtime/render |
| T-07-03 | Spoofing / Legal | assets/sfx | mitigate | Original placeholder WAVs only; no third-party game samples |
| T-07-04 | Information disclosure | package install | mitigate | Install only `expo-audio` via expo install; reject `expo-av` |
| T-07-05 | Tampering | layer contract | mitigate | Document LC-13/14; keep core→services banned |
</threat_model>

<verification>
`npx vitest run` on seven stub files exits 0; expo-audio 57.x present; eslint/runtime allow-list includes vfx; seven wav files exist.
</verification>

<success_criteria>
Wave 0 complete: expo-audio pinned, boundaries open for VFX + one audio hop, stub suites green with `it.todo`, original SFX placeholders present, VALIDATION.md updated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-feedback-neon-vfx-audio/07-00-SUMMARY.md`
</output>
