---
phase: 04-level-format-brick-types
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/core/levels/damageCues.ts
  - src/core/index.ts
  - src/render/recordSprites.ts
  - tests/levels.damage-cues.test.ts
autonomous: true
requirements:
  - LVL-02
  - LVL-03
must_haves:
  truths:
    - "Breakable remaining HP 3/2/1 map to distinct crack stroke plans (0/1/2 lines) without relying on color"
    - "Unbreakable bricks use a fixed hatch plan and never receive crack strokes"
    - "Crack/hatch drawing happens inside recordFrame brick loop after fill — no React HP overlays"
  artifacts:
    - path: "src/core/levels/damageCues.ts"
      provides: "pure stroke-plan helper for cracks/hatch"
      exports: ["planBrickDamageCues", "BrickCueStroke"]
    - path: "src/render/recordSprites.ts"
      provides: "Skia drawLine crack/hatch in entity layer"
    - path: "tests/levels.damage-cues.test.ts"
      provides: "LVL-02 stroke-count unit coverage"
  key_links:
    - from: "recordSprites.ts brick loop"
      to: "planBrickDamageCues / inline equivalent"
      via: "flags-first branch then hp cracks"
      pattern: "drawLine"
    - from: "damageCues"
      to: "BrickFlags.UNBREAKABLE"
      via: "hatch when flags&amp;1; never crack"
      pattern: "UNBREAKABLE|flags"
---

<objective>
Add lightweight non-color damage readability: pure `damageCues` stroke plans unit-tested in Node, and Skia `drawLine` crack/hatch strokes in `recordFrame` after brick fills (D-05…D-08, LVL-02, LVL-03 visuals).

Purpose: Remaining HP readable without color alone; steel stays visually structural.
Output: `damageCues.ts`, extended `recordSprites.ts`, green damage-cues tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-level-format-brick-types/04-CONTEXT.md
@.planning/phases/04-level-format-brick-types/04-RESEARCH.md
@.planning/phases/04-level-format-brick-types/04-PATTERNS.md
@src/render/recordSprites.ts
@src/render/colors.ts
@.planning/phases/03-first-playable-render-input-bricks-lives-pause/03-UI-SPEC.md

## Locked decisions
- D-05: crack/chip lines for remaining HP; lightweight
- D-06: steel/gray + fixed hatch; never morph into damage cracks
- D-07: cues in Skia entity layer inside recordFrame — no React overlays / no per-frame React HP state
- D-08: no neon/particles (Phase 7)

## Discretion defaults (lock in damageCues)
- Breakable: `hp >= 3` → 0 strokes; `hp === 2` → 1 chip; `hp === 1` → 2 cracks
- Unbreakable: 2–3 diagonal hatch segments in local brick AABB; ignore hp for stroke choice
- Stroke color in Skia: light gray/white (~`#E5E7EB` or `#F9FAFB`) — keep fills from Phase 3 palette

## Palette (unchanged fills)
- HP3 `#C44569`, HP2 `#E07A5F`, HP1 `#F2CC8F`, steel `#6B7280`

<interfaces>
From src/render/recordSprites.ts (existing):
```typescript
function brickFillLocal(hp: number, flags: number): string {
  'worklet';
  if ((flags & 1) !== 0) return '#6B7280';
  // …
}
// brick loop: drawRect fill — extend AFTER fill with stroke lines
```

Recommended pure helper (core — Node testable):
```typescript
export type BrickCueStroke = { x0: number; y0: number; x1: number; y1: number };
export function planBrickDamageCues(args: {
  x: number; y: number; w: number; h: number;
  hp: number; flags: number;
}): BrickCueStroke[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure damageCues stroke plans + Vitest</name>
  <files>src/core/levels/damageCues.ts, src/core/index.ts, tests/levels.damage-cues.test.ts</files>
  <read_first>.planning/phases/04-level-format-brick-types/04-RESEARCH.md, src/core/types.ts, docs/layer-contract.md</read_first>
  <behavior>
    - flags UNBREAKABLE → returns hatch strokes length ≥ 2; never equals breakable hp===1 plan
    - hp≥3 breakable → length 0
    - hp===2 → length 1; hp===1 → length 2
    - hp≤0 → length 0 (destroyed / skip)
    - All stroke endpoints lie within [x,x+w]×[y,y+h] expanded by ≤1px tolerance
  </behavior>
  <action>
1. Create `src/core/levels/damageCues.ts` — pure TS, no Skia, `'worklet'`-safe optional (prefer pure without worklet directive so Node tests import easily; if marked worklet, still callable from Node).

2. Implement `planBrickDamageCues` per discretion defaults. Flags check **first** (D-06). Use `BrickFlags.UNBREAKABLE` from `../types`.

3. Export from `src/core/index.ts`.

4. Replace todos in `tests/levels.damage-cues.test.ts` with green assertions covering `<behavior>`.

No changes to recordSprites in this task.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/levels.damage-cues.test.ts && npx eslint src/core/levels/damageCues.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `planBrickDamageCues` exported from core barrel
    - Damage-cues vitest exits 0
    - `rg -n "skia|react-native" src/core/levels/damageCues.ts -i` finds no matches
  </acceptance_criteria>
  <done>LVL-02 stroke plans unit-tested without Skia.</done>
</task>

<task type="auto">
  <name>Task 2: Skia drawLine crack/hatch in recordFrame</name>
  <files>src/render/recordSprites.ts</files>
  <read_first>src/render/recordSprites.ts, src/core/levels/damageCues.ts, .planning/phases/04-level-format-brick-types/04-PATTERNS.md</read_first>
  <action>
Extend the brick draw loop in `recordSprites.ts` **after** `canvas.drawRect` fill (alive bricks only, `hp > 0`):

1. Prefer **inlined** stroke geometry mirroring `planBrickDamageCues` (same constants) inside the worklet — imported core helpers can be JS remotes under worklets (Phase 3 pattern with `brickFillLocal`). Inline the stroke coordinates OR call a local `'worklet'` function `planBrickDamageCuesLocal` duplicated from damageCues with a comment `// keep in sync with src/core/levels/damageCues.ts`.

2. Set paint to stroke style, light gray color, thin stroke width (~1–1.5), `canvas.drawLine` for each segment (Skia 2.12.0 API — RESEARCH).

3. Flags-first: unbreakable → hatch only; never draw crack plan on steel (D-06).

4. Do **not** add React components, SharedValue HP mirrors, or glow/particles (D-07, D-08).

5. LC-08: read World only; no mutation.

Keep Phase 3 fill colors via `brickFillLocal`.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "drawLine" src/render/recordSprites.ts && npm run test:core && npx eslint src/render/recordSprites.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "drawLine" src/render/recordSprites.ts` matches ≥1
    - Brick loop still skips `hp &lt;= 0`
    - Unbreakable branch draws hatch before/without crack hp branches
    - No new overlay components under `src/runtime/overlays` for HP
    - `npm run test:core` exits 0
  </acceptance_criteria>
  <done>Skia entity layer paints non-color HP/structural cues.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| World SoA → render | Render reads hp/flags only; must not trust authoring JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | stroke count per brick | mitigate | Fixed small stroke plans (≤3 lines/brick); no unbounded loops from JSON |
| T-04-02 | Tampering | hp/flags in SoA | accept | Physics already owns values; render is read-only (LC-08) |
| T-04-03 | Tampering | World via render | mitigate | No World writes in recordSprites (LC-08) |
| T-04-04 | Tampering | N/A in render | accept | Authoring keys never reach render path |
</threat_model>

<verification>
`npx vitest run tests/levels.damage-cues.test.ts` + `npm run test:core`; `drawLine` present in recordSprites.
</verification>

<success_criteria>
LVL-02 non-color cues implemented and unit-tested; LVL-03 steel hatch distinct from damage cracks; no Phase 7 VFX creep.
</success_criteria>

<output>
After completion, create `.planning/phases/04-level-format-brick-types/04-03-SUMMARY.md`
</output>
