---
phase: 08-showpiece-level-performance-certification-launch-baseline
plan: 01
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - assets/levels/level-03.json
  - src/runtime/loadLevel.ts
  - app/_components/PlayingHost.tsx
  - tests/levels.compile.test.ts
  - tests/runtime.loadLevel.test.ts
autonomous: true
requirements:
  - LVL-04
must_haves:
  truths:
    - "Title → Play loads level-03 by default through the shared validate→compile pipeline"
    - "level-03 has three-act escalation with plateaus and an unbreakable Act 3 pocket"
    - "level-01 and level-02 remain unchanged regression fixtures"
    - "DEV-only level switch cycles 01/02/03 and is absent when __DEV__ is false"
  artifacts:
    - path: "assets/levels/level-03.json"
      provides: "Showpiece schema v1 level (D-05)"
      contains: "\"id\": \"level-03\""
    - path: "src/runtime/loadLevel.ts"
      provides: "LevelId level-03 + default load"
      exports: ["LevelId", "loadLevelById"]
    - path: "app/_components/PlayingHost.tsx"
      provides: "default levelId level-03 + DEV 3-way switch"
  key_links:
    - from: "PlayingHost useState levelId"
      to: "loadLevelById(levelId)"
      via: "useMemo loadResult"
      pattern: "loadLevelById\\(levelId\\)|useState<LevelId>\\('level-03'\\)"
    - from: "loadLevelById"
      to: "loadAndCompile"
      via: "Metro static require LEVEL_MODULES"
      pattern: "level-03.*require.*level-03\\.json"
---

<objective>
Author `level-03` showpiece, make it the Title→Play default, keep 01/02 fixtures unchanged, and extend the DEV-only level switch to cycle all three levels (LVL-04 / D-01…D-08).

Purpose: Deliver the MVP challenge level through the existing pipeline with no new mechanics or drop rules.
Output: `assets/levels/level-03.json`, updated `loadLevel.ts` + PlayingHost, green compile/loadLevel tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-showpiece-level-performance-certification-launch-baseline/08-CONTEXT.md
@.planning/phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md
@.planning/phases/08-showpiece-level-performance-certification-launch-baseline/08-PATTERNS.md
@assets/levels/level-02.json
@src/runtime/loadLevel.ts
@app/_components/PlayingHost.tsx
@src/core/levels/schema.ts
@tests/levels.compile.test.ts

<interfaces>
From src/runtime/loadLevel.ts (pre-Plan-01):
```typescript
export type LevelId = 'level-01' | 'level-02';
export function loadLevelById(id: LevelId = 'level-01'): LoadLevelResult;
```

From src/core/levels/load.ts:
```typescript
export function loadAndCompile(raw: unknown): LoadLevelResult;
```

Schema v1 brickTypes keys: `1|2|3|X` only; empty cell `.`; `cols*rows ≤ 256`; non-empty bricks ≤ MAX_BRICKS (256).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author level-03.json + compile fingerprint tests</name>
  <files>assets/levels/level-03.json, tests/levels.compile.test.ts</files>
  <read_first>assets/levels/level-01.json, assets/levels/level-02.json, src/core/levels/schema.ts, src/core/levels/validate.ts, tests/levels.compile.test.ts, .planning/phases/08-showpiece-level-performance-certification-launch-baseline/08-CONTEXT.md, .planning/phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md</read_first>
  <behavior>
    - level-03 loadAndCompile returns ok:true
    - compiled has ≥1 UNBREAKABLE brick
    - cols*rows ≤ 256 and brickCount ≤ 256
    - structural fingerprint ≠ level-01 and ≠ level-02
    - level-01 and level-02 existing expectations stay green (files unchanged)
  </behavior>
  <action>
1. Create `assets/levels/level-03.json` as schemaVersion 1 showpiece (D-05). Discretionary layout MUST encode three acts + short plateaus (D-01) and target a **~2–3 minute successful clear via layout density only** — no new mechanics (D-02):
   - **Act 1 (top rows):** mostly `1` bricks, sparse gaps — accessible opening.
   - **Plateau A:** 1–2 lighter rows (more `.` / single HP) between Act 1 and Act 2.
   - **Act 2 (middle):** denser `2`/`3` multi-HP clusters.
   - **Plateau B:** short breathing row(s).
   - **Act 3 (bottom):** pocket shaped by `X` unbreakables that forces angled paddle play (D-01, D-04) — solvable without relying on power-up RNG.
   - Use only brickTypes `1`,`2`,`3`,`X` matching level-02 shape; `id`=`"level-03"`; name e.g. `"Neon Gauntlet"`.
   - Hard caps: `cols * rows ≤ 256`, non-empty brick count ≤ 256 (RESEARCH Pitfall 6). Suggested ~9×14 or 10×16 within cap — dense enough that a clean clear feels ~2–3 minutes (D-02), verified later in Plan 06 human play.
   - Do **not** add drop-rate fields or level-specific mechanics (D-03). Do **not** modify `level-01.json` / `level-02.json` (D-05).

2. Convert Wave 0 `it.todo` entries in `tests/levels.compile.test.ts` into real tests:
   - `loadAndCompile(level-03)` ok + UNBREAKABLE count ≥ 1
   - fingerprint differs from 01 and 02
   - `cols*rows ≤ 256` and `brickCount ≤ 256`
   - Optionally assert ≥2 distinct breakable HP values appear (multi-HP mid-act signal)

3. Run RED→GREEN: todos fail until JSON exists; then pass.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npm test -- tests/levels.compile.test.ts && git diff --exit-code -- assets/levels/level-01.json assets/levels/level-02.json</automated>
  </verify>
  <acceptance_criteria>
    - `assets/levels/level-03.json` exists with `"id": "level-03"` and `"schemaVersion": 1`
    - `rg '"X"' assets/levels/level-03.json` matches (unbreakable glyphs present)
    - `npm test -- tests/levels.compile.test.ts` exits 0
    - `git diff --exit-code -- assets/levels/level-01.json assets/levels/level-02.json` exits 0 (unchanged)
    - No `it.todo` remaining for level-03 cases in that file
  </acceptance_criteria>
  <done>level-03 authored and compile fingerprint tests green; 01/02 untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Default loadLevel-03 + DEV three-way switch</name>
  <files>src/runtime/loadLevel.ts, app/_components/PlayingHost.tsx, tests/runtime.loadLevel.test.ts</files>
  <read_first>src/runtime/loadLevel.ts, app/_components/PlayingHost.tsx, tests/runtime.loadLevel.test.ts, .planning/phases/08-showpiece-level-performance-certification-launch-baseline/08-PATTERNS.md</read_first>
  <behavior>
    - LevelId union includes 'level-03'
    - loadLevelById() with no args returns compiled level-03
    - loadLevelById('level-03') ok:true
    - PlayingHost initial useState LevelId is 'level-03'
    - DEV toggle cycles level-01 → level-02 → level-03 → level-01
    - Production path: no Pressable when __DEV__ is false
  </behavior>
  <action>
1. Update `src/runtime/loadLevel.ts` (D-06, D-08):
   ```typescript
   export type LevelId = 'level-01' | 'level-02' | 'level-03';
   const LEVEL_MODULES: Record<LevelId, unknown> = {
     'level-01': require('../../assets/levels/level-01.json'),
     'level-02': require('../../assets/levels/level-02.json'),
     'level-03': require('../../assets/levels/level-03.json'), // literal Metro path
   };
   export function loadLevelById(id: LevelId = 'level-03'): LoadLevelResult { ... }
   ```
   Never use dynamic `require(path)`.

2. Update `PlayingHost.tsx` (D-06, D-07):
   - Change `useState<LevelId>('level-01')` → `useState<LevelId>('level-03')`.
   - Replace binary `toggleDevLevel` with cycle: `level-01` → `level-02` → `level-03` → `level-01`.
   - Keep the Pressable wrapped in `typeof __DEV__ !== 'undefined' && __DEV__` only — unavailable in production (D-07).
   - Update label text to show current id (e.g. `Lv 03`).
   - Do **not** add production level-select UI (deferred).

3. Convert `tests/runtime.loadLevel.test.ts` todos to real Vitest cases importing `loadLevelById` / `LevelId` from `src/runtime/loadLevel.ts`.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npm test -- tests/runtime.loadLevel.test.ts tests/levels.compile.test.ts && rg -n "level-03" src/runtime/loadLevel.ts && rg -n "useState<LevelId>\\('level-03'\\)|__DEV__" app/_components/PlayingHost.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/runtime/loadLevel.ts` LevelId union includes `level-03` and default arg is `'level-03'`
    - `rg "require\\('../../assets/levels/level-03.json'\\)" src/runtime/loadLevel.ts` matches
    - `PlayingHost` initial state is `useState<LevelId>('level-03')`
    - DEV switch Pressable remains inside `__DEV__` guard
    - `npm test -- tests/runtime.loadLevel.test.ts` exits 0
  </acceptance_criteria>
  <done>Default boot is level-03; DEV cycles all three; pipeline unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled level JSON ↔ World SoA | Authoring data enters via validate→compile only |
| DEV UI ↔ production players | Level switch must not ship in production builds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | assets/levels/level-03.json | mitigate | Must pass `validateLevel` fail-closed via `loadAndCompile`; PlayingHost already gates on `loadResult.ok` |
| T-08-02 | Tampering | brickTypes keys | mitigate | Reuse schema keys only; existing validate rejects `__proto__`/`constructor`/`prototype` — do not weaken |
| T-08-03 | Elevation | PlayingHost DEV switch | mitigate | Gate with `typeof __DEV__ !== 'undefined' && __DEV__` only (D-07) |
| T-08-04 | Information disclosure | level names | accept | No PII; cosmetic name string only |
| T-08-05 | Denial of Service | oversized grid | mitigate | Enforce MAX_BRICKS via validation + Vitest caps |
</threat_model>

<verification>
`npm test -- tests/levels.compile.test.ts tests/runtime.loadLevel.test.ts` green; level-01/02 unchanged; default LevelId is level-03.
</verification>

<success_criteria>
LVL-04 authored: three-act showpiece loads by default through shared pipeline; DEV-only 3-way switch; fixtures 01/02 untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/08-showpiece-level-performance-certification-launch-baseline/08-01-SUMMARY.md`
</output>
