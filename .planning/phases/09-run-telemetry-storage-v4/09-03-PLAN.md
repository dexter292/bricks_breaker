---
phase: 09-run-telemetry-storage-v4
plan: 03
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - src/services/storage/memoryStore.ts
  - src/services/storage/asyncStorageStore.ts
  - tests/storage.progress-v4.test.ts
autonomous: true
requirements:
  - N-STAT-01
  - N-STAT-02
must_haves:
  truths:
    - "recordRunEnd accepts mode/outcome-incl-abandoned/stats and merges them into telemetry on both stores, using the exact same sync-merge/void-persist/return-clone contract as today"
    - "The AsyncStorage hydrate chain reads the v4 key first, then falls back through v3/v2/v1, exactly mirroring today's v3/v2/v1 fallback shape"
    - "Lifetime and per-(mode, levelId) aggregates survive a simulated app kill (store re-instantiated from a persisted v4 JSON string)"
    - "Zero it.todo remain in tests/storage.progress-v4.test.ts"
  artifacts:
    - path: "src/services/storage/memoryStore.ts"
      provides: "createMemoryProgressStore with v4-shaped recordRunEnd + telemetry merge"
    - path: "src/services/storage/asyncStorageStore.ts"
      provides: "AsyncStorage-backed ProgressStore reading @nbb/progress/v4 first, migrate-on-read through v3/v2/v1"
    - path: "tests/storage.progress-v4.test.ts"
      provides: "GREEN N-STAT-02 unit coverage, zero it.todo remaining"
  key_links:
    - from: "PlayingHost.tsx (Plan 04)"
      to: "ProgressStore.recordRunEnd"
      via: "store.recordRunEnd({ mode, levelId, score, outcome, livesRemaining, stats })"
      pattern: "recordRunEnd\\(\\{"
    - from: "asyncStorageStore.ensureHydrated"
      to: "migrateOrDefault (v4) + mergeHighWatermark (v4)"
      via: "PROGRESS_KEY (v4) read first, fallback reads PROGRESS_KEY_V3/PROGRESS_KEY_V2/PERSONAL_BEST_KEY"
      pattern: "migrateOrDefault\\(v4Raw, v3Raw, v2Raw, v1Raw\\)"
---

<objective>
Extend both `ProgressStore` implementations (`memoryStore.ts`, `asyncStorageStore.ts`) to the v4
`recordRunEnd` contract from Plan 02, extend the AsyncStorage hydrate chain one key deeper, and
turn every remaining `it.todo` in `tests/storage.progress-v4.test.ts` green.

Purpose: this is the actual persistence path N-STAT-01's counters and N-STAT-02's migration flow
through at runtime.
Output: both stores extended, `tests/storage.progress-v4.test.ts` fully green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-run-telemetry-storage-v4/09-CONTEXT.md
@.planning/phases/09-run-telemetry-storage-v4/09-RESEARCH.md
@.planning/phases/09-run-telemetry-storage-v4/09-PATTERNS.md
@src/services/storage/memoryStore.ts
@src/services/storage/asyncStorageStore.ts
@src/services/storage/types.ts
@src/services/storage/telemetry.ts

<interfaces>
<!-- Contracts Plan 02 already created — use directly, no re-derivation. -->

// src/services/storage/types.ts (Plan 02)
export type GameMode = 'campaign' | 'endless' | 'daily';
export type RunOutcome = 'win' | 'lose' | 'abandoned';
export type RunStatsInput = { bricksBroken, bestCombo, pickupMultiball, pickupExpand, pickupExtraLife, pickupSlow, pickupFireball, livesLost, longestRally, largestCascade, ticksPlayed, wallClockMs: number };
export interface ProgressStore {
  recordRunEnd(args: { mode: GameMode; levelId: LevelId; score: number; outcome: RunOutcome; livesRemaining: number; stats: RunStatsInput }): ProgressBlob;
  // getBest/getBestForLevel/recordLevelBest/unlockAfterClear/isUnlocked/getSnapshot/flush — unchanged
}
export const PROGRESS_KEY = '@nbb/progress/v4';
export const PROGRESS_KEY_V3 = '@nbb/progress/v3';
export const PROGRESS_KEY_V2 = '@nbb/progress/v2'; // unchanged from before this phase

// src/services/storage/migrateProgress.ts (Plan 02)
export function migrateOrDefault(v4Raw: string | null, v3Raw: string | null, v2Raw: string | null, v1Raw: string | null): ProgressBlob;

// src/services/storage/watermark.ts (Plan 02)
export function mergeHighWatermark(memory: ProgressBlob, incoming: ProgressBlob): ProgressBlob;

// src/services/storage/telemetry.ts (Plan 02)
export function mergeRunIntoTelemetry(telemetry: TelemetryBlob, args: { mode: GameMode; levelId: string; outcome: RunOutcome; score: number; stats: RunStatsInput }): TelemetryBlob;
export function cloneTelemetryBlob(t: TelemetryBlob): TelemetryBlob;
</interfaces>

<interfaces>
<!-- Current memoryStore.ts / asyncStorageStore.ts recordRunEnd + cloneBlob shape (verified by direct read) -->

// memoryStore.ts current cloneBlob (v3-shaped) — extend to deep-copy telemetry too:
// function cloneBlob(b: ProgressBlob): ProgressBlob {
//   const bestByLevel = {...}; return { v: 3, unlocked: [...b.unlocked], bestByLevel, bestScore: b.bestScore, updatedAt: b.updatedAt };
// }
// recordRunEnd currently: computes starsFromWin only when outcome==='win', applyLevelBest, unlocks only on 'win', returns cloneBlob(blob).

// asyncStorageStore.ts current ensureHydrated (v3-shaped) — extend the read chain by one key:
// async function ensureHydrated() {
//   const v3Raw = await AsyncStorage.getItem(PROGRESS_KEY);  // <- becomes v4Raw, key is now PROGRESS_KEY (v4)
//   const parsed = parseProgressResult(v3Raw);                // <- now the v4 parser
//   if (parsed.status === 'ok') { memory = mergeHighWatermark(memory, parsed.progress); hydrated = true; return; }
//   const v2Raw = await AsyncStorage.getItem(PROGRESS_KEY_V2); // <- add v3Raw = await AsyncStorage.getItem(PROGRESS_KEY_V3) here too
//   const v1Raw = await AsyncStorage.getItem(PERSONAL_BEST_KEY);
//   const migrated = migrateOrDefault(v3Raw, v2Raw, v1Raw);    // <- now migrateOrDefault(v4Raw, v3Raw, v2Raw, v1Raw)
//   memory = mergeHighWatermark(memory, migrated);
//   ... wroteMigrateThrough guard unchanged ...
// }
// recordRunEnd currently: sync memory merge, void persist(memory), return cloneBlob(memory) BEFORE awaiting disk (D-10/F-26) — this contract is unchanged, only the payload grows.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend memoryStore.ts + asyncStorageStore.ts to v4</name>
  <files>src/services/storage/memoryStore.ts, src/services/storage/asyncStorageStore.ts</files>
  <read_first>src/services/storage/memoryStore.ts, src/services/storage/asyncStorageStore.ts, src/services/storage/telemetry.ts, src/services/storage/types.ts</read_first>
  <behavior>
    - recordRunEnd({ mode:'campaign', levelId, score, outcome:'win'|'lose'|'abandoned', livesRemaining, stats }) merges score/stars/unlock exactly as today for 'win'/'lose' (win-gated stars/unlock branch unchanged; 'abandoned' falls into the same else-branch 'lose' already used for skipping stars)
    - Every outcome (including 'abandoned') calls mergeRunIntoTelemetry so lifetime/per-level counters accumulate regardless of win/lose/abandon (research Assumption A1: telemetry accumulates for all outcomes; only unlockAfterClear/computeStars stay win-gated)
    - cloneBlob (both stores) deep-copies telemetry via cloneTelemetryBlob so callers cannot mutate the store's internal state through a returned snapshot
    - asyncStorageStore's ensureHydrated reads PROGRESS_KEY (v4) first; on absent/corrupt v4 it reads PROGRESS_KEY_V3 + PROGRESS_KEY_V2 + PERSONAL_BEST_KEY and calls migrateOrDefault(v4Raw, v3Raw, v2Raw, v1Raw); the write-migrate-through-once guard still fires when healing from any legacy source
    - Legacy keys (PROGRESS_KEY_V3, PROGRESS_KEY_V2, PERSONAL_BEST_KEY) are never deleted
  </behavior>
  <action>
**memoryStore.ts**: change the `recordRunEnd` parameter type to match the extended
`ProgressStore` interface (`mode: GameMode`, `outcome: RunOutcome`, `stats: RunStatsInput`
added). Inside the function body, after the existing `applyLevelBest(args.levelId, merged);` and
the existing `if (args.outcome === 'win') { ... }` block (leave that block's condition exactly
`=== 'win'` — do not add `'abandoned'` to it), add: `blob.telemetry = mergeRunIntoTelemetry(
blob.telemetry, { mode: args.mode, levelId: args.levelId, outcome: args.outcome, score:
args.score, stats: args.stats });` before the final `return cloneBlob(blob);`. Update `cloneBlob`
to add `telemetry: cloneTelemetryBlob(b.telemetry)` to its returned object literal, and change
its `v: 3` literal to `v: 4`. Import `mergeRunIntoTelemetry`, `cloneTelemetryBlob` from
`'./telemetry'`.

**asyncStorageStore.ts**: identical `recordRunEnd` extension (mirror the memoryStore change:
extend the parameter type, add the `mergeRunIntoTelemetry` call before building the return
snapshot, keep `void persist(memory)` fire-and-forget and the synchronous return-before-persist
contract exactly as today). Update its `cloneBlob` the same way as memoryStore's (add
`cloneTelemetryBlob`, change `v: 3` to `v: 4`).

For `ensureHydrated`: rename the local variable currently named `v3Raw` (holding
`AsyncStorage.getItem(PROGRESS_KEY)`) to `v4Raw` for clarity, add a new
`const v3Raw = await AsyncStorage.getItem(PROGRESS_KEY_V3);` read inside the fallback branch
(alongside the existing `v2Raw`/`v1Raw` reads), change `parseProgressResult(v3Raw)` to
`parseProgressResult(v4Raw)` (now importing the Plan-02 v4 parser — no import path change needed
since the name is unchanged, just re-targeted), and change
`migrateOrDefault(v3Raw, v2Raw, v1Raw)` to `migrateOrDefault(v4Raw, v3Raw, v2Raw, v1Raw)`. Update
the `fromV2`/`fromV1` write-migrate-through-once guard to also consider a successful v3 parse
(`parseProgressV3Result(v3Raw).status === 'ok'`, imported from `'./parseBlob'`) as a reason to
write through, mirroring the existing `fromV2`/`fromV1` pattern (add a `fromV3` boolean). Update
`AsyncStorage.setItem(PROGRESS_KEY, ...)` call sites — no change needed there since `PROGRESS_KEY`
already resolves to the v4 string via the Plan 02 rename; just confirm via read that `persist()`
still writes to `PROGRESS_KEY` (v4) exclusively, never to `PROGRESS_KEY_V3`.

Neither file deletes any legacy key read — `PROGRESS_KEY_V3`/`PROGRESS_KEY_V2`/`PERSONAL_BEST_KEY`
stay migrate-on-read-only sources, exactly as `PROGRESS_KEY_V2`/`PERSONAL_BEST_KEY` already are
today.
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mergeRunIntoTelemetry" src/services/storage/memoryStore.ts src/services/storage/asyncStorageStore.ts` matches in both files
    - `grep -n "PROGRESS_KEY_V3" src/services/storage/asyncStorageStore.ts` matches
    - `grep -n "cloneTelemetryBlob" src/services/storage/memoryStore.ts src/services/storage/asyncStorageStore.ts` matches in both files
    - `grep -n "outcome === 'win'" src/services/storage/memoryStore.ts src/services/storage/asyncStorageStore.ts` still matches unchanged (win-gate not widened to include abandoned)
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Both ProgressStore implementations accept and persist the v4 recordRunEnd contract; the AsyncStorage hydrate chain reads v4 first with a full v3/v2/v1 fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Fill tests/storage.progress-v4.test.ts to GREEN</name>
  <files>tests/storage.progress-v4.test.ts</files>
  <read_first>tests/storage.progress-v4.test.ts, src/services/storage/index.ts, src/services/storage/memoryStore.ts, src/services/storage/asyncStorageStore.ts</read_first>
  <action>
Replace every `it.todo(...)` from Plan 00 with real `it(...)` assertions, importing from
`'../src/services/storage'` (barrel) plus any Plan-02/03 exports not yet in the import list
(`PROGRESS_KEY`, `PROGRESS_VERSION`, `PROGRESS_KEY_V3`, `defaultProgressBlob`,
`defaultProgressBlobV3`, `parseProgressResult`, `migrateOrDefault`, `mergeHighWatermark`,
`createMemoryProgressStore`, `RECENT_RUNS_BOUND`, `type RunStatsInput`, `type TelemetryBlob`).
For each describe block from Plan 00:

- `PROGRESS_KEY / VERSION (v4)`: assert `PROGRESS_VERSION === 4`, `PROGRESS_KEY ===
  '@nbb/progress/v4'`, `PROGRESS_KEY_V3 === '@nbb/progress/v3'`.
- `migrateOrDefault (v4, ...)`: use `buildV3Fixture()` from this file (Plan 00) as the `v3Raw`
  argument in `migrateOrDefault(null, JSON.stringify(buildV3Fixture()), null, null)` — assert
  the result's `unlocked`/`bestByLevel`/`bestScore`/`updatedAt` equal the fixture's values
  exactly and `telemetry` equals `defaultTelemetryBlob()`. Add a case with a valid v4 raw string
  (constructed via `JSON.stringify(defaultProgressBlob())` with a few fields overridden) proving
  v4 is preferred over a present v3 fixture. Add the v2/v1-fallback and corrupt-v4-falls-back
  cases mirroring the existing `migrateOrDefaultV3` test shapes in `tests/storage.progress-v3.
  test.ts` (Plan 02 already renamed those — use them as the template, not a duplicate target).
- `parseProgressResult (v4, ...)`: construct a valid v4 JSON string, assert `status:'ok'` and all
  fields correct. Construct a v4 JSON string with `telemetry` set to something structurally
  invalid (e.g. `telemetry: 'not-an-object'` or `telemetry.lifetime` missing) but
  `unlocked`/`bestByLevel`/`bestScore` valid — assert `status:'ok'`, `telemetry` deep-equals
  `defaultTelemetryBlob()`, AND `unlocked`/`bestByLevel`/`bestScore` equal the valid input
  (proving Pitfall 4 independence). Construct fully malformed JSON — assert `status:'corrupt'`,
  `progress` deep-equals `defaultProgressBlob()`, and the call never throws.
- `recordRunEnd (v4, mode-aware, ...)`: use `createMemoryProgressStore()`. Build a minimal
  `RunStatsInput` fixture (all fields present, small positive numbers). Call `recordRunEnd({
  mode:'campaign', levelId:'level-01', score:100, outcome:'win', livesRemaining:2, stats })` —
  assert `bestByLevel['level-01']` gets stars exactly as the pre-existing win test did, AND
  `telemetry.lifetime.runsPlayed===1`, `telemetry.lifetime.runsWon===1`,
  `telemetry.byMode.campaign['level-01'].runsPlayed===1`. Call again with `outcome:'abandoned'`
  — assert `runsAbandoned` increments, `unlocked`/stars are untouched by this second call, but
  `telemetry.lifetime.bricksBroken` increased by the second call's `stats.bricksBroken` (proving
  counters accumulate regardless of outcome per research Assumption A1). Assert
  `bestComboEver`/`longestRallyEver`/`largestCascadeEver` take the MAX across the two calls
  (design a fixture where the second call's stats has a lower `bestCombo` than the first, and
  confirm `bestComboEver` does not regress) while `bricksBroken`/pickup counters/`livesLost` SUM.
- `recentRuns ring buffer (D-05)`: call `recordRunEnd` `RECENT_RUNS_BOUND + 5` times in a loop on
  a fresh `createMemoryProgressStore()`, assert the returned blob's
  `telemetry.recentRuns.length === RECENT_RUNS_BOUND` and the oldest 5 entries (by a
  distinguishing field, e.g. an incrementing `score` per iteration) are absent (FIFO eviction),
  and assert each surviving entry has exactly the six `RunLogEntry` fields (no stray `stats`
  leakage into the log).
- `lifetime + per-level aggregates survive an app kill (SC-2)`: construct a fake
  `AsyncStorageLike` test double (an in-memory `Map`-backed `getItem`/`setItem`, matching the
  style already used — check `tests/storage.progress-v2.test.ts` or
  `asyncStorageStore.ts`'s exported factory function if one exists for tests; otherwise build
  the double inline: `{ getItem: async (k) => map.get(k) ?? null, setItem: async (k,v) => {
  map.set(k, v); } }`). Since `createAsyncStorageProgressStoreFrom` is not exported (only
  `createDefaultProgressStore`/`createAsyncStorageProgressStore`, which do their own native-probe
  and would fall back to memory under Vitest), drive this test at the `memoryStore` level
  instead: call `recordRunEnd` on one `createMemoryProgressStore()`, `JSON.stringify` its
  `getSnapshot()` result, then construct a SECOND `createMemoryProgressStore(seed)` passing the
  parsed JSON as the `seed` argument (the existing `seed?: ProgressBlob` constructor parameter),
  and assert the second store's `getSnapshot().telemetry` deep-equals the first's — this
  round-trips through `JSON.stringify`/`JSON.parse` exactly as an app-kill-and-relaunch would,
  without needing to fake the AsyncStorage native bridge.
  </action>
  <verify>
    <automated>npx vitest run tests/storage.progress-v4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/storage.progress-v4.test.ts` exits 0 with 0 todo remaining
    - `grep -c "it.todo(" tests/storage.progress-v4.test.ts` reports 0
    - `npm test` (full suite) exits 0 — no regression in the 79-file / 403-test baseline (now +2 new files)
  </acceptance_criteria>
  <done>Every N-STAT-02 behavior is asserted and green; zero it.todo remain in this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage JSON ↔ ensureHydrated | Untrusted on-device storage; fail-soft parse only, never throws into app startup |
| recordRunEnd caller (Plan 04) ↔ store | Trusted in-process call; caller controls `mode`/`outcome`/`stats` shape via TypeScript, no runtime validation needed on this side (validation lives in parseBlob for the persisted-and-reloaded path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering | ensureHydrated fallback chain | mitigate | Reuses the already-hardened fail-soft parse/migrate chain (Plan 02); never clobbers a known watermark |
| T-09-09 | Denial of service | Writes on every run end | accept | Matches today's existing write-per-run-end cadence (no per-frame writes); payload growth is bounded by RECENT_RUNS_BOUND |
</threat_model>

<verification>
`npx vitest run tests/storage.progress-v4.test.ts tests/storage.progress-v3.test.ts` exits 0.
`npm run typecheck && npm run lint` both exit 0. `npm test` (full suite) exits 0.
`git diff --stat -- src/core` is empty.
</verification>

<success_criteria>
Both `ProgressStore` implementations persist the full v4 telemetry contract using the existing
sync-merge/void-persist/return-clone pattern; the AsyncStorage hydrate chain covers v4→v3→v2→v1;
`tests/storage.progress-v4.test.ts` is fully green.
</success_criteria>

<output>
Create `.planning/phases/09-run-telemetry-storage-v4/09-03-SUMMARY.md` when done
</output>
