---
phase: 09-run-telemetry-storage-v4
plan: 02
type: execute
wave: 1
depends_on:
  - "09-00"
files_modified:
  - src/services/storage/types.ts
  - src/services/storage/parseBlob.ts
  - src/services/storage/migrateProgress.ts
  - src/services/storage/watermark.ts
  - src/services/storage/telemetry.ts
  - src/services/storage/index.ts
  - tests/storage.progress-v3.test.ts
  - tests/storage.progress-v2.test.ts
  - tests/ui/SelectScreen.test.tsx
  - tests/ui/GameHost.test.tsx
autonomous: true
requirements:
  - N-STAT-01
  - N-STAT-02
must_haves:
  truths:
    - "ProgressBlob v4 adds a telemetry sub-object without changing the meaning of unlocked/bestByLevel/bestScore/updatedAt"
    - "The existing v3/v2/v1 migrate chain is reused unchanged (renamed, not rewritten) — zero regression risk on an already-tested path"
    - "Corrupt/partial telemetry degrades only the telemetry sub-object to defaults; unlocked/bestByLevel/bestScore survive independently (SC-4)"
    - "A v3 fixture migrates to v4 losing zero unlocked/bestByLevel/bestScore/updatedAt data (SC-3)"
    - "recentRuns is bounded to RECENT_RUNS_BOUND=50 entries, enforced on write, oldest evicted first"
    - "Every existing consumer of the renamed/bumped identifiers (both storage test files, both UI components' tests) typechecks and passes against the v4 shape — not just the two storage test files research/pattern-mapping originally listed"
  artifacts:
    - path: "src/services/storage/types.ts"
      provides: "ProgressBlob v4 (telemetry field), ProgressBlobV3 (renamed legacy), TelemetryBlob/TelemetryAggregate/RunLogEntry/RunStatsInput/GameMode/RunOutcome, RECENT_RUNS_BOUND"
      exports: ["ProgressBlob", "ProgressBlobV3", "TelemetryBlob", "TelemetryAggregate", "RunLogEntry", "RunStatsInput", "GameMode", "RunOutcome", "RECENT_RUNS_BOUND", "defaultProgressBlob", "defaultProgressBlobV3", "defaultTelemetryBlob"]
    - path: "src/services/storage/parseBlob.ts"
      provides: "parseProgressResult (v4, telemetry-independent fail-soft) + parseProgressV3Result (renamed legacy)"
      exports: ["parseProgressResult", "parseProgressV3Result"]
    - path: "src/services/storage/migrateProgress.ts"
      provides: "migrateOrDefault (v4, tries v4 then delegates to migrateOrDefaultV3) + migrateOrDefaultV3 (renamed) + v3ToV4"
      exports: ["migrateOrDefault", "migrateOrDefaultV3", "v3ToV4"]
    - path: "src/services/storage/telemetry.ts"
      provides: "pure merge helpers: mergeRunIntoTelemetry, mergeTelemetryBlobs, cloneTelemetryBlob"
      exports: ["mergeRunIntoTelemetry", "mergeTelemetryBlobs", "cloneTelemetryBlob", "defaultTelemetryAggregate"]
  key_links:
    - from: "migrateOrDefault (v4)"
      to: "migrateOrDefaultV3 (renamed, unchanged body)"
      via: "v3ToV4() pure mapping, called only when parseProgressResult(v4Raw) is not ok"
      pattern: "migrateOrDefaultV3\\(v3Raw, v2Raw, v1Raw\\)"
    - from: "parseProgressResult (v4)"
      to: "telemetry independence"
      via: "sanitizeTelemetry(raw) never causes the caller to discard unlocked/bestByLevel/bestScore"
      pattern: "defaultTelemetryBlob\\(\\)"
---

<objective>
Bump `ProgressBlob` from v3 to v4: add the `telemetry` sub-object (lifetime aggregate + per-
`(mode, levelId)` aggregate + a bounded recent-run ring), extend the fail-soft parse contract so
telemetry corruption cannot wipe progress, and extend the migrate chain one link deeper — all
while renaming (not rewriting) the current v3-active functions so the existing, already-tested
v3/v2/v1 path is reused verbatim. Then sync **every** existing consumer of the renamed/bumped
identifiers — not just `tests/storage.progress-v3.test.ts` (originally the only one PATTERNS.md
and RESEARCH.md flagged). Plan-check found the rename's real blast radius by grepping the whole
repo: `tests/storage.progress-v2.test.ts` (imports the bare v3-active names directly and calls
the old 3-arg `migrateOrDefault` signature), `tests/ui/SelectScreen.test.tsx` (a `snapshot()` test
helper hard-typed to the v3 literal shape), and `tests/ui/GameHost.test.tsx` (a mock fixture with
a stale `v: 3` literal). Production code (`app/_components/SelectScreen.tsx`,
`app/_components/GameHost.tsx`) needs no changes — both only call the bare-named functions
(`defaultProgressBlob()`, `createDefaultProgressStore()`), which automatically resolve to the new
v4 behavior with no source change required.

Purpose: N-STAT-02's lossless migration + fail-soft contract, and the schema N-STAT-01's runtime
counters persist into — landed without breaking the 403-test green baseline.
Output: `types.ts`/`parseBlob.ts`/`migrateProgress.ts`/`watermark.ts` extended, `telemetry.ts`
(new pure merge helpers), barrel updated, all four affected test files synced to the v4 shape.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-run-telemetry-storage-v4/09-CONTEXT.md
@.planning/phases/09-run-telemetry-storage-v4/09-RESEARCH.md
@.planning/phases/09-run-telemetry-storage-v4/09-PATTERNS.md
@src/services/storage/types.ts
@src/services/storage/parseBlob.ts
@src/services/storage/migrateProgress.ts
@src/services/storage/watermark.ts
@src/services/storage/index.ts
@tests/storage.progress-v3.test.ts
@tests/storage.progress-v2.test.ts
@tests/ui/SelectScreen.test.tsx
@tests/ui/GameHost.test.tsx
@app/_components/SelectScreen.tsx

<interfaces>
<!-- LOCKED RENAMING SCHEME — apply exactly. This mirrors the codebase's own v2→v3 precedent
(parseProgressV2Result / ProgressBlobV2 already exist as legacy-suffixed names); v3's
currently-bare-named functions get the same "V3" treatment now that v4 is active. -->

| Concept          | Before this plan (bare name = v3-active) | After this plan: legacy (renamed, body unchanged) | After this plan: NEW (bare name = v4-active) |
|------------------|-------------------------------------------|----------------------------------------------------|-----------------------------------------------|
| Type             | `ProgressBlob` (v3 shape)                  | `ProgressBlobV3`                                    | `ProgressBlob` (v4 shape, +telemetry)          |
| Version const    | `PROGRESS_VERSION = 3`                     | (inline literal `3` in renamed fns — no separate const existed) | `PROGRESS_VERSION = 4`             |
| Key const        | `PROGRESS_KEY = '@nbb/progress/v3'`        | `PROGRESS_KEY_V3 = '@nbb/progress/v3'` (NEW const, same string, legacy migrate-on-read source) | `PROGRESS_KEY = '@nbb/progress/v4'` |
| default()        | `defaultProgressBlob()`                    | `defaultProgressBlobV3()`                           | `defaultProgressBlob()` (returns v4 + `defaultTelemetryBlob()`) |
| parse() + result type | `parseProgressResult` / `ParseProgressResult` | `parseProgressV3Result` / `ParseProgressV3Result` (unchanged body, `blob.v!==3` check preserved) | `parseProgressResult` / `ParseProgressResult` (NEW: `blob.v!==4` check, telemetry validated independently) |
| migrate()        | `migrateOrDefault(v3Raw,v2Raw,v1Raw): ProgressBlob` | `migrateOrDefaultV3(v3Raw,v2Raw,v1Raw): ProgressBlobV3` (body untouched except return-type annotation + its internal `defaultProgressBlob()` call becomes `defaultProgressBlobV3()`) | `migrateOrDefault(v4Raw,v3Raw,v2Raw,v1Raw): ProgressBlob` (NEW: try `parseProgressResult(v4Raw)`, else `v3ToV4(migrateOrDefaultV3(v3Raw,v2Raw,v1Raw))`) |
| watermark()      | `mergeHighWatermark(memory,incoming): ProgressBlob` (v3-typed) | `mergeHighWatermarkV3(memory,incoming): ProgressBlobV3` (renamed, body unchanged) | `mergeHighWatermark(memory,incoming): ProgressBlob` (NEW: delegates unlocked/bestByLevel/bestScore/updatedAt merge to the same max-based logic as v3, PLUS merges `.telemetry` via `mergeTelemetryBlobs`) |

`PROGRESS_KEY_V2` and everything in `catalog.ts`/`unlock.ts`/`stars.ts`/`compareBest.ts`/
`memoryStore.ts`/`asyncStorageStore.ts` is NOT touched by this plan (memoryStore.ts and
asyncStorageStore.ts are Plan 03's scope — this plan only prepares the types/parse/migrate/
watermark/telemetry layer they will call).

<!-- Full consumer inventory, verified by `grep -rln "\bPROGRESS_KEY\b\|\bPROGRESS_VERSION\b\|
\bdefaultProgressBlob\b\|\bparseProgressResult\b\|\bmigrateOrDefault\b\|\bmergeHighWatermark\b\|
\bProgressBlob\b" --include='*.ts' --include='*.tsx' app src tests` across the WHOLE repo, not
just src/services/storage/ — this is the check plan-check flagged as missing the first time. -->

| File | Uses | Breaks without a fix? | Fixed in |
|------|------|------------------------|----------|
| `src/services/storage/{types,parseBlob,migrateProgress,watermark,memoryStore,asyncStorageStore,index}.ts` | defines/re-exports everything | n/a — this IS the rename | Task 1/2/3 below + Plan 03 |
| `tests/storage.progress-v3.test.ts` | bare `PROGRESS_KEY`, `defaultProgressBlob`, `parseProgressResult`, `migrateOrDefault`, `mergeHighWatermark`, `ProgressBlob` literals, `recordRunEnd` (old 4-arg) | YES — typecheck + runtime | Task 3 |
| `tests/storage.progress-v2.test.ts` | bare `PROGRESS_KEY` (`.toBe('@nbb/progress/v3')`), `PROGRESS_VERSION` (`.toBe(3)`), `defaultProgressBlob`, `migrateOrDefault` (old 3-arg, 4 call sites), `parseProgressResult` (1 call site) | YES — typecheck (3-arg call against a 4-arg signature) + runtime (`b.v` is now `4`, not `3`) | Task 3 |
| `tests/ui/SelectScreen.test.tsx` | `snapshot()` helper: `function snapshot(partial: Partial<ProgressBlob> = {}): ProgressBlob { return { v: 3, ...no telemetry... }; }` | YES — typecheck (object literal missing `telemetry`, `v` narrowed to `3` not `4`) | Task 3 |
| `tests/ui/GameHost.test.tsx` | `vi.mock('../../src/services/storage', ...)` factory returns a hand-built `getSnapshot` resolving to `{ v: 3 as const, ... }` (no `telemetry`) | NO hard typecheck break (factory return isn't structurally checked against the real module), but the fixture is stale relative to runtime reality | Task 3 (hygiene) |
| `app/_components/SelectScreen.tsx` | calls `defaultProgressBlob()` and types local state as `ProgressBlob` — both bare names | NO — bare names auto-resolve to the new v4 behavior; this file constructs nothing by literal | none needed |
| `app/_components/GameHost.tsx` | calls `createDefaultProgressStore()` only, never touches blob shape | NO | none needed |
| `app/_components/PlayingHost.tsx` | calls `store.recordRunEnd(...)` (old 4-arg) | YES, but this is Plan 04's file — Plan 04 already owns the extension | Plan 04 |

<!-- New v4 types this plan adds to types.ts (exact shape — do not deviate) -->
export type GameMode = 'campaign' | 'endless' | 'daily';
export type RunOutcome = 'win' | 'lose' | 'abandoned';
export const RECENT_RUNS_BOUND = 50 as const; // ~120 bytes/entry x 50 ~= 6KB, vs ~2MB Android CursorWindow practical ceiling (A3) — orders of magnitude headroom; "a round number in the tens" per CONTEXT discretion.

export type TelemetryAggregate = {
  runsPlayed: number; runsWon: number; runsLost: number; runsAbandoned: number;
  bricksBroken: number; bestComboEver: number;
  pickupMultiball: number; pickupExpand: number; pickupExtraLife: number; pickupSlow: number; pickupFireball: number;
  livesLost: number; longestRallyEver: number; largestCascadeEver: number;
  ticksPlayed: number; wallClockMsTotal: number;
};
export type RunLogEntry = { mode: GameMode; levelId: string; outcome: RunOutcome; score: number; ticks: number; timestamp: number };
export type RunStatsInput = {
  bricksBroken: number; bestCombo: number;
  pickupMultiball: number; pickupExpand: number; pickupExtraLife: number; pickupSlow: number; pickupFireball: number;
  livesLost: number; longestRally: number; largestCascade: number;
  ticksPlayed: number; wallClockMs: number;
}; // NOTE: intentionally excludes `rallyCurrent` (runtime-internal bookkeeping only, see runStats.ts) and never imports src/runtime/* (LC boundary: services may import core/services only, never runtime)
export type TelemetryBlob = {
  lifetime: TelemetryAggregate;
  byMode: { campaign: Partial<Record<string, TelemetryAggregate>>; endless: Partial<Record<string, TelemetryAggregate>>; daily: Partial<Record<string, TelemetryAggregate>> };
  recentRuns: RunLogEntry[];
};
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types.ts + parseBlob.ts — v4 schema, rename v3 to legacy, fail-soft parse</name>
  <files>src/services/storage/types.ts, src/services/storage/parseBlob.ts</files>
  <read_first>src/services/storage/types.ts, src/services/storage/parseBlob.ts</read_first>
  <behavior>
    - defaultProgressBlob() returns { v:4, unlocked:['level-01'], bestByLevel:{}, bestScore:0, updatedAt:0, telemetry: defaultTelemetryBlob() }
    - defaultProgressBlobV3() returns exactly what defaultProgressBlob() used to return (v:3, no telemetry field) — byte-identical to today's behavior
    - defaultTelemetryBlob() returns { lifetime: <all-zero TelemetryAggregate>, byMode: { campaign:{}, endless:{}, daily:{} }, recentRuns: [] }
    - ProgressStore.recordRunEnd's args type requires mode: GameMode, outcome now includes 'abandoned', and a new required stats: RunStatsInput field
    - parseProgressResult (v4) with a valid telemetry sub-object parses everything correctly
    - parseProgressResult (v4) with a structurally corrupt telemetry sub-object still returns status 'ok' for the top-level blob, with telemetry defaulted and unlocked/bestByLevel/bestScore/updatedAt preserved from the raw input (Pitfall 4 / SC-4)
    - parseProgressV3Result (renamed) behaves byte-identically to today's parseProgressResult, gated on blob.v===3
  </behavior>
  <action>
Apply the LOCKED RENAMING SCHEME from `<interfaces>` above exactly.

**types.ts:**

1. Rename the existing `export type ProgressBlob = { v: 3; ... }` to `export type ProgressBlobV3
   = { v: 3; ... }` (body unchanged). Rename `export function defaultProgressBlob(): ProgressBlob
   { return { v: 3, ... }; }` to `defaultProgressBlobV3(): ProgressBlobV3` (body unchanged).

2. Rename the const `PROGRESS_KEY = '@nbb/progress/v3'` to `PROGRESS_KEY_V3`, keeping the same
   string value, with an updated comment `/** Legacy v3 key — migrate-on-read source only; never
   delete. */` (matching the existing `PROGRESS_KEY_V2` comment style immediately above it).
   Leave `PROGRESS_KEY_V2` untouched. Bump `PROGRESS_VERSION` to `4` (it is the "active version"
   const).

3. Add the new types verbatim from `<interfaces>` above: `GameMode`, `RunOutcome`,
   `RECENT_RUNS_BOUND`, `TelemetryAggregate`, `RunLogEntry`, `RunStatsInput`, `TelemetryBlob`.
   Add `defaultTelemetryAggregate(): TelemetryAggregate` (all-zero) and
   `defaultTelemetryBlob(): TelemetryBlob`. On the `largestCascadeEver` field of
   `TelemetryAggregate`, add this exact comment (Phase 13 will read this schema, not the
   reducer, per plan-check): `/** Grid-adjacency-grouped cascade size (src/runtime/runStats.ts) —
   exact for every real playable level (all use the lattice broadphase). A narrow fallback
   treats a break with no resolvable lattice cell as its own singleton group, which can only
   ever UNDERcount, never overcount — safe for an achievement trigger (D-08); see
   09-CONTEXT.md "Notes for later phases". */`.

4. Add a NEW `export type ProgressBlob = { v: 4; unlocked: LevelId[]; bestByLevel:
   Partial<Record<LevelId, LevelBest>>; bestScore: number; updatedAt: number; telemetry:
   TelemetryBlob; }` and a NEW `export function defaultProgressBlob(): ProgressBlob` returning
   `{ v: 4, unlocked: ['level-01'], bestByLevel: {}, bestScore: 0, updatedAt: 0, telemetry:
   defaultTelemetryBlob() }`. `PROGRESS_KEY` becomes `'@nbb/progress/v4'`.

5. Extend `ProgressStore.recordRunEnd`'s argument type: add `mode: GameMode;` (required), widen
   `outcome: 'win' | 'lose' | 'abandoned';`, and add `stats: RunStatsInput;` (required). Do not
   add a second method — C2 lock: ride the existing `recordRunEnd`.

Do not touch `PersonalBestBlob`/`PersonalBestStore`/`PERSONAL_BEST_KEY`/`PERSONAL_BEST_VERSION`
(A4 — stays a separate migrate-input source).

**parseBlob.ts:**

Rename the current `parseProgressResult` function to `parseProgressV3Result` and its return type
`ParseProgressResult` to `ParseProgressV3Result` (bodies unchanged — still gate on `blob.v !== 3`,
still call `defaultProgressBlobV3()`/`sanitizeProgressV3()` internally). Add a NEW `export type
ParseProgressResult = { status:'ok'|'absent'|'corrupt'; progress: ProgressBlob }` (v4-typed) and
a NEW `export function parseProgressResult(raw: string | null): ParseProgressResult` that: on
`raw == null` returns absent+`defaultProgressBlob()`; on JSON parse failure or `blob.v !== 4` or
non-array `unlocked` or non-object `bestByLevel` or non-finite `bestScore` returns
corrupt+`defaultProgressBlob()` (identical field-by-field gating to the v3 parser, re-targeted at
v4's top-level shape); otherwise builds the result by reusing the private `sanitizeUnlocked`/
`sanitizeLevelBest` helpers directly for the shared fields (do NOT go through
`parseProgressV3Result`, since that function expects `blob.v===3`) PLUS a NEW private
`sanitizeTelemetry(raw: unknown): TelemetryBlob` that returns `defaultTelemetryBlob()` on ANY
structural failure of the `telemetry` field — critically, a `sanitizeTelemetry` failure must
NEVER cause `parseProgressResult` to return `status: 'corrupt'` for the whole blob (Pitfall 4).
Import `defaultTelemetryBlob` from `'./types'`.
  </action>
  <verify>
    <automated>npx vitest run tests/storage.progress-v3.test.ts tests/storage.progress-v2.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export type ProgressBlobV3" src/services/storage/types.ts` matches
    - `grep -n "v: 4;" src/services/storage/types.ts` matches (the new ProgressBlob)
    - `grep -n "PROGRESS_KEY_V3 = '@nbb/progress/v3'" src/services/storage/types.ts` matches
    - `grep -n "PROGRESS_KEY = '@nbb/progress/v4'" src/services/storage/types.ts` matches
    - `grep -n "stats: RunStatsInput" src/services/storage/types.ts` matches inside `recordRunEnd`
    - `grep -n "Grid-adjacency-grouped cascade size" src/services/storage/types.ts` matches on the `largestCascadeEver` field
    - `grep -n "export function parseProgressV3Result\|export function parseProgressResult" src/services/storage/parseBlob.ts` matches both
  </acceptance_criteria>
  <done>types.ts defines the full v4 schema (with the largestCascadeEver schema comment) and the renamed v3-legacy types/consts/defaults; parseBlob.ts's v4 parser validates telemetry independently of progress fields. Note: `npm run typecheck` is NOT expected to pass yet — migrateProgress.ts/watermark.ts/telemetry.ts/index.ts and the 4 consumer test files still reference pre-rename names until Tasks 2/3 land; this task's own verify only runs the two storage test files that Task 1's changes directly affect at the type level, not the full project build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: migrateProgress.ts + watermark.ts + telemetry.ts (NEW) — migrate chain + watermark + merge helpers</name>
  <files>src/services/storage/migrateProgress.ts, src/services/storage/watermark.ts, src/services/storage/telemetry.ts</files>
  <read_first>src/services/storage/migrateProgress.ts, src/services/storage/watermark.ts, src/services/storage/types.ts, src/services/storage/stars.ts</read_first>
  <behavior>
    - migrateOrDefault(null, v3Raw=buildV3Fixture(), null, null) returns a v4 blob with unlocked/bestByLevel/bestScore/updatedAt identical to the v3 fixture and telemetry === defaultTelemetryBlob()
    - migrateOrDefault(null, null, v2Raw, v1Raw) falls through to the unchanged v2/v1 chain exactly as migrateOrDefaultV3 already does
    - mergeHighWatermark (v4) never lowers a known bestByLevel/bestScore/unlocked watermark, and sums telemetry cumulative fields / maxes running-max fields / concatenates+truncates recentRuns when merging memory with a freshly-hydrated disk blob
    - recentRuns never exceeds RECENT_RUNS_BOUND after a merge
    - mergeRunIntoTelemetry appends a RunLogEntry with exactly the six D-05 fields (mode, levelId, outcome, score, ticks, timestamp) — no stats leakage into the log
  </behavior>
  <action>
**migrateProgress.ts**: rename `migrateOrDefault` to `migrateOrDefaultV3`, retype its return
annotation from `ProgressBlob` to `ProgressBlobV3`, and change its one internal
`defaultProgressBlob()` call (in the v1-fallback branch) to `defaultProgressBlobV3()` — no other
body changes; it still takes `(v3Raw, v2Raw, v1Raw)` and still prefers v3 > v2 > v1 exactly as
today. Add a new pure `v3ToV4(v3: ProgressBlobV3): ProgressBlob` that copies `unlocked`
(spread), `bestByLevel` (spread), `bestScore`, `updatedAt` verbatim and sets `telemetry:
defaultTelemetryBlob()`. Add a NEW `export function migrateOrDefault(v4Raw: string | null,
v3Raw: string | null, v2Raw: string | null, v1Raw: string | null): ProgressBlob` that: tries
`parseProgressResult(v4Raw)` (the new v4 parser from Task 1) first and returns `.progress` if
`status === 'ok'`; else calls `v3ToV4(migrateOrDefaultV3(v3Raw, v2Raw, v1Raw))` unchanged. Import
`parseProgressResult` from `'./parseBlob'`.

**watermark.ts**: rename `mergeHighWatermark` to `mergeHighWatermarkV3`, retype both params and
return to `ProgressBlobV3` (body unchanged). Add a NEW `export function mergeHighWatermark(
memory: ProgressBlob, incoming: ProgressBlob): ProgressBlob` that: builds the merged
`unlocked`/`bestByLevel`/`bestScore`/`updatedAt` fields using the SAME max-based logic as
`mergeHighWatermarkV3` (prefer calling it internally with `{...memory, v:3}`/`{...incoming, v:3}`
casts and re-tag `v:4` on the result, to avoid duplicating the four-field merge logic), PLUS
`telemetry: mergeTelemetryBlobs(memory.telemetry, incoming.telemetry)` (import from
`'./telemetry'`). Return `{ v: 4, unlocked, bestByLevel, bestScore, updatedAt, telemetry }`.

**telemetry.ts (NEW)**: pure functions, no I/O, mirroring `stars.ts`/`watermark.ts`'s "merge(prev,
incoming) → next" shape. Export:
- `defaultTelemetryAggregate` — re-export/reuse from `types.ts` (import it, do not duplicate).
- `cloneTelemetryBlob(t: TelemetryBlob): TelemetryBlob` — deep-enough clone (spread `lifetime`,
  spread each `byMode[mode]` map and each aggregate inside it, `.map()` over `recentRuns`).
- `mergeRunIntoTelemetry(telemetry: TelemetryBlob, args: { mode: GameMode; levelId: string;
  outcome: RunOutcome; score: number; stats: RunStatsInput }): TelemetryBlob` — a private
  `bumpAggregate(prev: TelemetryAggregate, outcome: RunOutcome, stats: RunStatsInput):
  TelemetryAggregate` increments `runsPlayed` always and exactly one of
  `runsWon`/`runsLost`/`runsAbandoned` by outcome, sums `bricksBroken`/pickup-type
  fields/`livesLost`/`ticksPlayed`/`wallClockMsTotal` from `stats`, and takes
  `Math.max(prev.X, stats.Y)` for `bestComboEver`/`longestRallyEver`/`largestCascadeEver`. Apply
  `bumpAggregate` to both `telemetry.lifetime` and `telemetry.byMode[mode][levelId]` (defaulting
  the per-level entry to `defaultTelemetryAggregate()` if absent). Append a `RunLogEntry` (
  `{ mode, levelId, outcome, score: Math.floor(Number.isFinite(score) ? score : 0), ticks:
  Math.floor(stats.ticksPlayed), timestamp: Date.now() }`) to `recentRuns` and
  `.slice(-RECENT_RUNS_BOUND)` (D-05 ring-buffer-on-write). `Date.now()` is fine here (services
  layer, not core — LC-01 only bans it in `src/core/**`).
- `mergeTelemetryBlobs(memory: TelemetryBlob, incoming: TelemetryBlob): TelemetryBlob` — sums
  every cumulative field and maxes every running-max field between `memory.lifetime` and
  `incoming.lifetime` (and per-mode-per-level in `byMode`, unioning the set of level keys present
  in either side), and merges `recentRuns` by concatenating both arrays, sorting ascending by
  `timestamp`, and taking the last `RECENT_RUNS_BOUND` entries.
  </action>
  <verify>
    <automated>npx eslint src/services/storage/migrateProgress.ts src/services/storage/watermark.ts src/services/storage/telemetry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function migrateOrDefaultV3\|export function v3ToV4\|export function migrateOrDefault\b" src/services/storage/migrateProgress.ts` matches all three
    - `grep -n "export function mergeHighWatermarkV3\|export function mergeHighWatermark\b" src/services/storage/watermark.ts` matches both
    - `grep -n "export function mergeRunIntoTelemetry\|export function mergeTelemetryBlobs\|export function cloneTelemetryBlob" src/services/storage/telemetry.ts` matches all three
    - `grep -n "RECENT_RUNS_BOUND" src/services/storage/telemetry.ts` matches
    - `npx eslint src/services/storage/migrateProgress.ts src/services/storage/watermark.ts src/services/storage/telemetry.ts` exits 0
  </acceptance_criteria>
  <done>Migrate chain extends v3→v4 without touching the v2/v1 links; watermark merge covers telemetry; telemetry.ts provides the three pure merge helpers Plan 03 will call.</done>
</task>

<task type="auto">
  <name>Task 3: Barrel + all four consumer test files synced to v4</name>
  <files>src/services/storage/index.ts, tests/storage.progress-v3.test.ts, tests/storage.progress-v2.test.ts, tests/ui/SelectScreen.test.tsx, tests/ui/GameHost.test.tsx</files>
  <read_first>src/services/storage/index.ts, tests/storage.progress-v3.test.ts, tests/storage.progress-v2.test.ts, tests/ui/SelectScreen.test.tsx, tests/ui/GameHost.test.tsx</read_first>
  <behavior>
    - Every renamed identifier (parseProgressV3Result, migrateOrDefaultV3, mergeHighWatermarkV3, defaultProgressBlobV3, v3ToV4) and every new v4 identifier (GameMode, RunOutcome, TelemetryBlob, TelemetryAggregate, RunLogEntry, RunStatsInput, RECENT_RUNS_BOUND, defaultTelemetryBlob, mergeRunIntoTelemetry, mergeTelemetryBlobs, cloneTelemetryBlob, PROGRESS_KEY_V3, ParseProgressV3Result) is exported from the barrel
    - tests/storage.progress-v3.test.ts and tests/storage.progress-v2.test.ts both typecheck and pass against the renamed functions
    - tests/ui/SelectScreen.test.tsx's snapshot() helper produces valid v4 ProgressBlob literals
    - npm run typecheck exits 0 for the WHOLE project (this is the task whose completion makes Plan 02's overall typecheck acceptance criterion true)
  </behavior>
  <action>
**index.ts barrel**: add exports for `PROGRESS_KEY_V3`, `type ProgressBlobV3`,
`defaultProgressBlobV3`, `type GameMode`, `type RunOutcome`, `type TelemetryBlob`, `type
TelemetryAggregate`, `type RunLogEntry`, `type RunStatsInput`, `RECENT_RUNS_BOUND`,
`defaultTelemetryBlob`, `parseProgressV3Result`, `type ParseProgressV3Result`,
`migrateOrDefaultV3`, `v3ToV4`, `mergeHighWatermarkV3`, `mergeRunIntoTelemetry`,
`mergeTelemetryBlobs`, `cloneTelemetryBlob` — alongside the existing (now v4-retargeted, same
export names) `PROGRESS_VERSION, PROGRESS_KEY, defaultProgressBlob, type ProgressBlob,
parseProgressResult, migrateOrDefault, mergeHighWatermark`.

**tests/storage.progress-v3.test.ts**: mechanical renames only (no new test cases beyond what is
noted):
- `describe('PROGRESS_KEY / VERSION (C2 Plan 01)')` block (asserts bare `PROGRESS_KEY ===
  '@nbb/progress/v3'` / `PROGRESS_VERSION === 3`): DELETE — those bare exports now describe v4;
  the equivalent v4 assertion belongs in `tests/storage.progress-v4.test.ts` (Plan 03).
- Every remaining reference to `PROGRESS_KEY` → `PROGRESS_KEY_V3`, `defaultProgressBlob` →
  `defaultProgressBlobV3`, `parseProgressResult` → `parseProgressV3Result`, `migrateOrDefault` →
  `migrateOrDefaultV3`, `mergeHighWatermark` → `mergeHighWatermarkV3`, and the `ProgressBlob`
  type annotations on the literal objects in the `mergeHighWatermark` describe block →
  `ProgressBlobV3`.
- `describe('recordRunEnd (C2 Plan 01)')` block (calls `createMemoryProgressStore().recordRunEnd(
  {...})` with the old 4-arg shape): DELETE. The store is always v4-shaped now — this coverage
  moves to `tests/storage.progress-v4.test.ts`'s "recordRunEnd (v4, mode-aware)" block (Plan 03).
- Update the import line to reflect the renamed names.

**tests/storage.progress-v2.test.ts** (found by plan-check's whole-repo grep — NOT in the
original file list):
- Update the header comment (lines 1-3) — it currently says "ProgressBlob is v3"; change to note
  v3 is now the legacy migrate-input shape.
- Line ~65-74 (`it('defaultProgressBlob shape (v3)', ...)`) : replace `defaultProgressBlob` with
  `defaultProgressBlobV3` in the import and at its one call site (`const b =
  defaultProgressBlobV3();`). DELETE the line `expect(b.v).toBe(PROGRESS_VERSION);` (that const
  is now `4`, and this test is specifically about the v3 shape) — keep `expect(b.v).toBe(3);`
  (still literally true against `defaultProgressBlobV3()`). Change `expect(PROGRESS_KEY).toBe(
  '@nbb/progress/v3');` to `expect(PROGRESS_KEY_V3).toBe('@nbb/progress/v3');`.
- `describe('parseProgressResult rejects v2 under v3 key', ...)`: rename the one call site from
  `parseProgressResult(...)` to `parseProgressV3Result(...)` — the describe title says "under v3
  key," so it must test the renamed v3-specific parser, not the new v4 one (both would currently
  pass, since v2 JSON is invalid for either version, but only the renamed function preserves the
  test's stated intent).
- `describe('migrateOrDefault (v1/v2→v3)', ...)`: rename every call site (4 total, lines ~175,
  191, 199, 204) from `migrateOrDefault(...)` to `migrateOrDefaultV3(...)` — same 3-arg
  signature, no other changes. Update the import line accordingly (remove bare
  `parseProgressResult`/`migrateOrDefault`/`defaultProgressBlob` if no longer referenced bare in
  this file, add `parseProgressV3Result`/`migrateOrDefaultV3`/`defaultProgressBlobV3`).
- Leave the `createMemoryProgressStore`/`createDefaultProgressStore`/`__resetSharedProgressStoreForTests`
  describe blocks (lines ~211-259) completely untouched — they call the store's bare methods,
  which now operate on v4 blobs internally, but none of their assertions read `telemetry` or
  construct a versioned literal, so they remain correct as-is with zero changes.

**tests/ui/SelectScreen.test.tsx**: update the `snapshot()` helper to produce a valid v4
`ProgressBlob`: change `v: 3,` to `v: 4,` and add `telemetry: { lifetime: { runsPlayed: 0,
runsWon: 0, runsLost: 0, runsAbandoned: 0, bricksBroken: 0, bestComboEver: 0, pickupMultiball: 0,
pickupExpand: 0, pickupExtraLife: 0, pickupSlow: 0, pickupFireball: 0, livesLost: 0,
longestRallyEver: 0, largestCascadeEver: 0, ticksPlayed: 0, wallClockMsTotal: 0 }, byMode: {
campaign: {}, endless: {}, daily: {} }, recentRuns: [] },` to the returned literal (before the
`...partial` spread, so a test-provided `partial.telemetry` can still override it). Prefer
importing `defaultTelemetryBlob` from `'../../src/services/storage'` and using
`telemetry: defaultTelemetryBlob(),` instead of hand-writing the literal, if that keeps the
helper shorter — either is acceptable as long as the result structurally satisfies `ProgressBlob`
(v4).

**tests/ui/GameHost.test.tsx** (hygiene — not required for typecheck, since this mock's return
type is not structurally checked against the real module, but kept in sync to prevent future
confusion): in the `vi.mock('../../src/services/storage', ...)` factory's `getSnapshot` fixture,
change `v: 3 as const` to `v: 4 as const` and add a `telemetry: { lifetime: { ...all-zero... },
byMode: { campaign: {}, endless: {}, daily: {} }, recentRuns: [] }` field mirroring the
SelectScreen.test.tsx fixture above (or import `defaultTelemetryBlob` here too).
  </action>
  <verify>
    <automated>npm run typecheck; npm run lint; npx vitest run tests/storage.progress-v3.test.ts tests/storage.progress-v2.test.ts tests/ui/SelectScreen.test.tsx tests/ui/GameHost.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function parseProgressV3Result\|export function migrateOrDefaultV3\|export function mergeHighWatermarkV3\|export function v3ToV4" src/services/storage/*.ts` matches all four (from Tasks 1/2, now barrel-exported)
    - `grep -n "recordRunEnd (C2 Plan 01)\|PROGRESS_KEY / VERSION (C2 Plan 01)" tests/storage.progress-v3.test.ts` returns no matches (both blocks deleted)
    - `grep -n "migrateOrDefaultV3" tests/storage.progress-v2.test.ts | wc -l` reports at least 4 (all 4 call sites renamed)
    - `grep -n "PROGRESS_KEY_V3" tests/storage.progress-v2.test.ts` matches
    - `grep -n "v: 4" tests/ui/SelectScreen.test.tsx` matches inside `snapshot()`
    - `npx vitest run tests/storage.progress-v3.test.ts tests/storage.progress-v2.test.ts tests/ui/SelectScreen.test.tsx tests/ui/GameHost.test.tsx` exits 0
    - `npm run typecheck` exits 0 (whole-project — this is the first point in the phase where that is expected to be true)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>All renamed v3 functions preserve today's exact behavior under new names and are barrel-exported; every consumer of the renamed/bumped identifiers found by the whole-repo grep — both storage test files AND both UI test files — typechecks and passes; whole-project typecheck is green for the first time this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage JSON ↔ parse/migrate | Untrusted on-device storage (rooted device / manual edit / future bug); fail-soft only |
| memory ↔ disk (watermark merge) | Trusted process-internal race (hydration-before-run-end); never lowers known data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | parseProgressResult (v4) | mitigate | Field-by-field validation; `blob.v!==4` → corrupt+defaults; telemetry validated independently (Pitfall 4) — never throws |
| T-09-05 | Tampering | migrateOrDefault (v4) | mitigate | Reuses the already-hardened v3/v2/v1 chain verbatim (renamed only); v3ToV4 is a pure structural copy, no new attack surface |
| T-09-06 | Denial of service | recentRuns unbounded growth | mitigate | `RECENT_RUNS_BOUND` (50) enforced on write inside `mergeRunIntoTelemetry`/`mergeTelemetryBlobs`, not left to the reader |
| T-09-07 | Information disclosure | Telemetry counters | accept | Local unencrypted, no PII, same posture as existing bestByLevel/bestScore |
| T-09-13 | Regression (rename blast radius) | Every consumer of the renamed identifiers across the repo | mitigate | Whole-repo grep (not just the files research/pattern-mapping listed) performed during plan revision; all four affected test files fixed in Task 3; whole-project `npm run typecheck` is a Task 3 acceptance criterion |
</threat_model>

<verification>
`npx vitest run tests/storage.progress-v3.test.ts tests/storage.progress-v2.test.ts
tests/ui/SelectScreen.test.tsx tests/ui/GameHost.test.tsx` exits 0. `npm run typecheck && npm run
lint` both exit 0 for the whole project. `git diff --stat -- src/core` is empty.
</verification>

<success_criteria>
`ProgressBlob` v4 schema, fail-soft parse, extended migrate chain, and telemetry merge helpers
all exist; the v3-active functions are preserved verbatim under legacy-suffixed names; every
consumer found by the whole-repo grep (not just the two originally-flagged files) compiles and
passes against the v4 shape.
</success_criteria>

<output>
Create `.planning/phases/09-run-telemetry-storage-v4/09-02-SUMMARY.md` when done
</output>
