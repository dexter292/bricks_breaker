---
phase: 09-run-telemetry-storage-v4
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/telemetry.reduce-run-events.test.ts
  - tests/storage.progress-v4.test.ts
autonomous: true
requirements:
  - N-STAT-01
  - N-STAT-02
must_haves:
  truths:
    - "A Vitest run of both new test files exits 0 before any implementation exists (all cases are it.todo stubs, matching the Phase 04 Wave-0 precedent)"
    - "Every behavior N-STAT-01/N-STAT-02 needs is named as a stub so later waves cannot silently skip a counter or a migration case"
    - "A realistic v3-fixture builder exists for the round-trip test so Plan 03 does not invent fixture shape ad hoc"
  artifacts:
    - path: "tests/telemetry.reduce-run-events.test.ts"
      provides: "it.todo scaffold for every N-STAT-01 counter, incl. the cascade heuristic"
      contains: "it.todo"
    - path: "tests/storage.progress-v4.test.ts"
      provides: "it.todo scaffold for N-STAT-02 migration/fail-soft/aggregation/ring-buffer cases + buildV3Fixture() helper"
      contains: "buildV3Fixture"
  key_links: []
---

<objective>
Create the two Wave-0 test scaffolds VALIDATION.md requires before any implementation:
`tests/telemetry.reduce-run-events.test.ts` (N-STAT-01) and `tests/storage.progress-v4.test.ts`
(N-STAT-02). Every case is `it.todo(...)` — no implementation imports yet — mirroring the
established Phase 04 Wave-0 convention ("invalid fixtures + it.todo stubs").

Purpose: Nyquist gate — later waves fill these in rather than inventing test shape under
implementation pressure, and the todo list is itself the acceptance checklist for N-STAT-01/02.
Output: Two Vitest files, all green (todo), plus a `buildV3Fixture()` helper representing a
realistic existing player for the migration round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-run-telemetry-storage-v4/09-CONTEXT.md
@.planning/phases/09-run-telemetry-storage-v4/09-RESEARCH.md
@.planning/phases/09-run-telemetry-storage-v4/09-PATTERNS.md
@.planning/phases/09-run-telemetry-storage-v4/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/telemetry.reduce-run-events.test.ts — it.todo scaffold (N-STAT-01)</name>
  <files>tests/telemetry.reduce-run-events.test.ts</files>
  <read_first>
    .planning/phases/09-run-telemetry-storage-v4/09-CONTEXT.md,
    .planning/phases/09-run-telemetry-storage-v4/09-RESEARCH.md (Common Pitfalls 1-3, Code Examples),
    tests/helpers/balanceBot.ts
  </read_first>
  <action>
Create `tests/telemetry.reduce-run-events.test.ts` with `import { describe, it } from 'vitest';` only —
no import of `src/runtime/runStats` (does not exist yet). Add one `describe` block per concern,
each containing `it.todo('...')` cases naming the exact assertion Plan 01 must satisfy. Do not
write any expect() calls yet. Required describe/it.todo groups (do not add extra scope, do not
omit any):

- `describe('reduceRunTelemetry — ring-walk basics')`: `it.todo('bestCombo tracks a running max of world.combo, never re-derived from events')`, `it.todo('bricksBroken counts one per BRICK_BREAK event, cumulative across calls')`.
- `describe('reduceRunTelemetry — per-pickup-type counts (D-07)')`: `it.todo('POWERUP_CATCH with evA=PickupType.MULTIBALL increments pickupMultiball only')`, one `it.todo` per remaining type (expand, extraLife, slow, fireball) — 5 total.
- `describe('reduceRunTelemetry — lives lost + longest rally (D-10, Pitfall 3)')`: `it.todo('livesLost counts LIFE_LOST events, not (3 - livesRemaining) endpoint diff')`, `it.todo('rallyCurrent increments on PADDLE_HIT and resets to 0 on LIFE_LOST')`, `it.todo('longestRally is the running max of rallyCurrent, distinct from bestCombo (D-10 — must not collapse the two)')`.
- `describe('reduceRunTelemetry — largest explosive cascade (D-08, chosen heuristic)')`: `it.todo('a substep with >=1 EXPLOSIVE-flagged BRICK_BREAK counts the whole substep BRICK_BREAK total as one cascade group (documented cheap heuristic — Plan 01 locks this, not the grid-adjacency alternative)')`, `it.todo('a substep with only non-EXPLOSIVE BRICK_BREAK events does not update largestCascade')`, `it.todo('largestCascade is a running max across substeps, never reset mid-run')`.
- `describe('reduceRunTelemetry — worklet-safe, zero core mutation')`: `it.todo('never writes to any world.* field (read-only contract, same as consumeEventsForVfx)')`.
- `describe('headless integration smoke (mirrors tests/helpers/balanceBot.ts)')`: `it.todo('driving a real level fixture through stepRun + reduceRunTelemetry produces bricksBroken <= level brickCount and non-negative counters')`.

Every `it.todo` string must describe an observable behavior, not an implementation step — the
string itself becomes the task-completion checklist for Plan 01.
  </action>
  <verify>
    <automated>npx vitest run tests/telemetry.reduce-run-events.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/telemetry.reduce-run-events.test.ts` exits 0
    - `grep -c "it.todo(" tests/telemetry.reduce-run-events.test.ts` reports at least 13
    - `grep -c "^import" tests/telemetry.reduce-run-events.test.ts` reports exactly 1 (only the vitest import — no import of not-yet-existing `src/runtime/runStats`)
  </acceptance_criteria>
  <done>File exists, all cases are it.todo, Vitest reports 0 failed / N todo.</done>
</task>

<task type="auto">
  <name>Task 2: tests/storage.progress-v4.test.ts — it.todo scaffold + v3 fixture builder (N-STAT-02)</name>
  <files>tests/storage.progress-v4.test.ts</files>
  <read_first>
    .planning/phases/09-run-telemetry-storage-v4/09-CONTEXT.md,
    .planning/phases/09-run-telemetry-storage-v4/09-RESEARCH.md (Common Pitfall 4, Validation Architecture),
    tests/storage.progress-v3.test.ts
  </read_first>
  <action>
Create `tests/storage.progress-v4.test.ts` with `import { describe, it } from 'vitest';` and
`import { PLAYABLE_LEVEL_ORDER } from '../src/services/storage';` only (that barrel export
already exists — safe to import today). Add a local helper function `buildV3Fixture()` (plain
TS, no imports beyond the above) that returns a `JSON.stringify`-able plain object literal
shaped like today's v3 blob: `{ v: 3, unlocked: PLAYABLE_LEVEL_ORDER.slice(0, 3), bestByLevel:
{ [PLAYABLE_LEVEL_ORDER[0]]: { score: 1200, stars: 3 }, [PLAYABLE_LEVEL_ORDER[1]]: { score: 640,
stars: 2 } }, bestScore: 1200, updatedAt: 1700000000000 }` — this represents "an existing player"
or the round-trip test in Plan 03. Then add `it.todo(...)` groups (no expect calls yet):

- `describe('PROGRESS_KEY / VERSION (v4)')`: `it.todo('PROGRESS_KEY is @nbb/progress/v4 and PROGRESS_VERSION is 4; PROGRESS_KEY_V3 preserves the old @nbb/progress/v3 string as a legacy migrate-on-read source')`.
- `describe('migrateOrDefault (v4, extends the existing v3/v2/v1 chain)')`: `it.todo('valid v4 is preferred over v3/v2/v1')`, `it.todo('absent/corrupt v4 + valid v3 (buildV3Fixture) heals into v4 losing zero unlocked/bestByLevel/bestScore entries — v3ToV4 is lossless')`, `it.todo('absent v4 and v3 falls back to the existing v2/v1 chain unchanged')`, `it.todo('corrupt v4 does not prevent the v3 fallback from running')`.
- `describe('parseProgressResult (v4, telemetry validated independently — Pitfall 4)')`: `it.todo('ok v4 blob parses unlocked/bestByLevel/bestScore/telemetry all correctly')`, `it.todo('corrupt telemetry sub-object alone degrades ONLY telemetry to defaultTelemetryBlob(); unlocked/bestByLevel/bestScore survive untouched')`, `it.todo('structurally corrupt top-level JSON degrades the whole blob to defaults and never throws')`.
- `describe('recordRunEnd (v4, mode-aware, D-03/D-04)')`: `it.todo('win/lose behave exactly as v3 (stars/unlock win-gated); abandoned outcome merges score/stats but never touches stars/unlock')`, `it.todo('stats aggregate into both telemetry.lifetime and telemetry.byMode.campaign[levelId]')`, `it.todo('per-pickup-type and cascade/rally fields aggregate with the correct sum-vs-max semantics (D-07/D-08/D-10)')`.
- `describe('recentRuns ring buffer (D-05)')`: `it.todo('recentRuns never exceeds RECENT_RUNS_BOUND entries; oldest is evicted first (FIFO)')`, `it.todo('each entry is the small shape: mode, levelId, outcome, score, ticks, timestamp — no full stats blob')`.
- `describe('lifetime + per-level aggregates survive an app kill (SC-2)')`: `it.todo('re-instantiating an AsyncStorage-backed store from a previously persisted v4 JSON string reproduces identical lifetime + byMode aggregates')`.

Every `it.todo` string is the completion checklist for Plans 02/03 — do not add cases beyond
N-STAT-02's scope (no achievements, no stats-screen formatting).
  </action>
  <verify>
    <automated>npx vitest run tests/storage.progress-v4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/storage.progress-v4.test.ts` exits 0
    - `grep -c "it.todo(" tests/storage.progress-v4.test.ts` reports at least 14
    - `grep -c "function buildV3Fixture" tests/storage.progress-v4.test.ts` reports exactly 1
    - `npx vitest run tests/storage.progress-v3.test.ts` still exits 0 (this task must not touch the existing v3 file)
  </acceptance_criteria>
  <done>File exists with buildV3Fixture() + all cases as it.todo; Vitest reports 0 failed / N todo; existing v3 suite untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A this plan | Test-scaffold-only plan; no runtime or storage code changes yet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-00 | N/A | N/A | accept | No production code touched this plan — threat model applies starting Plan 01/02 |
</threat_model>

<verification>
`npx vitest run tests/telemetry.reduce-run-events.test.ts tests/storage.progress-v4.test.ts` exits 0
(all todo). `npx vitest run tests/storage.progress-v3.test.ts` still exits 0 (untouched baseline).
</verification>

<success_criteria>
Both Wave-0 scaffold files exist, contain no implementation imports, and enumerate every
behavior N-STAT-01/N-STAT-02 requires as a named `it.todo`. `buildV3Fixture()` exists for the
Plan 03 round-trip test.
</success_criteria>

<output>
Create `.planning/phases/09-run-telemetry-storage-v4/09-00-SUMMARY.md` when done
</output>
