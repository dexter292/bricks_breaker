---
phase: 09-run-telemetry-storage-v4
plan: 04
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-03"
files_modified:
  - app/_components/PlayingHost.tsx
autonomous: false
requirements:
  - N-STAT-01
  - N-STAT-02
must_haves:
  truths:
    - "WON and LOST runs record telemetry through the same recordRunEnd call, extended, not a parallel call site"
    - "Exiting to Menu mid-run (Pause button OR Android hardware back) records exactly one abandoned run, using the single existing onMenu funnel point"
    - "wallClockMs is measured from Date.now() at the app layer, separate from ticks x FIXED_DT"
    - "hashWorld / golden-replay is byte-identical before and after this phase"
    - "src/core/** has zero changed lines across the whole phase"
  artifacts:
    - path: "app/_components/PlayingHost.tsx"
      provides: "handleRunEnded extended to 4 args, handleMenuPress wrapping the single onMenu reference, runStartedAtRef"
      contains: "handleMenuPress"
  key_links:
    - from: "applyChrome WON/LOST branches"
      to: "handleRunEnded(mirror.score, outcome, mirror.lives, stats)"
      via: "cloneRunStats(runStats.value) + world.value.tick + Date.now() - runStartedAtRef.current"
      pattern: "cloneRunStats\\(runStats\\.value\\)"
    - from: "BackHandler effect + GameScreen onMenu prop"
      to: "handleMenuPress"
      via: "both existing onMenu call sites replaced with the same handleMenuPress reference (single funnel point, Pitfall 5)"
      pattern: "handleMenuPress"
---

<objective>
Wire the Plan 01 runtime reducer and the Plan 03 storage contract together at the one place
allowed to import both (`app/_components/PlayingHost.tsx`, per LC-04): extend `handleRunEnded`
to pass `mode`/`stats` into `store.recordRunEnd`, and add the single `handleMenuPress` wrapper
that turns an exit-to-Menu mid-run into a recorded `'abandoned'` run. Then verify the phase-wide
SC-5 guarantee (core untouched) and gate the two behaviors that are not automatable
(abandon-flush, wall-clock-excludes-pause) behind a human check.

Purpose: this is the only task in the phase that touches gameplay UI wiring — everything else
was pure runtime/storage logic.
Output: `PlayingHost.tsx` extended; full-suite green; SC-5 confirmed; manual QA sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-run-telemetry-storage-v4/09-CONTEXT.md
@.planning/phases/09-run-telemetry-storage-v4/09-RESEARCH.md
@.planning/phases/09-run-telemetry-storage-v4/09-PATTERNS.md
@.planning/phases/09-run-telemetry-storage-v4/09-VALIDATION.md
@app/_components/PlayingHost.tsx
@src/runtime/runStats.ts
@src/services/storage/types.ts

<interfaces>
<!-- Exact current PlayingHost.tsx shapes this task extends (verified by direct read of the live file). -->

Refs declared together (around the vfx/haptics setup block):
  const previousBestRef = useRef(0);
  const runEndedRef = useRef(false);
  ADD: const runStartedAtRef = useRef(Date.now()); // initial value = mount time = first run's start

useGameLoop call site currently destructures:
  const { picture, surfaceSize, setActive, retry, injectCertWorstCase, certOut, certSeq } = useGameLoop({...});
  ADD `runStats, world` to this destructure (both already returned by GameLoopHandle after Plan 01).

handleRunEnded (current, 3-arg):
  const handleRunEnded = useCallback((runScore: number, outcome: 'win' | 'lose', livesRemaining: number) => {
    const blob = store.recordRunEnd({ levelId, score: runScore, outcome, livesRemaining });
    ...
  }, [platform, store, levelId]);

applyChrome WON/LOST branches (current):
  if (mirror.phase === SIM.WON) { if (!runEndedRef.current) { runEndedRef.current = true; handleRunEnded(mirror.score, 'win', mirror.lives); } setResult('win'); setActive(false); }
  else if (mirror.phase === SIM.LOST) { if (!runEndedRef.current) { runEndedRef.current = true; handleRunEnded(mirror.score, 'lose', mirror.lives); } setResult('lose'); setActive(false); }

The 4 run-restart sites that reset `runEndedRef.current = false;` (onRetry, goNext,
toggleDevLevel, remountDevSession) — grep for `runEndedRef.current = false` to find all 4 exact
locations; every one must ALSO reset `runStartedAtRef.current = Date.now();` alongside it.

The single onMenu prop reference, used at exactly two call sites:
  1. BackHandler effect: `if (result != null || uiPhase === 'paused') { onMenu(); return true; }`
  2. GameScreen render prop: `<GameScreen ... onMenu={onMenu} ... />`
Both must be changed to reference the SAME new `handleMenuPress` callback — do not add a third
detection site (Pitfall 5).

src/runtime/runStats.ts (Plan 01) exports: `cloneRunStats(stats: RunStats | null): RunStats`
src/services/storage/types.ts (Plan 02) — `RunStatsInput` field names intentionally differ from
`RunStats` by omitting `rallyCurrent` and adding `ticksPlayed`/`wallClockMs`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire PlayingHost.tsx end to end</name>
  <files>app/_components/PlayingHost.tsx</files>
  <read_first>app/_components/PlayingHost.tsx, src/runtime/runStats.ts, src/services/storage/types.ts</read_first>
  <action>
Import `cloneRunStats` from `'../../src/runtime/runStats'` (new import line). Import `type
RunStatsInput` from `'../../src/services/storage'` (barrel export added in Plan 02).

Add `const runStartedAtRef = useRef(Date.now());` beside the existing `previousBestRef`/
`runEndedRef` declarations.

Add `runStats, world` to the `useGameLoop({...})` destructure (alongside the existing `picture,
surfaceSize, setActive, retry, injectCertWorstCase, certOut, certSeq`).

At every one of the 4 existing `runEndedRef.current = false;` sites (`onRetry`, `goNext`,
`toggleDevLevel`, `remountDevSession` — locate each via the exact line already in the file), add
`runStartedAtRef.current = Date.now();` on the next line.

Add a small local helper (module scope or top of the component, either is fine) that builds a
`RunStatsInput` from the runtime `RunStats` plus the two app-layer-only fields:
`function buildRunStatsInput(raw: RunStats, ticksPlayed: number, wallClockMs: number):
RunStatsInput { return { bricksBroken: raw.bricksBroken, bestCombo: raw.bestCombo,
pickupMultiball: raw.pickupMultiball, pickupExpand: raw.pickupExpand, pickupExtraLife:
raw.pickupExtraLife, pickupSlow: raw.pickupSlow, pickupFireball: raw.pickupFireball, livesLost:
raw.livesLost, longestRally: raw.longestRally, largestCascade: raw.largestCascade, ticksPlayed,
wallClockMs }; }` — this exact field-by-field mapping is required (do NOT pass the cloned
`RunStats` object directly into `recordRunEnd`; it lacks `ticksPlayed`/`wallClockMs` and carries
an extra `rallyCurrent` field that must not leak into the persisted shape). Import `type
RunStats` from `'../../src/runtime/runStats'` for this helper's parameter type.

Extend `handleRunEnded`'s signature to `(runScore: number, outcome: 'win' | 'lose' |
'abandoned', livesRemaining: number, stats: RunStatsInput)`. Inside the function body, change
the `store.recordRunEnd({ levelId, score: runScore, outcome, livesRemaining });` call to
`store.recordRunEnd({ mode: 'campaign', levelId, score: runScore, outcome, livesRemaining,
stats });`. Every existing branch inside `handleRunEnded` (`evaluatePersonalBest`,
stars-setting, `nextGateId`) stays exactly as-is — the win-gated branches already fall through
correctly for `'lose'` today and will do the same for `'abandoned'` since both are handled by
the same `else` path. Add `runStats`/`world` are NOT needed inside `handleRunEnded` itself (the
caller passes the finished `stats` object in) — do not widen its dependency array beyond what it
already needs.

In `applyChrome`'s WON and LOST branches, before each `handleRunEnded(...)` call, build the
snapshot: `const stats = buildRunStatsInput(cloneRunStats(runStats.value), world.value ?
world.value.tick : 0, Date.now() - runStartedAtRef.current);` then call
`handleRunEnded(mirror.score, 'win', mirror.lives, stats)` (WON branch) /
`handleRunEnded(mirror.score, 'lose', mirror.lives, stats)` (LOST branch).

Add a new `handleMenuPress` callback, placed near `handleRunEnded`/`applyChrome`:
`const handleMenuPress = useCallback(() => { if (!runEndedRef.current) { runEndedRef.current =
true; const stats = buildRunStatsInput(cloneRunStats(runStats.value), world.value ?
world.value.tick : 0, Date.now() - runStartedAtRef.current); handleRunEnded(score, 'abandoned',
lives, stats); } onMenu(); }, [runStats, world, score, lives, handleRunEnded, onMenu]);` — using
the current `score`/`lives` React state values (already in scope — the same state `applyChrome`
writes via `setScore`/`setLives` on every chrome update) as the abandon snapshot's
score/lives-remaining. Guard with the SAME `runEndedRef` used by WON/LOST so a run that already
ended via WON/LOST cannot double-record as abandoned (Pitfall 5's guard reuse).

Replace the bare `onMenu` reference with `handleMenuPress` at BOTH existing call sites: the
`BackHandler` effect body (`onMenu();` → `handleMenuPress();`, and add `handleMenuPress` to that
effect's dependency array in place of `onMenu`) and the `<GameScreen ... onMenu={onMenu} ... />`
prop (→ `onMenu={handleMenuPress}`). Do not add a third abandon-detection site anywhere else.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npx vitest run tests/telemetry.reduce-run-events.test.ts tests/storage.progress-v4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "handleMenuPress" app/_components/PlayingHost.tsx | wc -l` reports at least 4 (declaration + BackHandler call site + GameScreen prop + dependency array)
    - `grep -n "onMenu={onMenu}" app/_components/PlayingHost.tsx` returns no matches (replaced by `onMenu={handleMenuPress}`)
    - `grep -n "runStartedAtRef.current = Date.now()" app/_components/PlayingHost.tsx | wc -l` reports at least 4 (one per run-restart site)
    - `grep -n "mode: 'campaign'" app/_components/PlayingHost.tsx` matches inside the `store.recordRunEnd` call
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>PlayingHost.tsx records telemetry on win/lose/abandon through the single extended recordRunEnd call site, with wall-clock and ticks read at the app layer and abandon detected through the single onMenu funnel.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide SC-5 verification</name>
  <files></files>
  <read_first>.planning/phases/09-run-telemetry-storage-v4/09-VALIDATION.md</read_first>
  <action>
Run the full verification sweep the phase gate requires:
1. `git diff --stat -- src/core` — must produce EMPTY output. This is the cheapest possible
   proof that no plan in this phase touched `src/core/**` (SC-5's "hashWorld and core
   simulation are untouched").
2. `npx vitest run tests/physics.golden-replay.test.ts tests/physics.hash-canonical.test.ts` —
   both existing suites must pass unchanged, proving `hashWorld` is byte-identical.
3. `npx eslint src/core src/runtime` — confirms no new import from `services/` leaked into
   `core/` or `runtime/` (LC-04/LC-09 boundary).
4. `npm test` (full suite) — the 79-file/403-test baseline plus this phase's 2 new files must
   all pass with zero regressions.
5. `npm run typecheck` — clean.

If step 1 shows ANY output, STOP — a prior plan in this phase touched `src/core/**` in
violation of the phase's hardest constraint; do not proceed to Task 3 until this is resolved
(revert the offending change) and this step is re-run clean.
  </action>
  <verify>
    <automated>git diff --stat -- src/core | tee /tmp/sc5-core-diff.txt; test ! -s /tmp/sc5-core-diff.txt &amp;&amp; npx vitest run tests/physics.golden-replay.test.ts tests/physics.hash-canonical.test.ts &amp;&amp; npx eslint src/core src/runtime &amp;&amp; npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- src/core` produces empty output
    - `npx vitest run tests/physics.golden-replay.test.ts tests/physics.hash-canonical.test.ts` exits 0
    - `npx eslint src/core src/runtime` exits 0
    - `npm test` exits 0 (full suite, including the 2 new files from this phase)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>SC-5 confirmed: core untouched, hashWorld byte-identical, full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual QA — abandon-flush and wall-clock-excludes-pause</name>
  <files>app/_components/PlayingHost.tsx</files>
  <read_first>.planning/phases/09-run-telemetry-storage-v4/09-VALIDATION.md (Manual-Only Verifications table)</read_first>
  <action>
Present the built abandon-detection and wall-clock-tracking behavior for human UAT on a
simulator or device (this task automates nothing new itself — Task 1 already did — it only
gates on a human confirming the two behaviors VALIDATION.md's Manual-Only Verifications table
marks as not headlessly-testable). Do not change gameplay code during this checkpoint unless the
user reports a failure; if they do, fix and re-run Task 1's automated verify before re-presenting.
  </action>
  <what-built>
Telemetry now records on every run boundary (win, lose, or exit-to-Menu). Two behaviors from
VALIDATION.md's Manual-Only Verifications table cannot be asserted by the headless Vitest suite
because they depend on real RN component lifecycle (unmount ordering) and real elapsed wall-clock
time with an AppState background transition — neither is reproducible in the Node test
environment.
  </what-built>
  <how-to-verify>
1. **Abandoned-run flush on exit-to-Menu (N-STAT-01 / D-02):** Launch the app on a simulator or
   device. Start a level, break several bricks and catch at least one power-up, then press Pause
   → Menu (or use the Android hardware back button from mid-run). Re-enter Playing on the same
   level and check the store's persisted state (a `console.log` of `store.getSnapshot()` in
   `__DEV__`, or inspect via a debugger) — confirm exactly ONE `abandoned` entry was appended to
   `telemetry.recentRuns` with `bricksBroken > 0` and a `pickup*` count matching what you caught.
   Repeat once using the Android hardware back path specifically (not just the Pause button) and
   confirm it also records exactly one abandoned run — not zero, not two.
2. **Wall-clock excludes pause (N-STAT-01 / D-09):** Start a level. Play for approximately 30
   seconds, then Pause and wait approximately 30 seconds before Resuming, then play about 30 more
   seconds before losing or winning the run. Inspect the recorded run's wall-clock value — it
   should read approximately 60 seconds (±5s), not approximately 90 seconds. (Ticks played,
   separately, should reflect only simulated time and will differ from both readings — this is
   expected per D-09, not a bug.)
  </how-to-verify>
  <resume-signal>Type "approved" if both checks match, or describe what you observed instead.</resume-signal>
  <verify>
    <automated>npx vitest run tests/telemetry.reduce-run-events.test.ts tests/storage.progress-v4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - User replies "approved" (or equivalent) after both manual checks
    - Automated telemetry/storage suites still green at checkpoint time
  </acceptance_criteria>
  <done>Abandon-flush and wall-clock-excludes-pause behaviors human-verified on device/simulator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cross-thread SharedValue read (runStats.value / world.value) | Same trusted process, JS thread reading UI-thread state; Reanimated-guaranteed synchronous read, no serialization boundary |
| PlayingHost ↔ ProgressStore | Cold-path call, app-layer only (LC-04); no worklet ever calls recordRunEnd |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10 | Repudiation | handleMenuPress double-fire | mitigate | Reuses the existing `runEndedRef` guard shared with WON/LOST — a run can only be finalized once, from whichever path reaches it first |
| T-09-11 | Tampering | src/core untouched guarantee | mitigate | Task 2's `git diff --stat -- src/core` gate blocks phase completion if violated |
</threat_model>

<verification>
`npm run typecheck && npm run lint && npm test` all exit 0. `git diff --stat -- src/core` is
empty. Golden-replay/hash-canonical suites pass unchanged. Manual QA (Task 3) approved.
</verification>

<success_criteria>
Every run (win, lose, or abandon) records through the single extended `recordRunEnd` call site;
wall-clock and ticks are read at the app layer, not duplicated in the runtime reducer; SC-5 is
verified; manual QA confirms the two non-automatable behaviors.
</success_criteria>

<output>
Create `.planning/phases/09-run-telemetry-storage-v4/09-04-SUMMARY.md` when done
</output>
