---
phase: 02-headless-core-simulation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - tests/core.purity.test.ts
autonomous: true
requirements:
  - PHYS-06
must_haves:
  truths:
    - "fast-check and @fast-check/vitest are installed as pinned devDependencies"
    - "ESLint fails on Math.random / Date.now / performance.now under src/core/**"
    - "eslint-plugin-boundaries patterns match nested src/core/** modules"
    - "purity Vitest scan rejects Math.random / Date.now / performance.now in core/"
  artifacts:
    - path: "package.json"
      provides: "fast-check@4.10.2 + @fast-check/vitest@0.5.0"
      contains: "fast-check"
    - path: "eslint.config.js"
      provides: "D-13 RNG/clock bans + nested core boundaries"
      contains: "Math.random"
    - path: "tests/core.purity.test.ts"
      provides: "Regex purity gate for RNG/clock"
      contains: "Math\\.random"
  key_links:
    - from: "eslint.config.js"
      to: "src/core/**/*"
      via: "no-restricted-syntax Math.random / Date.now / performance.now"
      pattern: "no-restricted-syntax"
    - from: "tests/core.purity.test.ts"
      to: "src/core"
      via: "walk + RNG/clock regex"
      pattern: "Math\\.random|Date\\.now|performance\\.now"
---

<objective>
Install property-test tooling and harden `core/` purity gates for D-13 (no `Math.random` / wall-clock) plus nested-directory ESLint boundaries — Wave 0 blockers before any physics code.

Purpose: Unlock PHYS-03 property suite and PHYS-06 determinism lint before World/physics land.
Output: pinned fast-check deps, extended eslint.config.js, extended purity test; existing smoke still green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-headless-core-simulation/02-CONTEXT.md
@.planning/phases/02-headless-core-simulation/02-RESEARCH.md
@.planning/phases/02-headless-core-simulation/02-VALIDATION.md
@eslint.config.js
@package.json
@tests/core.purity.test.ts
@docs/layer-contract.md

## Locked decisions
- D-13 dual seeded PRNG; ban Math.random / wall-clock in core/
- D-15 core/ purity / worklet-safe TS (Phase 1 LC-01/LC-06)

## Discretion
- Prefer `@fast-check/vitest` connector (research A5)
- Use Node 24 via `export PATH="/opt/homebrew/opt/node@24/bin:$PATH"` (or nvm) — default Homebrew node may be dyld-broken
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fast-check + @fast-check/vitest on Node 24</name>
  <files>package.json, package-lock.json</files>
  <read_first>package.json, .nvmrc, .planning/phases/02-headless-core-simulation/02-RESEARCH.md, .planning/phases/02-headless-core-simulation/02-VALIDATION.md</read_first>
  <action>
Confirm Node major is 24 (`node -v` starts with `v24`); if not, put `node@24` first on PATH (`.nvmrc` / Homebrew `node@24`) before continuing.

Install non-native test deps with plain npm (per D-13 research; not `expo install`):
```bash
npm install -D fast-check@4.10.2 @fast-check/vitest@0.5.0
```

Confirm `package.json` already has `"test:core": "vitest run src/core tests"` — do **not** remove it. Do not add native modules. Do not implement physics yet.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; node -e "const v=process.versions.node; if(!v.startsWith('24.')) { console.error('need Node 24, got', v); process.exit(1);} " && npm ls fast-check@4.10.2 @fast-check/vitest@0.5.0 && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies` lists `"fast-check": "4.10.2"`
    - `package.json` `devDependencies` lists `"@fast-check/vitest": "0.5.0"`
    - `package.json` scripts include `"test:core"`
    - `npm run test:core` exits 0 with current Phase 1 smoke/purity still passing
  </acceptance_criteria>
  <done>fast-check 4.10.2 + @fast-check/vitest 0.5.0 installed; existing core tests green on Node 24.</done>
</task>

<task type="auto">
  <name>Task 2: ESLint RNG/clock bans + nested core boundaries</name>
  <files>eslint.config.js</files>
  <read_first>eslint.config.js, docs/layer-contract.md, .planning/phases/02-headless-core-simulation/02-RESEARCH.md</read_first>
  <action>
Edit `eslint.config.js` only:

1. In the existing `files: ['src/core/**/*.{ts,tsx}']` block, **add** (keep existing `no-restricted-imports`):
   - `no-restricted-globals`: error on `performance` with message citing D-13 / PHYS-06
   - `no-restricted-syntax`: error selectors for:
     - `CallExpression[callee.object.name='Math'][callee.property.name='random']`
     - `CallExpression[callee.object.name='Date'][callee.property.name='now']`
     - `CallExpression[callee.object.name='performance'][callee.property.name='now']`
   Use the exact selectors from 02-RESEARCH.md § ESLint ban for RNG / clock. Messages must mention D-13.

2. In `boundaries/elements`, change every layer pattern from single-segment `src/X/*` to nested `src/X/**` (and `app/**`) so upcoming `src/core/physics/`, `events/`, `rng/` are still typed as `core`:
   - `{ type: 'core', pattern: 'src/core/**' }` (and same `**` for runtime, render, input, ui, vfx, services, app)

Do **not** weaken FORBIDDEN_IN_CORE or LC-07 hot-path rules. Do not add physics source files yet.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx eslint src/core && node -e "const fs=require('fs'); const s=fs.readFileSync('eslint.config.js','utf8'); if(!s.includes(\"Math'][callee.property.name='random'\") && !s.includes('Math.random')) { console.error('missing Math.random ban'); process.exit(1);} if(!s.includes(\"pattern: 'src/core/**'\") && !s.includes('src/core/**')) { console.error('missing nested core pattern'); process.exit(1);} "</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` contains a `no-restricted-syntax` selector for `Math` + `random`
    - `eslint.config.js` bans `Date.now` and `performance.now` under `src/core/**`
    - `boundaries/elements` uses `src/core/**` (not only `src/core/*`)
    - `npx eslint src/core` exits 0 on current stub sources
  </acceptance_criteria>
  <done>ESLint enforces D-13 RNG/clock bans and nested core boundaries without breaking current core lint.</done>
</task>

<task type="auto">
  <name>Task 3: Extend purity Vitest scan for RNG/clock</name>
  <files>tests/core.purity.test.ts</files>
  <read_first>tests/core.purity.test.ts, .planning/phases/02-headless-core-simulation/02-VALIDATION.md</read_first>
  <action>
Extend `tests/core.purity.test.ts` (keep existing platform-import + ≥5 module tests):

Add a second `it(...)` that walks `src/core` recursively (reuse `walk`) and fails if any non-test `.ts` file source matches `/Math\.random|Date\.now|performance\.now/`. Message should cite D-13.

Do not change module-count threshold (≥5). Do not stub physics files yet.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/core.purity.test.ts && grep -E 'Math\\.random|Date\\.now|performance\\.now' tests/core.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/core.purity.test.ts` contains a regex covering `Math.random`, `Date.now`, and `performance.now`
    - `npx vitest run tests/core.purity.test.ts` exits 0
    - Existing platform-import purity test still present
  </acceptance_criteria>
  <done>Purity suite gates D-13 RNG/clock in addition to platform imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Intent → core (future) | Untrusted numeric intent will enter stepWorld; Wave 0 only hardens purity so sim cannot call wall-clock/RNG globals |
| core → host | core must remain import-pure; illegal imports are elevation of privilege into RN/Skia |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Intent / future stepWorld | mitigate | Wave 0 installs gates; Wave 3 adds `Number.isFinite` on Intent (plan 02-04) |
| T-02-02 | Spoofing | Non-determinism via Math.random/Date | mitigate | ESLint `no-restricted-syntax` + purity regex ban under `src/core/**` (this plan) |
| T-02-03 | Elevation | Illegal platform imports in nested core dirs | mitigate | Update boundaries to `src/core/**`; keep FORBIDDEN_IN_CORE |
</threat_model>

<verification>
`npm ls fast-check@4.10.2 @fast-check/vitest@0.5.0` succeeds; `npx eslint src/core` and `npm run test:core` green.
</verification>

<success_criteria>
Wave 0 tooling complete: property-test deps installed; D-13 lint + purity bans live; nested core paths recognized by boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-core-simulation/02-00-SUMMARY.md`
</output>
