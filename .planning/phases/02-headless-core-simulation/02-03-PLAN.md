---
phase: 02-headless-core-simulation
plan: 03
type: tdd
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/core/physics/resolve.ts
  - src/core/constants.ts
  - src/core/index.ts
  - tests/physics.paddle.test.ts
autonomous: true
requirements:
  - PHYS-04
must_haves:
  truths:
    - "Paddle bounce maps impact t∈[-1,1] to outgoing angle within ±PADDLE_ANGLE_CLAMP_DEG"
    - "Paddle bounce preserves speed within 1e-4"
    - "Clamp edges (±1) and near-horizontal inputs produce non-degenerate trajectories (min vertical)"
  artifacts:
    - path: "src/core/physics/resolve.ts"
      provides: "reflectWallOrBrick + resolvePaddleEnglish"
      exports: ["resolvePaddleEnglish", "reflectVelocity"]
    - path: "tests/physics.paddle.test.ts"
      provides: "PHYS-04 clamp-edge + speed preservation tests"
  key_links:
    - from: "tests/physics.paddle.test.ts"
      to: "src/core/physics/resolve.ts"
      via: "resolvePaddleEnglish"
      pattern: "resolvePaddleEnglish|PADDLE_ANGLE_CLAMP"
    - from: "src/core/physics/resolve.ts"
      to: "src/core/constants.ts"
      via: "named clamp constants (tests import exports; worklets inline literals)"
      pattern: "PADDLE_ANGLE_CLAMP"
---

<objective>
Implement classic Breakout paddle english with configurable angle clamps and speed preservation, plus generic wall/brick velocity reflect — verified at clamp edges (PHYS-04 / D-01…D-03).

Purpose: Skill-expressive bounce without soft energy loss or degenerate angles.
Output: `src/core/physics/resolve.ts` + green `tests/physics.paddle.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-core-simulation/02-CONTEXT.md
@.planning/phases/02-headless-core-simulation/02-RESEARCH.md
@.planning/phases/02-headless-core-simulation/02-PATTERNS.md
@.planning/phases/02-headless-core-simulation/02-01-SUMMARY.md

## Locked decisions
- D-01 classic Breakout paddle-relative bounce
- D-02 preserve speed; configurable clamps (~±62°)
- D-03 prevent near-horizontal / near-vertical degenerates; test clamp edges
- Coordinate system: y-down; post-paddle bounce vy negative (up)

## Parallelism note
This plan does not modify `sweep.ts` — may run parallel with 02-02 after 02-01.
</context>

<interfaces>
```typescript
/** Reflect velocity about unit normal; preserve speed. */
export function reflectVelocity(vx: number, vy: number, nx: number, ny: number): { vx: number; vy: number };

/**
 * Classic english: t = clamp((ballX - paddleCx) / paddleHalfW, -1, 1)
 * angleFromUp = t * PADDLE_ANGLE_CLAMP_RAD
 * vx = sin(angle)*speed; vy = -cos(angle)*speed  // y-down
 */
export function resolvePaddleEnglish(
  ballX: number,
  paddleCx: number,
  paddleHalfW: number,
  vx: number,
  vy: number,
): { vx: number; vy: number };
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — paddle english / clamp tests</name>
  <files>tests/physics.paddle.test.ts</files>
  <read_first>.planning/phases/02-headless-core-simulation/02-RESEARCH.md, .planning/phases/02-headless-core-simulation/02-VALIDATION.md, src/core/constants.ts</read_first>
  <behavior>
    - t=+1 (ball at right paddle edge) → outgoing angle ≈ +PADDLE_ANGLE_CLAMP_DEG from vertical (within 1e-3 rad)
    - t=-1 → ≈ −clamp
    - t=0 → nearly straight up (vx≈0, vy&lt;0)
    - hypot(vx,vy) after resolve equals pre-hit speed within 1e-4 (D-02)
    - Near-horizontal inbound velocity still exits within clamp / min vertical ratio (D-03)
    - reflectVelocity on vertical wall negates vx only; speed preserved
  </behavior>
  <action>
Write `tests/physics.paddle.test.ts` importing `resolvePaddleEnglish`, `reflectVelocity`, and `PADDLE_ANGLE_CLAMP_DEG` from `../src/core`. Do not implement resolve yet — confirm RED. Title describe with PHYS-04.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/physics.paddle.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - Test file asserts clamp-edge angles and speed preservation
    - First vitest run fails (RED)
  </acceptance_criteria>
  <done>Failing PHYS-04 paddle tests exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — resolve.ts paddle english + reflect</name>
  <files>src/core/physics/resolve.ts, src/core/constants.ts, src/core/index.ts, tests/physics.paddle.test.ts</files>
  <read_first>tests/physics.paddle.test.ts, .planning/phases/02-headless-core-simulation/02-RESEARCH.md Pattern 2, src/core/constants.ts</read_first>
  <behavior>
    - Same as Task 1 — GREEN
  </behavior>
  <action>
Implement `resolve.ts` per RESEARCH Pattern 2 / D-01…D-03:

- Named exports matching `<interfaces>`
- `PADDLE_ANGLE_CLAMP_DEG = 62` already in constants — also export `PADDLE_ANGLE_CLAMP_RAD` and `MIN_VERTICAL_RATIO` (≈ cos(clamp)) for tests
- Worklet bodies **inline** matching literals (do not close over module consts)
- After english, enforce min |vy| / reject near-horizontal by re-clamping angle if needed
- `reflectVelocity` for walls/bricks: v' = v − 2 dot(v,n) n; preserve speed; if dot(v,n)≥0 after, nudge

Export from barrel. No sweep changes. No Math.random.

Run until GREEN: `npx vitest run tests/physics.paddle.test.ts`.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/physics.paddle.test.ts && npx eslint src/core && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/physics/resolve.ts` exports `resolvePaddleEnglish` and `reflectVelocity` with `'worklet'`
    - `grep PADDLE_ANGLE_CLAMP src/core/constants.ts` matches
    - `npx vitest run tests/physics.paddle.test.ts` exits 0
    - Speed preservation assertions use ε ≤ 1e-4
  </acceptance_criteria>
  <done>Classic Breakout english + clamps green under PHYS-04 tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hit → resolve | Collision normals/paddleX must be finite; resolve must not emit NaN velocity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Non-finite ball/paddle inputs | mitigate | If !Number.isFinite, return prior velocity unchanged |
| T-02-02 | Spoofing | Soft/random bounce | mitigate | Deterministic angle from t; no RNG; speed preserved exactly via hypot |
</threat_model>

<verification>
paddle unit tests green; core lint + full test:core green.
</verification>

<success_criteria>
PHYS-04 satisfied at unit level: paddle-relative clamps at edges, speed preserved, degenerates rejected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-core-simulation/02-03-SUMMARY.md`
</output>
