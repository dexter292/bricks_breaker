---
phase: 02-headless-core-simulation
plan: 02
type: tdd
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/core/physics/integrate.ts
  - src/core/physics/sweep.ts
  - src/core/physics/broadphase.ts
  - tests/physics.sweep.test.ts
autonomous: true
requirements:
  - PHYS-02
  - PHYS-03
must_haves:
  truths:
    - "Swept circle-vs-AABB reports earliest TOI in [0,1] with face or corner normals"
    - "Grid broadphase walks only candidate cells (no quadtree)"
    - "Unit tests hit wall, paddle AABB, and single brick at designed max speed without tunneling"
  artifacts:
    - path: "src/core/physics/sweep.ts"
      provides: "sweepCircleAabb TOI + normal"
      exports: ["sweepCircleAabb"]
    - path: "src/core/physics/broadphase.ts"
      provides: "grid cell candidate walk"
    - path: "src/core/physics/integrate.ts"
      provides: "advanceBall by t * vel"
    - path: "tests/physics.sweep.test.ts"
      provides: "PHYS-02 unit coverage for walls/paddle/brick"
  key_links:
    - from: "tests/physics.sweep.test.ts"
      to: "src/core/physics/sweep.ts"
      via: "deep-import sweepCircleAabb (barrel owned by 02-04)"
      pattern: "sweepCircleAabb|physics/sweep"
    - from: "src/core/physics/broadphase.ts"
      to: "World.cellToBrick"
      via: "grid as acceleration structure"
      pattern: "cellToBrick|gridCols"
---

<objective>
Implement Minkowski-expanded swept circle-vs-AABB, ball integrate helper, and brick-grid broadphase — proven by Vitest unit tests at designed max speed (PHYS-02 foundation; property suite lands in 02-05).

Purpose: Continuous collision primitives with no discrete-only path (D-05/D-12).
Output: `src/core/physics/{sweep,broadphase,integrate}.ts` + green `tests/physics.sweep.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-headless-core-simulation/02-CONTEXT.md
@.planning/phases/02-headless-core-simulation/02-RESEARCH.md
@.planning/phases/02-headless-core-simulation/02-PATTERNS.md
@.planning/phases/02-headless-core-simulation/02-01-SUMMARY.md

## Locked decisions
- D-04 MAX_BALL_SPEED constant (already in constants from 02-01)
- D-05 swept CCD required (2× prop in later plan)
- D-12 brick grid is the broadphase — no quadtree
- D-15 worklet + inline literals

## Do not
- Implement paddle english (plan 02-03)
- Wire full stepWorld CCD loop (plan 02-04)
- Build render/gestures/levels
</context>

<interfaces>
From `src/core/types.ts` (plan 02-01):
```typescript
export type Hit = {
  hit: boolean;
  t: number;
  nx: number;
  ny: number;
  kind: number;
  index: number;
};
```

Target APIs:
```typescript
// integrate.ts
export function advanceBall(world: World, i: number, t: number): void;

// sweep.ts — Minkowski expand AABB by radius; ray slab on center segment (cx,cy)+(dx,dy)
export function sweepCircleAabb(
  cx: number, cy: number, radius: number,
  dx: number, dy: number,
  minX: number, minY: number, maxX: number, maxY: number,
): { hit: boolean; t: number; nx: number; ny: number };

// broadphase.ts — given segment, fill `out` with unique brick indices overlapped by swept circle (hot path)
export function collectBrickCandidatesInto(
  world: World,
  x0: number, y0: number, x1: number, y1: number, radius: number,
  out: Int16Array,
): number;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — physics.sweep unit tests</name>
  <files>tests/physics.sweep.test.ts</files>
  <read_first>.planning/phases/02-headless-core-simulation/02-RESEARCH.md, .planning/phases/02-headless-core-simulation/02-VALIDATION.md, src/core/constants.ts, src/core/types.ts</read_first>
  <behavior>
    - Horizontal flight into a left/right wall AABB at speed MAX_BALL_SPEED → hit===true, t in [0,1], normal points inward
    - Vertical flight into top wall → hit; bottom death plane may be tested as wall or deferred to step backstop
    - Flight into paddle AABB from above at MAX_BALL_SPEED → hit with upward-ish normal (ny &lt; 0 in y-down? document: after expand, normal separates centers — assert hit and finite normal)
    - Flight through a single brick cell at MAX_BALL_SPEED starting outside → hit===true (no miss)
    - Miss case: displacement away from AABB → hit===false
    - Corner approach → non-axis-aligned normal (both nx, ny nonzero) within epsilon
  </behavior>
  <action>
Create `tests/physics.sweep.test.ts` deep-importing from `../src/core/physics/sweep` (and sibling physics modules as needed) — **do not** edit `src/core/index.ts` (barrel consolidation is plan 02-04; avoids wave-2 parallel race). Use Vitest `describe`/`it`/`expect`. Do **not** implement sweep yet — run once and confirm RED (import or assertion failure). Cover the behaviors above with explicit numeric fixtures (logical 360×640). Cite PHYS-02 in describe title.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/physics.sweep.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `tests/physics.sweep.test.ts` exists and deep-imports `sweepCircleAabb` from `src/core/physics/sweep` (not via barrel-only)
    - First run fails (RED) before Task 2 implementation
    - File includes cases for wall, paddle AABB, and brick
  </acceptance_criteria>
  <done>Failing PHYS-02 sweep unit tests committed as RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — integrate + sweepCircleAabb + broadphase</name>
  <files>src/core/physics/integrate.ts, src/core/physics/sweep.ts, src/core/physics/broadphase.ts, tests/physics.sweep.test.ts</files>
  <read_first>tests/physics.sweep.test.ts, .planning/phases/02-headless-core-simulation/02-RESEARCH.md, src/core/constants.ts</read_first>
  <behavior>
    - Same as Task 1 — all sweep unit tests GREEN
  </behavior>
  <action>
Implement per RESEARCH Pattern 1 (Shared Patterns from 02-PATTERNS.md: `'worklet'`, inline literals, SoA mutate in place, no alloc):

1. `integrate.ts` — `advanceBall(world, i, t)` mutates `ballX/Y` by `vel * t`; `'worklet'`; no alloc.
2. `sweep.ts` — expand AABB by radius; ray-vs-AABB slab for TOI in [0,1]; on face hit use face normal; on corner region use normalize(center − corner); reject grazing with careful eps; `'worklet'`; inline literals.
3. `broadphase.ts` — treat `gridCols/gridRows` + `cellToBrick` as acceleration structure (D-12); walk cells overlapped by swept expanded segment; skip `-1`; invoke visit once per unique brick index; `'worklet'`.

**Do not** edit `src/core/index.ts` — wave-2 parallel with 02-03; barrel re-exports land in plan 02-04 Task 1. Tests keep deep-imports. No paddle english. No Math.random. No platform imports.

Run until GREEN: `npx vitest run tests/physics.sweep.test.ts`.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/physics.sweep.test.ts && npx eslint src/core && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/physics/sweep.ts` exports `sweepCircleAabb` with `'worklet'`
    - `src/core/physics/broadphase.ts` references `cellToBrick` / grid dimensions
    - `src/core/physics/integrate.ts` exports `advanceBall`
    - `npx vitest run tests/physics.sweep.test.ts` exits 0
    - No `Math.random` under `src/core/physics`
  </acceptance_criteria>
  <done>Swept CCD primitives + broadphase green under PHYS-02 unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Geometry inputs → sweep | Callers pass finite positions/velocities; garbage in must not yield NaN TOI that poisons World |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | NaN displacement into sweep | mitigate | Early-out if !Number.isFinite on inputs; return hit=false |
| T-02-02 | Spoofing | Non-deterministic collision order | mitigate | Deterministic slab math only; no RNG in sweep/broadphase |
| T-02-05 | DoS | Infinite CCD candidate loops | mitigate | Broadphase visits finite grid cells only; iteration cap owned by stepWorld (02-04) |
</threat_model>

<verification>
`npx vitest run tests/physics.sweep.test.ts` green; eslint + test:core green.
</verification>

<success_criteria>
PHYS-02 unit primitives exist: swept hits against walls/paddle/brick AABBs with corner normals; grid broadphase only.
</success_criteria>

<output>
After completion, create `.planning/phases/02-headless-core-simulation/02-02-SUMMARY.md`
</output>
