---
phase: 01-foundation-thread-boundary-spike
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - docs/device-gate-results.md
  - docs/skia-version-decision.md
  - .planning/phases/01-foundation-thread-boundary-spike/01-04-SUMMARY.md
autonomous: false
requirements:
  - ARCH-01
user_setup:
  - service: apple-developer
    why: "D-17 — verify signing/provisioning before EAS iOS internal build; physical iPhone install is mandatory"
    dashboard_config:
      - task: "Run `npx eas-cli@latest credentials -p ios` and confirm distribution cert + provisioning profile"
        location: "Apple Developer + Expo credentials"
  - service: android-device
    why: "D-01 Pixel 6a is the FPS gate (D-04 substitute allowed with full docs)"
    dashboard_config:
      - task: "Attach Pixel 6a (or document substitute: model, chipset, OS, refresh rate)"
        location: "USB debugging / wireless adb"
must_haves:
  truths:
    - "Dev-client build installs and runs on a physical iPhone and on the named Android reference device"
    - "Release/profiling build on Android shows worklet world mutation PASS and ~200–300 sprites holding measured 60 FPS (or recorded failure + fallback decision)"
    - "Skia 2.12.0 bet confirmed on both platforms OR fallback to 2.6.2 recorded in docs/skia-version-decision.md"
    - "Production profile overlay flag verified OFF; methodology + device-gate results documented"
  artifacts:
    - path: "docs/device-gate-results.md"
      provides: "Recorded SC-1/SC-2/SC-3 evidence with device/build/tool attribution"
      contains: "Pixel"
    - path: "docs/skia-version-decision.md"
      provides: "D-16 confirmation or fallback"
      contains: "2.12.0"
    - path: "docs/measurement-methodology.md"
      provides: "Pre-existing methodology referenced by results"
  key_links:
    - from: "docs/device-gate-results.md"
      to: "adb dumpsys gfxinfo / Instruments"
      via: "recorded frame stats for profiling build"
      pattern: "gfxinfo|Instruments|framestats"
    - from: "docs/skia-version-decision.md"
      to: "package.json Skia version"
      via: "asserted installed version matches decision"
      pattern: "2\\.12\\.0|2\\.6\\.2"
---

<objective>
Prove the architectural bet on real hardware: EAS/local release+dev-client installs on physical iPhone and Android reference device, worklet mutation in release, ~200–300 sprite 60 FPS gate on Pixel 6a (or documented substitute), Skia version decision recorded, overlay absent from production profile.

Purpose: Close Phase 1 success criteria SC-1..SC-3 with evidence that cannot come from simulators or the RN perf monitor (D-05).
Output: `docs/device-gate-results.md`, `docs/skia-version-decision.md`, human-approved checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
@$HOME/.cursor/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-foundation-thread-boundary-spike/01-CONTEXT.md
@.planning/phases/01-foundation-thread-boundary-spike/01-RESEARCH.md
@.planning/phases/01-foundation-thread-boundary-spike/01-03-SUMMARY.md
@docs/measurement-methodology.md
@eas.json
@package.json

## Locked decisions
- D-01 Pixel 6a Android FPS gate
- D-02 physical iPhone install/feel
- D-03 release/profile required to close phase
- D-04 substitute Android docs + Pixel 6a re-cert before MVP
- D-05 never claim from sim/dev-only/untested HW
- D-16 Skia 2.12.0 or recorded fallback 2.6.2
- D-17 paid ADP + verify signing before iOS build
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Preflight — credentials, doctor, Skia assert, build both profiles</name>
  <files>docs/skia-version-decision.md, docs/device-gate-results.md</files>
  <read_first>eas.json, package.json, scripts/assert-skia-version.mjs, docs/measurement-methodology.md, .planning/phases/01-foundation-thread-boundary-spike/01-CONTEXT.md</read_first>
  <action>
1. **D-17 gate (hard):** Run `npx eas-cli@latest credentials -p ios` (or Expo dashboard equivalent) and confirm paid Apple Developer Program credentials + provisioning for EAS **internal** distribution. If credentials/provisioning/membership are missing, **STOP** — surface `checkpoint:human-action` blocker; do **not** claim iOS complete; do **not** fall back to free personal team / 7-day profiles. Physical iPhone install via paid ADP EAS internal distribution is mandatory for Phase 1 close (D-17).
2. Run `npm run assert:skia` and `npx expo-doctor@latest`. Expected: doctor may warn about Skia mismatch — that is intentional while on 2.12.0.
3. Create stub `docs/device-gate-results.md` with sections: Devices, Builds, SC-1 Install, SC-2 Worklet mutation, SC-3 FPS, Cliff ramp (research), Overlay production check, Failures/fallbacks.
4. Create `docs/skia-version-decision.md` initially stating target `2.12.0` and "pending first hardware build".
5. Build **development** (dev-client) and **profiling** profiles for Android and iOS via `npx eas-cli@latest build --profile development|profiling --platform android|ios` **or** local equivalents:
   - Android: `npx expo run:android --variant release` for profiling-equivalent; `npx expo run:android` with dev-client for daily
   - iOS: device install paths as above
6. If Skia 2.12.0 **fails to build/run** on either platform: fall back per D-16 — set dependency to `@shopify/react-native-skia@2.6.2`, keep `expo.install.exclude` removed or updated so fix aligns, update `scripts/assert-skia-version.mjs` expected version to `2.6.2`, rebuild, and record the decision + reason in `docs/skia-version-decision.md`. Do not silently stay on a broken pin.
7. After a successful profiling Android install, write a machine-checkable note into `docs/device-gate-results.md` that production env does not set overlay: `node -e` parse `eas.json` and assert `build.production.env` lacks `EXPO_PUBLIC_PERF_OVERLAY` (T-01-01). Optionally build production once if cheap and confirm overlay not visible — record result.
  </action>
  <verify>
    <automated>npm run assert:skia &amp;&amp; node -e "const e=require('./eas.json'); if(e.build?.production?.env?.EXPO_PUBLIC_PERF_OVERLAY) process.exit(1); console.log('production overlay unset OK');" &amp;&amp; test -f docs/device-gate-results.md &amp;&amp; test -f docs/skia-version-decision.md &amp;&amp; grep -E "2\\.12\\.0|2\\.6\\.2" docs/skia-version-decision.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/skia-version-decision.md` exists and states either confirmed `2.12.0` or fallback `2.6.2` with reason
    - `docs/device-gate-results.md` exists with SC-1/SC-2/SC-3 section headings
    - `eas.json` production profile does not set `EXPO_PUBLIC_PERF_OVERLAY` (script exit 0)
    - `npm run assert:skia` matches the decided Skia version
    - SUMMARY records which build path was used (EAS vs local) per platform
  </acceptance_criteria>
  <done>Credentials preflighted; builds produced; Skia decision doc started/updated; overlay production hygiene asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device gates — install, worklet PASS, Pixel 6a 60 FPS</name>
  <files>docs/device-gate-results.md, docs/skia-version-decision.md</files>
  <read_first>docs/device-gate-results.md, docs/measurement-methodology.md, docs/skia-version-decision.md</read_first>
  <what-built>
    Dev-client and profiling/release builds of the FPS harness (opaque Skia canvas, ~200–300 sprites, worklet loop, overlay when EXPO_PUBLIC_PERF_OVERLAY=1). Measurement methodology in docs/measurement-methodology.md. Results template in docs/device-gate-results.md.
  </what-built>
  <action>
Pause for human on-device verification of SC-1/SC-2/SC-3 per how-to-verify. Executor must not mark Phase 1 complete until resume-signal. After human fills docs/device-gate-results.md, continue to Task 3.
  </action>
  <how-to-verify>
1. **SC-1 iOS (D-02, D-17):** Install on physical iPhone. Launch harness. Confirm canvas renders, touch does not crash, overlay readable on profiling/dev overlay-on build. Record model + iOS version in docs/device-gate-results.md.
2. **SC-1 Android:** Install on Pixel 6a (or D-04 substitute with model/chipset/OS/refresh rate documented). Launch successfully.
3. **SC-2 worklet (dev + release/profiling):** Confirm overlay self-check shows PASS (tick advancing / sprites moving) on **both** a development build and a profiling/release build on Android; also verify on iOS at least once in release/profiling. Any freezeObjectInDev warning in Metro logs = FAIL until fixed.
4. **SC-3 FPS (D-01, D-03, D-05):** On Pixel 6a (or documented substitute) using **profiling/release** only:
   - `adb shell dumpsys gfxinfo &lt;package&gt; reset`
   - Run 30s measurement (keep-awake on), discard first ~2s mentally/in notes
   - `adb shell dumpsys gfxinfo &lt;package&gt; framestats` (or equivalent percentiles)
   - Cross-read in-app overlay rolling FPS / p95
   - Pass criterion: stable ~60 FPS at 256± sprites (within D-06 band). Record numbers, thermal note, two runs.
5. **D-07 cliff (research only):** Ramp sprite count until budget breaks; record cliff count — does **not** block phase pass.
6. **Compositing:** Confirm opaque Canvas SurfaceView path on Android if feasible (`dumpsys` SurfaceFlinger/window); note result.
7. Paste final numbers into docs/device-gate-results.md. Update docs/skia-version-decision.md to **Confirmed** or **Fallback adopted**.
8. Type `approved` if SC-1..SC-3 pass (or SC-3 fails with explicit architecture fallback decision recorded — still need human acknowledgment). Describe issues otherwise.
  </how-to-verify>
  <verify>
    <automated>test -f docs/device-gate-results.md &amp;&amp; grep -E "PASS|FAIL" docs/device-gate-results.md</automated>
  </verify>
  <acceptance_criteria>
    - Human typed `approved` (or recorded explicit fallback acknowledgment)
    - `docs/device-gate-results.md` contains PASS or FAIL for worklet check
    - Device model string present in results doc
  </acceptance_criteria>
  <done>Human verified SC-1/SC-2/SC-3 (or recorded accepted fallback); results doc updated.</done>
  <resume-signal>Type "approved" when device-gate-results.md is filled and you accept the evidence; or describe failures / fallback choice</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Finalize evidence docs after human approval</name>
  <files>docs/device-gate-results.md, docs/skia-version-decision.md</files>
  <read_first>docs/device-gate-results.md, docs/skia-version-decision.md, docs/measurement-methodology.md</read_first>
  <action>
After checkpoint resume:
1. Ensure `docs/device-gate-results.md` has non-placeholder values for: device model(s), OS versions, build profile names, tool used (`gfxinfo` / Instruments), FPS/frame-time numbers, SC-2 PASS/FAIL both build types, cliff ramp note, overlay production check.
2. Ensure `docs/skia-version-decision.md` final state matches installed package version (`npm run assert:skia`).
3. If D-04 substitute Android was used, add explicit line: `Pixel 6a re-certification required before MVP acceptance (D-04)`.
4. If SC-3 failed and a topology fallback was chosen (typed arrays in one shared value, or JS-thread rAF loop), document the decision as a PROJECT.md-bound note in the results file for Phase 2 planners — do not implement the fallback in this task unless human explicitly required it to unblock; if required, open a follow-up note in SUMMARY for a gap plan.
5. Run final automated suite: `npx vitest run && npx tsc --noEmit && npx eslint . && npm run assert:skia`.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx tsc --noEmit &amp;&amp; npm run assert:skia &amp;&amp; grep -E "PASS|FAIL|fps|FPS|gfxinfo|Instruments" docs/device-gate-results.md &amp;&amp; grep -E "Confirmed|Fallback|2\\.12\\.0|2\\.6\\.2" docs/skia-version-decision.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/device-gate-results.md` contains concrete device identifiers and at least one of `gfxinfo` or `Instruments`
    - `docs/device-gate-results.md` contains PASS or FAIL for worklet self-check
    - `docs/skia-version-decision.md` contains Confirmed or Fallback language plus version
    - If substitute Android: file contains `Pixel 6a re-certification`
    - Full suite `vitest` + `tsc` + `assert:skia` exits 0
  </acceptance_criteria>
  <done>Phase 1 hardware evidence documented; Skia decision finalized; automated suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine → store-bound artifacts | Production profile must not carry perf overlay |
| Unsigned/debug measurement APK → distribution | Debug-signed release APKs must not be distributed as product |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | production overlay | mitigate | Assert eas production env unset; human confirms overlay off / not shipped |
| T-01-02 | Tampering | Skia pin | mitigate | `assert:skia` + decision doc must match lockfile after any fallback |
| T-01-07 | Spoofing | debug-signed measurement APK | mitigate | Label measurement artifacts in device-gate-results.md; never distribute as release candidate |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Human checkpoint approved
- [ ] device-gate-results.md filled with attributed measurements
- [ ] skia-version-decision.md finalized
- [ ] `npx vitest run && npx tsc --noEmit && npm run assert:skia`
</verification>

<success_criteria>
- SC-1: installs on physical iPhone + Android reference device
- SC-2: worklet mutation PASS on dev and release/profiling
- SC-3: ~200–300 sprites @ measured 60 FPS on Pixel 6a (or D-04 docs + numbers)
- D-16 Skia bet resolved in writing
- T-01-01 production overlay off verified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-thread-boundary-spike/01-04-SUMMARY.md`
</output>
