---
phase: 01-foundation-thread-boundary-spike
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/devflags.ts
  - src/runtime/constants.ts
  - src/runtime/metrics.ts
  - src/runtime/useSpikeLoop.ts
  - src/runtime/SpikeScreen.tsx
  - src/render/SpikeCanvas.tsx
  - src/render/recordSprites.ts
  - src/render/recordOverlay.ts
  - app/_layout.tsx
  - app/index.tsx
  - docs/measurement-methodology.md
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "Spike host mounts an opaque Skia Canvas that draws a SharedValue SkPicture of ~200–300 sprites"
    - "UI-thread useFrameCallback allocates world via runOnUI, steps core/ stub with fixed timestep, records picture — no runOnJS/scheduleOnRN/setState on the hot path"
    - "When EXPO_PUBLIC_PERF_OVERLAY=1, overlay text inside the picture shows ms/frame, rolling FPS, and substep count"
    - "Cliff-ramp sprite count is triggerable and documented as research-only (D-07)"
  artifacts:
    - path: "src/runtime/useSpikeLoop.ts"
      provides: "Fixed-timestep worklet host"
      contains: "useFrameCallback"
    - path: "src/render/SpikeCanvas.tsx"
      provides: "Opaque Canvas + Picture"
      contains: "opaque"
    - path: "src/render/recordOverlay.ts"
      provides: "Overlay drawn into SkPicture (D-08/D-14)"
    - path: "src/devflags.ts"
      provides: "Build-time PERF_OVERLAY / CLIFF_RAMP flags"
      contains: "EXPO_PUBLIC_PERF_OVERLAY"
    - path: "app/index.tsx"
      provides: "Thin host mounting SpikeScreen"
    - path: "docs/measurement-methodology.md"
      provides: "D-08 methodology (how FPS derived, which build/device)"
  key_links:
    - from: "src/runtime/useSpikeLoop.ts"
      to: "src/core/step.ts"
      via: "stepStub inside useFrameCallback"
      pattern: "stepStub"
    - from: "src/runtime/useSpikeLoop.ts"
      to: "src/render/recordSprites.ts"
      via: "recordFrame → picture.value"
      pattern: "picture\\.value"
    - from: "src/render/SpikeCanvas.tsx"
      to: "SharedValue&lt;SkPicture&gt;"
      via: "Picture picture={picture}"
      pattern: "opaque"
    - from: "src/runtime/useSpikeLoop.ts"
      to: "runOnUI allocateWorld"
      via: "world allocated on UI runtime"
      pattern: "runOnUI|scheduleOnUI"
---

<objective>
Build the on-device FPS harness: UI-runtime world allocation, fixed-timestep `useFrameCallback`, immediate-mode `SkPicture` recording of ~200–300 sprites, overlay metrics drawn into the same picture, and a thin `app/` host — proving ARCH-01's worklet suitability seam without gameplay.

Purpose: This is the empirical surface for SC-2/SC-3 (validated on hardware in plan 04).
Output: `src/runtime/*`, `src/render/*`, host screen, measurement methodology doc.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-thread-boundary-spike/01-CONTEXT.md
@.planning/phases/01-foundation-thread-boundary-spike/01-RESEARCH.md
@.planning/phases/01-foundation-thread-boundary-spike/01-02-SUMMARY.md

## Interfaces from plan 02 (use as-is)

```ts
// src/core
export function allocateWorld(capacity: number): SpikeWorld;
export function stepStub(world: SpikeWorld, dt: number): void;
export type SpikeWorld = { x: Float32Array; y: Float32Array; /* ... */ spriteCount: number; accumulator: number; tick: number };
```

## Locked decisions
- D-06 minimal FPS harness ~200–300 sprites via SkPicture
- D-07 cliff ramp research-only
- D-08 overlay ms/FPS/substeps + methodology doc
- D-13 harness in src/runtime + src/render; thin app/ host
- D-14 no runOnJS/scheduleOnRN on hot path; no React re-render per frame
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Runtime loop — UI-allocated world + fixed timestep + metrics</name>
  <files>src/devflags.ts, src/runtime/constants.ts, src/runtime/metrics.ts, src/runtime/useSpikeLoop.ts</files>
  <read_first>src/core/index.ts, src/core/types.ts, .planning/phases/01-foundation-thread-boundary-spike/01-RESEARCH.md</read_first>
  <action>
Create `src/devflags.ts`:
```ts
export const PERF_OVERLAY = process.env.EXPO_PUBLIC_PERF_OVERLAY === '1';
export const CLIFF_RAMP = process.env.EXPO_PUBLIC_CLIFF_RAMP === '1';
```
(Do **not** gate overlay on `__DEV__` — that hides it in profiling builds; D-03/D-08.)

`src/runtime/constants.ts`: `FIXED_DT = 1/120`, `MAX_SUBSTEPS = 5`, `MAX_FRAME_TIME = 0.25`, `SPRITE_CAP = 256` (within D-06 ~200–300; allow const override up to 300), `METRICS_WINDOW = 60`.

`src/runtime/metrics.ts`: preallocated ring buffer of frame intervals (ms) + last/max substeps; `'worklet'` helpers `pushSample`, `rollingFps` = `1000 / mean(interval)` over last N (**not** mean of per-frame FPS), p95/p99 helpers, `overBudget` count for samples &gt; 16.7ms. No React imports.

`src/runtime/useSpikeLoop.ts` (per research Pattern A/B/F):
- `useSharedValue` for `world`, `picture`, `metrics`
- On mount: `runOnUI` (from `react-native-worklets`) allocates `world.value = allocateWorld(SPRITE_CAP)` — **never** allocate SpikeWorld on JS thread
- `useFrameCallback`: clamp `timeSincePreviousFrame` (null → 16.67ms), accumulate, while-loop `stepStub`, `pushSample`, `picture.value = recordFrame(...)` (import from render — stub import ok if Task 2 lands same plan wave; implement recordFrame in Task 2 first if needed for compile order)
- **Forbidden in this file:** any call to `runOnJS`, `scheduleOnRN`, `setState`, or reading `sharedValue.value` from React render for per-frame data
- Export a small worklet self-check flag path: after N frames, metrics expose whether `world.tick` advanced (for overlay PASS/FAIL in Task 2)

Wire TypeScript so `npx tsc --noEmit` can see these modules (ensure tsconfig includes `src/`).
  </action>
  <verify>
    <automated>test -f src/runtime/useSpikeLoop.ts &amp;&amp; grep -q "useFrameCallback" src/runtime/useSpikeLoop.ts &amp;&amp; grep -q "runOnUI\|scheduleOnUI" src/runtime/useSpikeLoop.ts &amp;&amp; grep -q "stepStub" src/runtime/useSpikeLoop.ts &amp;&amp; ! grep -E "runOnJS|scheduleOnRN|setState" src/runtime/useSpikeLoop.ts &amp;&amp; npx eslint src/runtime &amp;&amp; npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/devflags.ts` reads `EXPO_PUBLIC_PERF_OVERLAY` (not `__DEV__`)
    - `useSpikeLoop.ts` contains `useFrameCallback` and `stepStub`
    - `useSpikeLoop.ts` contains `runOnUI` or `scheduleOnUI`
    - `useSpikeLoop.ts` does not contain `runOnJS`, `scheduleOnRN`, or `setState`
    - `npx eslint src/runtime` exits 0
    - `npx vitest run` still exits 0
  </acceptance_criteria>
  <done>UI-runtime allocation + fixed-timestep host exists with D-14 hot-path hygiene.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SkPicture render path + overlay + thin app host</name>
  <files>src/render/recordSprites.ts, src/render/recordOverlay.ts, src/render/SpikeCanvas.tsx, src/runtime/SpikeScreen.tsx, app/_layout.tsx, app/index.tsx, docs/measurement-methodology.md</files>
  <read_first>src/runtime/useSpikeLoop.ts, src/runtime/metrics.ts, src/devflags.ts, .planning/phases/01-foundation-thread-boundary-spike/01-RESEARCH.md</read_first>
  <action>
**recordSprites.ts:** Module-scope `Skia.PictureRecorder()`, `Skia.Paint()`, reused rect. `recordFrame(world, metrics, bounds, drawOverlayFlag): SkPicture` with `'worklet'` — loop `spriteCount` drawRect; if flag, call `drawOverlay`. **No per-frame allocation** of Paint/Recorder/array literals.

**recordOverlay.ts:** Module-scope `matchFont({ fontFamily: 'monospace', fontSize: 12 })` + text paint. `drawOverlay(canvas, metrics)` draws lines for: last ms/frame, rolling FPS, last+max substeps, p95/p99, frames&gt;16.7, spriteCount, and worklet self-check PASS/FAIL (`tick` advanced). Only invoked when `PERF_OVERLAY` is true (passed as boolean into recordFrame from JS at mount — freeze the boolean into the worklet closure once, do not re-read React state per frame).

**SpikeCanvas.tsx:** `<Canvas style={{flex:1}} opaque>` → `<Fill color="black" />` → `<Picture picture={picture} />`. Canvas must be lowest z-order in host.

**SpikeScreen.tsx:** Composes canvas + loop hook; optional cliff-ramp control: a single RN `Pressable` **outside** the per-frame path that bumps a shared `spriteCount` target OR rebuilds picture capacity (D-07). Default start at 256 sprites. Document trigger in methodology doc. Keep awake via `expo-keep-awake` while screen focused. Portrait lock already from app.json; reinforce in `_layout` with `expo-screen-orientation` lock to PORTRAIT_UP if not already.

**app/_layout.tsx:** Dark system UI, portrait, hide splash after mount. **app/index.tsx:** render `<SpikeScreen />` only — no gameplay UI.

**docs/measurement-methodology.md:** Document D-08 exactly:
- ms/frame = `timeSincePreviousFrame`
- FPS = `1000/mean(interval)` over 60 frames
- substeps = last + session max
- Verdict tools: `adb shell dumpsys gfxinfo &lt;pkg&gt; framestats` (Android), Instruments (iOS); never RN perf monitor alone
- Build: profiling/release only for gate (D-03/D-05)
- Device: Pixel 6a (D-01); iPhone install/feel (D-02); substitute docs (D-04)
- Hygiene: discard first ~2s, 30s window, ≥2 runs, keep-awake, thermal note
- Cliff ramp: how to trigger + that cliff is research-only not completion gate

Ensure `npx eslint src/runtime src/render` and `npx tsc --noEmit` pass. `npx vitest run` still green.
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx eslint src/runtime src/render app &amp;&amp; npx tsc --noEmit &amp;&amp; grep -q "opaque" src/render/SpikeCanvas.tsx &amp;&amp; grep -q "PictureRecorder\|beginRecording" src/render/recordSprites.ts &amp;&amp; grep -q "rollingFps\|substep" src/render/recordOverlay.ts &amp;&amp; grep -q "SpikeScreen" app/index.tsx &amp;&amp; grep -q "1000" docs/measurement-methodology.md &amp;&amp; ! grep -E "runOnJS|scheduleOnRN" src/runtime src/render</automated>
  </verify>
  <acceptance_criteria>
    - `SpikeCanvas.tsx` contains `opaque`
    - `recordSprites.ts` uses `PictureRecorder` / `beginRecording` at module scope pattern
    - `recordOverlay.ts` draws ms/FPS/substep metrics
    - `app/index.tsx` mounts `SpikeScreen`
    - No `runOnJS` or `scheduleOnRN` under `src/runtime` or `src/render`
    - `docs/measurement-methodology.md` documents `1000/mean` FPS derivation and dumpsys/Instruments verdict tools
    - `npx vitest run` and `npx tsc --noEmit` exit 0
  </acceptance_criteria>
  <done>Opaque SkPicture harness + overlay + host screen + methodology doc complete (D-06/D-07/D-08/D-13/D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI runtime ↔ RN runtime | Only mount/unmount and discrete flag capture may cross; never per frame |
| Overlay flag → visible metrics | Profiling builds intentionally show overlay; production must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `src/devflags.ts` / overlay | mitigate | Flag from `EXPO_PUBLIC_PERF_OVERLAY` only; never `__DEV__`-only; production profile omits env (verified in plan 04) |
| T-01-05 | Denial of Service | Cliff-ramp affordance | mitigate | Ramp behind `EXPO_PUBLIC_CLIFF_RAMP` or explicit in-app control; not enabled on production profile |
| T-01-06 | Elevation of Privilege | N/A | accept | No auth/network surface in harness |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run && npx eslint src/runtime src/render && npx tsc --noEmit`
- [ ] Hot-path grep clean for runOnJS/scheduleOnRN
- [ ] Methodology doc present
</verification>

<success_criteria>
- Harness compiles and is host-mounted
- World allocation on UI runtime; picture updated each frame from worklet
- Overlay metrics match D-08; methodology documented
- Ready for plan 04 device builds and FPS gate
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-thread-boundary-spike/01-03-SUMMARY.md`
</output>
