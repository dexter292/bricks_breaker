---
phase: 06-ui-shell-hud-persistence-platform-seams
plan: 01
type: execute
wave: 1
depends_on:
  - "06-00"
files_modified:
  - src/services/storage/types.ts
  - src/services/storage/compareBest.ts
  - src/services/storage/parseBlob.ts
  - src/services/storage/memoryStore.ts
  - src/services/storage/asyncStorageStore.ts
  - src/services/storage/index.ts
  - tests/storage.personal-best.test.ts
autonomous: true
requirements:
  - RUN-04
must_haves:
  truths:
    - "evaluatePersonalBest uses strict > (equal score is not a new record)"
    - "Corrupt / invalid blobs fail soft to 0"
    - "memoryStore round-trips bestScore for Vitest without AsyncStorage"
  artifacts:
    - path: "src/services/storage/compareBest.ts"
      provides: "evaluatePersonalBest pure helper"
      exports: ["evaluatePersonalBest"]
    - path: "src/services/storage/asyncStorageStore.ts"
      provides: "PersonalBestStore AsyncStorage adapter @nbb/personal-best/v1"
    - path: "tests/storage.personal-best.test.ts"
      provides: "GREEN RUN-04 unit coverage"
  key_links:
    - from: "asyncStorageStore"
      to: "@react-native-async-storage/async-storage"
      via: "getItem/setItem KEY @nbb/personal-best/v1"
      pattern: "@nbb/personal-best/v1|AsyncStorage"
    - from: "tests/storage.personal-best.test.ts"
      to: "compareBest + memoryStore + parseBlob"
      via: "vitest imports"
      pattern: "evaluatePersonalBest|createMemoryPersonalBestStore|parsePersonalBestBlob"
---

<objective>
Implement `services/storage` personal-best layer (pure compare, memory + AsyncStorage adapters) and turn Wave 0 stubs GREEN for RUN-04.

Purpose: Offline high score survives app kills without blocking the sim; Node Vitest covers compare + fail-soft parse without RN mocks.
Output: `src/services/storage/**` + green `tests/storage.personal-best.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-CONTEXT.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-PATTERNS.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-00-SUMMARY.md

<interfaces>
```typescript
// types.ts — lock these shapes
export const PERSONAL_BEST_VERSION = 1 as const;
export const PERSONAL_BEST_KEY = '@nbb/personal-best/v1' as const;

export type PersonalBestBlob = {
  v: 1;
  bestScore: number;
  updatedAt: number; // ms epoch, informational only
};

export interface PersonalBestStore {
  getBest(): Promise<number>;
  setBest(bestScore: number): Promise<void>;
};

// compareBest.ts
export function evaluatePersonalBest(
  runScore: number,
  previousBest: number,
): { best: number; isNewRecord: boolean };

// parseBlob.ts — shared by async adapter + tests (T-06-01)
export function parsePersonalBestBlob(raw: string | null): number; // → floor best or 0

// Classic AsyncStorage 2.2.0 default export (NOT v3 createAsyncStorage):
import AsyncStorage from '@react-native-async-storage/async-storage';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure compare + parse + memoryStore (GREEN stubs)</name>
  <files>src/services/storage/types.ts, src/services/storage/compareBest.ts, src/services/storage/parseBlob.ts, src/services/storage/memoryStore.ts, tests/storage.personal-best.test.ts</files>
  <read_first>.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md, tests/storage.personal-best.test.ts, tests/runtime.freeze.test.ts</read_first>
  <behavior>
    - evaluatePersonalBest(100, 50) → { best: 100, isNewRecord: true }
    - evaluatePersonalBest(50, 50) → { best: 50, isNewRecord: false }  // strict >; never >=
    - evaluatePersonalBest(40, 50) → { best: 50, isNewRecord: false }
    - parsePersonalBestBlob(null) → 0; corrupt JSON → 0; `{v:2,…}` → 0; non-finite/negative → 0
    - createMemoryPersonalBestStore(): getBest starts 0; setBest(42) then getBest → 42
  </behavior>
  <action>
Per D-10 / D-13 / UI-SPEC fail-soft **Best · 0**:

1. Create `types.ts` with `PERSONAL_BEST_VERSION = 1`, `PERSONAL_BEST_KEY = '@nbb/personal-best/v1'`, `PersonalBestBlob`, `PersonalBestStore`.

2. Implement `evaluatePersonalBest` with **strict `>`** only (D-11 New Record rule).

3. Implement `parsePersonalBestBlob(raw)`:
   - null → 0
   - try JSON.parse; require `parsed.v === 1` and `typeof bestScore === 'number'`
   - reject `!Number.isFinite(bestScore) || bestScore < 0` → 0
   - return `Math.floor(bestScore)`
   - catch → 0 (T-06-01)

4. Implement `createMemoryPersonalBestStore(): PersonalBestStore` for Vitest (in-memory number).

5. Replace `it.todo` in `tests/storage.personal-best.test.ts` with real `it`/`expect` for compare, parse, and memoryStore. Do **not** import AsyncStorage in this test file (Node Vitest — no native).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/storage.personal-best.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "runScore > previousBest" src/services/storage/compareBest.ts` matches
    - `rg ">=" src/services/storage/compareBest.ts` has no `>=` for record detection
    - `rg "it\\.todo" tests/storage.personal-best.test.ts` returns no matches (all converted or removed)
    - Vitest exits 0 covering compare + parse + memoryStore
    - No import of `react` / `react-native` under `src/services/storage/`
  </acceptance_criteria>
  <done>Pure storage helpers + memory adapter tested green.</done>
</task>

<task type="auto">
  <name>Task 2: AsyncStorage adapter + barrel export</name>
  <files>src/services/storage/asyncStorageStore.ts, src/services/storage/index.ts</files>
  <read_first>src/services/storage/types.ts, src/services/storage/parseBlob.ts, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md</read_first>
  <action>
1. Create `asyncStorageStore.ts` using classic default export:
   ```typescript
   import AsyncStorage from '@react-native-async-storage/async-storage';
   ```
   - `createAsyncStoragePersonalBestStore(): PersonalBestStore`
   - `getBest`: `AsyncStorage.getItem(PERSONAL_BEST_KEY)` → `parsePersonalBestBlob` (fail soft)
   - `setBest`: write `{ v: 1, bestScore: Math.floor(bestScore), updatedAt: Date.now() }` via `setItem` + `JSON.stringify`
   - `setBest` may throw to caller; call sites in Plan 05 must `.catch(() => {})` — adapter itself need not swallow write errors (read must never throw — wrap getBest in try/catch → 0)

2. Barrel `src/services/storage/index.ts` re-exports: types, `evaluatePersonalBest`, `parsePersonalBestBlob`, `createMemoryPersonalBestStore`, `createAsyncStoragePersonalBestStore`, constants.

3. Do **not** import this module from `src/core` or `src/runtime` (LC-01 / LC-09 — app-only call sites in Plans 03/05).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "PERSONAL_BEST_KEY|@nbb/personal-best/v1|createAsyncStoragePersonalBestStore|from '@react-native-async-storage/async-storage'" src/services/storage/ && npx vitest run tests/storage.personal-best.test.ts && npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - `asyncStorageStore.ts` imports default `AsyncStorage` from `@react-native-async-storage/async-storage`
    - Key string is exactly `@nbb/personal-best/v1`
    - `index.ts` exports `evaluatePersonalBest` and both store factories
    - `rg "from ['\\\"].*services/storage" src/core src/runtime` returns no matches
    - Vitest still green; `tsc --noEmit` clean for new files
  </acceptance_criteria>
  <done>Device AsyncStorage adapter ready for GameHost cold path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage JSON → parsePersonalBestBlob | Untrusted device storage |
| services/storage → app only | Persistence never enters core/runtime hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / Integrity | parsePersonalBestBlob / getBest | mitigate | Schema `v===1`, finite ≥0, try/catch → 0; UI-SPEC **Best · 0** |
| T-06-01b | Tampering | Local unsigned high score | accept | PITFALLS: MVP local-only unsigned OK; never trust client if cloud sync added later |
| T-06-03 | Elevation of privilege | layer imports | mitigate | Storage lives under `src/services/`; no core/runtime imports in this plan |
| T-06-04 | Denial of service | Blocking I/O on hot path | mitigate | Pure sync helpers only; AsyncStorage only via async store methods (Plan 05 fire-and-forget) |
| T-06-02 | Information disclosure | Accidental SecureStore/crypto | mitigate | Plain AsyncStorage only — do not add SecureStore |
| T-06-05 | Spoofing | Monetization UI | accept | No UI in this plan |
</threat_model>

<verification>
`npx vitest run tests/storage.personal-best.test.ts` green; AsyncStorage adapter exports present; no core/runtime imports of storage.
</verification>

<success_criteria>
RUN-04 persistence primitives exist and are unit-tested: strict new-record compare, fail-soft parse, memory + AsyncStorage stores.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-01-SUMMARY.md`
</output>
