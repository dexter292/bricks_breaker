---
phase: 06-ui-shell-hud-persistence-platform-seams
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - app/_components/GameHost.tsx
  - app/_components/PlayingHost.tsx
  - app/_components/TitleScreen.tsx
  - src/runtime/GameScreen.tsx
  - src/runtime/overlays/PauseOverlay.tsx
  - src/runtime/overlays/ResultOverlay.tsx
autonomous: true
requirements:
  - RUN-03
must_haves:
  truths:
    - "Cold start lands on Title (Neon Brick Breaker, Best · N, Play) — not Playing"
    - "Pause and Results expose Menu → Title with no confirmation dialog"
    - "Retry remains instant one-tap from Pause and Results with no confirmation"
    - "Menu unmounts PlayingHost (loop torn down); Play remounts it"
  artifacts:
    - path: "app/_components/TitleScreen.tsx"
      provides: "Title shell per UI-SPEC"
    - path: "app/_components/PlayingHost.tsx"
      provides: "Extracted game hooks host"
    - path: "app/_components/GameHost.tsx"
      provides: "shellPhase title|playing + Best refresh"
  key_links:
    - from: "GameHost"
      to: "TitleScreen / PlayingHost"
      via: "shellPhase conditional mount"
      pattern: "shellPhase|'title'|'playing'"
    - from: "PauseOverlay/ResultOverlay"
      to: "onMenu"
      via: "Pressable accessibilityLabel Return to title"
      pattern: "onMenu|Return to title"
---

<objective>
Introduce Title ↔ Playing shell: extract `PlayingHost`, add `TitleScreen`, wire Menu on Pause/Results, keep instant Retry (RUN-03 / D-01…D-05).

Purpose: Cold start feels like a real app; Menu returns to Title and refreshes Best; hooks tear down cleanly on unmount.
Output: Shell orchestration in `GameHost`; gameplay body in `PlayingHost`; Menu CTAs on overlays.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-CONTEXT.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-PATTERNS.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-01-SUMMARY.md
@app/_components/GameHost.tsx
@src/runtime/GameScreen.tsx
@src/runtime/overlays/PauseOverlay.tsx
@src/runtime/overlays/ResultOverlay.tsx

<interfaces>
```typescript
// GameHost shell (Pattern 1)
type ShellPhase = 'title' | 'playing';
// TitleScreen props
{ best: number; onPlay: () => void }
// PlayingHost props (this plan)
{ onMenu: () => void }
// Overlay props this plan
PauseOverlay: { onResume; onRetry; onMenu }
ResultOverlay: { kind; onRetry; onMenu } // Score/Best/New Record → Plan 05
GameScreen: add onMenu: () => void
```

UI-SPEC copy/tokens (mandatory):
- Product name: **Neon Brick Breaker** (Display 48, `#FFFFFF`, SpaceMono 600)
- Best line: **Best · {n}** (Body 16); unread → **Best · 0**
- Play: filled `#FFFFFF` / label `#1a1a2e`; accessibilityLabel **Start game**
- Menu: outline `borderWidth:1` `borderColor:'#FFFFFF'`; label **Menu**; accessibilityLabel **Return to title**
- Root Title bg `#1a1a2e`; panel `#12121f`; min touch 44
- No confirmation dialogs on Retry or Menu (RUN-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TitleScreen + extract PlayingHost + GameHost shellPhase</name>
  <files>app/_components/TitleScreen.tsx, app/_components/PlayingHost.tsx, app/_components/GameHost.tsx</files>
  <read_first>app/_components/GameHost.tsx, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md, src/runtime/overlays/CountdownOverlay.tsx, src/services/storage/index.ts</read_first>
  <action>
Per D-01, D-02, D-05, D-12, RESEARCH Pattern 1 (unmount on Menu — locked):

1. **TitleScreen.tsx** (new in `app/_components/`):
   - `useSafeAreaInsets` padding on all four sides
   - Full-bleed `#1a1a2e`
   - Display text exactly `Neon Brick Breaker` (fontFamily `SpaceMono`, fontSize 48, fontWeight `'600'`, color `#FFFFFF`)
   - Body `Best · ${best}` (fontSize 16, color `#FFFFFF`)
   - Primary Pressable `Play` — accessibilityLabel `Start game`, filled `#FFFFFF`, label `#1a1a2e`, minHeight 44
   - Vertical rhythm: brand → Best → Play with lg/3xl spacing from UI-SPEC (24/64) — multiples of 4 only
   - No ads/shop/login chrome (D-18)

2. **PlayingHost.tsx**: Move **entire** current `GameHost` body (fonts, keep-awake, useGameLoop, gestures, reactions, pause FSM, retry, GameScreen render) into this file. Export `PlayingHost({ onMenu }: { onMenu: () => void })`. Pass `onMenu` into `GameScreen` (prop added in Task 2 if needed — may stub pass-through in same commit wave). Keep level default `'level-01'`. Preserve D-05: AppState auto-pause, no auto-resume, Resume → 3s countdown.

3. **GameHost.tsx** becomes shell only:
   ```tsx
   const [shellPhase, setShellPhase] = useState<'title' | 'playing'>('title');
   const [best, setBest] = useState(0);
   useEffect(() => {
     if (shellPhase !== 'title') return;
     void createAsyncStoragePersonalBestStore().getBest().then(setBest).catch(() => setBest(0));
   }, [shellPhase]);
   if (shellPhase === 'title') return <TitleScreen best={best} onPlay={() => setShellPhase('playing')} />;
   return <PlayingHost onMenu={() => setShellPhase('title')} />;
   ```
   - Cold start **must** be `'title'` (D-02)
   - Do **not** call `useGameLoop` in GameHost (Rules of Hooks / Pitfall 1)
   - Menu path unmounts PlayingHost (do not freeze-under-Title)
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "shellPhase|TitleScreen|PlayingHost|Neon Brick Breaker|Best ·" app/_components/ && rg -n "useGameLoop" app/_components/GameHost.tsx; test $? -ne 0 && rg -n "useGameLoop" app/_components/PlayingHost.tsx && npx tsc --noEmit -p . 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `GameHost.tsx` contains `shellPhase` default `'title'` and does **not** import/call `useGameLoop`
    - `PlayingHost.tsx` contains `useGameLoop`
    - `TitleScreen.tsx` contains exact strings `Neon Brick Breaker`, `Play`, `Best ·`
    - `rg "Alert\\.alert|Are you sure" app/_components src/runtime/overlays` returns no matches
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Title cold start + PlayingHost extract + Best refresh on Title enter.</done>
</task>

<task type="auto">
  <name>Task 2: Menu on Pause/Results + GameScreen onMenu (instant Retry preserved)</name>
  <files>src/runtime/overlays/PauseOverlay.tsx, src/runtime/overlays/ResultOverlay.tsx, src/runtime/GameScreen.tsx, app/_components/PlayingHost.tsx</files>
  <read_first>src/runtime/overlays/PauseOverlay.tsx, src/runtime/overlays/ResultOverlay.tsx, src/runtime/GameScreen.tsx, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md</read_first>
  <action>
Per D-03, D-04, RUN-03, UI-SPEC secondary CTA:

1. **PauseOverlay**: add required `onMenu: () => void`. After Retry, append outline Menu Pressable:
   - label `Menu`, accessibilityLabel `Return to title`
   - style: `borderWidth: 1`, `borderColor: '#FFFFFF'`, background transparent or `#12121f`, `minHeight: 44`
   - label Text: SpaceMono 14 `#FFFFFF`
   - Order: Resume (filled) → Retry (filled) → Menu (outline)
   - **No** confirmation dialog

2. **ResultOverlay**: add `onMenu` the same way after Retry (Score/Best/New Record fields come in Plan 05 — do not invent placeholder monetization UI). Keep existing Win/Lose headings.

3. **GameScreen**: add `onMenu` prop; pass to both overlays. Do not change HUD strip yet (Plan 04).

4. **PlayingHost**: wire `onMenu` from props into `GameScreen`. Retry handlers unchanged (instant, no confirm).

5. Confirm no `Alert.alert` / confirm modals anywhere on Retry/Menu paths.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "onMenu|Return to title|accessibilityLabel=\"Return to title\"" src/runtime/overlays/PauseOverlay.tsx src/runtime/overlays/ResultOverlay.tsx src/runtime/GameScreen.tsx app/_components/PlayingHost.tsx && ! rg -n "Alert\\.alert|Are you sure" app/ src/runtime/; npx tsc --noEmit -p . 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PauseOverlay and ResultOverlay both take `onMenu` and render label `Menu`
    - Both use accessibilityLabel `Return to title`
    - GameScreen passes `onMenu` to both overlays
    - No `Alert.alert` in app/ or src/runtime/
    - Existing Retry Pressables still present with accessibilityLabel `Retry level`
    - tsc clean
  </acceptance_criteria>
  <done>Menu → Title wired; Retry stays one-tap no-confirm (RUN-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Title cold path ↔ AsyncStorage getBest | Fail soft to 0 on read errors |
| PlayingHost mount boundary | Unmount tears down worklets; no hooks on Title |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Integrity | Title Best read | mitigate | `.getBest().catch(() => setBest(0))` — UI-SPEC fail soft |
| T-06-04 | Denial of service | Per-frame React | mitigate | Do not change SharedValue mirror path while extracting PlayingHost — keep existing `useAnimatedReaction` change-only `runOnJS` |
| T-06-05 | Spoofing | Monetization UI on Title | mitigate | Title inventory: brand, Best, Play only — grep forbids Shop/Watch Ad/Sign In |
| T-06-02 | Information disclosure | Network on Title | mitigate | Only local storage read; no platform SDK calls on Title |
| T-06-03 | Elevation of privilege | runtime→services | mitigate | Storage imports only from `app/_components/GameHost.tsx`, not from `src/runtime` |
</threat_model>

<verification>
tsc clean; shellPhase title default; PlayingHost owns useGameLoop; Menu on both overlays; no confirm dialogs.
</verification>

<success_criteria>
Player cold-starts on Title, can Play into the level, Pause/Results Menu returns to Title refreshing Best, Retry remains instant (RUN-03).
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-03-SUMMARY.md`
</output>
