---
phase: 06-ui-shell-hud-persistence-platform-seams
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.js
  - docs/layer-contract.md
  - tests/storage.personal-best.test.ts
  - tests/platform.seams.test.ts
  - .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-VALIDATION.md
autonomous: true
requirements:
  - RUN-04
  - ARCH-02
must_haves:
  truths:
    - "@react-native-async-storage/async-storage is pinned at 2.2.0 via expo install"
    - "ESLint allows app → services; runtime/core still cannot import services"
    - "Wave 0 Vitest stub files exist so Plans 01–02 have automated verify targets"
  artifacts:
    - path: "tests/storage.personal-best.test.ts"
      provides: "RUN-04 Nyquist stubs (it.todo)"
    - path: "tests/platform.seams.test.ts"
      provides: "ARCH-02 Nyquist stubs (it.todo)"
    - path: "eslint.config.js"
      provides: "app allow-list includes services"
    - path: "docs/layer-contract.md"
      provides: "LC-04 mentions services cold-path"
  key_links:
    - from: "06-VALIDATION.md Wave 0"
      to: "tests/storage.personal-best.test.ts + tests/platform.seams.test.ts"
      via: "automated vitest paths resolve"
      pattern: "storage\\.personal-best|platform\\.seams"
---

<objective>
Wave 0 Nyquist + install: pin AsyncStorage 2.2.0, open ESLint `app` → `services`, document LC-04, and create `it.todo` stubs for storage/platform suites.

Purpose: Later plans must not claim MISSING test files; Expo-managed pin must land before adapters; boundary gap must not block first `services/` import.
Output: Lockfile pin, eslint + layer-contract update, two stub test files, VALIDATION.md Wave 0 marked complete.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-CONTEXT.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-VALIDATION.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-PATTERNS.md
@docs/layer-contract.md
@eslint.config.js
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AsyncStorage 2.2.0 + allow app→services + LC-04</name>
  <files>package.json, package-lock.json, eslint.config.js, docs/layer-contract.md</files>
  <read_first>AGENTS.md, eslint.config.js, docs/layer-contract.md, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md, package.json</read_first>
  <action>
1. Install with Expo pin only (per D-13 / RESEARCH — never `npm install` latest):
   ```bash
   npx expo install @react-native-async-storage/async-storage
   ```
   Assert `package.json` / lockfile resolve to **exactly `2.2.0`** (Expo SDK 57 `bundledNativeModules`). Do **not** install AsyncStorage 3.x, `expo-secure-store`, MMKV, or any ads/IAP SDK (D-18, T-06-02).

2. In `eslint.config.js`, extend the `app` allow-list `anyOf` from
   `['runtime', 'render', 'input', 'ui', 'app']` to
   `['runtime', 'render', 'input', 'ui', 'app', 'services']`.
   Do **not** add `services` to `runtime` or `core` allow-lists (LC-01 / LC-09).

3. Update `docs/layer-contract.md` LC-04 row to:
   | LC-04 | `app/` → `runtime/`, `render/`, `services/` | Mount / unmount / cold I/O | Thin Expo host; AsyncStorage + platform seams from app only | ESLint boundaries |
   Keep banned rows unchanged (no `core`/`runtime` → AsyncStorage).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; node -e "const p=require('./node_modules/@react-native-async-storage/async-storage/package.json'); if(p.version!=='2.2.0') process.exit(1); console.log('async-storage', p.version)" && rg -n "anyOf: \['runtime', 'render', 'input', 'ui', 'app', 'services'\]" eslint.config.js && rg -n "app/` → `runtime/`, `render/`, `services/`|app/ → runtime/, render/, services/" docs/layer-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/@react-native-async-storage/async-storage/package.json` version is `2.2.0`
    - `rg "services" eslint.config.js` shows `services` inside the `from: app` allow `anyOf` array
    - `rg "services" docs/layer-contract.md` matches LC-04
    - `rg "@react-native-async-storage/async-storage" package.json` matches with `2.2.0`
    - No new dependency matching `ads|iap|purchases|revenuecat|applovin` in package.json
  </acceptance_criteria>
  <done>AsyncStorage 2.2.0 installed; app may import services; LC-04 documents cold-path services.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 it.todo stubs + VALIDATION.md</name>
  <files>tests/storage.personal-best.test.ts, tests/platform.seams.test.ts, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-VALIDATION.md</files>
  <read_first>.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-VALIDATION.md, tests/runtime.freeze.test.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-00-PLAN.md</read_first>
  <action>
Create two Vitest stub files that import only `{ describe, it } from 'vitest'` — do **not** import `src/services/**` yet (modules land in Plans 01–02).

1. `tests/storage.personal-best.test.ts` (RUN-04 / 06-W0-01 / T-06-01):
   - `it.todo('evaluatePersonalBest: strict greater-than sets isNewRecord and best = runScore')`
   - `it.todo('evaluatePersonalBest: equal score is not a new record')`
   - `it.todo('evaluatePersonalBest: lower runScore keeps previous best')`
   - `it.todo('memoryStore getBest/setBest round-trip')`
   - `it.todo('parsePersonalBestBlob: corrupt JSON / wrong v / non-finite / negative → 0')`

2. `tests/platform.seams.test.ts` (ARCH-02 / 06-W0-02 / T-06-02):
   - `it.todo('noopAds.onRunEnded does not throw')`
   - `it.todo('noopPurchases.onRunEnded does not throw')`
   - `it.todo('noopAccounts.onRunEnded does not throw')`
   - `it.todo('defaultPlatformServices returns ads/purchases/accounts with onRunEnded')`

3. Update `06-VALIDATION.md`:
   - `wave_0_complete: true` (leave `nyquist_compliant: false` until phase gate)
   - Check Wave 0 boxes for both stub files + AsyncStorage install + ESLint allow-list
   - Fill Per-Task Verification Map: Task IDs `06-W0-01` / `06-W0-02`, Plan `00`, File Exists ✅
   - Add rows for later plans: `06-01-*` storage GREEN, `06-02-*` platform GREEN (placeholders OK)
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/storage.personal-best.test.ts tests/platform.seams.test.ts && rg -n "wave_0_complete: true" .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-VALIDATION.md && rg -n "it\\.todo" tests/storage.personal-best.test.ts tests/platform.seams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist under `tests/`
    - Each file has ≥1 `it.todo` and zero `expect(` calls
    - Neither file imports `src/services`
    - Command exits 0
    - VALIDATION frontmatter `wave_0_complete: true`
  </acceptance_criteria>
  <done>Nyquist Wave 0 stubs + VALIDATION updated for RUN-04 / ARCH-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device AsyncStorage ↔ app cold path | Untrusted persisted JSON may be corrupt or tampered |
| app → services | Only cold-path host may touch persistence / platform stubs |
| MVP offline surface | No network SDK may enter via this install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / Integrity | AsyncStorage blob | mitigate | Plan 01 parse: `v===1`, finite ≥0 `bestScore`, try/catch → 0 (Wave 0 stubs encode corrupt→0 cases) |
| T-06-02 | Information disclosure | package install | mitigate | Only `expo install` AsyncStorage 2.2.0; reject ads/IAP/analytics SDKs in this task's acceptance grep |
| T-06-03 | Elevation of privilege (arch) | eslint boundaries | mitigate | Add `services` to **app** allow-list only; keep core/runtime banned from services |
| T-06-04 | Denial of service | Per-frame React HUD | accept (deferred to Plans 03–04) | No HUD code in Wave 0; existing SharedValue pattern preserved by later plans |
| T-06-05 | Spoofing | Visible monetization UI | accept (deferred to Plan 02/05) | No UI components created in Wave 0 |
</threat_model>

<verification>
`npx vitest run tests/storage.personal-best.test.ts tests/platform.seams.test.ts` exits 0; AsyncStorage 2.2.0 present; eslint app allow-list includes services.
</verification>

<success_criteria>
Wave 0 complete: install pinned, boundaries open for app→services, stub suites green with `it.todo`, VALIDATION.md updated.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-00-SUMMARY.md`
</output>
