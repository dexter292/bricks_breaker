---
phase: 06-ui-shell-hud-persistence-platform-seams
plan: 04
type: execute
wave: 3
depends_on:
  - "06-03"
files_modified:
  - src/runtime/HudStrip.tsx
  - src/runtime/GameScreen.tsx
autonomous: true
requirements:
  - PLT-02
must_haves:
  truths:
    - "HUD is a 48px content strip under the top safe-area inset with rgba(18,18,31,0.8) backing"
    - "playfieldSafe.top = insets.top + 48 so the strip sits above the letterboxed field"
    - "HUD shows Score · N, ×combo, Lives · N, Stall! · T (gated), and Pause — via discrete React mirrors only"
    - "Letterbox aspect 360∶640 preserved; field not stretched"
  artifacts:
    - path: "src/runtime/HudStrip.tsx"
      provides: "Compact top HUD strip component"
    - path: "src/runtime/GameScreen.tsx"
      provides: "playfieldTop = insets.top + 48; uses HudStrip"
  key_links:
    - from: "GameScreen playfieldSafe"
      to: "HudStrip"
      via: "top inset + 48 content"
      pattern: "HUD_STRIP_CONTENT|insets\\.top \\+ 48|48"
    - from: "HudStrip Stall"
      to: "showStall gate"
      via: "PLAYING + stallTier > 0"
      pattern: "Stall! ·|showStall|SIM_PLAYING"
---

<objective>
Replace free-floating Phase 5 HUD chrome with a compact safe-area top strip above the letterboxed playfield (PLT-02 / D-06…D-09 / D-14…D-16).

Purpose: Metrics stay readable without covering brick rows; SharedValue discrete mirrors remain the only live data path (D-08).
Output: `HudStrip` + `GameScreen` layout math `playfieldSafe.top = insets.top + 48`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-PATTERNS.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-03-SUMMARY.md
@src/runtime/GameScreen.tsx

<interfaces>
```typescript
// UI-SPEC + RESEARCH Pattern 4 — lock these literals
const HUD_STRIP_CONTENT = 48;
const stripTop = insets.top;
const playfieldTop = insets.top + HUD_STRIP_CONTENT;
// strip backgroundColor: 'rgba(18,18,31,0.8)'  // #12121f @ 80%
// Copy:
//   Score · ${score}
//   ×${combo}
//   Lives · ${lives}
//   Stall! · ${stallTier}  // only when showStall
// Pause trailing — accessibilityLabel "Pause game"
// Stall gate (D-09) — preserve exactly:
const showStall =
  stallTier > 0 &&
  result == null &&
  uiPhase === 'playing' &&
  simPhaseNum === SIM_PLAYING;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HudStrip component per UI-SPEC</name>
  <files>src/runtime/HudStrip.tsx</files>
  <read_first>src/runtime/GameScreen.tsx, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-PATTERNS.md</read_first>
  <action>
Create `src/runtime/HudStrip.tsx` accepting props from discrete React state only (never SharedValues):

Props: `{ score, combo, lives, stallTier, showStall, showPause, onPause, top, left, right }` (or equivalent — `top` must be `insets.top`).

Layout contract (UI-SPEC Playing HUD):
- Position absolute; `top` = safe-area top; `height: 48`; full width between left/right insets
- `backgroundColor: 'rgba(18,18,31,0.8)'`
- Horizontal padding `md` = 16; metric gaps `xs` = 4
- Leading row Text (SpaceMono Label 14 `#FFFFFF`): `Score · ${score}`, `×${combo}`, `Lives · ${lives}`, and if `showStall` then `Stall! · ${stallTier}`
- Trailing Pause Pressable when `showPause`: label `Pause`, accessibilityLabel `Pause game`, outline style (existing pause button tokens), min 44 touch
- `pointerEvents="box-none"` on strip; Pause `pointerEvents="auto"`; metric texts `pointerEvents="none"`
- No center-screen HUD; no neon/glow/blur
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "rgba\\(18,18,31,0\\.8\\)|height:\\s*48|Score ·|Lives ·|Stall! ·|Pause game" src/runtime/HudStrip.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `rgba(18,18,31,0.8)` and height 48
    - Contains copy prefixes `Score ·`, `Lives ·`, `Stall! ·`, and `×`
    - Pause accessibilityLabel is `Pause game`
    - No `useSharedValue` / `useAnimatedReaction` / `useFrameCallback` in HudStrip
  </acceptance_criteria>
  <done>HudStrip implements UI-SPEC compact strip chrome.</done>
</task>

<task type="auto">
  <name>Task 2: GameScreen — strip above letterbox (playfieldTop)</name>
  <files>src/runtime/GameScreen.tsx</files>
  <read_first>src/runtime/GameScreen.tsx, src/runtime/HudStrip.tsx, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md</read_first>
  <action>
Per D-06, D-07, D-14, D-15, PLT-02, RESEARCH Pattern 4:

1. Replace free-floating `styles.hud` block with `<HudStrip … />`.

2. Change playfield safe box from `top: insets.top` to:
   ```tsx
   const HUD_STRIP_CONTENT = 48;
   const playfieldTop = insets.top + HUD_STRIP_CONTENT;
   // playfieldSafe style top: playfieldTop
   ```
   Keep `bottom/left/right` as insets. Do **not** stretch logical 360×640 — letterbox unchanged inside canvas.

3. Preserve `showStall` gate exactly (D-09). Preserve overlay z-order (chrome above Skia). Preserve serve hint / countdown / pause / result / level-error overlays.

4. Move `__DEV__` level switch so it does not sit under the strip (e.g. `top: insets.top + 48 + 8` or below strip).

5. Do **not** introduce per-frame `setState` in GameScreen; continue receiving score/combo/lives/stallTier as props from PlayingHost mirrors (D-08 / T-06-04).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "HUD_STRIP_CONTENT|insets\\.top \\+ 48|HudStrip|playfieldTop" src/runtime/GameScreen.tsx && rg -n "showStall" src/runtime/GameScreen.tsx && npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - `GameScreen.tsx` sets playfield safe `top` to `insets.top + 48` (or `playfieldTop` derived that way)
    - Imports and renders `HudStrip`
    - Old free-floating multi-`Text` HUD column for Score/Lives is removed (strip owns it)
    - `showStall` condition still requires `simPhaseNum === SIM_PLAYING` (or equivalent constant)
    - tsc clean
  </acceptance_criteria>
  <done>PLT-02: strip above letterbox; brick rows not covered by opaque HUD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SharedValue mirrors → React HUD props | Discrete change-only updates; strip is presentational |
| Safe-area insets → layout | Notch/home-indicator must not clip chrome |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Denial of service | Per-frame React HUD | mitigate | HudStrip is props-only; no SharedValue reads in render; no setState in GameScreen frame path — PlayingHost mirrors unchanged |
| T-06-05 | Spoofing | Monetization chrome in strip | mitigate | Strip contents limited to Score/Combo/Lives/Stall/Pause — no ad slots |
| T-06-01 | Tampering | Storage | accept | Not in this plan |
| T-06-02 | Information disclosure | SDK | accept | Not in this plan |
| T-06-03 | Elevation of privilege | runtime→services | mitigate | GameScreen/HudStrip must not import `src/services/**` |
</threat_model>

<verification>
HudStrip tokens match UI-SPEC; playfieldTop = insets.top + 48; tsc clean; stall gate preserved.
</verification>

<success_criteria>
Playfield and HUD lay out correctly with safe-area: strip under notch, letterbox below strip, metrics via discrete mirrors (PLT-02 / SC-2).
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-04-SUMMARY.md`
</output>
