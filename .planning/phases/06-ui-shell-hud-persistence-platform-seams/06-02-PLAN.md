---
phase: 06-ui-shell-hud-persistence-platform-seams
plan: 02
type: execute
wave: 1
depends_on:
  - "06-00"
files_modified:
  - src/services/platform/types.ts
  - src/services/platform/noopAds.ts
  - src/services/platform/noopPurchases.ts
  - src/services/platform/noopAccounts.ts
  - src/services/platform/index.ts
  - tests/platform.seams.test.ts
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - "AdService / PurchaseService / AccountService expose onRunEnded and do not throw"
    - "defaultPlatformServices composes three no-ops with zero network"
    - "No visible ads/IAP/login UI components exist under services/platform"
  artifacts:
    - path: "src/services/platform/types.ts"
      provides: "RunEndedPayload + service interfaces"
    - path: "src/services/platform/index.ts"
      provides: "defaultPlatformServices()"
      exports: ["defaultPlatformServices", "noopAds", "noopPurchases", "noopAccounts"]
    - path: "tests/platform.seams.test.ts"
      provides: "GREEN ARCH-02 seam coverage"
  key_links:
    - from: "tests/platform.seams.test.ts"
      to: "noopAds.onRunEnded"
      via: "vitest expect not.toThrow"
      pattern: "onRunEnded"
---

<objective>
Add code-only platform monetization/account seams (interfaces + no-op stubs) and turn Wave 0 platform stubs GREEN for ARCH-02.

Purpose: Real `onRunEnded` call sites can land in Plan 05 without shipping SDKs or UI (D-17…D-19).
Output: `src/services/platform/**` + green `tests/platform.seams.test.ts`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-CONTEXT.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-UI-SPEC.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-PATTERNS.md
@.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-00-SUMMARY.md

<interfaces>
```typescript
export type RunEndedPayload = {
  score: number;
  outcome: 'win' | 'lose';
  isNewRecord?: boolean;
};

export interface AdService {
  onRunEnded(payload: RunEndedPayload): void;
}

export interface PurchaseService {
  onRunEnded(payload: RunEndedPayload): void;
}

export interface AccountService {
  onRunEnded(payload: RunEndedPayload): void;
}

export function defaultPlatformServices(): {
  ads: AdService;
  purchases: PurchaseService;
  accounts: AccountService;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Platform types + three no-ops (GREEN stubs)</name>
  <files>src/services/platform/types.ts, src/services/platform/noopAds.ts, src/services/platform/noopPurchases.ts, src/services/platform/noopAccounts.ts, tests/platform.seams.test.ts</files>
  <read_first>.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-RESEARCH.md, .planning/phases/06-ui-shell-hud-persistence-platform-seams/06-CONTEXT.md, tests/platform.seams.test.ts</read_first>
  <behavior>
    - noopAds.onRunEnded({ score: 10, outcome: 'win', isNewRecord: true }) does not throw
    - noopPurchases.onRunEnded({ score: 0, outcome: 'lose' }) does not throw
    - noopAccounts.onRunEnded({ score: 1, outcome: 'win' }) does not throw
  </behavior>
  <action>
Per D-17 / D-18 / D-19 / ARCH-02:

1. Create `types.ts` with `RunEndedPayload` and the three interfaces exactly as in `<interfaces>` (method name **`onRunEnded`** — locked research recommendation).

2. Implement `noopAds.ts`, `noopPurchases.ts`, `noopAccounts.ts` — each exports a const object whose `onRunEnded` is an empty function body (comment `/* no-op — ARCH-02 */`). No `fetch`, no timers, no imports from React, Expo modules, or ad/IAP SDKs (T-06-02).

3. Replace `it.todo` cases in `tests/platform.seams.test.ts` with real assertions (`expect(() => …).not.toThrow()`).

4. **Forbidden:** any `View`/`Pressable`/`Text` under `services/platform`; any string copy like "Watch Ad", "Shop", "Sign In" (D-18 / UI-SPEC).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/platform.seams.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Three noop files export `onRunEnded`
    - `rg "Watch Ad|Sign In|Shop|Rewarded" src/services/platform` returns no matches
    - `rg "from 'react'|from \\\"react-native\\\"|fetch\\(" src/services/platform` returns no matches
    - `rg "it\\.todo" tests/platform.seams.test.ts` returns no matches
    - Vitest exits 0
  </acceptance_criteria>
  <done>No-op Ad/Purchase/Account services callable without throw.</done>
</task>

<task type="auto">
  <name>Task 2: defaultPlatformServices barrel</name>
  <files>src/services/platform/index.ts, tests/platform.seams.test.ts</files>
  <read_first>src/services/platform/noopAds.ts, src/core/index.ts, tests/platform.seams.test.ts</read_first>
  <action>
1. Create `src/services/platform/index.ts`:
   ```typescript
   export function defaultPlatformServices() {
     return { ads: noopAds, purchases: noopPurchases, accounts: noopAccounts };
   }
   ```
   Re-export types + individual noops.

2. Add/keep test: `defaultPlatformServices()` returns object with `ads`, `purchases`, `accounts`, each having callable `onRunEnded`.

3. Do not wire call sites yet (Plan 05 owns PlayingHost `onRunEnded` cold path).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/platform.seams.test.ts && rg -n "defaultPlatformServices" src/services/platform/index.ts tests/platform.seams.test.ts && npx tsc --noEmit -p . 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `index.ts` exports `defaultPlatformServices`
    - Test asserts all three keys present
    - Vitest + tsc clean
  </acceptance_criteria>
  <done>Platform seam barrel ready for Plan 05 call sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future SDK ↔ app | Seams isolate future network/monetization from gameplay |
| Airplane / offline MVP | No-ops must never require network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Information disclosure | Accidental SDK install | mitigate | No-op files only; acceptance forbids fetch/RN UI; no new deps in this plan |
| T-06-05 | Spoofing | Visible monetization UI | mitigate | D-18: zero UI under `services/platform`; grep bans Watch Ad / Shop / Sign In |
| T-06-04 | Denial of service | Throwing seams crashing Results | mitigate | Tests require `onRunEnded` does not throw |
| T-06-01 | Tampering | Storage | accept | Out of scope for this plan (Plan 01) |
| T-06-03 | Elevation of privilege | core importing platform | mitigate | Platform under services/; call sites app-only in Plan 05 |
</threat_model>

<verification>
`npx vitest run tests/platform.seams.test.ts` green; no monetization UI or network in platform folder.
</verification>

<success_criteria>
ARCH-02 code seams exist as interfaces + no-ops with tested `onRunEnded`; MVP offline-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-shell-hud-persistence-platform-seams/06-02-SUMMARY.md`
</output>
