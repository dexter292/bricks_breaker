---
phase: 05-run-rules-score-combo-power-ups-anti-stall
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/rules.scoring.test.ts
  - tests/rules.pickups.test.ts
  - tests/rules.effects.test.ts
  - tests/rules.multiball.test.ts
  - tests/rules.stall.test.ts
  - tests/rules.lives.test.ts
  - .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-VALIDATION.md
autonomous: true
requirements:
  - RUN-01
  - PWR-01
  - PWR-02
  - PWR-03
  - PHYS-07
must_haves:
  truths:
    - "Wave 0 Vitest stub files exist for scoring/pickups/effects/multiball/stall so later plans have automated verify targets"
    - "lives.test.ts documents last-ball life semantics as it.todo (no longer asserts any-BALL_OUT → −life)"
  artifacts:
    - path: "tests/rules.scoring.test.ts"
      provides: "RUN-01 suite stub (it.todo)"
    - path: "tests/rules.pickups.test.ts"
      provides: "PWR-01/03 drop/catch/miss stub"
    - path: "tests/rules.effects.test.ts"
      provides: "PWR-02 expand refresh/expire stub"
    - path: "tests/rules.multiball.test.ts"
      provides: "PWR-01 +2 spawn / maxBalls stub"
    - path: "tests/rules.stall.test.ts"
      provides: "PHYS-07 tier/freeze stub"
    - path: "tests/rules.lives.test.ts"
      provides: "PWR-01 last-ball life todos"
  key_links:
    - from: "05-VALIDATION.md Wave 0"
      to: "tests/rules.*.test.ts"
      via: "automated vitest paths resolve"
      pattern: "rules\\.(scoring|pickups|effects|multiball|stall|lives)\\.test"
---

<objective>
Wave 0 Nyquist scaffolding: create Vitest `it.todo` stubs for Phase 5 rule suites and rewrite `tests/rules.lives.test.ts` to last-ball life todos so Plans 01–06 have real `<automated>` verify targets.

Purpose: Satisfy Nyquist — no feature task may claim a missing test file; remove locked-wrong lives assertions before the lives rewrite.
Output: Five new stub test files + updated lives stubs; VALIDATION.md Wave 0 boxes checked / task IDs filled.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-VALIDATION.md
@docs/layer-contract.md
@tests/rules.lives.test.ts
@tests/rules.win.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 it.todo stubs for scoring/pickups/effects/multiball/stall</name>
  <files>tests/rules.scoring.test.ts, tests/rules.pickups.test.ts, tests/rules.effects.test.ts, tests/rules.multiball.test.ts, tests/rules.stall.test.ts</files>
  <read_first>.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-VALIDATION.md, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md, tests/rules.win.test.ts</read_first>
  <action>
Create five Vitest files that import only `{ describe, it } from 'vitest'` and declare `it.todo(...)` titles. Do **not** import Phase 5 rule modules that do not exist yet (would break `npm test`).

1. `tests/rules.scoring.test.ts` (RUN-01 / 05-W0-01):
   - `it.todo('BRICK_HIT awards SCORE_HIT * combo then increments combo')`
   - `it.todo('BRICK_BREAK awards (SCORE_HIT + SCORE_BREAK_BONUS) * combo then increments combo')`
   - `it.todo('PADDLE_HIT resets combo to 1')`
   - `it.todo('unbreakable BRICK_HIT awards no score and does not change combo')`
   - `it.todo('simultaneous multi-ball brick damage is deterministic (ring order)')`

2. `tests/rules.pickups.test.ts` (PWR-01/03 / 05-W0-02):
   - `it.todo('BRICK_BREAK rolls rngGameplay; DROP_CHANCE 0.2 spawns pickup at evX/evY')`
   - `it.todo('intermediate BRICK_HIT never spawns pickup')`
   - `it.todo('AABB overlap with paddle catches pickup; no auto-collect by proximity')`
   - `it.todo('pickup y > LOGICAL_HEIGHT is removed')`
   - `it.todo('drop rolls never call Math.random or rngCosmetic')`

3. `tests/rules.effects.test.ts` (PWR-02 / 05-W0-03):
   - `it.todo('expand sets paddleW = PADDLE_WIDTH * 1.5 for 10s (1200 ticks at FIXED_DT)')`
   - `it.todo('second expand refreshes effectUntilTick without stacking width past 1.5×')`
   - `it.todo('expire restores paddleW = 72 and clamps paddleX in field')`
   - `it.todo('resolvePaddleEnglish uses current paddleW half-width while expanded')`

4. `tests/rules.multiball.test.ts` (PWR-01 / 05-W0-04):
   - `it.todo('multi-ball catch spawns min(2, freeSlots) from paddle with ±18° and ±36° offsets')`
   - `it.todo('existing ball velocities are preserved')`
   - `it.todo('never exceeds maxBalls; never replaces active balls')`
   - `it.todo('spawn speeds are finite and |vy|/speed >= MIN_VERTICAL_RATIO')`

5. `tests/rules.stall.test.ts` (PHYS-07 / 05-W0-05):
   - `it.todo('idle 960 ticks without breakable damage → stallTier 1')`
   - `it.todo('tier 2 at 960+240 ticks applies 1.08× speed clamp to MAX_BALL_SPEED')`
   - `it.todo('tier 3 at 960+480 ticks applies deterministic angle nudge; no Math.random')`
   - `it.todo('breakable BRICK_HIT/BREAK resets stallIdleTicks and stallTier to 0')`
   - `it.todo('skipping stepRun (pause/freeze) does not advance stallIdleTicks')`
   - `it.todo('same seed + intents → identical hashWorld including stall fields')`

Suite must stay green (`it.todo` does not fail). Mirror Phase 04 Wave 0 stub style.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.scoring.test.ts tests/rules.pickups.test.ts tests/rules.effects.test.ts tests/rules.multiball.test.ts tests/rules.stall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist under `tests/`
    - Each file contains `it.todo` (grep `it\.todo` matches ≥1 per file)
    - No imports of `src/core/rules/scoring`, `pickups`, `effects`, `multiball`, or `stall` yet
    - Command exits 0
  </acceptance_criteria>
  <done>Nyquist Wave 0 stubs present for RUN-01 / PWR-* / PHYS-07.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite lives.test.ts to last-ball todos + update VALIDATION.md</name>
  <files>tests/rules.lives.test.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-VALIDATION.md</files>
  <read_first>tests/rules.lives.test.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-VALIDATION.md, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md</read_first>
  <action>
1. Replace the body of `tests/rules.lives.test.ts` so it no longer asserts “any BALL_OUT → −life” (deprecated semantics). Keep a `describe('lives rules (last-ball)', ...)` with only `it.todo` titles:
   - `it.todo('activeBallCount === 0 after step decrements exactly one life')`
   - `it.todo('BALL_OUT while other balls remain does not change lives')`
   - `it.todo('life reset clears pickups, expires expand, docks one ball, preserves score and brick HP')`
   - `it.todo('lives === 0 → SimPhase.LOST')`
   - `it.todo('WON/LOST phases are no-ops for stepRun')`
   Do not import `applyLivesFromEvents` as the primary API under test (Plan 04 renames to `applyLivesFromBallCount`). Stub file may import only `{ describe, it } from 'vitest'`.

2. Update `05-VALIDATION.md`:
   - Set frontmatter `wave_0_complete: true` (leave `nyquist_compliant: false` until phase gate).
   - Check Wave 0 requirement boxes for the five new files + lives update.
   - Fill Per-Task Verification Map Task ID column for 05-W0-01…05-W0-05 with Plan `00`, and note lives rewrite under Plan `04` as `05-04-*` placeholders if not yet known — at minimum mark W0 rows File Exists ✅.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.lives.test.ts && rg -n "wave_0_complete: true" .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-VALIDATION.md && rg -n "it\\.todo" tests/rules.lives.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/rules.lives.test.ts` has zero `expect(` calls (stubs only)
    - `rg "applyLivesFromEvents" tests/rules.lives.test.ts` returns no matches
    - `05-VALIDATION.md` has `wave_0_complete: true`
    - `npx vitest run tests/rules.lives.test.ts` exits 0
  </acceptance_criteria>
  <done>Wrong lives assertions removed; VALIDATION Wave 0 marked complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs → core | Stubs must not import unfinished modules (suite stay green) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Drop RNG | mitigate | Stub titles require gameplay-RNG-only tests (implemented Plan 03) |
| T-05-02 | Elevation | Stall pause bypass | mitigate | Stall freeze todo in rules.stall.test.ts (Plan 05) |
| T-05-03 | DoS | NaN stall nudge | mitigate | Multiball/stall todos require finite velocity checks |
| T-05-04 | Tampering | Event ring overflow | accept | Wave 0 documents ring-order scoring; soak assert deferred to Plan 02/04 (cap 128 unchanged) |
</threat_model>

<verification>
`npx vitest run tests/rules.scoring.test.ts tests/rules.pickups.test.ts tests/rules.effects.test.ts tests/rules.multiball.test.ts tests/rules.stall.test.ts tests/rules.lives.test.ts` exits 0.
</verification>

<success_criteria>
All Wave 0 stub files exist; lives suite no longer encodes any-BALL_OUT life loss; VALIDATION Wave 0 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-00-SUMMARY.md`
</output>
