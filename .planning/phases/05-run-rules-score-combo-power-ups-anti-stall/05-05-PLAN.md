---
phase: 05-run-rules-score-combo-power-ups-anti-stall
plan: 05
type: execute
wave: 4
depends_on:
  - "05-04"
files_modified:
  - src/core/rules/stall.ts
  - src/core/stepRun.ts
  - src/core/index.ts
  - tests/rules.stall.test.ts
  - tests/physics.golden-replay.test.ts
autonomous: true
requirements:
  - PHYS-07
must_haves:
  truths:
    - "After 960 PLAYING steps without breakable damage, stallTier becomes 1"
    - "Tier 2 multiplies live ball speeds by 1.08 clamped to MAX_BALL_SPEED; tier 3 applies deterministic angle nudge"
    - "Pause/skipping stepRun does not advance stallIdleTicks"
    - "Breakable BRICK_HIT/BREAK resets stallIdleTicks and stallTier to 0; no Math.random in stall path"
  artifacts:
    - path: "src/core/rules/stall.ts"
      provides: "stepAntiStall"
      exports: ["stepAntiStall"]
    - path: "tests/rules.stall.test.ts"
      provides: "PHYS-07 green coverage"
  key_links:
    - from: "stepRun PLAYING"
      to: "stepAntiStall"
      via: "call after stepEffects, before lives"
      pattern: "stepAntiStall"
    - from: "breakable damage events"
      to: "stallIdleTicks = 0"
      via: "ring scan in stepAntiStall"
      pattern: "stallIdleTicks"
---

<objective>
Implement deterministic visible anti-stall escalation (PHYS-07 / D-15…D-18) with no random bounce jitter, wired into `stepRun`.

Purpose: No rally can dead-end; stall escape is honest and sim-time based (pause cannot burn the timer).
Output: `stall.ts` + `stepAntiStall` in stepRun + green stall tests + hash/replay consistency.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-PATTERNS.md
@src/core/stepRun.ts
@src/core/constants.ts
@src/core/physics/resolve.ts
@src/core/rules/win.ts
@src/runtime/freeze.ts

<interfaces>
Analog for stall gate (05-PATTERNS.md → `src/core/rules/win.ts`): event-ring / PLAYING early-out — copy the same shape as `applyWinCheck`:
```typescript
export function applyWinCheck(world: World): void {
  'worklet';
  if (world.simPhase !== SimPhase.PLAYING) {
    return;
  }
  // … body only when PLAYING
}
```
`stepAntiStall` must use the same PLAYING early-out before idle increment or tier mutation.

Tier schedule (A4 locked):
- Each PLAYING `stepAntiStall` call: if no breakable damage this step → `stallIdleTicks += 1`; else reset `stallIdleTicks=0`, `stallTier=0`
- Breakable damage = ring has BRICK_HIT or BRICK_BREAK whose brick is not UNBREAKABLE
- Tier thresholds (idle ticks):
  - `stallIdleTicks >= 960` → `stallTier = 1` (warning only — no velocity change)
  - `stallIdleTicks >= 960 + 240` (=1200) → `stallTier = 2`: once when crossing into tier 2 (or every step while tier>=2 — prefer **apply speed mult once when entering tier 2**, track via previous tier or apply idempotent clamp). Spec: when `stallTier` transitions to 2, multiply each live ball speed by 1.08, clamp magnitude to `MAX_BALL_SPEED=720`, preserve direction; ensure finite
  - `stallIdleTicks >= 960 + 480` (=1440) → `stallTier = 3`: when entering tier 3, rotate each live ball velocity by `±STALL_ANGLE_NUDGE_DEG` (8°) toward steeper (increase |vy| share): sign = `(ballIndex % 2 === 0) ? +1 : -1` degrees from current heading; re-clamp `|vy|/speed >= MIN_VERTICAL_RATIO`; no RNG (T-05-01/T-05-03)

stepRun order after this plan:
```
clearEvents; stepWorld;
applyScoringFromEvents; applyDropsFromBreaks; stepPickups; stepEffects;
stepAntiStall;
applyLivesFromBallCount; applyWinCheck;
```

Freeze (D-15): stall only runs inside stepRun; runtime already skips stepRun when uiFrozen — unit test by simply not calling stepRun for N iterations and asserting idle unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement stepAntiStall + green stall tests</name>
  <files>src/core/rules/stall.ts, tests/rules.stall.test.ts, src/core/index.ts</files>
  <read_first>tests/rules.stall.test.ts, src/core/constants.ts, src/core/types.ts, src/core/physics/resolve.ts, src/core/rules/win.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-PATTERNS.md</read_first>
  <behavior>
    - 959 idle steps → stallTier 0; 960th → stallTier 1
    - Reach 1200 idle → tier 2; ball speed magnitude *= 1.08 (within epsilon) and &lt;= 720
    - Reach 1440 idle → tier 3; velocity finite; |vy|/speed &gt;= MIN_VERTICAL_RATIO
    - Push breakable BRICK_HIT mid-idle → stallIdleTicks=0, stallTier=0
    - Wall/paddle-only events do not reset
    - No Math.random in stall.ts
    - Skipping calls leaves stallIdleTicks unchanged
  </behavior>
  <action>
Implement `stepAntiStall(world: World): void` following the `win.ts` pattern (05-PATTERNS.md analog): `'worklet'` directive, PLAYING early-out identical to `applyWinCheck`, then ring scan for breakable damage reset, idle increment, tier transitions with speed/angle mutations as specified.

Inline literals matching constants: `960`, `240`, `480`, `1.08`, `720`, `8` (deg nudge).

Export from index. Replace all stall `it.todo` with green tests. Include an explicit `rg`-friendly comment: `// PHYS-07: no random bounce jitter`.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.stall.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export function stepAntiStall" src/core/rules/stall.ts` matches
    - `rg "Math\\.random" src/core/rules/stall.ts` returns no matches
    - `rg "1\\.08|960|1440|1200" src/core/rules/stall.ts` matches tier schedule literals
    - Tests cover freeze-by-skip and breakable reset
    - vitest exits 0
  </acceptance_criteria>
  <done>PHYS-07 stall module green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire stepAntiStall into stepRun + replay hash</name>
  <files>src/core/stepRun.ts, tests/rules.stall.test.ts, tests/physics.golden-replay.test.ts</files>
  <read_first>src/core/stepRun.ts, src/core/rules/stall.ts, tests/physics.golden-replay.test.ts</read_first>
  <action>
1. Insert `stepAntiStall(world)` in `stepRun` after `stepEffects` and before `applyLivesFromBallCount`.

2. Add integration test: call `stepRun` 960 times with no bricks (or unbreakable-only / empty grid with ball bouncing walls) and assert `stallTier === 1`. Ensure pause simulation: after some idle, skip 100 calls, assert idle unchanged, resume and continue.

3. Confirm `hashWorld` includes stall fields (Plan 01); add stall determinism test: two worlds same seed + same stepRun count → equal `hashWorld`.

4. Re-run golden-replay; update expected hashes only if stall zeros change mix (should be identical at zero stall for short fixtures — if fixtures run long enough to stall, adjust intentionally or keep brick damage in golden so stall stays 0).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.stall.test.ts tests/physics.golden-replay.test.ts tests/core.purity.test.ts && rg -n "stepAntiStall" src/core/stepRun.ts</automated>
  </verify>
  <acceptance_criteria>
    - `stepRun.ts` call order includes `stepEffects` then `stepAntiStall` then `applyLivesFromBallCount`
    - Determinism test passes (`hashWorld` equal)
    - vitest commands exit 0
  </acceptance_criteria>
  <done>Anti-stall live in stepRun; replay-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI pause → sim | Stall must not read wall clock |
| Stall nudge → ball velocity | Must stay finite and clamped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Elevation | Stall pause bypass | mitigate | Idle +=1 only inside stepAntiStall called from stepRun; unit test skipping calls; runtime freeze already skips stepRun |
| T-05-03 | DoS | NaN after angle nudge | mitigate | Number.isFinite checks; clamp speed to MAX_BALL_SPEED; enforce MIN_VERTICAL_RATIO; tests assert finite |
| T-05-01 | Tampering | Random jitter "cheat" | mitigate | No RNG in stall.ts; purity + explicit no-jitter comment; tests fail if Math.random present |
</threat_model>

<verification>
`npx vitest run tests/rules.stall.test.ts tests/physics.golden-replay.test.ts tests/core.purity.test.ts` green; stepAntiStall wired.
</verification>

<success_criteria>
Visible deterministic stall tiers escalate on sim time only; breakable damage resets; no random jitter.
</success_criteria>

<output>
After completion, create `.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-05-SUMMARY.md`
</output>
