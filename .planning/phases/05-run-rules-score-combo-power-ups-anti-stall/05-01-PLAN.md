---
phase: 05-run-rules-score-combo-power-ups-anti-stall
plan: 01
type: execute
wave: 1
depends_on:
  - "05-00"
files_modified:
  - src/core/constants.ts
  - src/core/types.ts
  - src/core/allocate.ts
  - src/core/reset.ts
  - src/core/hash.ts
  - src/core/step.ts
  - src/core/index.ts
  - tests/physics.golden-replay.test.ts
autonomous: true
requirements:
  - RUN-01
  - PWR-01
  - PWR-02
  - PWR-03
  - PHYS-07
must_haves:
  truths:
    - "World carries score, combo, pickup SoA, stall scalars, and EffectType/PickupType enums"
    - "activeBallCount equals the count of live balls after each stepWorld (dense [0, count) after compact)"
    - "hashWorld mixes score/combo/pickups/stall so drop/stall replay tests can assert equality"
  artifacts:
    - path: "src/core/constants.ts"
      provides: "SCORE_*, DROP_*, EXPAND_*, STALL_*, MAX_PICKUPS, spawn angles"
      contains: "SCORE_HIT"
    - path: "src/core/types.ts"
      provides: "World score/combo/pickup/stall fields"
      contains: "stallIdleTicks"
    - path: "src/core/step.ts"
      provides: "compactBallPool after BALL_OUT / ball loop"
      contains: "compactBallPool"
  key_links:
    - from: "stepWorld BALL_OUT"
      to: "activeBallCount"
      via: "compactBallPool after per-ball CCD"
      pattern: "compactBallPool|activeBallCount"
    - from: "hashWorld"
      to: "score/combo/pickups/stall"
      via: "mixU32/mixTyped"
      pattern: "world\\.score|world\\.combo|pickup"
---

<objective>
Extend the World SoA and constants for Phase 5 run rules, compact the ball pool so `activeBallCount` means live balls, and hash the new gameplay fields.

Purpose: Unlock every rules module without inventing ad-hoc World fields later; fix the critical PWR-01/D-12 gap where `activeBallCount` was a high-water mark.
Output: Typed World + constants + allocate/reset/hash + `compactBallPool` in `step.ts`; barrel exports; golden-replay still green (or expectations updated for new hash inputs at zero defaults).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-PATTERNS.md
@src/core/constants.ts
@src/core/types.ts
@src/core/allocate.ts
@src/core/reset.ts
@src/core/hash.ts
@src/core/step.ts
@src/core/index.ts
@docs/layer-contract.md

<interfaces>
<!-- Current contracts executors extend — do not invent parallel World shapes -->

From src/core/types.ts (excerpt):
```typescript
export type World = {
  ballActive: Uint8Array;
  activeBallCount: number;
  maxBalls: number;
  paddleW: number;
  lives: number;
  simPhase: number;
  effectCount: number;
  effectType: Uint8Array;
  effectUntilTick: Int32Array;
  maxEffects: number;
  rngGameplay: Uint32Array;
  tick: number;
  // … bricks, events …
};
```

From src/core/constants.ts (existing):
```typescript
export const FIXED_DT = 1 / 120;
export const PADDLE_WIDTH = 72;
export const MAX_BALLS = 8;
export const MAX_EFFECTS = 16;
export const MAX_BALL_SPEED = 720;
export const SERVE_SPEED = 360;
export const MIN_VERTICAL_RATIO = Math.cos(PADDLE_ANGLE_CLAMP_RAD);
```

From src/core/step.ts BALL_OUT (today — must compact after):
```typescript
pushEvent(world, EventCode.BALL_OUT, bi, -1, hx, hy);
world.ballActive[bi] = 0;
break;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 5 constants + World type fields</name>
  <files>src/core/constants.ts, src/core/types.ts, src/core/index.ts</files>
  <read_first>src/core/constants.ts, src/core/types.ts, src/core/index.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md</read_first>
  <action>
**Locked decisions to encode (verbatim values from RESEARCH recommendations / Claude's Discretion locked by planner):**

Add to `src/core/constants.ts` (export + document that worklets must inline matching literals):

| Constant | Value | Decision |
|----------|-------|----------|
| `SCORE_HIT` | `10` | D-01 / A1 |
| `SCORE_BREAK_BONUS` | `50` | D-01 / A1 |
| `DROP_CHANCE` | `0.2` | D-09 |
| `DROP_TYPE_MULTIBALL_THRESHOLD` | `0.5` | A2 (50/50 after chance roll) |
| `MAX_PICKUPS` | `16` | pool cap |
| `PICKUP_WIDTH` | `20` | flat AABB |
| `PICKUP_HEIGHT` | `12` | flat AABB |
| `PICKUP_FALL_SPEED` | `120` | constant vy (y-down positive) |
| `EXPAND_SCALE` | `1.5` | D-08 |
| `EXPAND_DURATION_SEC` | `10` | D-08 |
| `EXPAND_DURATION_TICKS` | `1200` | `10 / FIXED_DT` |
| `EFFECT_TYPE_EXPAND` | `1` | effects SoA |
| `PICKUP_TYPE_MULTIBALL` | `1` | D-06 |
| `PICKUP_TYPE_EXPAND` | `2` | D-06 |
| `MULTIBALL_SPAWN_OFFSET_DEG` | `[18, 36]` as two named consts `MULTIBALL_ANGLE_A_DEG = 18`, `MULTIBALL_ANGLE_B_DEG = 36` | D-14 / A3 |
| `STALL_IDLE_SEC` | `8` | D-15 |
| `STALL_IDLE_TICKS` | `960` | `8 / FIXED_DT` |
| `STALL_TIER2_EXTRA_TICKS` | `240` | +2s → tier 2 (A4) |
| `STALL_TIER3_EXTRA_TICKS` | `480` | +4s → tier 3 (A4) |
| `STALL_SPEED_MULT` | `1.08` | A4 |
| `STALL_ANGLE_NUDGE_DEG` | `8` | discretionary within clamp |

Also export `PickupType` / `EffectType` as const objects on `types.ts` mirroring EventCode style:
```typescript
export const PickupType = { MULTIBALL: 1, EXPAND: 2 } as const;
export const EffectType = { EXPAND: 1 } as const;
```

Extend `World` with:
- `score: number` (default 0)
- `combo: number` (default 1 — never 0)
- Pickup SoA: `pickupX/Y: Float32Array`, `pickupType: Uint8Array`, `pickupActive: Uint8Array`, `pickupCount: number`, `maxPickups: number`
- Stall: `stallIdleTicks: number`, `stallTier: number` (0 = none, 1..3)

Export new constants + `PickupType` / `EffectType` from `src/core/index.ts`.

Do **not** implement scoring/pickups/stall logic yet.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "SCORE_HIT|DROP_CHANCE|EXPAND_DURATION_TICKS|STALL_IDLE_TICKS|MAX_PICKUPS|stallIdleTicks|pickupCount" src/core/constants.ts src/core/types.ts src/core/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "SCORE_HIT = 10" src/core/constants.ts` matches
    - `rg "DROP_CHANCE = 0\\.2" src/core/constants.ts` matches
    - `rg "EXPAND_DURATION_TICKS = 1200" src/core/constants.ts` matches
    - `rg "STALL_IDLE_TICKS = 960" src/core/constants.ts` matches
    - `World` type includes `score`, `combo`, `stallIdleTicks`, `stallTier`, `pickupX`
    - `index.ts` re-exports `SCORE_HIT` and `PickupType`
  </acceptance_criteria>
  <done>Constants and World shape ready for rules modules.</done>
</task>

<task type="auto">
  <name>Task 2: allocate/reset/hash + compactBallPool in stepWorld</name>
  <files>src/core/allocate.ts, src/core/reset.ts, src/core/hash.ts, src/core/step.ts, tests/physics.golden-replay.test.ts</files>
  <read_first>src/core/allocate.ts, src/core/reset.ts, src/core/hash.ts, src/core/step.ts, tests/physics.golden-replay.test.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md</read_first>
  <action>
1. **allocateWorld / resetWorld:** Allocate pickup SoA length `MAX_PICKUPS` (default 16). Initialize `score = 0`, `combo = 1`, `pickupCount = 0`, all `pickupActive = 0`, `stallIdleTicks = 0`, `stallTier = 0`. On reset, clear pickups like effects; restore score=0, combo=1, stall zeros; do **not** leave stale expand effects (effectCount=0 already).

2. **hashWorld (open question lock — extend hash):** After existing effects mixes, also mix:
   - `score`, `combo` (as u32 / f32 as appropriate — use `mixU32` for integers)
   - `pickupCount`, `pickupType`, `pickupActive`, `pickupX`, `pickupY`
   - `stallIdleTicks`, `stallTier`
   Omit nothing new that affects gameplay determinism.

3. **Ball pool compact (critical gap D-12 / Pitfall 1):** In `step.ts`, after the per-ball CCD loop (before `world.tick += 1`), call a local worklet helper `compactBallPool(world)`:
   - Scan indices `[0, maxBalls)` for `ballActive[i] === 1`
   - Pack live balls into dense prefix `[0, live)` by swapping SoA fields (`ballX/Y/Vx/Vy/Radius/Active`) with the write head
   - Set `world.activeBallCount = live`
   - Keep BALL_OUT as `ballActive[bi] = 0` + event (no mid-loop compact)
   Document in a one-line comment: `activeBallCount === live dense count (Phase 5 / D-12)`.

4. **Golden replay:** Re-run `tests/physics.golden-replay.test.ts`. If hash golden constants break solely because new zero fields are mixed, update expected hashes to the new deterministic values (same seed/intents still identical across chunkings). Do not weaken the identity assertion.

5. Ensure TypeScript compiles: any `World` object literals in tests still typecheck via allocate/reset only.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/physics.golden-replay.test.ts tests/core.purity.test.ts && rg -n "compactBallPool|stallIdleTicks|pickupCount" src/core/step.ts src/core/hash.ts src/core/allocate.ts src/core/reset.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "function compactBallPool" src/core/step.ts` matches
    - After a fixture that sets `ballActive[0]=0` with `activeBallCount=1` and runs one `stepWorld` with no other balls, `activeBallCount === 0` (add a tiny assertion in golden or a one-off expect in an existing step test if needed — preferred: extend golden-replay or add `it` in a small `tests/rules.ball-pool.test.ts` only if required for green; otherwise covered when Plan 04 lives tests land)
    - `hash.ts` contains `world.score` and `world.stallTier` mixes
    - `npx vitest run tests/physics.golden-replay.test.ts` exits 0
    - No `Math.random` introduced in `src/core/`
  </acceptance_criteria>
  <done>World foundation + live ball count semantics + extended hash.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → core World | Only allocate/reset/step mutate SoA; hash must include all gameplay fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | RNG / hash omission | mitigate | Extend `hashWorld` to score/combo/pickups/stall so cheat via omitted state fails replay tests |
| T-05-04 | Tampering | Event ring overflow | accept | Keep `EVENT_RING_CAPACITY = 128`; no change this plan — scoring still drains ring in later plans |
| T-05-03 | DoS | Corrupt ball SoA after compact | mitigate | Compact only swaps finite existing slots; no new velocity math |
</threat_model>

<verification>
`npx vitest run tests/physics.golden-replay.test.ts tests/core.purity.test.ts` green; new fields present in types/allocate/hash/step.
</verification>

<success_criteria>
World shape complete; `activeBallCount` is live dense count after each step; hash includes Phase 5 gameplay fields.
</success_criteria>

<output>
After completion, create `.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-01-SUMMARY.md`
</output>
