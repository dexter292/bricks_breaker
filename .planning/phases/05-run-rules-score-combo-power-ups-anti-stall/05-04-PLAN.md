---
phase: 05-run-rules-score-combo-power-ups-anti-stall
plan: 04
type: execute
wave: 3
depends_on:
  - "05-02"
  - "05-03"
files_modified:
  - src/core/rules/lives.ts
  - src/core/rules/serve.ts
  - src/core/stepRun.ts
  - src/core/index.ts
  - tests/rules.lives.test.ts
  - tests/rules.pickups.test.ts
  - tests/rules.scoring.test.ts
autonomous: true
requirements:
  - PWR-01
  - PWR-02
  - PWR-03
  - RUN-01
must_haves:
  truths:
    - "Life decrements only when activeBallCount === 0 after the PLAYING step"
    - "Losing one ball while others remain does not change lives"
    - "Life reset clears pickups, expires expand, docks one ball, preserves score and brick HP"
    - "stepRun PLAYING path runs score → drops → pickups → effects → lives → win in that order"
  artifacts:
    - path: "src/core/rules/lives.ts"
      provides: "applyLivesFromBallCount + life-reset cleanup"
      exports: ["applyLivesFromBallCount"]
    - path: "src/core/stepRun.ts"
      provides: "Phase 5 rules orchestration"
      contains: "applyScoringFromEvents"
  key_links:
    - from: "stepRun"
      to: "rules modules"
      via: "ordered calls after stepWorld"
      pattern: "applyScoringFromEvents|stepPickups|applyLivesFromBallCount"
    - from: "activeBallCount === 0"
      to: "lives--"
      via: "applyLivesFromBallCount"
      pattern: "activeBallCount"
---

<objective>
Rewrite last-ball life semantics (D-12/D-13) and wire all Wave 2 rules into `stepRun` so a PLAYING step awards score, spawns/catches pickups, derives expand width, and only loses a life when the last ball is gone.

Purpose: Close the critical lives/BALL_OUT gap; make headless `stepRun` the single orchestration path for run rules (stall lands in Plan 05); consolidate Phase 5 Wave 2 barrel exports into `src/core/index.ts`.
Output: `applyLivesFromBallCount`, updated `dockBall` cleanup, full `stepRun` pipeline (minus stall), barrel exports for scoring/pickups/effects/multiball, green lives + integration asserts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-PATTERNS.md
@src/core/stepRun.ts
@src/core/rules/lives.ts
@src/core/rules/serve.ts
@src/core/rules/scoring.ts
@src/core/rules/pickups.ts
@src/core/rules/effects.ts

<interfaces>
Current stepRun PLAYING path (replace):
```typescript
clearEvents(world);
stepWorld(world, intent, dt);
applyLivesFromEvents(world);
applyWinCheck(world);
```

Target PLAYING path (Pattern 1 — stall added in Plan 05):
```typescript
clearEvents(world);
stepWorld(world, intent, dt);
applyScoringFromEvents(world);
applyDropsFromBreaks(world);
stepPickups(world, dt);
stepEffects(world);
applyLivesFromBallCount(world);
applyWinCheck(world);
```

D-13 life-reset checklist when `lives > 0` after decrement:
1. Clear all pickups (`pickupCount=0`, all `pickupActive=0`)
2. Expire expand (`effectCount=0` or remove EXPAND; `derivePaddleWidth`)
3. Deactivate all balls; `dockBall` exactly one (activeBallCount=1)
4. Preserve `score`, `combo` (CONTEXT says preserve score; combo: reset to 1 on life loss — set `combo = 1`), brick HP grid unchanged
5. `simPhase = DOCKED`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite lives to last-ball + D-13 cleanup</name>
  <files>src/core/rules/lives.ts, src/core/rules/serve.ts, src/core/index.ts, tests/rules.lives.test.ts</files>
  <read_first>src/core/rules/lives.ts, src/core/rules/serve.ts, tests/rules.lives.test.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md</read_first>
  <behavior>
    - activeBallCount=0, lives=3 → lives=2, DOCKED, one docked ball, score unchanged
    - Two balls: deactivate one via compact so count=1 → lives unchanged
    - Life reset with falling pickup + expand active → pickups cleared, paddleW=72, score preserved, brickHp preserved
    - lives=1 and count=0 → LOST, lives=0
  </behavior>
  <action>
1. Replace `applyLivesFromEvents` with `applyLivesFromBallCount(world)`:
   - PLAYING only
   - If `world.activeBallCount > 0` return (do **not** scan BALL_OUT events for life)
   - Else decrement lives exactly once; apply D-13 cleanup; DOCKED or LOST

2. Update `dockBall` in `serve.ts` to deactivate all ball slots first (`ballActive[i]=0` for all i), then activate/dock index 0 and set `activeBallCount = 1`. Do not touch `score`.

3. Export `applyLivesFromBallCount` from `index.ts`. Also add barrel re-exports deferred from Plans 02–03 (Wave 2 avoided `index.ts` conflict):
   - `applyScoringFromEvents` from `./rules/scoring`
   - `applyDropsFromBreaks`, `stepPickups` from `./rules/pickups`
   - `applyOrRefreshExpand`, `stepEffects`, `derivePaddleWidth` from `./rules/effects`
   - `spawnMultiballFromPaddle` from `./rules/multiball`
   Prefer delete old name `applyLivesFromEvents` and fix all imports (grep repo) — no deprecated alias unless compile requires it.

4. Replace lives `it.todo` with green tests covering behaviors above (may use `stepRun` once Task 2 lands — write unit-level tests calling `applyLivesFromBallCount` directly first).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.lives.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "applyLivesFromBallCount" src/core/rules/lives.ts src/core/index.ts` matches
    - `rg "applyScoringFromEvents|applyDropsFromBreaks|stepPickups|stepEffects|derivePaddleWidth|spawnMultiballFromPaddle" src/core/index.ts` matches all Wave 2 rule exports
    - `rg "EventCode\\.BALL_OUT" src/core/rules/lives.ts` returns no matches
    - `rg "activeBallCount" src/core/rules/lives.ts` matches a last-ball gate (`=== 0` or equivalent)
    - Life-reset test expects score preserved and pickupActive all 0
    - vitest exits 0
  </acceptance_criteria>
  <done>PWR-01 last-ball life + D-13 cleanup implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire stepRun orchestration (score/pickups/effects/lives)</name>
  <files>src/core/stepRun.ts, tests/rules.lives.test.ts, tests/rules.scoring.test.ts, tests/rules.pickups.test.ts</files>
  <read_first>src/core/stepRun.ts, src/core/rules/scoring.ts, src/core/rules/pickups.ts, src/core/rules/effects.ts, src/core/rules/lives.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-RESEARCH.md</read_first>
  <action>
Update `stepRun` PLAYING path to the exact order in the interfaces block (no `stepAntiStall` yet).

Add at least one integration test (in lives or scoring file):
- Drive a ball into a breakable brick via `stepRun` and assert `score > 0` after hits (proves scoring wired).
- Optional: spawn pickup by forcing BREAK + seed, step until catch — if too heavy, keep module tests and one `stepRun` score smoke.

Ensure DOCKED path still clamps paddle with `world.paddleW * 0.5` (expand-aware while docked after life reset should be base width).

Grep: no remaining `applyLivesFromEvents` call sites in `src/`.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.lives.test.ts tests/rules.scoring.test.ts tests/rules.pickups.test.ts tests/rules.effects.test.ts tests/rules.multiball.test.ts && rg -n "applyScoringFromEvents|applyDropsFromBreaks|stepPickups|stepEffects|applyLivesFromBallCount" src/core/stepRun.ts && ! rg -n "applyLivesFromEvents" src/ --glob '!**/node_modules/**'</automated>
  </verify>
  <acceptance_criteria>
    - `stepRun.ts` contains calls in order: `applyScoringFromEvents`, `applyDropsFromBreaks`, `stepPickups`, `stepEffects`, `applyLivesFromBallCount`, `applyWinCheck`
    - `rg "applyLivesFromEvents" src/` returns no matches (verify uses `! rg` — leftover call sites fail the check)
    - Full listed vitest command exits 0
  </acceptance_criteria>
  <done>stepRun orchestrates Phase 5 rules except stall.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Physics BALL_OUT → lives | Must not trust per-event life loss; only live count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | stepRun drop path | mitigate | Drops still only via gameplay RNG inside applyDropsFromBreaks |
| T-05-04 | Tampering | Ring overflow during multi-ball | mitigate | Orchestration scores/drops immediately after stepWorld before more events; keep cap 128; add `expect(world.evOverflow).toBe(0)` in one multi-ball stepRun smoke if feasible |
| T-05-03 | DoS | Life reset leaves NaN paddleW | mitigate | derivePaddleWidth after clear effects; dockBall finite positions |
</threat_model>

<verification>
All Phase 5 rules unit tests (except stall) green; stepRun contains full non-stall pipeline.
</verification>

<success_criteria>
Last-ball life works under multi-ball; life reset cleanup matches D-13; score/pickups/effects run inside stepRun.
</success_criteria>

<output>
After completion, create `.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-04-SUMMARY.md`
</output>
