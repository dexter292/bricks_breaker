---
phase: 05-run-rules-score-combo-power-ups-anti-stall
plan: 02
type: execute
wave: 2
depends_on:
  - "05-00"
  - "05-01"
files_modified:
  - src/core/rules/scoring.ts
  - tests/rules.scoring.test.ts
autonomous: true
requirements:
  - RUN-01
must_haves:
  truths:
    - "Every breakable BRICK_HIT awards SCORE_HIT * current combo then combo += 1"
    - "BRICK_BREAK awards (SCORE_HIT + SCORE_BREAK_BONUS) * current combo then combo += 1"
    - "Any PADDLE_HIT resets combo to 1"
    - "Unbreakable hits award neither score nor combo change"
  artifacts:
    - path: "src/core/rules/scoring.ts"
      provides: "applyScoringFromEvents"
      exports: ["applyScoringFromEvents"]
    - path: "tests/rules.scoring.test.ts"
      provides: "RUN-01 green unit coverage"
  key_links:
    - from: "event ring BRICK_HIT/BREAK/PADDLE_HIT"
      to: "world.score / world.combo"
      via: "applyScoringFromEvents ring scan"
      pattern: "applyScoringFromEvents"
---

<objective>
Implement deterministic score + combo from the event ring (RUN-01 / D-01…D-04) with award-then-increment locked.

Purpose: Skill reward for consecutive brick control; paddle contact is the intentional combo reset.
Output: `src/core/rules/scoring.ts` + green `tests/rules.scoring.test.ts`. Barrel export + stepRun wiring deferred to Plan 04 (keeps Wave 2 parallel with Plan 03 — no `src/core/index.ts` ownership).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md
@.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-PATTERNS.md
@src/core/rules/lives.ts
@src/core/types.ts
@src/core/constants.ts

<interfaces>
From src/core/rules/lives.ts — copy event-ring scan pattern (do not clear ring):
```typescript
export function applyLivesFromEvents(world: World): void {
  'worklet';
  if (world.simPhase !== SimPhase.PLAYING) return;
  const n = world.evCount;
  const start = (world.evHead - n + world.evCap) % world.evCap;
  for (let i = 0; i < n; i++) {
    const idx = (start + i) % world.evCap;
    // branch on world.evCode[idx]
  }
}
```

Locked scoring math (D-01, D-02, RESEARCH open Q1):
- combo starts at 1
- On breakable BRICK_HIT: `score += SCORE_HIT * combo;` then `combo += 1`
- On breakable BRICK_BREAK: `score += (SCORE_HIT + SCORE_BREAK_BONUS) * combo;` then `combo += 1`
  (BREAK already implies the destroy event — do **not** also process a separate HIT for the same brick/step if the ring only emits BREAK; if both exist for same brick, award only once per research: ring already emits one damage event per brick via brickDamagedThisStep — handle whichever codes appear, typically HIT for non-fatal and BREAK for fatal)
- On PADDLE_HIT (any ball): `combo = 1`
- Unbreakable: if `evCode` is BRICK_HIT and `brickFlags[evB] & UNBREAKABLE`, skip score/combo (D-18 / A5)
- Inline literals in worklet: `scoreHit = 10`, `scoreBreakBonus = 50`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED→GREEN scoring unit tests</name>
  <files>tests/rules.scoring.test.ts, src/core/rules/scoring.ts</files>
  <read_first>tests/rules.scoring.test.ts, src/core/rules/lives.ts, src/core/constants.ts, src/core/types.ts, .planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md</read_first>
  <behavior>
    - BRICK_HIT with combo=1 → score 10, combo becomes 2
    - Second BRICK_HIT → score += 20 (10*2), combo becomes 3
    - BRICK_BREAK with combo=2 → score += (10+50)*2 = 120, combo becomes 3
    - PADDLE_HIT → combo = 1 (score unchanged)
    - Unbreakable BRICK_HIT → score/combo unchanged
    - Two BRICK_HIT events in one ring drain → deterministic ordered awards
  </behavior>
  <action>
1. Replace `it.todo` entries in `tests/rules.scoring.test.ts` with real tests using `allocateWorld`, `resetWorld`, `clearEvents`, `pushEvent`, `SimPhase.PLAYING`, `BrickFlags`, `EventCode`.

2. Implement `applyScoringFromEvents(world: World): void` in `src/core/rules/scoring.ts`:
   - `'worklet'` directive
   - Early-out unless `simPhase === PLAYING`
   - Scan ring oldest→newest (same start formula as lives.ts)
   - Award-then-increment per D-01/D-02 / RESEARCH Q1 lock
   - Never call `Math.random` / never touch RNG streams (T-05-01)
   - Do not clear the ring

3. Do **not** modify `src/core/index.ts` or `stepRun.ts` in this plan — Plan 04 owns barrel exports and wiring (avoids Wave 2 conflict with Plan 03).

4. Tests import `applyScoringFromEvents` from `../src/core/rules/scoring` (direct module path), not the barrel.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.scoring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export function applyScoringFromEvents" src/core/rules/scoring.ts` matches
    - `rg "applyScoringFromEvents" src/core/index.ts` returns no matches yet (barrel deferred to Plan 04)
    - `rg "Math\\.random" src/core/rules/scoring.ts` returns no matches
    - File contains award-then-increment: score uses combo before `combo += 1` (grep `combo \\+= 1` or `world.combo =` after score add)
    - `npx vitest run tests/rules.scoring.test.ts` exits 0 with zero todos for the behaviors listed
  </acceptance_criteria>
  <done>RUN-01 scoring module green; barrel export deferred to Plan 04.</done>
</task>

<task type="auto">
  <name>Task 2: Document scoring constants parity + purity smoke</name>
  <files>src/core/rules/scoring.ts, tests/rules.scoring.test.ts</files>
  <read_first>src/core/rules/scoring.ts, src/core/constants.ts, tests/core.purity.test.ts</read_first>
  <action>
Add a comment at top of `scoring.ts` listing inlined literals that must match `SCORE_HIT=10` and `SCORE_BREAK_BONUS=50` in constants.ts (worklet close-over ban).

Add one test that after `resetWorld`, `combo === 1` and `score === 0` before events.

Run purity suite to ensure scoring file has no RN/Math.random imports.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/rules.scoring.test.ts tests/core.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "SCORE_HIT|10" src/core/rules/scoring.ts` shows inlined 10 (or named local const inside function)
    - Purity test exits 0
  </acceptance_criteria>
  <done>Scoring stays core-pure with constant parity documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event ring → score | Untrusted only in the sense of physics-produced events; must not use host RNG |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | scoring.ts | mitigate | No RNG; pure function of ring + World; covered by unit tests + purity gate |
| T-05-04 | Tampering | Lost BREAK via overflow | accept | Scoring drains whatever is in the ring; capacity 128 unchanged; document that overflow increments `evOverflow` (assert in Plan 04 soak if added) |
</threat_model>

<verification>
`npx vitest run tests/rules.scoring.test.ts tests/core.purity.test.ts` green.
</verification>

<success_criteria>
Combo and score behave per D-01/D-02 with award-then-increment; paddle resets combo; unbreakable ignored.
</success_criteria>

<output>
After completion, create `.planning/phases/05-run-rules-score-combo-power-ups-anti-stall/05-02-SUMMARY.md`
</output>
