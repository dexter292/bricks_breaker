---
phase: 01-foundation-thread-boundary-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .nvmrc
  - app.json
  - eas.json
  - babel.config.js
  - tsconfig.json
  - app/_layout.tsx
  - app/index.tsx
  - .gitignore
  - scripts/assert-skia-version.mjs
autonomous: true
requirements:
  - ARCH-01
user_setup:
  - service: apple-developer
    why: "D-17 — paid Apple Developer Program required for EAS internal iOS distribution to a physical iPhone"
    dashboard_config:
      - task: "Confirm paid Apple Developer Program membership is active"
        location: "https://developer.apple.com/account"
      - task: "Verify Expo/EAS has Apple credentials + provisioning ready before any iOS EAS build"
        location: "npx eas-cli@latest credentials -p ios"
  - service: expo-eas
    why: "Cloud/dev-client builds need an Expo account and EAS project"
    dashboard_config:
      - task: "Log in to Expo (`npx eas-cli@latest login`) and create/link EAS project"
        location: "https://expo.dev"
must_haves:
  truths:
    - "Repo root is an Expo SDK 57 app (not nested under a subdirectory)"
    - "Node 24 is pinned via .nvmrc and package.json engines"
    - "Skia resolves to exactly 2.12.0 and is listed in expo.install.exclude"
    - "EAS profiles exist for development (dev-client + overlay on), profiling (release-style + overlay on), and production (overlay unset)"
  artifacts:
    - path: "package.json"
      provides: "SDK 57 matrix deps, engines.node, expo.install.exclude for Skia"
      contains: "engines"
    - path: ".nvmrc"
      provides: "Node 24 LTS pin (D-18)"
      contains: "24"
    - path: "eas.json"
      provides: "development / profiling / production build profiles"
      contains: "EXPO_PUBLIC_PERF_OVERLAY"
    - path: "babel.config.js"
      provides: "worklets plugin last"
      contains: "react-native-worklets/plugin"
    - path: "scripts/assert-skia-version.mjs"
      provides: "Pre-build Skia pin integrity check (T-01-02)"
  key_links:
    - from: "package.json"
      to: "@shopify/react-native-skia"
      via: "exact 2.12.0 dep + expo.install.exclude"
      pattern: "react-native-skia.*2\\.12\\.0"
    - from: "eas.json"
      to: "EXPO_PUBLIC_PERF_OVERLAY"
      via: "development + profiling only; absent from production"
      pattern: "EXPO_PUBLIC_PERF_OVERLAY"
---

<objective>
Bootstrap the greenfield Expo SDK 57 project at the repo root from `expo-template-default@57`, pin Node 24 + Vitest-ready engines, install the native matrix (including Skia 2.12.0 override), and configure EAS/babel/app plugins so later plans can build the FPS harness without fighting the scaffold.

Purpose: Phase 1 is empty-repo → empirical architecture gate. Wrong scaffold (`with-skia`) or missing `expo.install.exclude` silently invalidates D-16.
Output: Runnable Expo app shell at repo root with correct pins, EAS profiles, and Skia assertion script.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-CONTEXT.md
@.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-RESEARCH.md
@.planning/research/STACK.md

## Locked decisions this plan implements
- D-15: no monorepo / nested package
- D-16: Skia 2.12.0 first (exclude before install)
- D-17: paid ADP gate prepared via user_setup (builds in plan 04)
- D-18: Node 24 LTS via `.nvmrc` + `engines`
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Expo SDK 57 at repo root</name>
  <files>package.json, package-lock.json, app/_layout.tsx, app/index.tsx, app.json, babel.config.js, tsconfig.json, .gitignore</files>
  <read_first>.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-RESEARCH.md, .planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-CONTEXT.md, .planning/research/STACK.md</read_first>
  <action>
1. Confirm repo root has `.planning/` and `.cursor/` only (no existing `package.json`). Do **NOT** use `npx create-expo-app -e with-skia` (research rejects it — off-matrix pins + web CanvasKit postinstall).
2. Scaffold to a temp dir: `npx create-expo-app@latest /tmp/bb-scaffold --template expo-template-default@57.0.26` (if the CLI rejects the versioned template flag, use default template and then `npx expo install expo@^57.0.24 --fix` so resolved versions match SDK 57).
3. `rsync -a --exclude .git /tmp/bb-scaffold/ ./` into the repo root so `app/` and `package.json` live at repo root (D-13, D-15). Preserve `.planning/` and `.cursor/`.
4. Strip unused default-template demo surface: remove tab-navigation example screens/assets that are not needed for a single spike host; keep `expo-router` `app/` entry. Prefer deleting unused deps (`@expo/ui`, `expo-glass-effect`, `expo-symbols`, `expo-image`, `expo-web-browser`, `expo-device`) only if removal does not break the router scaffold (research A5).
5. Ensure `.gitignore` includes `node_modules/`, `ios/`, `android/`, `.expo/`.
6. Do not create `src/` yet (plans 02–03 own that). Leave `app/index.tsx` as a thin placeholder that renders a simple Text ("Spike host — pending") so Metro can start.
  </action>
  <verify>
    <automated>test -f package.json &amp;&amp; test -d app &amp;&amp; ! test -d bricks_breaker &amp;&amp; node -e "const p=require('./package.json'); if(!p.dependencies?.expo) process.exit(1); console.log('expo', p.dependencies.expo)"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at repo root (not under a nested project folder)
    - `app/_layout.tsx` and `app/index.tsx` exist
    - No `with-skia` / `copy-canvaskit` postinstall script in `package.json`
    - `.gitignore` contains `node_modules`, `.expo`, and either `ios`/`android` or `/ios` `/android`
  </acceptance_criteria>
  <done>Expo SDK 57 default template lives at repo root with `app/` host entry and no nested project.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pin Node 24, install native matrix + Skia 2.12.0, configure babel/EAS</name>
  <files>package.json, .nvmrc, app.json, eas.json, babel.config.js, scripts/assert-skia-version.mjs</files>
  <read_first>package.json, babel.config.js, app.json, .planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-RESEARCH.md</read_first>
  <action>
**Node (D-18):** Write `.nvmrc` with exact contents `24`. Add to `package.json` engines field exactly: `"node": ">=24 <25"`. If this machine is on Node 25, install/switch to Node 24 via `brew install node@24` or install `fnm`/`nvm` and `fnm use` / `nvm use` before Vitest installs in plan 02 — record which manager was used in the SUMMARY.

**Native modules (use `npx expo install`, never plain npm for these):**
`npx expo install expo-dev-client expo-build-properties expo-screen-orientation expo-keep-awake expo-system-ui`

**Skia override (D-16) — order matters:**
1. Add to `package.json` **before** installing Skia:
```json
"expo": { "install": { "exclude": ["@shopify/react-native-skia"] } }
```
(merge with any existing `expo` key from the template).
2. Then: `npm install @shopify/react-native-skia@2.12.0` (exact, no caret).
3. Write `scripts/assert-skia-version.mjs` that reads `require('@shopify/react-native-skia/package.json').version` (or resolves from node_modules) and `process.exit(1)` unless version === `2.12.0`. Add npm script `"assert:skia": "node scripts/assert-skia-version.mjs"`.

**Forbidden versions — do not install:** `gesture-handler@3.x`, `reanimated@4.7.x`, `worklets@0.12.x`, `typescript@7.x`, `expo-av`, anything that pulls Expo Go as the runtime.

**babel.config.js:** presets `babel-preset-expo`; plugins array MUST end with `'react-native-worklets/plugin'` (last). Do not use `react-native-reanimated/plugin`.

**app.json:** `orientation: "portrait"`, `userInterfaceStyle: "dark"`, plugins include `expo-router`, `expo-dev-client`, and `["expo-build-properties", { "ios": { "enableSceneSupport": true } }]`.

**eas.json** (T-01-01 overlay flag hygiene):
```json
{
  "cli": { "version": ">= 24.7.0" },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "env": { "EXPO_PUBLIC_PERF_OVERLAY": "1" }
    },
    "profiling": {
      "distribution": "internal",
      "android": { "buildType": "apk" },
      "env": { "EXPO_PUBLIC_PERF_OVERLAY": "1" }
    },
    "production": {
      "autoIncrement": true
    }
  }
}
```
Production profile must **not** set `EXPO_PUBLIC_PERF_OVERLAY`.

Finally run: `npx expo install --fix` (must not downgrade Skia), `npm run assert:skia`, `npx expo-doctor@latest`.
  </action>
  <verify>
    <automated>test "$(cat .nvmrc)" = "24" &amp;&amp; npm run assert:skia &amp;&amp; node -e "const p=require('./package.json'); if(p.engines?.node!=='>=24 <25' &amp;&amp; !String(p.engines?.node).includes('24')) process.exit(1); const ex=p.expo?.install?.exclude||[]; if(!ex.includes('@shopify/react-native-skia')) process.exit(1);" &amp;&amp; grep -q "react-native-worklets/plugin" babel.config.js &amp;&amp; grep -q "EXPO_PUBLIC_PERF_OVERLAY" eas.json &amp;&amp; ! grep -A20 '"production"' eas.json | grep -q EXPO_PUBLIC_PERF_OVERLAY</automated>
  </verify>
  <acceptance_criteria>
    - `.nvmrc` file content is exactly `24`
    - `package.json` `engines.node` constrains to Node 24 major
    - `package.json` `expo.install.exclude` includes `@shopify/react-native-skia`
    - Installed Skia version is exactly `2.12.0` (`npm run assert:skia` exits 0)
    - `babel.config.js` contains `react-native-worklets/plugin` as the last plugin
    - `eas.json` sets `EXPO_PUBLIC_PERF_OVERLAY=1` on `development` and `profiling` only
    - `eas.json` `production` block does not mention `EXPO_PUBLIC_PERF_OVERLAY`
    - `app.json` includes `expo-build-properties` with `enableSceneSupport: true`
  </acceptance_criteria>
  <done>Node 24 pinned; SDK-native deps + Skia 2.12.0 locked behind exclude; babel/EAS/app plugins match research; assert script green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config → shipped binary | EAS env flags and dependency pins cross into release artifacts |
| npm lockfile → native binaries | Skia prebuilt binaries are the sole off-matrix native package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `eas.json` / `EXPO_PUBLIC_PERF_OVERLAY` | mitigate | Overlay env set only on `development` + `profiling`; production omits flag; plan 04 verifies production build has overlay off |
| T-01-02 | Tampering / Supply chain | `@shopify/react-native-skia@2.12.0` | mitigate | Exact pin (no caret), `expo.install.exclude`, `scripts/assert-skia-version.mjs` before builds, commit lockfile |
| T-01-03 | Tampering | `ios/` / `android/` hand-edits | mitigate | Keep native dirs gitignored; all config via `app.json` + config plugins |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `package.json` at repo root with expo dependency
- [ ] `npm run assert:skia` exits 0
- [ ] `npx expo-doctor@latest` run (record intentional Skia mismatch warning as expected)
- [ ] `.nvmrc` = 24 and engines pin present
</verification>

<success_criteria>
- Greenfield Expo SDK 57 project at repo root from default template (not with-skia)
- Node 24 + Skia 2.12.0 + worklets babel last + EAS profiles with overlay hygiene
- Ready for plan 02 to add `src/core/`, Vitest, and ESLint boundaries
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-01-SUMMARY.md`
</output>
