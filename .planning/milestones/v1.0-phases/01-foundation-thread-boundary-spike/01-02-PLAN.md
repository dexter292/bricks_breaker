---
phase: 01-foundation-thread-boundary-spike
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/core/index.ts
  - src/core/types.ts
  - src/core/allocate.ts
  - src/core/step.ts
  - src/core/constants.ts
  - vitest.config.ts
  - tests/core.smoke.test.ts
  - tests/core.purity.test.ts
  - eslint.config.js
  - docs/layer-contract.md
  - package.json
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "core/ allocates and mutates a stub world in Node under Vitest with zero RN/Skia/Reanimated imports"
    - "Illegal platform imports in core/ fail ESLint"
    - "docs/layer-contract.md states allowed and banned layer crossings as a checkable table"
    - "core/ is split across ≥5 modules so cross-module worklet imports are exercisable later"
  artifacts:
    - path: "src/core/index.ts"
      provides: "Public core surface (allocateWorld, stepStub)"
      exports: ["allocateWorld", "stepStub"]
    - path: "tests/core.smoke.test.ts"
      provides: "D-09 Node smoke"
    - path: "tests/core.purity.test.ts"
      provides: "Static purity + ≥5 module gate"
    - path: "eslint.config.js"
      provides: "D-11 restricted imports + D-14 no runOnJS/scheduleOnRN in runtime/render"
    - path: "docs/layer-contract.md"
      provides: "Written ARCH-01 crossing contract"
      contains: "core/"
    - path: "vitest.config.ts"
      provides: "Node environment Vitest config"
  key_links:
    - from: "tests/core.smoke.test.ts"
      to: "src/core"
      via: "import allocateWorld/stepStub"
      pattern: "from ['\"].*core"
    - from: "eslint.config.js"
      to: "src/core/**/*"
      via: "no-restricted-imports FORBIDDEN_IN_CORE"
      pattern: "no-restricted-imports"
    - from: "docs/layer-contract.md"
      to: "eslint.config.js"
      via: "each rule cites a contract row"
      pattern: "ARCH-01|D-11|D-14"
---

<objective>
Create the pure TypeScript `core/` stub (≥5 modules), Vitest Node smoke + purity tests, ESLint layer boundaries, and the written layer contract — the checkable half of ARCH-01 before any Skia/worklet harness exists.

Purpose: Prove simulation code can run unchanged in Node and that illegal imports fail the build (D-09, D-11, D-12).
Output: `src/core/*`, Vitest suite green, ESLint boundary rules that actually error, `docs/layer-contract.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-CONTEXT.md
@.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-RESEARCH.md
@.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-01-PLAN.md

## Locked decisions
- D-09 Vitest smoke in Node
- D-10 no gameplay / collision physics / advanced VFX in Phase 1
- D-11 ESLint boundaries from Phase 1
- D-12 core/ is pure stub only (no physics)
- D-14 ban runOnJS/scheduleOnRN on hot-path dirs (rules land now; dirs filled in plan 03)
- D-18 Vitest 5.0.1 on Node 24
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Vitest 5 + write failing core smoke/purity tests</name>
  <files>package.json, vitest.config.ts, tests/core.smoke.test.ts, tests/core.purity.test.ts</files>
  <read_first>package.json, .nvmrc, .planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-RESEARCH.md, .planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-VALIDATION.md</read_first>
  <behavior>
    - smoke: `allocateWorld(256)` then two `stepStub(w, 1/120)` calls → `w.x[0]` changes and stays finite
    - purity: every `.ts` under `src/core/` (excluding tests) has zero forbidden import/require of react/react-native/skia/expo/@react-native
    - purity: non-test `src/core/` module count ≥ 5
  </behavior>
  <action>
Confirm Node major is 24 (`node -v` starts with `v24`); if not, switch using the manager chosen in plan 01 before continuing.

Install (plain npm — not native): `npm install -D vitest@5.0.1 @vitest/coverage-v8@5.0.1`

Create `vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/core/**/*.test.ts', 'tests/**/*.test.ts'],
  },
});
```
Add scripts: `"test": "vitest run"`, `"test:core": "vitest run src/core tests"`.

Write `tests/core.smoke.test.ts` and `tests/core.purity.test.ts` exactly as specified in 01-RESEARCH.md "Vitest config + the two core/ tests" (FORBIDDEN regex + walk ≥5 modules). Do **not** implement `src/core/` yet — run `npx vitest run tests/core.smoke.test.ts` and confirm RED (import failure).
  </action>
  <verify>
    <automated>node -e "const v=process.versions.node; if(!v.startsWith('24.')) { console.error('need Node 24, got', v); process.exit(1);} " &amp;&amp; npx vitest run tests/core.smoke.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies include `vitest` at `5.0.1`
    - `vitest.config.ts` sets `environment: 'node'`
    - `tests/core.smoke.test.ts` imports from `../src/core`
    - `tests/core.purity.test.ts` asserts module count `>= 5` and forbidden-import regex
    - First vitest run of smoke test fails (RED) before Task 2
  </acceptance_criteria>
  <done>Vitest 5 installed on Node 24; smoke/purity tests exist and smoke is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure core/ stub (≥5 modules) until Vitest green</name>
  <files>src/core/types.ts, src/core/constants.ts, src/core/allocate.ts, src/core/step.ts, src/core/index.ts</files>
  <read_first>tests/core.smoke.test.ts, tests/core.purity.test.ts, .planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-RESEARCH.md</read_first>
  <behavior>
    - Same as Task 1 — tests must go GREEN
  </behavior>
  <action>
Implement exactly five (or more) pure TS modules under `src/core/` with **zero** imports from react / react-native / skia / reanimated / expo:

1. `types.ts` — `SpikeWorld` with SoA typed arrays: `x,y,vx,vy,w,h,color` as `Float32Array`/`Uint32Array`, plus `spriteCount`, `accumulator`, `tick` (number).
2. `constants.ts` — export `DEFAULT_SPRITE_CAP = 256` (and any stub sizing constants). Mark nothing RN-specific.
3. `allocate.ts` — `export function allocateWorld(capacity: number): SpikeWorld` — creates typed arrays sized to capacity, initializes `spriteCount` to `min(capacity, 256)` (or capacity), seeds positions/velocities with deterministic math (no `Math.random()`). Include `'worklet';` directive at top of function body.
4. `step.ts` — `export function stepStub(world: SpikeWorld, dt: number): void` with `'worklet';` — advances positions by velocity * dt, wraps/bounces inside a fixed 0..logicalWidth/Height box (constants), increments `world.tick`. No allocations inside the loop (reuse indices only).
5. `index.ts` — re-export `allocateWorld`, `stepStub`, types.

Do not add physics collision. Do not import anything outside `src/core/`.

Run until green: `npx vitest run tests/core.smoke.test.ts tests/core.purity.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/core.smoke.test.ts tests/core.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/` contains ≥5 non-test `.ts` files
    - `allocate.ts` and `step.ts` contain the string `'worklet'`
    - `rg -n "from ['\"]react|react-native|@shopify/react-native-skia|expo" src/core` returns no matches
    - `npx vitest run tests/core.smoke.test.ts tests/core.purity.test.ts` exits 0
  </acceptance_criteria>
  <done>core/ stub mutates in Node; purity + ≥5-module gates pass (D-09, D-12).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ESLint boundaries + written layer contract</name>
  <files>eslint.config.js, docs/layer-contract.md, package.json</files>
  <read_first>.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-RESEARCH.md, src/core/index.ts, package.json</read_first>
  <action>
Install: `npm install -D eslint@^9.39.5 eslint-config-expo@~57.0.2 typescript-eslint@8.70.0 eslint-plugin-boundaries@7.2.0`

Create flat `eslint.config.js` per research "Layer Enforcement":
- Spread `eslint-config-expo/flat`
- `files: ['src/core/**/*.{ts,tsx}']` → `no-restricted-imports` with FORBIDDEN_IN_CORE patterns (react, react-native, skia, expo, @react-native) and message citing ARCH-01 / D-11
- `files: ['src/runtime/**/*.{ts,tsx}', 'src/render/**/*.{ts,tsx}']` → `no-restricted-syntax` banning `CallExpression[callee.name='runOnJS']` and `scheduleOnRN` (D-14)
- Configure `eslint-plugin-boundaries` elements for `core`, `runtime`, `render`, `app`, and future `input`/`ui`/`vfx`/`services` with one-way allow rules matching the contract (even if future dirs are empty)

Write `docs/layer-contract.md` with a table of **allowed** crossings (runtime→core worklet call; render reads world only; app mounts runtime/render; input→runtime via shared value — future; core→services via event ring — future) and **banned** crossings (core→RN/Skia/Reanimated; render→mutate world; hot-path runOnJS/scheduleOnRN; React state per frame). Each ESLint rule comment must cite a contract row ID (e.g. `LC-01`).

Add script `"lint": "eslint ."`.

Negative probe (must fail then clean): temporarily add `import 'react-native'` to `src/core/step.ts`, run `npx eslint src/core/step.ts` and confirm non-zero exit, then revert the import. Record the probe in SUMMARY.

Also run: `npx vitest run && npx eslint .` (may warn on empty runtime/render globs — ensure config does not crash).
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npx eslint src/core &amp;&amp; test -f docs/layer-contract.md &amp;&amp; grep -q "LC-" docs/layer-contract.md &amp;&amp; grep -q "no-restricted-imports" eslint.config.js &amp;&amp; grep -q "scheduleOnRN" eslint.config.js</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` exists and contains `no-restricted-imports` and `scheduleOnRN`
    - `docs/layer-contract.md` exists and contains at least one `LC-` row id and mentions `core/`
    - Temporary illegal `react-native` import in `core/` causes `eslint` non-zero (proved in SUMMARY), then reverted
    - `npx vitest run` exits 0
    - `npx eslint src/core` exits 0 on clean tree
  </acceptance_criteria>
  <done>Layer contract written; ESLint fails illegal core imports and bans hot-path bridge hops (D-11, D-14, ARCH-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `core/` ↔ platform packages | Pure simulation must not pull native modules into Node tests |
| Lint config ↔ CI | Boundary rules are the enforcement plane for ARCH-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `src/core/**` imports | mitigate | ESLint `no-restricted-imports` + Vitest purity scan + Node resolution (three layers) |
| T-01-01 | Information Disclosure | N/A in this plan | accept | Overlay flag handled in plans 01/03/04; no new surface here |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run` green
- [ ] `npx eslint src/core` green
- [ ] Negative eslint probe documented in SUMMARY
- [ ] ≥5 files under `src/core/` excluding tests
</verification>

<success_criteria>
- D-09 smoke green in Node
- D-11/D-14 ESLint rules fail illegal patterns
- Written `docs/layer-contract.md` checkable against eslint
- core/ ready for worklet import from plan 03 frame callback
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/01-foundation-thread-boundary-spike/01-02-SUMMARY.md`
</output>
