---
phase: 08-showpiece-level-performance-certification-launch-baseline
plan: 04
type: execute
wave: 4
depends_on:
  - "00"
  - "01"
  - "03"
files_modified:
  - app/_components/GameHost.tsx
  - tests/audio.release.test.ts
  - docs/phase8-certification.md
autonomous: true
requirements:
  - PLT-03
must_haves:
  truths:
    - "DEV soak can auto-run 100 Title↔Playing mount cycles then a 15-minute continuous play session"
    - "Audio release / pool assertions prove idempotent cleanup after unmount"
    - "Soak harness is development-only and never runs on the per-frame hot path"
  artifacts:
    - path: "app/_components/GameHost.tsx"
      provides: "DEV soak driver (100 cycles + 15 min)"
    - path: "tests/audio.release.test.ts"
      provides: "Automated lifecycle asserts (D-20/D-22)"
    - path: "docs/phase8-certification.md"
      provides: "Soak procedure + memory/frame-time record fields"
  key_links:
    - from: "GameHost soak driver"
      to: "PlayingHost unmount"
      via: "shellPhase title↔playing setState"
      pattern: "SOAK|shellPhase|setTimeout|InteractionManager"
    - from: "PlayingHost unmount"
      to: "audio.release()"
      via: "existing cleanup useEffect"
      pattern: "release\\(\\)"
---

<objective>
Add a DEV-only Title↔Playing soak harness (100 cycles + 15 min continuous) with automated audio/lifecycle asserts and documentation for device memory/frame-time recording (PLT-03 / D-19…D-23).

Purpose: Prove mount/unmount does not leak loops/listeners/audio; final device soak remains a human gate in Plan 06.
Output: GameHost soak driver, green `audio.release` tests, soak section in phase8-certification.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-CONTEXT.md
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md
@app/_components/GameHost.tsx
@app/_components/PlayingHost.tsx
@src/devflags.ts
@src/services/audio/expoAudioService.ts
@tests/audio.mapping.test.ts
@docs/phase8-certification.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Automated audio.release / lifecycle asserts</name>
  <files>tests/audio.release.test.ts</files>
  <read_first>tests/audio.mapping.test.ts, src/services/audio/expoAudioService.ts, src/services/audio/types.ts, src/services/audio/memoryAudioService.ts, tests/vfx.particles.test.ts</read_first>
  <behavior>
    - createMemoryAudioService (or default mock): release() clears pools
    - second release() does not throw (idempotent)
    - after release, playBatch/map paths soft-fail or no-op without throwing
  </behavior>
  <action>
Convert Wave 0 `tests/audio.release.test.ts` todos into real tests using `createMemoryAudioService` (preferred for Node) or the patterns in `tests/audio.mapping.test.ts`:
- Call `preload` / play a mapped event if required by API, then `release()`, assert internal pool maps empty or length 0 (inspect via test-only surface if already exposed; otherwise assert idempotent release + subsequent `playBatch([],0)` / play does not throw).
- Double `release()` must not throw (D-20).
- Do not require real expo-audio native module in Vitest.
Optional: add a small unit asserting `allocateVfx` fresh state has `particleCount === 0` and `shakeAmp === 0` after new allocate (pool-reset signal) in this file or reuse particles test — keep scope to audio if particles already covered in Plan 02.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npm test -- tests/audio.release.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - No `it.todo` remaining in `tests/audio.release.test.ts`
    - File contains `release` and `idempotent` or double-release assertion
    - Command exits 0
  </acceptance_criteria>
  <done>Automated soak-related audio lifecycle asserts green.</done>
</task>

<task type="auto">
  <name>Task 2: GameHost DEV soak driver + docs section</name>
  <files>app/_components/GameHost.tsx, docs/phase8-certification.md</files>
  <read_first>app/_components/GameHost.tsx, src/devflags.ts, docs/phase8-certification.md, .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md</read_first>
  <action>
1. Import `SOAK_HARNESS` from `src/devflags.ts` (defined in Plan 03 alongside `CERT_HARNESS`). Production `eas.json` must **not** set `EXPO_PUBLIC_SOAK=1`. Do not redefine the flag here unless missing after Plan 03 merge.

2. In `GameHost.tsx` (D-19, D-23):
   - When `typeof __DEV__ !== 'undefined' && __DEV__ && SOAK_HARNESS`, start a soak driver in `useEffect`:
     a. Perform **100** Title↔Playing cycles: `setShellPhase('playing')` → wait ~500–1000 ms via `setTimeout` (or `InteractionManager`) → `setShellPhase('title')` → wait → repeat. **Never** schedule from `useFrameCallback`.
     b. After 100 cycles, set `playing` and leave continuous play for **15 * 60 * 1000** ms, then return to title and `console.log` completion.
   - Clear all timeouts on unmount.
   - If not `__DEV__` or not `SOAK_HARNESS`, zero soak code paths run (no timers).
   - Do not alter production Title→Play UX when flag unset.

3. Append **Soak test** section to `docs/phase8-certification.md` (Plan 03 created the protocol body — append only; do not rewrite cert thresholds):
   - How to arm: `EXPO_PUBLIC_SOAK=1` + `__DEV__` / dev-client.
   - 100 cycles then 15 min continuous.
   - Record memory (`adb shell dumpsys meminfo com.dexter292.bricksbreaker` / Xcode gauges) and frame-time at **start and end**; fail on sustained RSS growth, rising p95, crash, unresponsive controls (D-21).
   - Note: unit asserts cover audio release; **final device soak remains mandatory human gate** (D-22).
   - Empty soak Results rows for Plan 06.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "SOAK_HARNESS|EXPO_PUBLIC_SOAK|100|15 \\*|900000|__DEV__" app/_components/GameHost.tsx src/devflags.ts && rg -n "Soak|100|15.min|meminfo" docs/phase8-certification.md && npm test -- tests/audio.release.test.ts && (rg -n "EXPO_PUBLIC_SOAK" eas.json && exit 1 || true)</automated>
  </verify>
  <acceptance_criteria>
    - `SOAK_HARNESS` defined in `src/devflags.ts`
    - `GameHost` references `__DEV__` and `SOAK_HARNESS` and uses `setTimeout`/`InteractionManager` (not frame callback)
    - Docs contain soak procedure with meminfo / 100 / 15
    - `eas.json` does not enable `EXPO_PUBLIC_SOAK=1` on production
    - audio.release tests pass
  </acceptance_criteria>
  <done>DEV soak harness + docs ready; automated lifecycle asserts green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Soak env flag ↔ production gameplay | Must not run in store builds |
| Timer-driven shell ↔ worklets | Unmount must tear down game loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Elevation | SOAK_HARNESS | mitigate | Require `__DEV__ && EXPO_PUBLIC_SOAK`; never set on production profile (D-23) |
| T-08-17 | Denial of Service | 15 min soak | mitigate | DEV-only; discrete timers; clear on unmount |
| T-08-18 | Tampering | Hot-path injection | mitigate | Forbid frame-callback soak logic |
| T-08-19 | Information disclosure | console logs | mitigate | No PII; cycle counts only |
| T-08-20 | Spoofing | Claiming soak pass from units alone | mitigate | Docs: device soak mandatory (D-22) |
</threat_model>

<verification>
audio.release tests green; GameHost soak gated; docs describe 100+15min procedure.
</verification>

<success_criteria>
Soak automation (DEV) + lifecycle unit asserts land; device soak recording fields ready for Plan 06.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-04-SUMMARY.md`
</output>
