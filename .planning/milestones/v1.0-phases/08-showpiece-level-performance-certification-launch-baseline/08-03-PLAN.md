---
phase: 08-showpiece-level-performance-certification-launch-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/devflags.ts
  - app/_components/PlayingHost.tsx
  - src/runtime/useGameLoop.ts
  - docs/phase8-certification.md
  - docs/phase7-vfx-measurement.md
autonomous: true
requirements:
  - PLT-03
must_haves:
  truths:
    - "A DEV-only scripted worst-case on level-03 forces ≥3 balls, near-cap particles, and shake in a fixed measurement window"
    - "phase8-certification.md documents exact conditions, gfxinfo commands, and operational pass thresholds"
    - "Cert harness adds no per-frame work when disabled and is unavailable in production"
  artifacts:
    - path: "docs/phase8-certification.md"
      provides: "Protocol + empty Results + operational thresholds (D-14/D-15)"
    - path: "src/devflags.ts"
      provides: "optional EXPO_PUBLIC_CERT env (DEV hygiene)"
    - path: "app/_components/PlayingHost.tsx"
      provides: "DEV Cert worst-case control"
  key_links:
    - from: "DEV cert trigger"
      to: "multi-ball + particle flood + shake"
      via: "runtime inject / debug Intent — not core RNG rule changes"
      pattern: "__DEV__|CERT|worst-case|worstCase"
    - from: "docs/phase8-certification.md"
      to: "docs/measurement-methodology.md"
      via: "explicit citation"
      pattern: "measurement-methodology|gfxinfo|Pixel 6a"
---

<objective>
Ship a reproducible DEV scripted worst-case certification harness on level-03 and flesh out `docs/phase8-certification.md` with exact conditions and operational pass numbers (PLT-03 / D-14…D-18).

Purpose: Make Pixel 6a Mid gfxinfo runs repeatable and close Phase 7 “Results TBD” procedure gap (actual Results rows filled in Plan 06).
Output: DEV cert control, documentation with thresholds + empty Results table ready for device fills.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-CONTEXT.md
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md
@docs/phase7-vfx-measurement.md
@docs/measurement-methodology.md
@docs/phase8-certification.md
@src/devflags.ts
@app/_components/PlayingHost.tsx
@src/runtime/useGameLoop.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEV scripted worst-case cert trigger</name>
  <files>src/devflags.ts, app/_components/PlayingHost.tsx, src/runtime/useGameLoop.ts</files>
  <read_first>docs/phase7-vfx-measurement.md, src/devflags.ts, app/_components/PlayingHost.tsx, src/runtime/useGameLoop.ts, src/vfx/particles.ts, src/core/index.ts, .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md</read_first>
  <action>
Implement D-14 reproducible worst-case **without changing production gameplay or core drop RNG** (D-03/D-04/D-18):

1. Flags in `src/devflags.ts` (Plan 03 owns this file for Wave 3):
   ```typescript
   export const CERT_HARNESS = process.env.EXPO_PUBLIC_CERT === '1';
   export const SOAK_HARNESS = process.env.EXPO_PUBLIC_SOAK === '1'; // consumed by Plan 04 GameHost
   ```
   Document: env alone must **not** enable UI in production profiles — UI still requires `__DEV__`. Prefer `__DEV__` Pressable as primary cert trigger; env may auto-arm only when `__DEV__ && CERT_HARNESS`. Do not set either flag on the production EAS profile.

2. In `PlayingHost`, under `__DEV__` only, add a Pressable labeled e.g. `Cert WC` that:
   - Ensures `levelId === 'level-03'` (set if needed) and Mid tier override (call existing DEV tier force → remount if required) (D-15).
   - Invokes a runtime helper (new function in `useGameLoop` handle or adjacent runtime module) that on the **next cold path / discrete** invocation:
     a. Spawns/forces **≥3 active balls** (reuse multiball spawn helpers or debug Intent — do not permanently alter DROP_CHANCE).
     b. Floods particles near Mid `particleCap` via `spawnBurst` / allocate path (cosmetic only).
     c. Punches shake (`punchShake` destroy/life impulse) so amp is decaying during the window.
   - Must **not** run inside `useFrameCallback` every frame after trigger (D-23 analogue for cert — one-shot / short timer only).

3. Guard: if `!__DEV__`, no Pressable, no auto-arm from env. Profiling builds with overlay may still be release-like — keep cert UI `__DEV__`-only; document that cert runs use a **dev-client or profiling build that still has __DEV__** OR document adb/manual procedure when UI unavailable — prefer documenting that gate runs use development/profiling **with** the Pressable present (dev-client). If profiling strips `__DEV__`, provide an alternate: `EXPO_PUBLIC_CERT=1` gated additionally by `PERF_OVERLAY` already true only on profiling — **still never enable on production profile** (`eas.json` production must not set EXPO_PUBLIC_CERT). Assert in docs.

4. Do not modify `src/core/**` drop rules or add level-specific RNG.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "CERT_HARNESS|EXPO_PUBLIC_CERT|Cert WC|worstCase|worst-case|__DEV__" src/devflags.ts app/_components/PlayingHost.tsx src/runtime/useGameLoop.ts && ! rg -n "EXPO_PUBLIC_CERT" eas.json || rg -n "production" -A20 eas.json | rg -v "EXPO_PUBLIC_CERT" >/dev/null; npm test -- tests/core.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Cert UI / trigger references `__DEV__`
    - `src/devflags.ts` defines `CERT_HARNESS` from `EXPO_PUBLIC_CERT`
    - `eas.json` production profile does not set `EXPO_PUBLIC_CERT=1`
    - No DROP_CHANCE changes under `src/core`
    - `tests/core.purity.test.ts` still passes
  </acceptance_criteria>
  <done>DEV one-shot worst-case trigger exists; production clean.</done>
</task>

<task type="auto">
  <name>Task 2: phase8-certification protocol + thresholds + Phase 7 pointer</name>
  <files>docs/phase8-certification.md, docs/phase7-vfx-measurement.md</files>
  <read_first>docs/phase8-certification.md, docs/phase7-vfx-measurement.md, docs/measurement-methodology.md, docs/device-gate-results.md, .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-CONTEXT.md</read_first>
  <action>
1. Expand `docs/phase8-certification.md` from Wave 0 stub into the authoritative Phase 8 gate doc (D-14…D-18):
   - **Scene:** level-03 + Mid tier + scripted worst-case (balls≥3, particles near Mid cap, shake decaying, glow visible at glowScale 1).
   - **Build:** profiling profile; package `com.dexter292.bricksbreaker`.
   - **Android mandatory:** Pixel 6a + Mid; ≥2 runs × ≥30 s after ~2 s warmup; keep **worse** run (D-15). Substitute Android = preliminary only (D-17).
   - **Commands:** `adb shell dumpsys gfxinfo com.dexter292.bricksbreaker reset` then `framestats`.
   - **Operational pass lock (A1):** p50 frame time ≤ 16.7 ms; p95 ≤ 20 ms **OR** janky/missed-vsync share ≤ 5%; hard fail on crash / unresponsive touch / progressive degradation.
   - Explicit: RN Perf Monitor alone invalid; on fail → optimize/retune budgets + **rerun same scenario** — do not map Pixel to Low or disable required effects solely to pass (D-13/D-18).
   - **iOS (D-16):** physical iPhone required; Instruments Core Animation / Game; render + touch + stability notes; install alone ≠ evidence.
   - Empty Results table rows ready for Plan 06 fills (Pixel Mid run1/run2, iPhone Instruments, optional substitute preliminary).
   - How to arm harness: DEV `Cert WC` (+ Mid force) steps numbered 1…n.

2. Update `docs/phase7-vfx-measurement.md` Results / debt section: point OPEN Pixel gfxinfo debt to `docs/phase8-certification.md` (do not claim closed until Plan 06).
  </action>
  <verify>
    <automated>rg -n "Pixel 6a|16\\.7|p95|gfxinfo|level-03|Mid|Instruments|RN Perf Monitor|phase8-certification" docs/phase8-certification.md && rg -n "phase8-certification" docs/phase7-vfx-measurement.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/phase8-certification.md` contains `Pixel 6a`, `16.7`, `gfxinfo`, `level-03`, `Instruments`
    - States RN Perf Monitor invalid
    - Contains Results table header
    - `docs/phase7-vfx-measurement.md` references `phase8-certification`
  </acceptance_criteria>
  <done>Certification protocol documented with locked thresholds; Phase 7 debt points here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DEV cert injector ↔ World/VFX | Debug spawn must not become production cheat |
| Measurement docs ↔ store claims | Protocol docs must not claim FPS pass without Results |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Elevation | Cert WC control | mitigate | `__DEV__` (+ no production EXPO_PUBLIC_CERT) (D-14/D-23 pattern) |
| T-08-12 | Tampering | Multi-ball inject | mitigate | One-shot runtime inject; do not change DROP_CHANCE / core RNG |
| T-08-13 | Spoofing | False Mid cert | mitigate | Docs forbid silent Low remap; Results require Pixel 6a Mid (D-13/D-15) |
| T-08-14 | Information disclosure | Docs | accept | No PII; device model names only |
| T-08-15 | Denial of Service | Particle flood | mitigate | Respect Mid particleCap; one-shot not per-frame |
</threat_model>

<verification>
Harness references exist under __DEV__; phase8-certification.md has protocol + thresholds; Phase 7 doc points here.
</verification>

<success_criteria>
Scripted worst-case is reproducible and documented; ready for Plan 06 device Results fills.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-03-SUMMARY.md`
</output>
