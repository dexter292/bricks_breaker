---
phase: 08-showpiece-level-performance-certification-launch-baseline
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tests/levels.compile.test.ts
  - tests/runtime.quality-tiers.test.ts
  - tests/runtime.loadLevel.test.ts
  - tests/audio.release.test.ts
  - tests/vfx.particles.test.ts
  - scripts/assert-privacy-manifest.mjs
  - docs/phase8-certification.md
  - docs/store/privacy-policy.md
  - docs/store/privacy-policy.html
  - docs/store/play-data-safety.md
  - docs/store/age-rating.md
  - docs/store/name-clearance.md
  - docs/store/originality-attestation.md
  - .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-VALIDATION.md
autonomous: true
requirements:
  - LVL-04
  - PLT-03
  - PLT-04
must_haves:
  truths:
    - "expo-device is installed via npx expo install with an SDK 57 pin"
    - "Nyquist stub/extend targets exist so Plans 01–06 have automated verify paths"
    - "Docs stubs exist for phase8 certification and store paperwork"
    - "assert-privacy-manifest.mjs exists (may soft-fail until Plan 05 fills app.json)"
  artifacts:
    - path: "tests/runtime.quality-tiers.test.ts"
      provides: "PLT-03 tier heuristic Nyquist stubs"
    - path: "tests/runtime.loadLevel.test.ts"
      provides: "LVL-04 default level-03 Nyquist stubs"
    - path: "tests/audio.release.test.ts"
      provides: "Soak lifecycle Nyquist stubs"
    - path: "scripts/assert-privacy-manifest.mjs"
      provides: "PLT-04 privacyManifests smoke assert"
    - path: "docs/phase8-certification.md"
      provides: "PLT-03 Results stub + protocol skeleton"
    - path: "docs/store/privacy-policy.md"
      provides: "PLT-04 policy source stub"
  key_links:
    - from: "08-VALIDATION.md Wave 0"
      to: "stub test files + expo-device + docs stubs"
      via: "automated vitest / node paths resolve"
      pattern: "quality-tiers|loadLevel|audio\\.release|assert-privacy-manifest|phase8-certification"
---

<objective>
Wave 0 Nyquist + install: pin `expo-device`, create failing/todo stubs for level-03 / quality tiers / audio release / privacy manifest assert, and author docs stubs for certification + store paperwork.

Purpose: Later plans must not claim MISSING test files; Expo 57 Device API must land before tier resolution; PLT-04 docs placeholders unblock Plan 05.
Output: Lockfile pin, stub/extend tests, assert script, docs stubs, VALIDATION.md Wave 0 marked complete.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-CONTEXT.md
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-VALIDATION.md
@.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-PATTERNS.md
@AGENTS.md
@package.json
@scripts/assert-skia-version.mjs
@docs/phase7-vfx-measurement.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install expo-device + docs stubs + privacy assert script</name>
  <files>package.json, package-lock.json, scripts/assert-privacy-manifest.mjs, docs/phase8-certification.md, docs/store/privacy-policy.md, docs/store/privacy-policy.html, docs/store/play-data-safety.md, docs/store/age-rating.md, docs/store/name-clearance.md, docs/store/originality-attestation.md</files>
  <read_first>AGENTS.md, https://docs.expo.dev/versions/v57.0.0/sdk/device/, package.json, scripts/assert-skia-version.mjs, docs/phase7-vfx-measurement.md, .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-RESEARCH.md, .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-VALIDATION.md</read_first>
  <action>
1. Install Device API with Expo pin only (per AGENTS.md / D-10 research — never `npm install react-native-device-info`):
   ```bash
   npx expo install expo-device
   ```
   Assert `package.json` resolves to `~57.0.x` or `57.0.x` matching SDK 57. Do **not** add `react-native-device-info`.

2. Create `scripts/assert-privacy-manifest.mjs` mirroring `scripts/assert-skia-version.mjs` style:
   - Read `app.json` via `fs.readFileSync` + `JSON.parse`
   - Assert `expo.ios.privacyManifests` is an object
   - Assert keys present: `NSPrivacyTracking`, `NSPrivacyTrackingDomains`, `NSPrivacyCollectedDataTypes`, `NSPrivacyAccessedAPITypes`
   - On failure: `console.error` + `process.exit(1)`; on success print `privacyManifests OK`
   - Wave 0: if keys missing, script must still **exist** and be runnable; it will fail until Plan 05 — that is expected. Document in script header comment: `Plan 05 fills app.json; Wave 0 creates the assert.`

3. Create docs stubs (placeholders with `STATUS: STUB — Plan 0N fills`):
   - `docs/phase8-certification.md`: title, link to `docs/measurement-methodology.md` + `docs/phase7-vfx-measurement.md`, empty Results table with columns Device | Tier | Build | Run | p50 ms | p95 ms | Verdict | Notes; note "Pixel 6a Mid mandatory (D-15)"; package id `com.dexter292.bricksbreaker`; RN Perf Monitor invalid.
   - `docs/store/privacy-policy.md`: short MVP draft bullets — offline play; local high score via AsyncStorage; no ads/IAP/accounts/analytics; no PII collected/transmitted. Include `LIVE_URL: TBD` placeholder.
   - `docs/store/privacy-policy.html`: minimal static HTML mirroring the markdown body (for hosting in Plan 05).
   - `docs/store/play-data-safety.md`: stub table (Collects personal info? / Shares? / Ads / IAP) with TBD answers.
   - `docs/store/age-rating.md`: stub Everyone / PEGI 3 note + TBD confirmation.
   - `docs/store/name-clearance.md`: stub for "Neon Brick Breaker" clearance notes.
   - `docs/store/originality-attestation.md`: stub attesting original levels/SFX/art; no Maker/Shatter assets.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; node -e "const p=require('./node_modules/expo-device/package.json'); if(!String(p.version).startsWith('57.')) process.exit(1); console.log('expo-device', p.version)" && test -f scripts/assert-privacy-manifest.mjs && test -f docs/phase8-certification.md && test -f docs/store/privacy-policy.md && test -f docs/store/privacy-policy.html && test -f docs/store/play-data-safety.md && test -f docs/store/age-rating.md && test -f docs/store/name-clearance.md && test -f docs/store/originality-attestation.md && rg -n "Pixel 6a|measurement-methodology|com\.dexter292\.bricksbreaker" docs/phase8-certification.md</automated>
  </verify>
  <acceptance_criteria>
    - `node_modules/expo-device/package.json` version starts with `57.`
    - `rg "react-native-device-info" package.json` returns no matches
    - All seven docs paths listed in `<files>` exist
    - `docs/phase8-certification.md` contains `Pixel 6a`, `measurement-methodology`, and `com.dexter292.bricksbreaker`
    - `scripts/assert-privacy-manifest.mjs` exists and is executable via `node`
  </acceptance_criteria>
  <done>expo-device pinned; certification + store stubs present; privacy assert script exists.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 Nyquist stubs + VALIDATION.md</name>
  <files>tests/levels.compile.test.ts, tests/runtime.quality-tiers.test.ts, tests/runtime.loadLevel.test.ts, tests/audio.release.test.ts, tests/vfx.particles.test.ts, .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-VALIDATION.md</files>
  <read_first>.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-VALIDATION.md, tests/levels.compile.test.ts, tests/audio.mapping.test.ts, tests/vfx.particles.test.ts, tests/core.purity.test.ts, .planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-00-PLAN.md</read_first>
  <action>
Create/extend Nyquist targets so Plans 01–04 have automated verify paths (08-W0-01…05 per VALIDATION.md).

1. Extend `tests/levels.compile.test.ts` with **it.todo** stubs only (do not invent level-03 JSON yet — Plan 01):
   - `it.todo('level-03 validates and compiles with UNBREAKABLE present')`
   - `it.todo('level-03 fingerprint differs from level-01 and level-02')`
   - `it.todo('level-03 cols*rows and brickCount ≤ MAX_BRICKS (256)')`
   Keep all existing level-01/02 tests green (no edits that break them).

2. Create `tests/runtime.quality-tiers.test.ts` importing only `{ describe, it } from 'vitest'`:
   - `it.todo('null/invalid totalMemory → tier low (D-10)')`
   - `it.todo('~6GB memory maps to mid (Pixel 6a band)')`
   - `it.todo('≥8GB maps to high; &lt;4GB maps to low')`
   - `it.todo('BUDGETS.low|mid|high expose particleCap trailMax glowScale')`
   - `it.todo('trailMax never below 2 (FX-01)')`

3. Create `tests/runtime.loadLevel.test.ts` stubs:
   - `it.todo('LevelId includes level-03')`
   - `it.todo('loadLevelById() default is level-03')`
   - `it.todo('loadLevelById(\"level-03\") returns ok:true')`

4. Create `tests/audio.release.test.ts` stubs (soak D-20):
   - `it.todo('release clears pools and is idempotent')`
   - `it.todo('second release does not throw')`

5. Extend `tests/vfx.particles.test.ts` with todos (API lands Plan 02):
   - `it.todo('allocateVfx respects particleCap from caps')`
   - `it.todo('trailMax from caps clamps trailLength result ≥2')`
   - `it.todo('glowScale 0 is distinguishable from 1 in caps')`

6. Update `08-VALIDATION.md`:
   - `wave_0_complete: true` (leave `nyquist_compliant: false` until phase gate)
   - Check Wave 0 boxes for stubs + expo-device + docs stubs
   - Mark File Exists ✅ for 08-W0-01…05 rows where stubs now exist
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/levels.compile.test.ts tests/runtime.quality-tiers.test.ts tests/runtime.loadLevel.test.ts tests/audio.release.test.ts tests/vfx.particles.test.ts && rg -n "wave_0_complete: true" .planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-VALIDATION.md && rg -n "it\\.todo" tests/runtime.quality-tiers.test.ts tests/runtime.loadLevel.test.ts tests/audio.release.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Stub files exist under `tests/` as listed
    - New stub files have ≥1 `it.todo` each
    - Existing level-01/02 compile tests still pass (not converted to todo)
    - Command exits 0
    - VALIDATION frontmatter `wave_0_complete: true`
  </acceptance_criteria>
  <done>Nyquist Wave 0 stubs + VALIDATION updated for LVL-04 / PLT-03 / PLT-04 / soak.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry ↔ app native modules | New `expo-device` native code enters the binary |
| Docs stubs ↔ public claims | Placeholder privacy/store text must not over-claim before Plan 05 |
| Assert script ↔ app.json | Fail-closed when privacyManifests missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Tampering | Level JSON pipeline (stubs only) | mitigate | Wave 0 stubs point at existing `validateLevel` fail-closed path; Plan 01 keeps validate→compile |
| T-08-00-02 | Tampering | brickTypes keys | accept | No new JSON in Wave 0; proto-pollution already rejected in `validate.ts` — reassert in Plan 01 |
| T-08-00-03 | Information disclosure | docs/store stubs | mitigate | Stubs marked STATUS:STUB; no fake LIVE_URL; no PII in placeholders |
| T-08-00-04 | Elevation | DEV harness | accept | No harness code in Wave 0 |
| T-08-00-05 | Spoofing | privacyManifests assert | mitigate | Script fails closed until Plan 05 fills keys |
</threat_model>

<verification>
`npx vitest run` on Wave 0 stub files exits 0; expo-device 57.x present; docs stubs exist; VALIDATION `wave_0_complete: true`.
</verification>

<success_criteria>
Wave 0 complete: expo-device pinned, Nyquist stubs green with `it.todo`, certification + store stubs present, privacy assert script exists, VALIDATION.md updated.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/08-showpiece-level-performance-certification-launch-baseline/08-00-SUMMARY.md`
</output>
