---
phase: 07-feedback-neon-vfx-audio
plan: 05
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
  - "04"
files_modified:
  - src/runtime/eventBridge.ts
  - src/runtime/useVfxIntensity.ts
  - src/runtime/useGameLoop.ts
  - app/_components/PlayingHost.tsx
  - tests/runtime.event-drain.test.ts
autonomous: true
requirements:
  - FX-01
  - FX-02
  - FX-03
must_haves:
  truths:
    - "After each stepRun, VFX consume + audio batch append run before the next clearEvents"
    - "Exactly one scheduleOnRN(playBatch) per frame from eventBridge.ts"
    - "vfxIntensity SharedValue defaults from AccessibilityInfo reduce-motion (1.0 / 0.2)"
    - "PlayingHost preloads AudioService and bakes glow before setActive(true); release on unmount"
  artifacts:
    - path: "src/runtime/eventBridge.ts"
      provides: "scheduleOnRN audio hop allowlisted"
    - path: "src/runtime/useVfxIntensity.ts"
      provides: "AccessibilityInfo → SharedValue"
    - path: "src/runtime/useGameLoop.ts"
      provides: "per-substep snapshot + stepVfx + recordFrame(vfx)"
    - path: "app/_components/PlayingHost.tsx"
      provides: "preload + bake + release wiring"
  key_links:
    - from: "useGameLoop while accumulator"
      to: "consumeEventsForVfx + appendEventsForAudio"
      via: "after each stepRun"
      pattern: "consumeEventsForVfx|appendEventsForAudio"
    - from: "useGameLoop after substep loop"
      to: "stepVfx"
      via: "import from src/vfx (Plan 02)"
      pattern: "stepVfx"
    - from: "eventBridge"
      to: "AudioService.playBatch"
      via: "scheduleOnRN once/frame"
      pattern: "scheduleOnRN"
    - from: "PlayingHost"
      to: "createDefaultAudioService().preload"
      via: "useEffect before setActive(true)"
      pattern: "preload\\(|bakeGlowSprites"
---

<objective>
Integrate VFX + audio into the live game loop and PlayingHost: reduce-motion intensity, per-substep event drain, one batched audio hop, glow bake + SFX preload before play.

Purpose: Success criteria 1–4 require the cosmetic layer wired without regressing physics or spawning per-event JS hops.
Output: `eventBridge`, `useVfxIntensity`, updated `useGameLoop` + `PlayingHost`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-CONTEXT.md
@.planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-RESEARCH.md
@.planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-PATTERNS.md
@.planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md
@src/runtime/useGameLoop.ts
@src/runtime/appStatePause.ts
@app/_components/PlayingHost.tsx
@src/vfx/index.ts
@src/services/audio/index.ts
@eslint.config.js

<interfaces>
From Plan 02/03/04:
```typescript
consumeEventsForVfx(world, vfx, intensity): void
appendEventsForAudio(world, batch): void
stepVfx(vfx, dt, intensity): void  // Plan 02 export — compose stepParticles + stepShake; do NOT reimplement in runtime
AudioService.playBatch(codes, count): void
bakeGlowSprites(): GlowAtlas
intensityFromReduceMotion(enabled: boolean): number
```

useGameLoop substep region today (insert after stepRun):
```typescript
while (w.accumulator >= fixedDt && steps < maxSubsteps) {
  stepRun(w, intent, fixedDt);
  // INSERT: consumeEventsForVfx; appendEventsForAudio; pushTrail from ball SoA
  ...
}
// AFTER loop: stepVfx(vfx, fixedDt, intensity); flushAudioBatch via eventBridge once; recordFrame(..., vfx, intensity)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useVfxIntensity + eventBridge + useGameLoop drain</name>
  <files>src/runtime/useVfxIntensity.ts, src/runtime/eventBridge.ts, src/runtime/useGameLoop.ts, tests/runtime.event-drain.test.ts</files>
  <read_first>src/runtime/useGameLoop.ts, src/runtime/appStatePause.ts, src/vfx/index.ts, src/vfx/stepVfx.ts, src/vfx/audioBatch.ts, eslint.config.js, docs/layer-contract.md, .planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-RESEARCH.md</read_first>
  <action>
1. **`src/runtime/useVfxIntensity.ts`** (per D-03):
   - On mount: `AccessibilityInfo.isReduceMotionEnabled()` → write SharedValue `vfxIntensity` via `intensityFromReduceMotion`.
   - Subscribe `AccessibilityInfo.addEventListener('reduceMotionChanged', …)` for live updates.
   - Cleanup remove subscription. Prefer AccessibilityInfo over `useReducedMotion()` alone (RESEARCH).

2. **`src/runtime/eventBridge.ts`** (sole LC-07 exception file from Plan 00):
   ```typescript
   import { scheduleOnRN } from 'react-native-worklets';
   export function flushAudioBatchOnJS(
     playBatch: (codes: ArrayLike<number>, count: number) => void,
     codes: ArrayLike<number>,
     count: number,
   ): void {
     'worklet';
     scheduleOnRN(playBatch, codes, count);
   }
   ```
   Exactly one call site from the frame callback after the substep loop. Pass a JS-thread-bound `playBatch` from the host (closure / ref). Do **not** import `src/services/audio` into `runtime/` (boundaries: runtime still cannot import services — host passes the function).

3. **`src/runtime/useGameLoop.ts`**:
   - Allocate `VfxState` + `AudioBatchSoA` once with the world (UI-thread).
   - Read `vfxIntensity` SharedValue each frame.
   - Inside `while` **after each** `stepRun`: `consumeEventsForVfx`, `appendEventsForAudio`, update trails from active balls' x/y via `pushTrail` (not inside `stepVfx`).
   - After loop: import and call **`stepVfx(vfx, dt, intensity)`** from `src/vfx` (Plan 02) — do **not** call `stepParticles`/`stepShake` directly from runtime, and do **not** create `src/vfx/` modules here; `flushAudioBatchOnJS` **once** if `batch.count > 0`; `resetAudioBatch`.
   - Pass `vfx` + intensity into `recordFrame`.
   - When frozen (pause/won/lost), still allow recordFrame refresh; do not spawn new audio from cleared rings incorrectly — if sim frozen, skip stepRun but may still step cosmetic decay via `stepVfx`.

4. Keep unit coverage: extend `tests/runtime.event-drain.test.ts` if needed for pure helpers still imported from vfx (already GREEN from Plan 02).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "scheduleOnRN" src/runtime/eventBridge.ts && ! rg -n "scheduleOnRN" src/runtime/useGameLoop.ts; rg -n "consumeEventsForVfx|appendEventsForAudio|flushAudioBatchOnJS|stepVfx" src/runtime/useGameLoop.ts; rg -n "reduceMotionChanged|isReduceMotionEnabled" src/runtime/useVfxIntensity.ts; ! rg -n "stepParticles|stepShake" src/runtime/useGameLoop.ts; npx eslint src/runtime/eventBridge.ts src/runtime/useGameLoop.ts src/runtime/useVfxIntensity.ts; npx vitest run tests/runtime.event-drain.test.ts tests/vfx.intensity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scheduleOnRN` appears in `src/runtime/eventBridge.ts` and **not** in `src/runtime/useGameLoop.ts`
    - `useGameLoop.ts` contains `consumeEventsForVfx`, `appendEventsForAudio`, and `stepVfx` (called from after the accumulator loop)
    - `useGameLoop.ts` does **not** call `stepParticles` or `stepShake` directly
    - `useVfxIntensity.ts` references `isReduceMotionEnabled` and `reduceMotionChanged`
    - `npx eslint src/runtime/eventBridge.ts src/runtime/useGameLoop.ts` exits 0
    - Vitest intensity + event-drain exit 0
  </acceptance_criteria>
  <done>Per-substep VFX/audio drain + intensity hook + single audio hop wired via Plan 02 stepVfx.</done>
</task>

<task type="auto">
  <name>Task 2: PlayingHost preload, bake, release</name>
  <files>app/_components/PlayingHost.tsx</files>
  <read_first>app/_components/PlayingHost.tsx, src/services/audio/index.ts, src/render/textures/bakeGlowSprites.ts, src/runtime/useGameLoop.ts, .planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-UI-SPEC.md</read_first>
  <action>
1. In `PlayingHost`, `useMemo(() => createDefaultAudioService(), [])`.
2. On mount / before enabling the loop:
   - `await audio.preload()` — catch errors, continue silent (UI-SPEC soft-fail, no Alert).
   - `const atlas = bakeGlowSprites()` — store in ref/SharedValue handed to the loop/canvas path Plan 04 expects.
3. Only call `setActive(true)` after preload attempt settles (success or soft-fail) and glow bake completes — same cold-path gate style as level compile.
4. On unmount: `audio.release()`; do not leave AudioPlayers alive.
5. Pass `playBatch` binding into the game loop handle / eventBridge registration as required by Task 1 API.
6. Wire `useVfxIntensity()` SharedValue into the loop.
7. Do **not** add VFX/audio settings UI, music, or haptics (D-04 / deferred).
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "createDefaultAudioService|preload\\(|\\.release\\(|bakeGlowSprites|useVfxIntensity" app/_components/PlayingHost.tsx; npx eslint app/_components/PlayingHost.tsx; npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `PlayingHost.tsx` contains `createDefaultAudioService`, `preload(`, `.release(`, `bakeGlowSprites`, `useVfxIntensity`
    - No `Alert.alert` for audio failure
    - `rg "expo-haptics|BackgroundMusic|settings.*[Vv]fx" app/_components/PlayingHost.tsx` returns no matches
    - eslint on PlayingHost exits 0
    - `npm run test:core` exits 0
  </acceptance_criteria>
  <done>PlayingHost preloads SFX, bakes glow, releases audio; intensity + drain live in play.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-thread worklet → JS AudioService | Single scheduleOnRN batch / frame |
| AccessibilityInfo → intensity | OS a11y flag; clamp already in vfx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-21 | Denial of Service | per-event scheduleOnRN | mitigate | One hop/frame via eventBridge only; eslint enforces |
| T-07-22 | Denial of Service | audio player leak | mitigate | PlayingHost unmount calls `release()` |
| T-07-23 | Denial of Service | preload hang blocking play | mitigate | Soft-fail catch; still setActive after attempt |
| T-07-24 | Information disclosure | console spam | accept | __DEV__ warn only on soft-fail (optional, match storage pattern) |
</threat_model>

<verification>
eslint runtime+PlayingHost clean; scheduleOnRN only in eventBridge; PlayingHost wires preload/bake/release; core tests green.
</verification>

<success_criteria>
Live play drains events every substep into VFX+audio, intensity follows OS reduce-motion, and deleting wiring would leave core tests unchanged.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/07-feedback-neon-vfx-audio/07-05-SUMMARY.md`
</output>
