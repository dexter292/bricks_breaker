# Deferred items — Phase 06

Logged during plan execution; out of current-task scope (do not fix inline).

| Found in | Item | Why deferred |
|----------|------|--------------|
| 06-01 | `tsc --noEmit`: `app/index.tsx` Property `fill` (suggests `fills`); overlays use `StyleSheet.absoluteFillObject` | Pre-existing; not introduced by storage plan; storage files tsc-clean |
