---
phase: 04-level-format-brick-types
plan: 04
type: execute
wave: 4
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - src/runtime/loadLevel.ts
  - app/_components/GameHost.tsx
  - src/runtime/useGameLoop.ts
  - src/runtime/overlays/LevelErrorOverlay.tsx
  - src/runtime/GameScreen.tsx
  - src/core/index.ts
  - src/core/reset.ts
  - src/core/levels/phase3Grid.ts
autonomous: false
requirements:
  - LVL-01
  - LVL-02
  - LVL-03
must_haves:
  truths:
    - "Default boot loads level-01 via JS-thread validate→compile before gameplay; worklets only applyCompiledLevel"
    - "Invalid level shows console errors + LevelErrorOverlay and never mutates/plays World"
    - "__DEV__ switch reloads level-01|level-02 through the same pipeline; Retry reloads current level only"
    - "phase3Grid.ts / loadPhase3Grid are removed; app and tests share loadAndCompile only"
  artifacts:
    - path: "app/_components/GameHost.tsx"
      provides: "require JSON + loadAndCompile + dev switch + error gate"
    - path: "src/runtime/useGameLoop.ts"
      provides: "CompiledLevel apply on init/retry; no authoring parse"
    - path: "src/runtime/overlays/LevelErrorOverlay.tsx"
      provides: "actionable validation error UI"
    - path: "src/core/levels/phase3Grid.ts"
      provides: "DELETED"
  key_links:
    - from: "GameHost"
      to: "loadAndCompile"
      via: "JS useEffect / load helper before setActive"
      pattern: "loadAndCompile"
    - from: "useGameLoop"
      to: "applyCompiledLevel"
      via: "first frame + retry"
      pattern: "applyCompiledLevel"
    - from: "GameHost levelError"
      to: "LevelErrorOverlay"
      via: "blocks playfield when issues non-null"
      pattern: "LevelErrorOverlay"
---

<objective>
Wire the host cold path: Metro-require level JSON → `loadAndCompile` on JS → pass `CompiledLevel` into `useGameLoop` for `applyCompiledLevel` only; add minimal LevelErrorOverlay + `__DEV__` level switch; delete `phase3Grid.ts` (D-11…D-15).

Purpose: Prove both levels play through one pipeline; fail loudly on invalid data; remove hardcoded grid forever.
Output: Updated GameHost/useGameLoop/GameScreen, LevelErrorOverlay, deleted phase3Grid, human UAT checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-CONTEXT.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-RESEARCH.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-PATTERNS.md
@app/_components/GameHost.tsx
@src/runtime/useGameLoop.ts
@src/runtime/GameScreen.tsx
@src/runtime/overlays/ResultOverlay.tsx
@assets/levels/level-01.json
@assets/levels/level-02.json
@docs/layer-contract.md

## Locked decisions
- D-11: `__DEV__` switch only; no menu; Retry does **not** cycle levels
- D-12: default boot `level-01.json`
- D-13: fail loudly — console + overlay; never play invalid; never partial World mutate
- D-14: validate+compile once on JS before reset/apply; off hot path
- D-15: delete `phase3Grid.ts`; single pipeline

## LC constraints
- `app/` must not import deep `src/core` if eslint forbids — prefer importing `loadAndCompile` via a thin `src/runtime/loadLevel.ts` wrapper **or** existing allowed path. Check eslint boundaries: if `app`→`core` banned, put `require`+`loadAndCompile` in `src/runtime/loadLevel.ts` and call from GameHost (RESEARCH: host orchestration in app+runtime).
- Prefer: `src/runtime/loadLevel.ts` exports `loadLevelById(id)` using `require` of assets + `loadAndCompile` from core (runtime→core allowed LC-02).

<interfaces>
From src/core (Plan 02):
```typescript
export function loadAndCompile(raw: unknown):
  | { ok: true; compiled: CompiledLevel }
  | { ok: false; issues: ValidationIssue[] };
export function applyCompiledLevel(world: World, compiled: CompiledLevel): void;
export type CompiledLevel = { /* … */ };
export type ValidationIssue = { path: string; message: string };
```

Current useGameLoop (replace loadPhase3Grid):
```typescript
// first frame / retry currently call loadPhase3Grid(w)
// → applyCompiledLevel(w, compiledSv.value) when non-null
```

ResultOverlay pattern — mirror for LevelErrorOverlay (absolute scrim, SpaceMono, issue list).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: runtime loadLevel helper + useGameLoop CompiledLevel apply + delete phase3Grid</name>
  <files>src/runtime/loadLevel.ts, src/runtime/useGameLoop.ts, src/core/index.ts, src/core/reset.ts, src/core/levels/phase3Grid.ts</files>
  <read_first>src/runtime/useGameLoop.ts, src/core/index.ts, src/core/levels/phase3Grid.ts, eslint.config.js, docs/layer-contract.md</read_first>
  <action>
1. Create `src/runtime/loadLevel.ts` (JS thread):
   - `export type LevelId = 'level-01' | 'level-02'`
   - `export function loadLevelById(id: LevelId)` → `require('../../assets/levels/level-0N.json')` → `loadAndCompile(raw)` → return Result.
   - Default id `level-01` (D-12).
   - No worklet; no World writes.

2. Update `useGameLoop`:
   - Accept `compiled: SharedValue&lt;CompiledLevel | null&gt;` (or hold compiled on a ref SharedValue set from host).
   - Replace **all** `loadPhase3Grid` calls with: if `compiled.value == null` skip apply / skip starting playable bricks; else `applyCompiledLevel(w, compiled.value)`.
   - First-frame allocate still `resetWorld` then apply.
   - `retry()` on JS: `resetWorld` + `applyCompiledLevel` with **current** compiled (D-11 — no level cycle).
   - **Never** `require` / `validateLevel` / `JSON.parse` under `'worklet'` (D-04, D-14).

3. Delete `src/core/levels/phase3Grid.ts`. Ensure `assignSpatialBrickCells` remains exported from `spatial.ts` via barrel. Remove `loadPhase3Grid` from `src/core/index.ts`. Update `reset.ts` comments.

4. Grep repo for `loadPhase3Grid|phase3Grid` — zero remaining references outside `.planning/` / docs history.

5. Run full core tests — fix any stray imports.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; ! rg -q "loadPhase3Grid|phase3Grid" src tests app; test ! -f src/core/levels/phase3Grid.ts; npx vitest run tests/levels.apply.test.ts tests/levels.compile.test.ts tests/levels.phase3-grid.test.ts tests/core.purity.test.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `src/core/levels/phase3Grid.ts` does not exist
    - `rg -n "loadPhase3Grid" src tests app` finds no matches
    - `useGameLoop` references `applyCompiledLevel`
    - `src/runtime/loadLevel.ts` exists and uses `loadAndCompile`
    - `npm run test:core` exits 0
  </acceptance_criteria>
  <done>Hardcoded grid removed; loop applies compiled levels only.</done>
</task>

<task type="auto">
  <name>Task 2: GameHost load gate + LevelErrorOverlay + __DEV__ switch</name>
  <files>app/_components/GameHost.tsx, src/runtime/overlays/LevelErrorOverlay.tsx, src/runtime/GameScreen.tsx, src/runtime/loadLevel.ts</files>
  <read_first>app/_components/GameHost.tsx, src/runtime/GameScreen.tsx, src/runtime/overlays/ResultOverlay.tsx, .planning/milestones/v1.0-phases/04-level-format-brick-types/04-PATTERNS.md</read_first>
  <action>
1. `LevelErrorOverlay.tsx` — mirror ResultOverlay chrome (absolute fill scrim, panel, SpaceMono): list `issues` as `path: message`. No playfield interaction. Minimal (discretion).

2. `GameScreen.tsx` — accept optional `levelError` + render overlay when non-null; when error, suppress pause/result chrome that would conflict (PATTERNS).

3. `GameHost.tsx`:
   - State: `levelId` default `'level-01'`; `levelError: ValidationIssue[] | null`.
   - SharedValue `compiledSv` for CompiledLevel | null.
   - On mount and when `levelId` changes: `loadLevelById(levelId)`; on fail → `console.error('[level]', issues)`, set `levelError`, set compiled null, **do not** `setActive(true)`, **do not** retry/apply (D-13); on ok → clear error, set compiledSv, call `retry()` / allow active (D-14).
   - Gate initial `setActive(true)` until first successful compile.
   - `__DEV__` only: tiny pressable `Lv 01|02` (or similar) toggling `levelId` between level-01 and level-02 — **no** production menu (D-11).
   - `onRetry` keeps current `levelId` (reload same compiled) — never auto-advance.

4. Ensure production builds without `__DEV__` switch still boot level-01.

No UI-SPEC; no level select menu; no neon.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; test -f src/runtime/overlays/LevelErrorOverlay.tsx && rg -n "loadLevelById|LevelErrorOverlay|__DEV__" app/_components/GameHost.tsx src/runtime && npm test</automated>
  </verify>
  <acceptance_criteria>
    - LevelErrorOverlay exists and is wired from GameScreen/GameHost
    - GameHost defaults to level-01 load path
    - `__DEV__` guards the level switch control
    - `npm test` (full suite) exits 0
    - `rg -n "loadPhase3Grid" src tests app` still clean
  </acceptance_criteria>
  <done>Host fails loudly on bad levels; dev can switch level-01↔02; production boots level-01.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Device/sim UAT — two levels + error overlay + cues</name>
  <files>none</files>
  <read_first>.planning/ROADMAP.md, .planning/milestones/v1.0-phases/04-level-format-brick-types/04-VALIDATION.md</read_first>
  <what-built>
    Data-driven level pipeline with two JSON levels, Skia crack/hatch cues, GameHost load gate, `__DEV__` switch, LevelErrorOverlay, and deleted phase3Grid.
  </what-built>
  <action>
Pause for human UAT against ROADMAP Phase 4 success criteria 1–5 and locked D-11…D-15. Do not change code unless the human reports failures — then fix in a follow-up commit before re-requesting approval.
  </action>
  <how-to-verify>
    1. Start the app (simulator OK). Confirm **level-01** boots and is playable (multi-HP + steel).
    2. In `__DEV__`, use the level switch to load **level-02**. Confirm corridor/channel feel; unbreakables bounce and do not break; win still possible by clearing breakables only.
    3. Confirm crack/chip lines appear as multi-HP bricks take damage (readable beyond color alone); steel shows hatch, not cracks.
    4. Temporarily point the loader at an invalid fixture or break a character in a copy used only for this check — confirm **console errors + on-screen overlay**, and that gameplay does **not** start / World does not play with bad data.
    5. Retry from pause/lose — confirm it reloads the **current** level (does not cycle 01↔02).
  </how-to-verify>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npm test</automated>
  </verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe failures.</resume-signal>
  <acceptance_criteria>
    - Human replies `approved` or lists concrete failures tied to SC-1…5 / D-11…D-15
    - `npm test` remains green at checkpoint time
  </acceptance_criteria>
  <done>Phase 4 level-format UAT signed off for roadmap success criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Metro JSON require → loadAndCompile | Bundled assets still validated before apply |
| Host error UI → World | Error path must not call apply/retry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | host load of huge JSON | mitigate | validate caps before compile; refuse setActive on failure |
| T-04-02 | Tampering | non-finite grid in asset | mitigate | validate rejects; overlay; no apply |
| T-04-03 | Tampering | partial World on bad load | mitigate | On failure: compiled=null, skip apply/retry, keep prior good compiled if switching fails mid-session (prefer keep last-good or hard-stop — **hard-stop with overlay per D-13**) |
| T-04-04 | Tampering | brickTypes keys in assets | mitigate | validate rejects dangerous keys before compile |
</threat_model>

<verification>
`npm test` green; phase3Grid gone; human UAT checkpoint approved.
</verification>

<success_criteria>
Roadmap Phase 4 success criteria 1–5 satisfied for LVL-01/02/03: second level plays without code forks; invalid rejected; HP cues + steel hatch; unbreakables channel and ignore win; schemaVersion + migrations path present.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/04-level-format-brick-types/04-04-SUMMARY.md`
</output>
