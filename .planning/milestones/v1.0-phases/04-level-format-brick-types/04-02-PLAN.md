---
phase: 04-level-format-brick-types
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/core/levels/compile.ts
  - src/core/levels/apply.ts
  - src/core/levels/spatial.ts
  - src/core/levels/load.ts
  - src/core/index.ts
  - src/core/reset.ts
  - assets/levels/level-01.json
  - assets/levels/level-02.json
  - tests/levels.compile.test.ts
  - tests/levels.apply.test.ts
  - tests/levels.phase3-grid.test.ts
# accepted_scope: single wave ships compile+apply+two JSON levels+test retarget (11 files) — splitting would leave half-pipeline unplayable
autonomous: true
requirements:
  - LVL-01
  - LVL-03
must_haves:
  truths:
    - "level-01.json migrates phase3Grid layout (7×5, brickCount 35, multi-HP + steel) through validate→compile→apply"
    - "level-02.json is a corridor/channel with unbreakables and a different structural fingerprint than level-01"
    - "applyCompiledLevel fills SoA, sets gridRows>1 via assignSpatialBrickCells, and never runs after failed validate"
    - "Playable level tests use loadAndCompile + apply — not loadPhase3Grid"
  artifacts:
    - path: "assets/levels/level-01.json"
      provides: "migrated phase3 playable layout (D-09)"
    - path: "assets/levels/level-02.json"
      provides: "corridor structural level (LVL-03 proof)"
    - path: "src/core/levels/compile.ts"
      provides: "compileLevel → CompiledLevel typed arrays"
      exports: ["compileLevel"]
    - path: "src/core/levels/apply.ts"
      provides: "applyCompiledLevel worklet SoA fill"
      exports: ["applyCompiledLevel"]
    - path: "src/core/levels/spatial.ts"
      provides: "assignSpatialBrickCells moved from phase3Grid"
      exports: ["assignSpatialBrickCells"]
  key_links:
    - from: "compileLevel"
      to: "BrickFlags.UNBREAKABLE"
      via: "brickTypes.unbreakable → flags bit"
      pattern: "UNBREAKABLE"
    - from: "applyCompiledLevel"
      to: "assignSpatialBrickCells"
      via: "after copy when gridRows>1"
      pattern: "assignSpatialBrickCells"
    - from: "tests/levels.phase3-grid.test.ts"
      to: "assets/levels/level-01.json"
      via: "loadAndCompile pipeline"
      pattern: "loadAndCompile"
---

<objective>
Replace the compile stub with real packing, implement worklet-safe `applyCompiledLevel` + spatial broadphase helper, ship `level-01.json` / `level-02.json`, and green the compile/apply/phase3-grid Vitest suites through the shared pipeline (D-09, D-10, LVL-01, LVL-03).

Purpose: Data-driven levels exist and prove second-level + unbreakable channeling without host wiring yet.
Output: compile/apply/spatial modules, two assets, green compile/apply/grid tests.

**Note:** Keep `phase3Grid.ts` + `loadPhase3Grid` for GameHost until Plan 04 (D-15 deletion). Re-export `assignSpatialBrickCells` from `spatial.ts` (or `apply.ts`) and update `index.ts` so spatial lives outside the doomed file. `phase3Grid.ts` may import spatial from the new module to avoid duplication until deletion.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-CONTEXT.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-RESEARCH.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-PATTERNS.md
@src/core/levels/phase3Grid.ts
@src/core/levels/schema.ts
@src/core/levels/validate.ts
@src/core/levels/load.ts
@src/core/reset.ts
@src/core/hash.ts
@tests/rules.win.test.ts

## Locked decisions
- D-01…D-04: authoring JSON → validate → compile once
- D-09: exactly two levels — level-01 from phase3Grid, level-02 corridor
- D-10: same code path for both
- D-15: remove phase3Grid **in Plan 04** — this plan migrates data + tests off it

## level-01 fingerprint (from phase3Grid.ts)
- cols=7, rows=5, brickW=44, brickH=18, gapX=4, gapY=4, originY=56
- originX = (360 - (7*44+6*4)) * 0.5 = **14**
- HP rows `[3,2,2,1,1]`; steel at (0,0),(0,6),(2,3)
- cells:
  - `X33333X`
  - `2222222`
  - `22X2222`
  - `1111111`
  - `1111111`
- brickTypes: `"1":{hp:1}`, `"2":{hp:2}`, `"3":{hp:3}`, `"X":{hp:99,unbreakable:true}`
- Expected `brickCount === 35` (all cells occupied)

## level-02 discretion
- Same schema; 7×5 or similar; **corridor/channel of `X`** forcing different trajectories than open level-01
- Must include ≥1 unbreakable channeling pattern; fingerprint (steel positions / row string join) **≠** level-01

<interfaces>
From Plan 01 schema:
```typescript
export function compileLevel(level: LevelFileV1): CompiledLevel;
export function loadAndCompile(raw: unknown):
  | { ok: true; compiled: CompiledLevel }
  | { ok: false; issues: ValidationIssue[] };
```

From src/core/reset.ts:
```typescript
export function loadTestGrid(world: World, bricks: TestBrickSpec[]): void; // KEEP for physics unit tests
export function resetWorld(world: World, seedG: number, seedC: number): void;
```

From src/core/hash.ts:
```typescript
export function hashWorld(world: World): number;
```

assignSpatialBrickCells — move body from phase3Grid.ts lines 11–66 unchanged into `spatial.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compileLevel + assets level-01/02 + compile tests</name>
  <files>src/core/levels/compile.ts, assets/levels/level-01.json, assets/levels/level-02.json, tests/levels.compile.test.ts</files>
  <read_first>src/core/levels/phase3Grid.ts, src/core/levels/schema.ts, src/core/levels/compile.ts, .planning/milestones/v1.0-phases/04-level-format-brick-types/04-PATTERNS.md</read_first>
  <behavior>
    - loadAndCompile(level-01) ok; brickCount===35; ≥1 UNBREAKABLE flag; ≥2 distinct breakable HP among compiled hp[]
    - loadAndCompile(level-02) ok; brickCount&gt;0; ≥1 UNBREAKABLE; structural fingerprint ≠ level-01 (e.g. hash of cells join or steel index set)
    - compile skips `.` cells; packs x/y from origin+gap formula; unbreakable → flags&amp;1
    - Invalid raw still ok:false (regression)
  </behavior>
  <action>
1. Replace compile stub with real `compileLevel` (JS thread, **no** `'worklet'`, **no** World writes):
   - Nested r/c scan; skip `.`; lookup `brickTypes[ch]`; push positions using grid metrics; pack typed arrays into `CompiledLevel`.
   - `flags = unbreakable ? BrickFlags.UNBREAKABLE : 0`.
   - Set `gridCols`/`gridRows` from level.grid.
   - Assume pre-validated input.

2. Author `assets/levels/level-01.json` with baked numeric originX=14 (and other metrics) matching phase3Grid — do **not** recompute at runtime.

3. Author `assets/levels/level-02.json` corridor/channel (Claude discretion) with same brickTypes charset; visually/structurally different steel layout.

4. Green `tests/levels.compile.test.ts`: import JSON via `import level01 from '../assets/levels/level-01.json'` (tsconfig resolveJsonModule) or readFileSync; cover `<behavior>`.

Do not delete phase3Grid yet. Do not wire GameHost.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/levels.compile.test.ts tests/levels.validate.test.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - Both JSON assets exist under `assets/levels/`
    - `compileLevel` no longer throws stub Error for valid levels
    - level-01 brickCount === 35 in test
    - level-01 vs level-02 fingerprint assertion present
    - `npm run test:core` exits 0
  </acceptance_criteria>
  <done>Two authored levels compile through shared pipeline with distinct layouts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: applyCompiledLevel + spatial move + apply/grid tests</name>
  <files>src/core/levels/apply.ts, src/core/levels/spatial.ts, src/core/levels/phase3Grid.ts, src/core/index.ts, src/core/reset.ts, tests/levels.apply.test.ts, tests/levels.phase3-grid.test.ts</files>
  <read_first>src/core/levels/phase3Grid.ts, src/core/reset.ts, src/core/hash.ts, tests/levels.phase3-grid.test.ts, tests/rules.win.test.ts</read_first>
  <behavior>
    - resetWorld → applyCompiledLevel(level-01 compiled) → brickCount 35, gridRows===5, gridCols===7, some UNBREAKABLE
    - After apply, spatial path usable (`gridRows &gt; 1`)
    - Given a world already loaded: validate fail path must not call apply — hashWorld/brickCount unchanged (T-04-03)
    - Optional: apply capped at world.brickX.length
    - phase3-grid test uses loadAndCompile(level-01)+apply; tunneling smoke retained; **no** `loadPhase3Grid` import
  </behavior>
  <action>
1. Move `assignSpatialBrickCells` to `src/core/levels/spatial.ts` (same `'worklet'` body). Update `phase3Grid.ts` to re-export or import from spatial so existing callers compile until Plan 04.

2. Create `src/core/levels/apply.ts`:
```typescript
export function applyCompiledLevel(world: World, compiled: CompiledLevel): void {
  'worklet';
  // clear cellToBrick / damaged flags similar to loadTestGrid
  // copy min(compiled.brickCount, world.brickX.length) into SoA
  // world.brickCount = n
  // assignSpatialBrickCells(world, compiled.gridCols, compiled.gridRows) when rows>1
}
```
   - Cap against capacity (T-04-01 defense in depth).
   - Do **not** parse JSON here.

3. Update `src/core/index.ts` exports: `applyCompiledLevel`, `assignSpatialBrickCells` from spatial/apply; keep `loadPhase3Grid` temporarily.

4. Update `reset.ts` comment: callers use `applyCompiledLevel` or `loadTestGrid` (mention phase3Grid deprecated).

5. Green `tests/levels.apply.test.ts` covering `<behavior>` including hashWorld unchanged when skipping apply on invalid load.

6. Rewrite `tests/levels.phase3-grid.test.ts` to use `assets/levels/level-01.json` + `loadAndCompile` + `applyCompiledLevel`. Drop `gridRows===1` assertion; expect `gridRows===5`. Keep multi-HP, unbreakable, and tunneling smoke against compiled level-01.

Keep `loadTestGrid` for physics unit tests (RESEARCH Q3). Do not delete `phase3Grid.ts` yet.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/levels.apply.test.ts tests/levels.compile.test.ts tests/levels.phase3-grid.test.ts tests/rules.win.test.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `applyCompiledLevel` exported; `'worklet'` present
    - `tests/levels.phase3-grid.test.ts` has zero `loadPhase3Grid` references
    - apply tests prove no-mutate-on-failed-validate
    - `rg -n "gridRows\\)\\.toBe\\(1\\)" tests/levels.phase3-grid.test.ts` finds no matches
    - Full `npm run test:core` exits 0
  </acceptance_criteria>
  <done>Compiled levels apply into SoA with spatial grids; tests share app pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CompiledLevel → World SoA | Only trusted compiled blobs may mutate bricks |
| Asset JSON → loadAndCompile | Still must pass validate before apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | compile/apply brickCount | mitigate | Validate caps earlier; apply also min() with array length |
| T-04-02 | Tampering | grid metrics | accept | Already rejected in Plan 01 validate; compile assumes finite |
| T-04-03 | Tampering | `applyCompiledLevel` | mitigate | Tests assert hash/brickCount unchanged when validate fails; host must not call apply on error (Plan 04) |
| T-04-04 | Tampering | brickTypes | accept | Mitigated in validate; compile only reads validated LevelFileV1 |
</threat_model>

<verification>
Wave verify: `npm test` after both tasks. Per-task: listed vitest commands + `npm run test:core`.
</verification>

<success_criteria>
LVL-01 data files + compile/apply pipeline green; LVL-03 unbreakable flags present in both levels with corridor differentiation; playable tests no longer depend on loadPhase3Grid.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/04-level-format-brick-types/04-02-SUMMARY.md`
</output>
