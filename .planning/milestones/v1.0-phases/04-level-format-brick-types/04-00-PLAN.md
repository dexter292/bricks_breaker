---
phase: 04-level-format-brick-types
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/levels.validate.test.ts
  - tests/levels.compile.test.ts
  - tests/levels.apply.test.ts
  - tests/levels.damage-cues.test.ts
  - tests/fixtures/levels/invalid-schema-version.json
  - tests/fixtures/levels/invalid-row-length.json
  - tests/fixtures/levels/invalid-unknown-char.json
  - tests/fixtures/levels/invalid-space-in-row.json
  - tests/fixtures/levels/invalid-non-finite-grid.json
  - tests/fixtures/levels/invalid-proto-key.json
accepted_scope: stub-heavy-nyquist-wave0
autonomous: true
requirements:
  - LVL-01
  - LVL-02
  - LVL-03
must_haves:
  truths:
    - "Wave 0 Vitest stub files exist for validate/compile/apply/damage-cues so later plans have automated verify targets"
    - "Invalid JSON fixtures cover unsupported schemaVersion, bad row length, unknown char, space-in-row, non-finite grid, and __proto__ brickTypes key"
  artifacts:
    - path: "tests/levels.validate.test.ts"
      provides: "LVL-01 validate suite stub (it.todo)"
    - path: "tests/levels.compile.test.ts"
      provides: "LVL-01/03 compile suite stub (it.todo)"
    - path: "tests/levels.apply.test.ts"
      provides: "apply + no-mutate-on-failure stub (it.todo)"
    - path: "tests/levels.damage-cues.test.ts"
      provides: "LVL-02 crack stroke-plan stub (it.todo)"
    - path: "tests/fixtures/levels/invalid-schema-version.json"
      provides: "unsupported schemaVersion fixture"
  key_links:
    - from: "tests/fixtures/levels/*.json"
      to: "validateLevel (Plan 01)"
      via: "imported by green validate tests"
      pattern: "invalid-"
---

<objective>
Wave 0 Nyquist scaffolding: create Vitest `it.todo` stubs and invalid level fixtures listed in 04-VALIDATION.md so Plans 01–03 have real `<automated>` verify targets before feature code lands.

Purpose: Satisfy Dimension 8 / Nyquist — no feature task may claim a missing test file.
Output: Four stub test files + six invalid JSON fixtures under `tests/fixtures/levels/`.

**Accepted scope note:** Stub-heavy Wave 0; fixtures are real JSON (no code deps). Do not implement validate/compile yet.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-CONTEXT.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-RESEARCH.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-VALIDATION.md
@docs/layer-contract.md

## Locked decisions (do not implement pipeline here)
- D-01…D-03: versioned row-string JSON + schemaVersion 1
- D-02: never silently trim; spaces disallowed in Phase 4
- Threat fixtures: T-04-01 huge/malformed, T-04-02 NaN, T-04-04 __proto__

## Threat focus
- Fixtures must be usable by Plan 01 validate tests for fail-closed behavior
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invalid level JSON fixtures for validator security cases</name>
  <files>tests/fixtures/levels/invalid-schema-version.json, tests/fixtures/levels/invalid-row-length.json, tests/fixtures/levels/invalid-unknown-char.json, tests/fixtures/levels/invalid-space-in-row.json, tests/fixtures/levels/invalid-non-finite-grid.json, tests/fixtures/levels/invalid-proto-key.json</files>
  <read_first>.planning/milestones/v1.0-phases/04-level-format-brick-types/04-RESEARCH.md, .planning/milestones/v1.0-phases/04-level-format-brick-types/04-VALIDATION.md, .planning/milestones/v1.0-phases/04-level-format-brick-types/04-CONTEXT.md</read_first>
  <action>
Create `tests/fixtures/levels/` with six minimal authoring-shaped JSON files (same top-level keys as RESEARCH example: `schemaVersion`, `id`, `name`, `grid`, `brickTypes`, `cells`). Do **not** silently fix the defects — each file must trigger a specific validation failure in Plan 01:

1. `invalid-schema-version.json` — `schemaVersion: 99` (else valid 2×2 with `"1"` and `.`).
2. `invalid-row-length.json` — `grid.cols: 3`, `rows: 2`, but one `cells` row has length ≠ 3.
3. `invalid-unknown-char.json` — cell char `Z` not present in `brickTypes` (and not `.`).
4. `invalid-space-in-row.json` — a row containing a space character (D-02 — must reject; never trim).
5. `invalid-non-finite-grid.json` — e.g. `originX: null` or `"originX": "NaN"` / `brickW: null` so Plan 01 rejects non-finite numbers (T-04-02). Prefer numeric `null` or omit + use `Infinity` if JSON allows — use `"originX": null` plus comment in filename purpose.
6. `invalid-proto-key.json` — `brickTypes` includes a `"__proto__"` key (T-04-04). Other keys may be normal `"1"`.

Keep grids tiny (≤3×3) so fixtures stay readable. No TypeScript imports of these files yet.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; test -f tests/fixtures/levels/invalid-schema-version.json && test -f tests/fixtures/levels/invalid-row-length.json && test -f tests/fixtures/levels/invalid-unknown-char.json && test -f tests/fixtures/levels/invalid-space-in-row.json && test -f tests/fixtures/levels/invalid-non-finite-grid.json && test -f tests/fixtures/levels/invalid-proto-key.json && node -e "for (const f of ['invalid-schema-version','invalid-row-length','invalid-unknown-char','invalid-space-in-row','invalid-non-finite-grid','invalid-proto-key']) JSON.parse(require('fs').readFileSync('tests/fixtures/levels/'+f+'.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - All six fixture paths exist and parse as JSON
    - `invalid-schema-version.json` has `schemaVersion` !== 1
    - `invalid-space-in-row.json` contains a space inside a `cells` string (`rg ' ' tests/fixtures/levels/invalid-space-in-row.json`)
    - `invalid-proto-key.json` `brickTypes` has `__proto__` key
  </acceptance_criteria>
  <done>Invalid fixtures ready for Plan 01 validate RED→GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 it.todo Vitest stubs for validate/compile/apply/damage-cues</name>
  <files>tests/levels.validate.test.ts, tests/levels.compile.test.ts, tests/levels.apply.test.ts, tests/levels.damage-cues.test.ts</files>
  <read_first>.planning/milestones/v1.0-phases/04-level-format-brick-types/04-VALIDATION.md, tests/levels.phase3-grid.test.ts</read_first>
  <action>
Create four Vitest files that **only** import `{ describe, it } from 'vitest'` and declare `it.todo(...)` titles matching 04-VALIDATION.md Task IDs. Do **not** import `src/core/levels/*` yet (modules do not exist — importing would break `npm test`).

1. `tests/levels.validate.test.ts` — todos covering:
   - schemaVersion 1 accepted / unsupported rejected with path `schemaVersion` (04-W0-01)
   - unknown char / bad row length / space-in-row rejected (04-W0-02)
   - non-finite grid metrics rejected; `__proto__` brickTypes key rejected (T-04-02 / T-04-04)
   - brickCount/cols×rows over `MAX_BRICKS` rejected (T-04-01)

2. `tests/levels.compile.test.ts` — todos covering:
   - valid compile brickCount &gt; 0 + UNBREAKABLE flags (04-W0-03 / 04-W0-07)
   - level-01 vs level-02 structural fingerprint differs (04-W0-05)
   - level-02 contains ≥1 unbreakable channeling pattern (04-W0-07)

3. `tests/levels.apply.test.ts` — todos covering:
   - apply fills SoA + `gridRows &gt; 1` spatial path
   - failed validate / skipped apply never mutates World / `hashWorld` (04-W0-04 / T-04-03)

4. `tests/levels.damage-cues.test.ts` — todos covering:
   - HP 3/2/1 distinct crack stroke counts; unbreakable uses hatch plan not cracks (04-W0-06 / LVL-02 / LVL-03)

Suite must stay green (`it.todo` does not fail). Mirror Phase 03 Wave 0 stub style.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/levels.validate.test.ts tests/levels.compile.test.ts tests/levels.apply.test.ts tests/levels.damage-cues.test.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - All four test files exist
    - `npx vitest run tests/levels.validate.test.ts tests/levels.compile.test.ts tests/levels.apply.test.ts tests/levels.damage-cues.test.ts` exits 0
    - Files do not import missing `src/core/levels/*` modules yet
    - `npm run test:core` still exits 0
  </acceptance_criteria>
  <done>Nyquist Wave 0 stubs present; VALIDATION automated commands resolve to existing files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bundled/authored JSON → validator | Untrusted level objects cross into core before World touch |
| Fixtures → CI | Malicious-looking keys (`__proto__`) must stay in fixtures only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | level JSON size / brickCount | mitigate | Fixture + Plan 01 validate caps cols×rows / brickCount ≤ MAX_BRICKS (256) |
| T-04-02 | Tampering | grid numeric fields | mitigate | `invalid-non-finite-grid.json` drives Plan 01 Number.isFinite rejects |
| T-04-03 | Tampering | World SoA | mitigate | Plan 02 apply-guard test; Wave 0 stub documents no-mutate-on-failure |
| T-04-04 | Tampering | `brickTypes` keys | mitigate | `invalid-proto-key.json`; Plan 01 iterates with Object.hasOwn / allowlist |
</threat_model>

<verification>
Stub suite green; six invalid fixtures parse; `npm run test:core` green.
</verification>

<success_criteria>
04-VALIDATION.md Wave 0 file checklist items for stubs + invalid fixtures exist; no feature pipeline code yet.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/04-level-format-brick-types/04-00-SUMMARY.md`
</output>
