---
phase: 04-level-format-brick-types
plan: 01
type: execute
wave: 1
depends_on:
  - "04-00"
files_modified:
  - src/core/levels/schema.ts
  - src/core/levels/validate.ts
  - src/core/levels/migrations/index.ts
  - src/core/levels/migrations/README.md
  - src/core/levels/load.ts
  - src/core/levels/compile.ts
  - src/core/index.ts
  - tests/levels.validate.test.ts
autonomous: true
requirements:
  - LVL-01
must_haves:
  truths:
    - "schemaVersion 1 authoring objects validate successfully with actionable issues array on failure"
    - "Unsupported schemaVersion, bad row length, unknown char, spaces, non-finite metrics, oversize grids, and __proto__ keys are rejected without World mutation"
    - "loadAndCompile composes validate → migrate → compile stub path returns ok:false on validate failure before any compile"
  artifacts:
    - path: "src/core/levels/schema.ts"
      provides: "SCHEMA_VERSION, LevelFileV1, BrickTypeDef, CompiledLevel, ValidationIssue"
      exports: ["SCHEMA_VERSION", "LevelFileV1", "BrickTypeDef", "CompiledLevel", "ValidationIssue"]
    - path: "src/core/levels/validate.ts"
      provides: "hand-rolled validateLevel (no Zod)"
      exports: ["validateLevel"]
    - path: "src/core/levels/migrations/index.ts"
      provides: "migrateLevel v1 identity + reject unsupported"
      exports: ["migrateLevel"]
    - path: "src/core/levels/load.ts"
      provides: "loadAndCompile entry (validate→migrate→compile)"
      exports: ["loadAndCompile"]
  key_links:
    - from: "src/core/levels/load.ts"
      to: "validateLevel"
      via: "fail closed before compile"
      pattern: "validateLevel"
    - from: "validateLevel"
      to: "MAX_BRICKS"
      via: "cap cols*rows and projected brickCount"
      pattern: "MAX_BRICKS"
---

<objective>
Implement versioned level schema types, hand-rolled `validateLevel` with actionable paths, v1-identity migrations, and `loadAndCompile` orchestration (compile call wired but compile body lands in Plan 02 — stub compile to throw/return only after validate ok, OR export compile placeholder that Plan 02 replaces).

Purpose: LVL-01 validation gate + security mitigations before any SoA writes (D-03, D-04, D-13).
Output: `schema.ts`, `validate.ts`, `migrations/*`, `load.ts`, green `tests/levels.validate.test.ts`.

**Compile note:** `loadAndCompile` must call `compileLevel` from `./compile`. Plan 02 owns full `compile.ts`. In this plan either (a) create a minimal `compile.ts` that throws `Error('compileLevel not implemented')` so load can import it, and only green-test `validateLevel` + migrate directly; or (b) implement `loadAndCompile` to short-circuit after validate+migrate returning a typed placeholder. Prefer (a) tiny stub `compile.ts` exporting `compileLevel` that Plan 02 replaces — list it in files only if created. **Do not** put full packing here.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-CONTEXT.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-RESEARCH.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-PATTERNS.md
@.planning/milestones/v1.0-phases/04-level-format-brick-types/04-VALIDATION.md
@docs/layer-contract.md
@src/core/constants.ts
@src/core/types.ts
@tests/fixtures/levels/

## Locked decisions
- D-01: row-strings + per-level `brickTypes` table
- D-02: empty = `.`; never silently trim; Phase 4 disallow spaces
- D-03: `schemaVersion: 1`; unsupported rejected; migrations/ for future
- D-04: validate before compile; JS thread only
- D-13: invalid data must not mutate World (validate never touches World)

## Discretion defaults
- Char keys: `"1"|"2"|"3"|"X"` documented in types comments (actual level files Plan 02)
- `ValidationIssue = { path: string; message: string }`

<interfaces>
From src/core/constants.ts:
```typescript
export const MAX_BRICKS = 256;
```

From src/core/types.ts:
```typescript
export const BrickFlags = { UNBREAKABLE: 1 } as const;
```

Target schema (RESEARCH / PATTERNS):
```typescript
export const SCHEMA_VERSION = 1 as const;
export type BrickTypeDef = { hp: number; unbreakable?: boolean };
export type LevelFileV1 = {
  schemaVersion: 1;
  id: string;
  name: string;
  grid: {
    cols: number; rows: number;
    originX: number; originY: number;
    brickW: number; brickH: number;
    gapX: number; gapY: number;
  };
  brickTypes: Record<string, BrickTypeDef>;
  cells: string[];
};
export type CompiledLevel = {
  brickCount: number;
  x: Float32Array; y: Float32Array; w: Float32Array; h: Float32Array;
  hp: Int16Array; flags: Uint8Array;
  gridCols: number; gridRows: number;
};
export type ValidationIssue = { path: string; message: string };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: schema types + validateLevel with security caps</name>
  <files>src/core/levels/schema.ts, src/core/levels/validate.ts, tests/levels.validate.test.ts</files>
  <read_first>.planning/milestones/v1.0-phases/04-level-format-brick-types/04-RESEARCH.md, .planning/milestones/v1.0-phases/04-level-format-brick-types/04-PATTERNS.md, tests/fixtures/levels/invalid-schema-version.json, src/core/constants.ts, docs/layer-contract.md</read_first>
  <behavior>
    - Minimal valid object (schemaVersion 1, 1×1 cell `.`, empty-ish brickTypes ok) → `{ ok: true, value }`
    - schemaVersion 99 → `{ ok: false }` with issue path `schemaVersion` mentioning supported: 1
    - Fixture invalid-row-length / unknown-char / space-in-row → ok:false with path containing `cells`
    - Fixture invalid-non-finite-grid → ok:false on grid field path
    - Fixture invalid-proto-key → ok:false (reject `__proto__` / dangerous keys)
    - cols*rows &gt; MAX_BRICKS or projected non-empty cells &gt; MAX_BRICKS → ok:false (T-04-01)
    - Never trim row strings; space char fails even if lengths match
  </behavior>
  <action>
1. Create `src/core/levels/schema.ts` with `SCHEMA_VERSION`, `BrickTypeDef`, `LevelFileV1`, `CompiledLevel`, `ValidationIssue` as plain TS (no Zod — RESEARCH reject).

2. Create `src/core/levels/validate.ts` — `validateLevel(raw: unknown)` Result type from RESEARCH. Rules (per D-01…D-03, threats):
   - Root must be non-null object; use `Object.hasOwn` / own-property checks — do **not** `for…in` merge into prototypes (T-04-04).
   - Reject `brickTypes` keys `__proto__`, `constructor`, `prototype`.
   - `schemaVersion === 1` only (path `schemaVersion`).
   - Required string `id`/`name`; `grid` all numeric fields finite (`Number.isFinite`) and positive where required (cols/rows integers ≥1; sizes/gaps finite ≥0 as sensible — reject NaN/Infinity) (T-04-02).
   - `cells.length === rows`; each `cells[r].length === cols` — **never** `.trim()` (D-02).
   - Each char is `.` or a key in `brickTypes` (own keys only); **reject space** ` `.
   - Each brick type: finite `hp` &gt; 0; `unbreakable` boolean if present.
   - Cap: `cols * rows <= MAX_BRICKS` and count of non-`.` cells `<= MAX_BRICKS` (T-04-01). Import `MAX_BRICKS` from `../constants`.
   - Actionable messages: include row/char index when possible (`cells[2]`, `cells[2][4]`).

3. Replace `tests/levels.validate.test.ts` todos with real tests importing fixtures via `resolveJsonModule` or `fs.readFileSync`+`JSON.parse`. Cover all `<behavior>` cases. No World imports needed.

LC-01: no React/RN/Skia/Expo in these files.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/levels.validate.test.ts && npx eslint src/core/levels/schema.ts src/core/levels/validate.ts && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `validateLevel` exported; all Wave 0 validate todos replaced with green `it`s
    - `rg -n "\\.trim\\(" src/core/levels/validate.ts` finds no matches
    - `rg -n "from ['\\\"]zod" src/core/levels` finds no matches
    - Invalid fixtures each produce `ok: false`
    - `npm run test:core` exits 0
  </acceptance_criteria>
  <done>Hand-rolled validator rejects bad levels with path-aware issues; security caps in place.</done>
</task>

<task type="auto">
  <name>Task 2: migrations identity + loadAndCompile compose + compile stub</name>
  <files>src/core/levels/migrations/index.ts, src/core/levels/migrations/README.md, src/core/levels/load.ts, src/core/levels/compile.ts, src/core/index.ts</files>
  <read_first>.planning/milestones/v1.0-phases/04-level-format-brick-types/04-RESEARCH.md, src/core/index.ts, src/core/levels/validate.ts</read_first>
  <action>
1. `src/core/levels/migrations/index.ts`:
   - `migrateLevel(level: LevelFileV1): LevelFileV1` — identity for v1 (return same object or shallow copy).
   - If somehow called with wrong version, throw or return never — validate already gated; document that unsupported versions never reach migrate (D-03).
   - Short `migrations/README.md`: how to add v1→v2 (bump SCHEMA_VERSION, add migrator, keep reject for unknown).

2. `src/core/levels/compile.ts` **stub only** for this plan:
```typescript
import type { CompiledLevel, LevelFileV1 } from './schema';
export function compileLevel(_level: LevelFileV1): CompiledLevel {
  throw new Error('compileLevel: implement in plan 04-02');
}
```

3. `src/core/levels/load.ts`:
```typescript
export function loadAndCompile(raw: unknown):
  | { ok: true; compiled: CompiledLevel }
  | { ok: false; issues: ValidationIssue[] } {
  const v = validateLevel(raw);
  if (!v.ok) return v;
  const migrated = migrateLevel(v.value);
  return { ok: true; compiled: compileLevel(migrated) };
}
```
   - Must **not** touch World. On validate failure never call compile.

4. Barrel-export from `src/core/index.ts`: `SCHEMA_VERSION`, types as `export type`, `validateLevel`, `migrateLevel`, `loadAndCompile`, `compileLevel` (stub). Keep `loadPhase3Grid` until Plan 04.

5. Add one validate-focused test that `loadAndCompile(invalidFixture)` returns `ok:false` and does not throw from compile stub (compile must not run). Optional: assert stub throws only on valid input if you call `compileLevel` directly — not required for suite green.

Do not create `assets/levels/*.json` yet (Plan 02). Do not implement apply.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/levels.validate.test.ts && npx eslint src/core/levels && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `loadAndCompile` exported from `src/core/index.ts`
    - `migrations/README.md` exists
    - `compileLevel` stub throws if invoked
    - Invalid raw → `loadAndCompile` returns `{ ok: false }` without throwing
    - `rg -n "react|skia|expo" src/core/levels -i` finds no forbidden imports
  </acceptance_criteria>
  <done>Validate→migrate→compile compose exists; compile body deferred to Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unknown JSON → LevelFileV1 | Only validated objects proceed |
| brickTypes map keys | Attacker-controlled strings must not pollute Object.prototype |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `validateLevel` grid size | mitigate | Reject when `cols*rows` or non-empty cell count &gt; MAX_BRICKS |
| T-04-02 | Tampering | `grid.*` numbers | mitigate | `Number.isFinite` on all grid metrics; reject null/NaN/Infinity |
| T-04-03 | Tampering | World SoA | mitigate | validate/load never write World; compile stub unused on failure |
| T-04-04 | Tampering | `brickTypes` keys | mitigate | Reject `__proto__`/`constructor`/`prototype`; iterate own keys only |
</threat_model>

<verification>
`npx vitest run tests/levels.validate.test.ts` green; eslint clean on `src/core/levels`; `npm run test:core` green.
</verification>

<success_criteria>
LVL-01 validation gate is Node-tested with actionable errors and security caps; migration path stub documented; `loadAndCompile` fail-closed before compile.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/04-level-format-brick-types/04-01-SUMMARY.md`
</output>
