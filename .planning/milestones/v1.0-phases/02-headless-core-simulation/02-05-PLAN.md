---
phase: 02-headless-core-simulation
plan: 05
type: tdd
wave: 4
depends_on:
  - "02-04"
files_modified:
  - tests/physics.tunneling.prop.test.ts
  - tests/physics.golden-replay.test.ts
  - tests/core.purity.test.ts
  - tests/core.smoke.test.ts
autonomous: true
requirements:
  - PHYS-02
  - PHYS-03
  - PHYS-04
  - PHYS-06
must_haves:
  truths:
    - "Property test at 2× MAX_BALL_SPEED through dense grid reports zero tunneling and zero missed collisions"
    - "Property tests assert paddle clamp band and speed preservation (PROP-CLAMP / PROP-SPEED)"
    - "Golden-replay: same seed + intents under different chunkings → identical hashWorld"
    - "Purity/lint still ban Math.random and wall-clock in core/"
  artifacts:
    - path: "tests/physics.tunneling.prop.test.ts"
      provides: "PROP-TUNNEL / PROP-SPEED / PROP-CLAMP"
    - path: "tests/physics.golden-replay.test.ts"
      provides: "PROP-DETERM / D-14 golden-replay"
  key_links:
    - from: "tests/physics.tunneling.prop.test.ts"
      to: "src/core"
      via: "stepWorld @ 2*MAX_BALL_SPEED"
      pattern: "MAX_BALL_SPEED|test\\.prop|fc\\."
    - from: "tests/physics.golden-replay.test.ts"
      to: "src/core/hash.ts"
      via: "hashWorld equality across chunkings"
      pattern: "hashWorld"
---

<objective>
Prove PHYS-02/03/04/06 end-to-end with fast-check property suites (2× tunneling, clamp, speed) and golden-replay chunking identity — the phase gate before any pixels.

Purpose: Nyquist sampling of extreme speeds + determinism; close D-05 / D-14.
Output: green prop + golden-replay tests; full `npm test` + eslint core green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/02-headless-core-simulation/02-CONTEXT.md
@.planning/milestones/v1.0-phases/02-headless-core-simulation/02-RESEARCH.md
@.planning/milestones/v1.0-phases/02-headless-core-simulation/02-PATTERNS.md
@.planning/milestones/v1.0-phases/02-headless-core-simulation/02-VALIDATION.md
@.planning/milestones/v1.0-phases/02-headless-core-simulation/02-04-SUMMARY.md
@src/core/step.ts
@src/core/hash.ts
@src/core/constants.ts

## Locked decisions
- D-05 property test at **2×** designed max speed; zero tunneling / missed collisions
- D-14 golden-replay identical hash across intent chunkings
- D-13 / D-15 purity still enforced
- Dual oracle for tunneling (RESEARCH Open Q2 RESOLVED): (1) no center inside solid brick; (2) crossed cells produce hit or earlier TOI stop

## Patterns
- Vitest Node smoke style + golden-replay host helper from 02-PATTERNS.md (copy accumulator semantics into `runChunked`; never partial dt)

## Nyquist minima (VALIDATION.md)
- PROP-TUNNEL ≥ 100 runs
- PROP-SPEED ≥ 50
- PROP-CLAMP ≥ 50
- PROP-DETERM ≥ 20
- **Latency:** PROP-TUNNEL suite may approach the **60s** VALIDATION max feedback latency; per-task quick path remains `npm run test:core`. Do not use watch mode.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tunneling + clamp + speed property tests</name>
  <files>tests/physics.tunneling.prop.test.ts</files>
  <read_first>.planning/milestones/v1.0-phases/02-headless-core-simulation/02-RESEARCH.md, .planning/milestones/v1.0-phases/02-headless-core-simulation/02-VALIDATION.md, src/core/constants.ts, package.json</read_first>
  <behavior>
    - PROP-TUNNEL: for random angle ∈ (ε, π−ε) and launch X along top band, speed = 2*MAX_BALL_SPEED, dense grid world, after many FIXED_DT steps: never ball center inside solid brick AABB; every expanded cell crossing yields BRICK_HIT/BREAK/unbreakable bounce or earlier stop; no escape through side/top without WALL_HIT (D-05 / PHYS-02 / PHYS-03)
    - PROP-SPEED: after paddle/wall/brick resolve paths exercised, |hypot(vx,vy) − speed0| ≤ 1e-4 (PHYS-04 / D-02)
    - PROP-CLAMP: outgoing paddle angle always within ±PADDLE_ANGLE_CLAMP_DEG; |vy| ≥ min vertical (PHYS-04 / D-03)
  </behavior>
  <action>
Create `tests/physics.tunneling.prop.test.ts` using `@fast-check/vitest` (`import { test, fc } from '@fast-check/vitest'`), following 02-PATTERNS.md Vitest Node smoke style. Build dense grid via `loadTestGrid` / helper (inline, D-11). Use `test.prop({...}, { numRuns: 100 })` for tunneling; separate props for SPEED (50) and CLAMP (50). Dual oracle per RESEARCH Q2 RESOLVED.

Expect this file’s run to approach VALIDATION’s **60s** max latency budget — acceptable; keep `npm run test:core` for faster per-task loops when iterating.

If prop tests flake due to MAX_BALL_SPEED=720, tune constant **only in constants.ts** and document in SUMMARY — do not weaken assertions to “mostly no tunneling”.

Do not add UI. Fix core bugs uncovered by props until GREEN.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/physics.tunneling.prop.test.ts && npx eslint src/core</automated>
  </verify>
  <acceptance_criteria>
    - File imports from `@fast-check/vitest` or uses `fc.assert`
    - Speed multiplier `2 * MAX_BALL_SPEED` (or `MAX_BALL_SPEED * 2`) appears in source
    - `numRuns` ≥ 100 for tunneling property
    - `npx vitest run tests/physics.tunneling.prop.test.ts` exits 0
  </acceptance_criteria>
  <done>2× tunneling + clamp + speed properties green (PHYS-02/03/04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Golden-replay chunking + purity/smoke final gate</name>
  <files>tests/physics.golden-replay.test.ts, tests/core.purity.test.ts, tests/core.smoke.test.ts</files>
  <read_first>.planning/milestones/v1.0-phases/02-headless-core-simulation/02-RESEARCH.md golden-replay section, src/core/hash.ts, src/runtime/useSpikeLoop.ts, tests/core.purity.test.ts</read_first>
  <behavior>
    - Same seed + identical Intent sequence length N: run A = N×1 step; run B = random partition of N into chunk sizes summing to N (each chunk = that many consecutive stepWorld(FIXED_DT)); hashWorld(A)===hashWorld(B); tick, ball bits, brickHp, both RNG states equal (D-14 / PHYS-06)
    - PROP-DETERM ≥ 20 random intent sequences → identical hashes for duplicate runs
    - Purity still bans platform imports + Math.random/Date.now/performance.now
    - Smoke still allocates and steps with finite ball state
  </behavior>
  <action>
Implement `tests/physics.golden-replay.test.ts` with pure helper `runChunked(world, intents, chunkSteps)` that **only** calls `stepWorld(..., FIXED_DT)` — never partial dt (D-06). Follow RESEARCH golden-replay protocol.

Extend purity/smoke if exports changed. Run full gate:

```bash
npm test && npx eslint src/core
```

Do not integrate leftover accumulator inside core. Document Node-stable hash (not cross-device bit lock) in test comment.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx vitest run tests/physics.golden-replay.test.ts && npm test && npx eslint src/core</automated>
  </verify>
  <acceptance_criteria>
    - `tests/physics.golden-replay.test.ts` asserts `hashWorld` equality across different chunkings
    - Helper never calls `stepWorld` with dt other than `FIXED_DT`
    - `npm test` exits 0
    - `npx eslint src/core` exits 0
    - Purity test still scans for `Math.random`
  </acceptance_criteria>
  <done>Golden-replay + full suite green — Phase 2 physics gate closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Property generators → World | Random angles/positions must be finite (fc.double noNaN) |
| Hash / replay | Test integrity only — not cryptographic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | fc-generated Intent | mitigate | Use `noNaN: true` / finite paddles; stepWorld finite guards already in 02-04 |
| T-02-02 | Spoofing | Flaky non-determinism | mitigate | Golden-replay + hash includes RNG; purity bans wall-clock |
| T-02-06 | Info disclosure | hashWorld | accept | Local offline game; hash is test integrity only (ASVS V6 N/A) |
</threat_model>

<verification>
All physics + purity + smoke tests green; eslint src/core green; PROP-TUNNEL and golden-replay are phase merge gates.
</verification>

<success_criteria>
Roadmap Phase 2 success criteria 1–4 met under automated Vitest: fixed-step golden-replay, 2× tunneling props, paddle clamps, purity/RNG bans.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/02-headless-core-simulation/02-05-SUMMARY.md`
</output>
