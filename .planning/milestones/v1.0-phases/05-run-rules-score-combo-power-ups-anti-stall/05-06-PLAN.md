---
phase: 05-run-rules-score-combo-power-ups-anti-stall
plan: 06
type: execute
wave: 5
depends_on:
  - "05-04"
  - "05-05"
files_modified:
  - src/runtime/useGameLoop.ts
  - app/_components/GameHost.tsx
  - src/runtime/GameScreen.tsx
  - src/render/recordSprites.ts
  - src/runtime/useSpikeLoop.ts
autonomous: false
requirements:
  - RUN-01
  - PHYS-07
  - PWR-03
must_haves:
  truths:
    - "HUD shows Score · N and combo via SharedValue mirrors (not per-frame React from physics)"
    - "Stall! · tier appears when stallTier > 0"
    - "Active pickups draw as flat Skia rects; expanded paddle uses world.paddleW"
  artifacts:
    - path: "src/runtime/useGameLoop.ts"
      provides: "scoreOut/comboOut/stallTierOut mirrors"
    - path: "app/_components/GameHost.tsx"
      provides: "React chrome state for score/combo/stall"
    - path: "src/runtime/GameScreen.tsx"
      provides: "Score · N, combo, Stall! text"
    - path: "src/render/recordSprites.ts"
      provides: "flat pickup draw loop"
  key_links:
    - from: "useGameLoop"
      to: "GameHost reactions"
      via: "SharedValue score/combo/stallTier"
      pattern: "scoreOut|comboOut|stallTierOut"
    - from: "recordFrame"
      to: "pickup SoA"
      via: "drawRect for active pickups"
      pattern: "pickupActive|pickupX"
---

<objective>
Minimal SharedValue chrome for Score · N, combo, and Stall! (D-03/D-17) plus flat Skia pickup drawing — no polished HUD, no neon.

Purpose: Satisfy visibility success criteria SC-1 and SC-5 without Phase 6 HUD polish.
Output: Loop mirrors + GameHost/GameScreen text + flat pickups in `recordFrame`; human verify checkpoint.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md
@.planning/milestones/v1.0-phases/05-run-rules-score-combo-power-ups-anti-stall/05-PATTERNS.md
@src/runtime/useGameLoop.ts
@app/_components/GameHost.tsx
@src/runtime/GameScreen.tsx
@src/render/recordSprites.ts

<interfaces>
Lives mirror pattern to clone (useGameLoop + GameHost):
```typescript
// useGameLoop options:
livesOut: SharedValue<number>;
simPhaseOut: SharedValue<number>;
// add:
scoreOut: SharedValue<number>;
comboOut: SharedValue<number>;
stallTierOut: SharedValue<number>;

// every frame publish:
livesOut.value = w.lives;
scoreOut.value = w.score;
comboOut.value = w.combo;
stallTierOut.value = w.stallTier;
```

GameScreen display strings (exact prefixes):
- `Score · ${score}`
- combo: `×${combo}` when combo > 1, otherwise omit or show `×1` — discretion: show `×${combo}` always when playing
- when stallTier > 0: `Stall! · ${stallTier}`

Pickup draw: flat fill `#FBBF24` (amber) rect using PICKUP_WIDTH/HEIGHT centered on pickupX/Y; no glow/shadow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SharedValue mirrors + GameHost/GameScreen chrome</name>
  <files>src/runtime/useGameLoop.ts, app/_components/GameHost.tsx, src/runtime/GameScreen.tsx, src/runtime/useSpikeLoop.ts</files>
  <read_first>src/runtime/useGameLoop.ts, app/_components/GameHost.tsx, src/runtime/GameScreen.tsx, src/runtime/useSpikeLoop.ts, .planning/milestones/v1.0-phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md</read_first>
  <action>
1. Extend `useGameLoop` options with `scoreOut`, `comboOut`, `stallTierOut` SharedValues; write them every frame alongside lives (all publish sites in the file — cold start, frame callback, retry).

2. In `GameHost.tsx`: create SharedValues + React state for score/combo/stallTier; pass into useGameLoop; use **separate** `useAnimatedReaction` per mirror (or two reactions) — do not pack score into the 32-bit lives/phase pack (scores exceed 8 bits). `runOnJS` setters update React state only on change.

3. Extend `GameScreen` props with `score`, `combo`, `stallTier`; render in existing hud row:
   - `{`Score · ${score}`}`
   - `{`×${combo}`}`
   - `{stallTier > 0 ? `Stall! · ${stallTier}` : null}`
   Keep `Lives · ${lives}`. No new card layouts / neon.

4. Update `useSpikeLoop.ts` to pass dummy SharedValues for the new outs so it still typechecks.

Do not implement Phase 6 menus/high score.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "scoreOut|comboOut|stallTierOut|Score ·|Stall!" src/runtime/useGameLoop.ts app/_components/GameHost.tsx src/runtime/GameScreen.tsx && npx tsc --noEmit -p . 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `rg "Score ·" src/runtime/GameScreen.tsx` matches
    - `rg "Stall!" src/runtime/GameScreen.tsx` matches
    - `rg "scoreOut\\.value = w\\.score" src/runtime/useGameLoop.ts` matches
    - `rg "setState|useState" src/runtime/useGameLoop.ts` does not show per-frame React score writes inside the frame callback (only SharedValue writes)
    - tsc exits 0 (or project’s existing tsc script succeeds)
  </acceptance_criteria>
  <done>Minimal Score/combo/Stall chrome mirrored like Lives.</done>
</task>

<task type="auto">
  <name>Task 2: Flat pickup sprites in recordFrame</name>
  <files>src/render/recordSprites.ts</files>
  <read_first>src/render/recordSprites.ts, src/core/types.ts, src/core/constants.ts, .planning/milestones/v1.0-phases/05-run-rules-score-combo-power-ups-anti-stall/05-PATTERNS.md</read_first>
  <action>
In `recordFrame`, after bricks (or before paddle), loop `i in [0, world.maxPickups)` (or `pickupCount` if dense — if pool is sparse, scan maxPickups and skip inactive):
- if `!world.pickupActive[i]` continue
- draw flat rect centered at `(pickupX[i], pickupY[i])` size 20×12
- fill color `#FBBF24` via `Skia.Color` — **no** shadow, blur, or neon paint
- Paddle already uses `world.paddleW` — no paddle draw change required beyond verifying expanded width renders

Do not add particle/glow code.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "pickupActive|FBBF24|pickupX" src/render/recordSprites.ts && npx vitest run tests/core.purity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "pickupActive" src/render/recordSprites.ts` matches
    - `rg "FBBF24" src/render/recordSprites.ts` matches
    - No `Blur|Glow|shadow` added for pickups in this file (grep those words only near new pickup block — ensure new block is flat)
    - purity test still exits 0
  </acceptance_criteria>
  <done>Flat pickups visible in Skia picture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Score/combo/Stall chrome + pickup catch</name>
  <files>app/_components/GameHost.tsx, src/runtime/GameScreen.tsx</files>
  <read_first>.planning/milestones/v1.0-phases/05-run-rules-score-combo-power-ups-anti-stall/05-CONTEXT.md</read_first>
  <action>
Pause for human verification of chrome and catch-only pickups after Tasks 1–2. Do not change code unless the user reports defects.
  </action>
  <what-built>
    Headless rules for score/combo/pickups/expand/multiball/stall plus minimal SharedValue chrome and flat pickup rects.
  </what-built>
  <how-to-verify>
    1. Launch the playable app (`npx expo start` / existing dev client).
    2. Break bricks: confirm HUD shows `Score · N` increasing and `×combo` rising until you touch the paddle (combo resets toward ×1).
    3. When a amber pickup falls, confirm it only applies when overlapping the paddle (missing lets it fall off).
    4. Idle without damaging breakable bricks for ~8+ seconds of active play (not paused): confirm `Stall! · 1` (then higher tiers) appears; pause mid-idle and resume — stall should not instantly max out from wall-clock pause.
    5. Confirm no neon/particle polish (flat shapes only).
  </how-to-verify>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; rg -n "Score ·|Stall!" src/runtime/GameScreen.tsx</automated>
  </verify>
  <resume-signal>Type "approved" or list chrome/catch/stall issues</resume-signal>
  <acceptance_criteria>
    - User replies with approval or concrete defects
    - GameScreen still contains `Score ·` and `Stall!` strings
  </acceptance_criteria>
  <done>Human confirmed SC-1/SC-5 visibility and catch-only pickups on device/simulator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-thread World → JS chrome | SharedValue mirrors only; no trust boundary for network |
| Render pickup positions | Visual only; gameplay authority stays in core |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Chrome spoof | accept | Offline single-player; chrome is display-only mirrors of World |
| T-05-02 | Elevation | Stall chrome vs pause | mitigate | Stall tier only updates when sim steps; verify in checkpoint step 4 |
| T-05-03 | DoS | Render NaN pickup coords | mitigate | Skip inactive; core finite guards already on spawn; draw only active pickups |
</threat_model>

<verification>
Automated: mirrors + pickup draw greps + tsc/purity. Manual: checkpoint for Score/combo/Stall visibility and catch-only behavior.
</verification>

<success_criteria>
Player can see score, combo, and Stall! escalation; pickups are flat and catch-only visually; no Phase 6/7 polish invented.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/05-run-rules-score-combo-power-ups-anti-stall/05-06-SUMMARY.md`
</output>
