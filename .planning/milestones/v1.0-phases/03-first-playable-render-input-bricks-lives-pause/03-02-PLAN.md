---
phase: 03-first-playable-render-input-bricks-lives-pause
plan: 02
type: execute
wave: 1
depends_on:
  - "03-00"
files_modified:
  - src/render/camera.ts
  - src/render/colors.ts
  - src/render/recordSprites.ts
  - src/render/GameCanvas.tsx
  - src/render/SpikeCanvas.tsx
autonomous: true
requirements:
  - PHYS-01
  - RUN-02
must_haves:
  truths:
    - "Playfield uses uniform letterbox scale into surface (no non-uniform stretch)"
    - "Letterbox bars are black; navy fill only inside 360×640 field"
    - "recordFrame draws bricks (HP/unbreakable colors), white paddle, white ball from World SoA"
    - "GameCanvas remains opaque SkPicture consumer (D-01/D-03)"
  artifacts:
    - path: "src/render/camera.ts"
      provides: "makeCamera letterbox math"
      exports: ["makeCamera", "LOGICAL_W", "LOGICAL_H"]
    - path: "src/render/colors.ts"
      provides: "UI-SPEC hex fills"
      exports: ["FIELD_NAVY", "LETTERBOX_BLACK", "BALL_PADDLE", "brickFill"]
    - path: "src/render/recordSprites.ts"
      provides: "entity recordFrame with letterbox"
      contains: "makeCamera"
    - path: "src/render/GameCanvas.tsx"
      provides: "opaque Canvas + Picture host"
      exports: ["GameCanvas"]
  key_links:
    - from: "src/render/recordSprites.ts"
      to: "src/render/camera.ts"
      via: "translate(ox,oy)+uniform scale before field draw"
      pattern: "makeCamera"
    - from: "src/render/recordSprites.ts"
      to: "World brick/ball/paddle SoA"
      via: "read-only draw (LC-03/LC-08)"
      pattern: "brickHp|ballX|paddleX"
    - from: "src/render/GameCanvas.tsx"
      to: "Skia Canvas opaque"
      via: "opaque prop retained"
      pattern: "opaque"
---

<objective>
Evolve blank spike renderer into letterboxed immediate-mode playfield: camera math, UI-SPEC colors, SkPicture draw of bricks/paddle/ball, GameCanvas rename — flat solids only (D-01…D-03).

Purpose: Visible rally surface for PHYS-01 paddle feel and RUN-02 brick/lives readability.
Output: `camera.ts`, `colors.ts`, updated `recordFrame`, `GameCanvas.tsx` (+ SpikeCanvas re-export).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/03-first-playable-render-input-bricks-lives-pause/03-CONTEXT.md
@.planning/milestones/v1.0-phases/03-first-playable-render-input-bricks-lives-pause/03-UI-SPEC.md
@.planning/milestones/v1.0-phases/03-first-playable-render-input-bricks-lives-pause/03-RESEARCH.md
@.planning/milestones/v1.0-phases/03-first-playable-render-input-bricks-lives-pause/03-PATTERNS.md
@src/render/recordSprites.ts
@src/render/SpikeCanvas.tsx
@docs/layer-contract.md

## Locked decisions
- D-01: flat untextured shapes only — no neon/particles/trails
- D-02: navy field + white ball/paddle + row/HP brick colors from UI-SPEC
- D-03: opaque SkPicture path; letterbox into safe/content box (black bars)

## UI-SPEC colors (exact)
- Field navy `#1a1a2e`; letterbox `#000000`
- Ball/paddle `#FFFFFF`
- HP3 `#C44569`; HP2 `#E07A5F`; HP1 `#F2CC8F`; unbreakable `#6B7280`
- Never use cliff cyan `#00ffaa` for gameplay

<interfaces>
From src/render/SpikeCanvas.tsx (evolve):
```typescript
export function SpikeCanvas({ picture, onSize }: Props): JSX.Element;
// Canvas opaque + Fill black + Picture
```

From src/render/recordSprites.ts (current — replace stretch):
```typescript
export function recordFrame(world, metrics, surfaceW, surfaceH, drawOverlayFlag, hudFont): SkPicture;
// BAD today: canvas.scale(wPx/360, hPx/640) non-uniform
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: camera.ts + colors.ts per UI-SPEC</name>
  <files>src/render/camera.ts, src/render/colors.ts</files>
  <read_first>.planning/milestones/v1.0-phases/03-first-playable-render-input-bricks-lives-pause/03-UI-SPEC.md, .planning/milestones/v1.0-phases/03-first-playable-render-input-bricks-lives-pause/03-RESEARCH.md, src/render/recordSprites.ts</read_first>
  <action>
1. Create `src/render/camera.ts`:
```typescript
export const LOGICAL_W = 360;
export const LOGICAL_H = 640;

export type Camera = { scale: number; ox: number; oy: number };

export function makeCamera(safeW: number, safeH: number): Camera {
  'worklet';
  const sw = safeW > 1 ? safeW : LOGICAL_W;
  const sh = safeH > 1 ? safeH : LOGICAL_H;
  const scale = Math.min(sw / LOGICAL_W, sh / LOGICAL_H);
  return {
    scale,
    ox: (sw - LOGICAL_W * scale) * 0.5,
    oy: (sh - LOGICAL_H * scale) * 0.5,
  };
}
```
Guard non-finite safeW/H → fall back to LOGICAL dims (T-03-01 adjacent).

2. Create `src/render/colors.ts` with string hex constants from UI-SPEC exactly:
`FIELD_NAVY`, `LETTERBOX_BLACK`, `BALL_PADDLE`, `BRICK_HP3`, `BRICK_HP2`, `BRICK_HP1`, `BRICK_UNBREAKABLE`.
Export `brickFill(hp: number, flags: number): string` — if UNBREAKABLE bit set → steel; else hp≥3 → HP3, hp===2 → HP2, else HP1.

No React imports. Do not modify recordSprites yet.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; node -e "const fs=require('fs'); for (const f of ['src/render/camera.ts','src/render/colors.ts']) { const s=fs.readFileSync(f,'utf8'); if(!s.includes('makeCamera') && f.includes('camera')) process.exit(1); if(!s.includes('#1a1a2e') && f.includes('colors')) process.exit(1);} console.log('ok')" && npx eslint src/render/camera.ts src/render/colors.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/render/camera.ts` exports `makeCamera` using `Math.min(safeW/360, safeH/640)`
    - `src/render/colors.ts` contains `#1a1a2e`, `#C44569`, `#6B7280`, `#FFFFFF`
    - `rg -n "00ffaa" src/render/colors.ts` finds no matches
    - eslint exits 0 on both files
  </acceptance_criteria>
  <done>Letterbox camera + UI-SPEC color tokens exist for recordFrame.</done>
</task>

<task type="auto">
  <name>Task 2: recordFrame entities + GameCanvas rename</name>
  <files>src/render/recordSprites.ts, src/render/GameCanvas.tsx, src/render/SpikeCanvas.tsx</files>
  <read_first>src/render/recordSprites.ts, src/render/SpikeCanvas.tsx, src/render/camera.ts, src/render/colors.ts, src/core/types.ts, docs/layer-contract.md</read_first>
  <action>
1. Rewrite `recordFrame` in `src/render/recordSprites.ts` (keep PictureRecorder tools pattern on `global.__spikeRecorderTools` or rename global key to `__gameRecorderTools` with fallback read of old key once):
   - Clear / fill entire surface with LETTERBOX_BLACK (or rely on Canvas Fill — still draw black rect for letterbox inside picture)
   - `const cam = makeCamera(wPx, hPx)`; `canvas.save(); canvas.translate(cam.ox, cam.oy); canvas.scale(cam.scale, cam.scale);`
   - Draw navy field rect 0,0,360,640
   - For i in brickCount: if hp>0 OR unbreakable with hp>0 (unbreakables keep hp): draw brick rect with `brickFill`
   - Draw paddle rect from paddleX/Y/W/H (center-based: left = paddleX - paddleW/2)
   - Draw ball circle(s) for active balls — white
   - `canvas.restore()`
   - Optional PERF_OVERLAY path unchanged via drawOverlay
   - **LC-08:** read World only — never mutate SoA
   - **D-01:** no glow/shadow/blur APIs
   - Remove non-uniform `scale(wPx/LOGICAL_W, hPx/LOGICAL_H)`

2. Create `src/render/GameCanvas.tsx` — copy SpikeCanvas: `opaque`, absolute fill, black Fill, Picture. Export `GameCanvas`.

3. Change `SpikeCanvas.tsx` to re-export `GameCanvas` as `SpikeCanvas` (thin compatibility) OR make SpikeCanvas a one-line re-export — keep imports compiling until Plan 05 renames hosts.

Do not wire safe-area insets here (GameScreen Plan 05 passes content size into loop/onSize). Surface size remains the canvas onSize box; letterbox math still correct when parent sizes to safe area.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; npx eslint src/render && rg -n "scale\(wPx / LOGICAL_W,\s*hPx / LOGICAL_H\)" src/render/recordSprites.ts; test $? -eq 1 && rg -n "makeCamera" src/render/recordSprites.ts && rg -n "opaque" src/render/GameCanvas.tsx && npm run test:core</automated>
  </verify>
  <acceptance_criteria>
    - `recordSprites.ts` calls `makeCamera` and does not use non-uniform stretch scale
    - `recordSprites.ts` draws bricks/paddle/ball (rg `brickX`/`paddleX`/`ballX`)
    - `GameCanvas.tsx` exports `GameCanvas` with `opaque`
    - `npm run test:core` still exits 0
    - eslint `src/render` exits 0
  </acceptance_criteria>
  <done>Opaque letterboxed SkPicture shows playfield entities with flat UI-SPEC colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| World SoA → Skia draw | Render must not mutate simulation state |
| Surface size → camera | Bad sizes could NaN the transform |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `makeCamera` / recordFrame | mitigate | Fallback when surface <1 or non-finite; no world writes |
| T-03-02 | Elevation (UX) | GameCanvas | accept | No gesture handling in render layer |
| T-03-03 | Tampering | recordFrame | accept | No timestep/accumulator in render |
</threat_model>

<verification>
eslint src/render clean; stretch scale gone; makeCamera used; GameCanvas opaque; core tests still green.
</verification>

<success_criteria>
Players can see a correctly letterboxed navy field with bricks, paddle, and ball via the existing Picture path — no neon.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-first-playable-render-input-bricks-lives-pause/03-02-SUMMARY.md`
</output>
