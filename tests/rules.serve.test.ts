// tests/rules.serve.test.ts — Wave 0 stub; Plan 01 fills dock/launch contract
import { describe, it } from 'vitest';

describe('serve rules (dock / launch)', () => {
  it.todo('docked ball launches only on Intent.launch when sim phase is docked');
});
