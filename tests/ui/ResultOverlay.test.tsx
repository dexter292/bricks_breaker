/**
 * N-PROG-04 — ResultOverlay Next + stars Nyquist stubs (Plan 03).
 *
 * Wave 0: file exists + todos. Implementation lands in Plan 03.
 *
 * @vitest-environment jsdom
 */
import { describe, it } from 'vitest';

describe('ResultOverlay Next + stars (C2 Plan 03 todos)', () => {
  it.todo('win + onNext shows Next button');
  it.todo('win without onNext / level-06 omits Next');
  it.todo('lose does not show Next');
  it.todo('win shows ★/☆ when stars set');
});
