/**
 * N-LVL-02 — SelectScreen Nyquist stubs (Plan 02).
 *
 * Wave 0: file exists + todos. Implementation lands in Plan 02.
 *
 * @vitest-environment jsdom
 */
import { describe, it } from 'vitest';

describe('SelectScreen (C2 Plan 02 todos)', () => {
  it.todo('renders locked / uncleared / cleared row states from snapshot');
  it.todo('locked tap does not call onChoose');
  it.todo('mount calls getSnapshot');
  it.todo('Back invokes onBack');
});
