// tests/rules.lives.test.ts — Wave 0 stub; Plan 01 fills BALL_OUT lives contract
import { describe, it } from 'vitest';

describe('lives rules (BALL_OUT)', () => {
  it.todo('BALL_OUT decrements lives and re-docks when lives remain');
});
