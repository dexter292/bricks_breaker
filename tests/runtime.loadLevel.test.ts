/**
 * LVL-04 — default level-03 load stubs (Wave 0).
 * Plan 01 fills LevelId + loadLevelById wiring.
 */
import { describe, it } from 'vitest';

describe('runtime.loadLevel', () => {
  it.todo('LevelId includes level-03');
  it.todo('loadLevelById() default is level-03');
  it.todo('loadLevelById("level-03") returns ok:true');
});
