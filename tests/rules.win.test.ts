// tests/rules.win.test.ts — Wave 0 stub; Plan 01 fills last-breakable win contract
import { describe, it } from 'vitest';

describe('win rules (last breakable)', () => {
  it.todo('clearing the last breakable brick declares win');
});
