/**
 * Wave 0 Nyquist stubs — apply + no-mutate-on-failure (Plan 02 fills).
 * Do not import src/core/levels/* until modules exist.
 */
import { describe, it } from 'vitest';

describe('levels.apply', () => {
  it.todo('apply fills SoA and gridRows > 1 spatial path');

  // 04-W0-04 / T-04-03
  it.todo('failed validate / skipped apply never mutates World / hashWorld');
});
