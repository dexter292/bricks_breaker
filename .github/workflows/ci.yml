name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - run: npm ci
      - run: npx expo config --type public
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run assert:skia
      - run: npm run assert:privacy-manifest
